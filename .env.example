# Local development (.env.local) — copy this file to .env.local and fill in
# NEXT_PUBLIC_SITE_URL: leave blank to default to http://localhost:3000, or
# set it explicitly to override.
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Vercel preview and production — supplied by the hosting environment, never
# committed. Same names, different values per environment.
#
# NEXT_PUBLIC_SITE_URL may be left unset on Vercel: it is then derived at
# build time from Vercel's system environment (production -> the project's
# production URL, preview -> the branch/deployment URL). Set it explicitly
# here only to override that derived value.

# Server-only, secret — never prefix with NEXT_PUBLIC_, never exposed to the browser
SUPABASE_SERVICE_ROLE_KEY=

# Optional at boot (D-48); the contact route requires it at send time and
# throws a typed transport error if unset. Never commit a key here.
RESEND_API_KEY=

# Optional, defaults to "supports" — the private storage bucket name CPT-07
# reads for signed-URL generation. Not a secret.
SUPABASE_SUPPORTS_BUCKET=

# Read by the Supabase CLI for the local stack only (supabase/config.toml's
# [auth.external.google] block), not by the Next.js app. Supplied by the CIO,
# never committed.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=

# Bootstrap variable for scripts/seed-agenda.mjs only (D-21) -- not read by
# the Next.js app, so it does NOT go into src/lib/env/server.ts. The trainer's
# email; the profil matching this address is promoted to administrator. Never
# commit a real value here.
FORMATEUR_EMAIL=

# Optional at boot (D-48), server-only, never NEXT_PUBLIC_ (D-15/T-04-24) --
# the trainer's fixed video-conferencing room, injected into the .ics and the
# AGD-06 emails. Read strictly at request time inside
# src/app/api/reservation/route.ts. Never commit a real value here.
FORMATEUR_LIEN_VISIO=
