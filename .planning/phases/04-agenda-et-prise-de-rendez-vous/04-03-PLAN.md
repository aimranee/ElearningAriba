---
phase: 04-agenda-et-prise-de-rendez-vous
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/lib/i18n/fr.ts
  - src/lib/agenda/creneaux.ts
  - src/lib/agenda/types-rendez-vous.ts
  - src/locales/fr/agenda.json
  - src/components/agenda/calendrier-mois.tsx
  - src/components/agenda/liste-creneaux.tsx
  - src/components/agenda/booker-skeleton.tsx
  - src/components/agenda/agenda-booker.tsx
  - src/app/agenda/page.tsx
autonomous: false
requirements: [AGD-02]
must_haves:
  truths:
    - "A visitor opens /agenda and sees a monthly calendar plus a list of free slots, in Europe/Paris, without signing in (AGD-02, D-09)"
    - "Past, blocked, booked and holiday cells all render identically as Indisponible in muted grey — never red, never distinguished (D-23, D-U3)"
    - "The public legend reads exactly Libre / Indisponible / Passé; no visitor-facing surface renders Réservé or Bloqué (D-24)"
    - "/agenda still prerenders as a static route — the 13 pre-existing static routes are unchanged (D-06)"
    - "The calendar opens on the first slot-carrying day, never a blank month, and holds its layout with a skeleton while the client fetch resolves"
    - "Every date, hour and price on the surface is produced by a formatter in src/lib/i18n/fr.ts — no hand-written 'h', '€' or weekday anywhere"
    - "The appointment-type chooser renders label, duration and price read from app.type_rendez_vous — the same store the booking RPC computes from — never from agenda.json"
    - "The sessionStorage key carrying a chosen slot is a single exported constant in src/lib/agenda/creneaux.ts, written once and imported by every consumer"
  artifacts:
    - path: "src/lib/i18n/fr.ts"
      provides: "a prose hour-of-day formatter (14 h 30) and a weekday-bearing date formatter (mardi 8 septembre), each with its formatX wrapper"
      contains: "weekday"
    - path: "src/lib/agenda/creneaux.ts"
      provides: "Intl-only, side-effect-free grouping and month-grid helpers usable from a client component, plus the single exported sessionStorage key constant for the slot handoff"
      contains: "CLE_CRENEAU_CHOISI"
    - path: "src/lib/agenda/types-rendez-vous.ts"
      provides: "the server-only build-time read of app.type_rendez_vous through the anonymous cookie-free client, keeping /agenda static"
      contains: "type_rendez_vous"
    - path: "src/components/agenda/agenda-booker.tsx"
      provides: "the client island that reads app.creneaux_libres through the browser Supabase client"
      contains: "creneaux_libres"
    - path: "src/app/agenda/page.tsx"
      provides: "the static shell: title, intro, type chooser, legend, empty state"
    - path: "src/locales/fr/agenda.json"
      provides: "the three-entry public legend"
      contains: "indisponible"
  key_links:
    - from: "src/components/agenda/agenda-booker.tsx"
      to: "src/lib/supabase/client.ts"
      via: "createClient() browser factory, anon key"
      pattern: "@/lib/supabase/client"
    - from: "src/components/agenda/agenda-booker.tsx"
      to: "app.creneaux_libres"
      via: "supabase.rpc"
      pattern: "rpc\\(\"creneaux_libres\""
---

<objective>
Deliver the first user-visible slice: a visitor lands on `/agenda` and sees the trainer's real free
slots for a chosen appointment type, on a monthly grid in Europe/Paris, with everything unavailable
greyed out identically.

Purpose: `/agenda` is the phase's shop window and the entry point of the conversion. It is also the
one surface with a hard structural constraint — D-06 requires it to stay prerendered, so the data
has to arrive through a client island rather than a server read.

Output: two new `Intl` formatters, a client-safe helper module, four agenda components, a rewritten
static `/agenda` page, and a three-entry public legend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-agenda-et-prise-de-rendez-vous/04-CONTEXT.md
@.planning/phases/04-agenda-et-prise-de-rendez-vous/04-UI-SPEC.md
@.planning/phases/04-agenda-et-prise-de-rendez-vous/04-PATTERNS.md
@.planning/phases/04-agenda-et-prise-de-rendez-vous/04-RESEARCH.md
@src/app/agenda/page.tsx
@src/lib/i18n/fr.ts
@src/lib/supabase/client.ts
@src/components/forms/contact-form.tsx
@src/locales/fr/agenda.json
@.planning/phases/04-agenda-et-prise-de-rendez-vous/04-02-SUMMARY.md

<interfaces>
The read path this plan consumes, from plan 04-01:

```sql
app.creneaux_libres(p_type_id text, p_du date, p_au date) returns table (debut timestamptz, fin timestamptz)
-- granted to anon and authenticated; returns ONLY free instants
```

The browser client factory — `src/lib/supabase/client.ts`, already typed and schema-defaulted, with
**zero consumers today**. This island is its first:

```ts
export function createClient() {
  return createBrowserClient<Database, "app">(
    clientEnv.NEXT_PUBLIC_SUPABASE_URL,
    clientEnv.NEXT_PUBLIC_SUPABASE_ANON_KEY,
    { db: { schema: "app" } },
  );
}
```

Existing pinned constants and formatters in `src/lib/i18n/fr.ts` — reuse, never redeclare:

```ts
export const LOCALE = "fr-FR" as const;
export const TIME_ZONE = "Europe/Paris" as const;
export const dateFormatter, dateTimeFormatter, numberFormatter, currencyFormatter,
             timeFormatter, hourFormatter;
export function formatDate, formatDateTime, formatNumber, formatCurrency, formatTime, formatHours;
```
</interfaces>

<scope_rulings>
Binding rulings from the phase orchestrator — do not relitigate:
- **D-06 names `src/lib/supabase/public.ts` for the client-side load. That is a naming slip, and it
  applies to the *slot* read only.** `public.ts` opens with `import "server-only"`, so importing it
  from a `"use client"` component is a build error; the live free-slot fetch therefore goes through
  `src/lib/supabase/client.ts`. Carry that note in a code comment so no reviewer "fixes" it back.
  D-06's intent — an anonymous read, `/agenda` stays static — is preserved exactly.
  **`public.ts` is still the right module for the appointment-type read**, which happens
  server-side at build/revalidate time, cookie-free, and is precisely what that file exists for.
- **`app.type_rendez_vous` is the single source of truth for the appointment types** — label,
  duration, `tampon_minutes` and `prix_centimes`. The learner-facing chooser and the price the RPC
  records must not come from two different places. `src/locales/fr/agenda.json`'s `typesRendezVous`
  array stays byte-identical because it is the bootstrap seed input (D-21/D-22) and because the D-24
  fence forbids editing it — but **no runtime consumer reads it any more.**
- **The 15-minute slot hold is OUT OF SCOPE.** Ignore every `04-UI-SPEC.md` line that depends on it:
  the hold-timer badge, the hold-timer notice, the "slot held throughout" wording and UI-SPEC
  acceptance criterion 7. Every other part of `04-UI-SPEC.md` is binding.
- **CLAUDE.md's "use queryKeys factory, prefer optimistic updates" is NOT APPLICABLE to this phase.**
  There is no TanStack Query in `package.json` and the dependency budget is zero. The in-repo idiom
  is the `useState` status machine in `src/components/forms/contact-form.tsx`. Do not invent or
  install a query library. Every other CLAUDE.md rule applies in full.
- **Zero new runtime dependencies.** No date library: Postgres owns the zoned arithmetic and every
  instant that reaches the browser is already a correct `timestamptz`. The browser only *formats*
  (`Intl.DateTimeFormat` with `timeZone: "Europe/Paris"`) and does civil-calendar grid maths.
- **CONTEXT's "no new `Intl` formatters" fence means "no formatter authored outside
  `src/lib/i18n/fr.ts`, no hand-written ' h' or '€' at a call site".** `04-UI-SPEC.md` D-U4 requires
  exactly two new ones inside that same file, in that same idiom. Adding them there honours both.
  Record the reconciliation in the summary.
- **Write-forbidden for the whole phase:** `src/app/globals.css`, the nine
  `src/components/ui/*.tsx` primitives, `src/components/layout/*`. Add alongside; never edit.
</scope_rulings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two formatters, the client-safe slot helpers, and the D-24 legend</name>
  <files>src/lib/i18n/fr.ts, src/lib/agenda/creneaux.ts, src/locales/fr/agenda.json</files>
  <read_first>
    - `src/lib/i18n/fr.ts` — the whole file: the pinned `LOCALE`/`TIME_ZONE` constants, the `why:`
      comments on `currencyFormatter` and `hourFormatter` naming the bug class they prevent, and the
      rule that every exported formatter ships with a matching `formatX(value)` wrapper
    - `src/locales/fr/agenda.json` — the current four-entry `legende` object at lines 6-11 and the
      `typesRendezVous` array at lines 12-15
    - `.planning/phases/04-agenda-et-prise-de-rendez-vous/04-UI-SPEC.md` § Typography (the two
      required formatters and what each produces) and § French typography and culture
    - `.planning/phases/04-agenda-et-prise-de-rendez-vous/04-RESEARCH.md` § Code Examples, the
      "`Intl` covers every rendering need" block — the `en-CA`/`sv-SE` `formatToParts` idiom that
      yields an ISO `YYYY-MM-DD` **in Europe/Paris**, which is how a list of `timestamptz` is grouped
      into calendar days with no date library
  </read_first>
  <action>
Add two formatters to `src/lib/i18n/fr.ts`, in the file's existing idiom — a `why:` comment naming
the bug class, the exported `Intl` instance, and a matching `formatX` wrapper. Do not touch the six
existing formatters and do not redeclare `LOCALE` or `TIME_ZONE`:

- a prose hour-of-day formatter producing `14 h 30` — French prose never writes clock time in the
  CLDR `14:30` shape. Build it from `Intl.DateTimeFormat` with `timeZone: TIME_ZONE`, `hourCycle:
  "h23"`, `hour` and `minute` numeric, and assemble the `formatToParts` output with the literal
  separator ` h ` inside the wrapper — the parts come from `Intl`, so no hour value is hand-written.
- a weekday-bearing date formatter producing `mardi 8 septembre` — `Intl.DateTimeFormat` with
  `timeZone: TIME_ZONE`, `weekday: "long"`, `day: "numeric"`, `month: "long"`. The existing
  `dateFormatter` (`dateStyle: "long"`) omits the weekday, which an agenda cannot.

State in a comment on each that the existing `timeFormatter` (`14:30`) stays correct and unchanged
for dense slot chips — the two registers coexist by design and are never interchanged.

Create `src/lib/agenda/creneaux.ts`. This module is imported by a `"use client"` component, so it
**must NOT carry `import "server-only"`** and must stay side-effect free and `Intl`-only. No `any`;
export explicit types. It provides:

- `type Creneau = { debut: Date; fin: Date }` and a Zod schema validating the RPC row shape
  (`{ debut: string; fin: string }`) at the boundary before conversion, per CLAUDE.md.
- `jourIsoParis(d: Date): string` — the `YYYY-MM-DD` **in Europe/Paris** for an instant, via
  `Intl.DateTimeFormat("en-CA", { timeZone: TIME_ZONE })`. This is the grouping key; never use
  `toISOString().slice(0,10)`, which is UTC and shifts the day for any slot before 02:00 Paris.
- `grouperParJour(creneaux)` → a `Map<string, Creneau[]>` keyed by `jourIsoParis`, values ordered by
  `debut`.
- `grouperMatinApresMidi(creneaux)` → `{ matin: Creneau[]; apresMidi: Creneau[] }`, split on the
  Paris hour read from `formatToParts`, so the slot list never dumps a day's whole grid flat.
- `grilleDuMois(annee, mois)` → the array of `YYYY-MM-DD` day keys of the calendar grid, padded to
  whole ISO weeks (lundi first) so `grid-cols-7` aligns. Civil-calendar maths only — this is
  not timezone arithmetic.
- `premierJourPorteur(creneaux)` → the earliest `YYYY-MM-DD` key that carries at least one slot, so
  the calendar opens on a day with availability and never a blank month.
- `estPasse(jour: string)` — whether a day key is strictly before today in Europe/Paris.
- `CLE_CRENEAU_CHOISI` — the single exported `const` holding the `sessionStorage` key for the chosen
  slot (a stable literal such as `"ariba.creneau.choisi"`), plus the Zod schema for the stored
  `{ typeId, debut }` payload and a typed read/clear helper pair. **This module is the key's only
  owner.** `agenda-booker.tsx` writes through it in Task 2, and plan 04-05's
  `connexion-form.tsx`, `inscription-form.tsx` and `parcours-reservation.tsx` read through it — a
  second literal anywhere would silently break the sign-in return path (D-09) with no type error and
  no failing build. `creneaux.ts` is client-safe, so a client island may import it directly.

Edit `src/locales/fr/agenda.json`, the **one sanctioned content edit** (D-24): replace the four-entry
`legende` with exactly three entries — `libre: "Libre"`, `indisponible: "Indisponible"`,
`passe: "Passé"`. `Réservé` and `Bloqué` move to `src/locales/fr/admin.json`, where the distinction
is useful; plan 04-08 consumes them there. **Do not touch `typesRendezVous`** — its prices and
labels are Lot 1-validated and must stay byte-identical. Do not touch `titre`, `intro`,
`moisPrecedent`, `moisSuivant` or `aucunCreneau`.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; node -e "const a=require('./src/locales/fr/agenda.json');const t=JSON.stringify(a.typesRendezVous);if(t!==JSON.stringify([{id:'decouverte',libelle:'Appel découverte',dureeMinutes:30,prix:0},{id:'individuelle',libelle:'Session individuelle',dureeMinutes:60,prix:90}]))throw new Error('typesRendezVous changed');if(Object.keys(a.legende).length!==3)throw new Error('legende must have 3 entries');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/i18n/fr.ts` exports two new formatter instances and two new `formatX` wrappers; the
      six pre-existing exports are unchanged (`git diff` touches only added lines)
    - `node --input-type=module -e "..."` rendering `2026-09-08T12:30:00Z` through the prose hour
      wrapper outputs `14 h 30`, and through the weekday-bearing wrapper outputs
      `mardi 8 septembre`
    - `src/lib/agenda/creneaux.ts` does not contain `server-only`:
      `grep -c "server-only" src/lib/agenda/creneaux.ts` returns 0
    - `src/lib/agenda/creneaux.ts` contains no `any`:
      `grep -nE "\bany\b" src/lib/agenda/creneaux.ts` returns nothing
    - `agenda.json.legende` has exactly the keys `libre`, `indisponible`, `passe`
    - `src/lib/agenda/creneaux.ts` exports `CLE_CRENEAU_CHOISI`, and it is the only occurrence of the
      literal key string in `src/`: `grep -rn "ariba.creneau.choisi" src/ --include=*.ts --include=*.tsx`
      returns exactly one line, in `creneaux.ts`
    - `agenda.json.typesRendezVous` is byte-identical to its pre-plan value
    - `npm run lint` and `npm run typecheck` exit 0
  </acceptance_criteria>
  <done>The rendering vocabulary and the client-safe slot maths exist, and the public legend is the three-state one D-24 requires.</done>
</task>

<task type="auto">
  <name>Task 2: The /agenda static shell and its client availability island</name>
  <files>src/lib/agenda/types-rendez-vous.ts, src/components/agenda/calendrier-mois.tsx, src/components/agenda/liste-creneaux.tsx, src/components/agenda/booker-skeleton.tsx, src/components/agenda/agenda-booker.tsx, src/app/agenda/page.tsx</files>
  <read_first>
    - `src/app/agenda/page.tsx` — the whole current file: the header/intro block and the
      `typesRendezVous` chooser at lines 32-63 (markup and `formatCurrency`/`formatNumber` call
      sites survive the rewrite), the maquette data and `legendeVariant` map at lines 13-30 and
      74-96 (deleted wholesale), and the `EmptyState tone="waiting"` wired to `agenda.aucunCreneau`
      at lines 98-111 (kept verbatim)
    - `src/app/a-propos/page.tsx` lines 15-17 — the `export const revalidate = 3600` static/ISR
      declaration and its `why:` comment
    - `src/components/forms/contact-form.tsx` — the in-repo client-component idiom: the
      `type Status = "idle" | …` union, the `useState` machine, the early success return, and the
      hydration-safety comment at lines 38-46 explaining why mount-time values are set in an effect
      rather than during render
    - `src/lib/supabase/client.ts` — the browser factory, its `<Database, "app">` generic and its
      schema default (no `.schema("app")` call needed)
    - `src/lib/agenda/creneaux.ts` (from Task 1) — the helpers this island consumes
    - `.planning/phases/04-agenda-et-prise-de-rendez-vous/04-UI-SPEC.md` § Surfaces and their
      states, § Color (the accent reserve list and D-U3's muted grey-out), § Spacing (44×44px touch
      targets via an `h-11` call-site class, never a component edit), and § Mobile contract
  </read_first>
  <action>
Build four components under `src/components/agenda/` and rewrite `src/app/agenda/page.tsx`.

Create `src/lib/agenda/types-rendez-vous.ts` first — `import "server-only"`, using the anonymous
cookie-free client from `src/lib/supabase/public.ts`. It exports an explicit `TypeRendezVous` type
(`id`, `libelle`, `dureeMinutes`, `tamponMinutes`, `prixCentimes`, `ordre`) and one function
selecting the `actif` rows of `app.type_rendez_vous` ordered by `ordre`, Zod-parsed at the boundary
and returned in the `QueryResult<T>` shape from `src/lib/content/queries.ts`. No `any`. On a failed
read the caller renders the existing `agenda.aucunCreneau` empty state rather than throwing — a
database hiccup must not take the shop window down.

`src/app/agenda/page.tsx` — server component, static. Keep `export const revalidate = 3600` with the
`a-propos` `why:` comment. Keep the existing header and intro markup, and keep the chooser's markup
and its `formatCurrency` / `formatNumber` call sites — but **feed the chooser from
`src/lib/agenda/types-rendez-vous.ts` instead of from `agenda.json.typesRendezVous`.**
`app.type_rendez_vous` is where the booking RPC reads the duration, the buffer and the price it
records, so it must also be where the buyer's label, duration and price come from; two sources for
one number is a recette failure and a commercial hazard. Prices arrive in **centimes** and are
divided by 100 before `formatCurrency`. Pass the resulting array down to `<AgendaBooker />` as a
prop — the island needs the labels for its own chooser and must not fetch them a second time.
This read is a cookie-free server read under `revalidate = 3600`, so the route is still prerendered:
it must stay a plain module-scope `await` in the component, with no `cookies()`, no `headers()` and
no `searchParams`. Delete `joursMaquette` and `legendeVariant` entirely. Render the three-entry legend from `agenda.legende` with `Badge variant="muted"` for
`indisponible` and `passe` — never `destructive` (D-U3): "Indisponible" is a neutral state, and a
red-tinted unavailable slot would read as a system failure. Mount `<AgendaBooker />` below the
chooser. **This file must not call `cookies()`, `headers()` or read `searchParams` at the top
level** — any of those forces the whole route dynamic and breaks D-06. If the island needs a URL
parameter it reads it itself via `useSearchParams()` under a `Suspense` boundary.

`src/components/agenda/agenda-booker.tsx` — `"use client"`. The island. It owns:
- the selected `typeId` (defaulting to the first `agenda.typesRendezVous` entry), the displayed
  month, and the selected day;
- a `useState` status machine in the `contact-form.tsx` idiom
  (`"chargement" | "pret" | "vide" | "erreur"`) — no query library, no optimistic update;
- the fetch: `createClient()` from `@/lib/supabase/client`, then
  `supabase.rpc("creneaux_libres", { p_type_id, p_du, p_au })` for the displayed month clamped to
  the D-13 horizon. Carry a code comment stating that `src/lib/supabase/public.ts` is
  `import "server-only"` and cannot be used here despite D-06's wording — the intent (anonymous
  browser read, static route) is unchanged;
- Zod-validating the returned rows through the `creneaux.ts` schema before use, and mapping any
  error to a locale key, never surfacing a raw `PostgrestError`;
- opening on `premierJourPorteur(...)` once the first response lands, never a blank month, and
  setting that mount-time state inside an effect rather than during render, per the
  `contact-form.tsx` hydration comment;
- month navigation wired to the existing `agenda.moisPrecedent` / `agenda.moisSuivant` copy;
- when a slot is chosen: persist `{ typeId, debut }` under `CLE_CRENEAU_CHOISI`, the constant
  exported by `src/lib/agenda/creneaux.ts` in Task 1 — import it, never re-type the literal — and
  navigate to `/reservation`. **Do not put the slot in
  the URL** — a `searchParams` read is exactly what makes this route dynamic. Plan 04-05 owns the
  `/reservation` side of that handoff and the sign-in return path (D-09).

`src/components/agenda/calendrier-mois.tsx` — the hand-written monthly grid. `grid-cols-7`, lundi
first, no min-width escape hatch and no horizontal scroll. Each day cell is a focusable button of at
least 44×44px via an `h-11 min-w-11` call-site class. Three visual states only, from the D-24
legend: carries slots → selectable, accent ring/fill when selected; no slots for any reason (past,
booked, blocked, holiday) → `--muted` / `text-muted-foreground`, disabled, labelled `Indisponible`;
day before today in Europe/Paris → labelled `Passé`, same muted treatment. The component receives
only the set of day keys that carry slots — it must not be told *why* a day is empty, which is how
D-23 is enforced structurally rather than by discipline.

`src/components/agenda/liste-creneaux.tsx` — the slot list for the selected day, grouped
matin / après-midi via `grouperMatinApresMidi`, showing 5–8 chips at a time rather than a flat dump.
Each chip is a `Button`/`Badge`-based control of at least 44×44px rendering the dense
`timeFormatter` register (`14:30`); the selected chip takes `Badge variant="default"` (accent). The
day heading uses the new weekday-bearing formatter (`mardi 8 septembre`). When the selected day has
no slots, render the existing `EmptyState tone="waiting"` wired verbatim to `agenda.aucunCreneau.*`.

`src/components/agenda/booker-skeleton.tsx` — a layout-holding skeleton occupying the exact
calendar + slot-list panel geometry, shown while status is `"chargement"`. Never a bare spinner and
never a content jump. It adds no new animation curve: the codebase has exactly one `cubic-bezier`
(`--ease-brand`) and this phase adds no second.

Every string comes from `src/locales/fr/agenda.json` or `src/locales/fr/common.json`. No literal
French text in any component. No hand-written `€`, `h` or weekday — everything through
`src/lib/i18n/fr.ts`. Nothing in this task edits `globals.css`, the nine `src/components/ui/*`
primitives or `src/components/layout/*`.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run build 2>&amp;1 | tee /tmp/lot4-build.log &amp;&amp; grep -E "^\s*○\s+/agenda" /tmp/lot4-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and its route table shows `○ /agenda` — static. The guard is
      `grep -E "^\s*○\s+/agenda"` on the captured build log and matches `○` only: it must **fail**
      if the line reads `ƒ /agenda`, which is the exact regression D-06 exists to prevent. Confirm
      that by temporarily adding a top-level `await cookies()` to the page, watching the command exit
      non-zero, then removing it. The build output's count of `○` routes is greater than or equal to
      the 13 recorded before this phase; no previously-static public page has become `ƒ`
    - The prerendered `.next/server/app/agenda.html` renders the two appointment types with the
      label, duration and price currently stored in `app.type_rendez_vous`. Change
      `prix_centimes` for `individuelle` to `12345`, rebuild, and confirm the page renders
      `123,45 €`; restore `9000` and rebuild. This is what proves the chooser reads the store and
      not `agenda.json`
    - `grep -rn "typesRendezVous" src/app/agenda/ src/components/agenda/` returns nothing — no
      runtime consumer reads the seed array
    - `grep -nE "cookies\(|headers\(|searchParams" src/app/agenda/page.tsx` returns nothing
    - `grep -c "supabase/public" src/components/agenda/agenda-booker.tsx` returns 0, and
      `grep -c "@/lib/supabase/client" src/components/agenda/agenda-booker.tsx` returns 1
    - `grep -c "creneaux_libres" src/components/agenda/agenda-booker.tsx` is ≥ 1
    - The prerendered `.next/server/app/agenda.html` contains `Libre`, `Indisponible` and `Passé`,
      and contains neither `Réservé` nor `Bloqué`
    - The prerendered `.next/server/app/agenda.html` contains `90,00 €` (the `currencyFormatter`
      output for the individual session) — proving no hand-written `€`
    - `grep -rnE "\bany\b" src/components/agenda/ src/app/agenda/page.tsx` returns nothing
    - `grep -rn "destructive" src/components/agenda/` returns nothing — the grey-out is `muted`
      (D-U3)
    - `git diff --stat src/app/globals.css src/components/ui/ src/components/layout/` is empty
    - `git diff package.json package-lock.json` is empty
    - `grep -rn "cubic-bezier" src/components/agenda/` returns nothing
  </acceptance_criteria>
  <done>A visitor can open /agenda, browse months, pick a day and see that day's real free slots, on a route that is still statically prerendered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Founder visual review — /agenda (D-26)</name>
  <action>
STOP. Do not start the next plan. Present the verification steps below to the founder and
wait for their ruling. This is a D-26 blocking gate: one review per surface-producing plan,
never grouped onto a final plan. If the founder reports a problem, fix it in this plan and
re-present the gate before proceeding.
  </action>
  <what-built>
    The public agenda: a static `/agenda` shell with the Lot 1 header, intro and appointment-type
    chooser preserved, a hand-written monthly calendar grid, a matin/après-midi slot list fed live
    from `app.creneaux_libres` through the browser anon client, a layout-holding skeleton, and the
    new three-state legend (Libre / Indisponible / Passé).
  </what-built>
  <how-to-verify>
    1. `npm run build`, then confirm the route table line for `/agenda` shows `○` (static).
    2. Open `/agenda` in a browser at a 375px viewport width.
    3. Confirm the calendar opens on a day that has slots — not a blank month.
    4. Confirm the legend reads exactly **Libre / Indisponible / Passé** and that "Réservé" and
       "Bloqué" appear nowhere.
    5. Confirm every unavailable cell — past days, holidays, days you have blocked — looks
       identical in muted grey. Nothing red, nothing distinguishing *why* a day is unavailable
       (D-23).
    6. Switch between "Appel découverte" and "Session individuelle" and confirm the slot list
       re-computes: the 30-minute type must offer more slots per day than the 60-minute one, and
       consecutive slots must be 45 and 75 minutes apart respectively.
    7. Confirm slot chips read `14:30` (dense register) while the day heading reads
       `mardi 8 septembre` (weekday-bearing register). Confirm prices read exactly **`90,00 €`** and
       **`0,00 €`**, with a non-breaking space before the euro sign — that is what the shared
       `currencyFormatter` produces for `fr-FR` / `EUR`, and it is the correct output. A price
       rendered as `90 €` would mean someone hand-wrote the euro sign or introduced a second
       formatter; report it if you see it.
    8. Confirm the label, duration and price shown for each appointment type are the ones configured
       in the back-office (`/admin/types-de-rendez-vous`, delivered in plan 04-09), not a second
       hard-coded copy.
    9. Confirm every day cell and slot chip is comfortably tappable (44×44px minimum) and that the
       page never scrolls sideways, down to a 320px width.
    10. Confirm the skeleton holds the panel layout during the load — no content jump, no bare
       spinner.
    11. **Product decision to rule on (research assumption A5):** the step between offered slots is
        `duration + buffer`, so a 09:00–12:00 range yields 09:00 / 09:45 / 10:30 / 11:15 for the
        30-minute call. Confirm that is the intended density, or say what it should be.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what is wrong.</resume-signal>
  <done>The founder has explicitly approved /agenda, or their reported issues have been fixed and the gate re-presented and approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (`anon`) → PostgREST | the island calls `app.creneaux_libres` with the publishable anon key; an attacker controls every parameter |
| PostgREST response → React state | untrusted JSON crosses into rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | `/agenda` availability read | mitigate | `app.creneaux_libres` returns only free instants; the calendar component is never told why a day is empty, so no reason can leak through the DOM. `anon` holds no privilege on `app.reservation` (plan 04-01) |
| T-04-14 | Information Disclosure | `NEXT_PUBLIC_SUPABASE_ANON_KEY` in the bundle | accept | The anon key is designed to ship to the browser and is already shipped by `src/lib/env/client.ts`. The service-role key stays structurally unreachable: `src/lib/supabase/service.ts` is `server-only` and this plan does not import it |
| T-04-15 | Tampering | crafted `p_du` / `p_au` from the browser | mitigate | The function clamps its own window to `[today, today + 8 weeks]` and its own 24-hour floor as SQL literals (D-13); a widened client range returns nothing extra |
| T-04-16 | Input Validation | RPC rows rendered without checking | mitigate | Rows are Zod-parsed through `src/lib/agenda/creneaux.ts` before conversion to `Date`, per CLAUDE.md's boundary rule; a malformed row yields the error state, never a thrown render |
| T-04-17 | Information Disclosure | raw `PostgrestError` shown to a visitor | mitigate | The island maps any error to a locale key from `agenda.json`; no Supabase error text, table name or column name reaches the DOM |
| T-04-18 | Denial of Service | `/agenda` silently going dynamic | mitigate | No top-level `cookies()`, `headers()` or `searchParams`; the chosen slot travels in `sessionStorage`, not the URL. Asserted by grep and by the `○ /agenda` build-table check |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies — no date library, no query library, no calendar library. `git diff package.json package-lock.json` must be empty; any install requires a blocking human legitimacy checkpoint first |
</threat_model>

<verification>
- `npm run lint && npm run typecheck && npm run build` exit 0
- The build route table shows `○ /agenda`
- `.next/server/app/agenda.html` contains `Libre`, `Indisponible`, `Passé`, `90,00 €`, and neither
  `Réservé` nor `Bloqué`
- The rendered appointment types match the rows in `app.type_rendez_vous`, not `agenda.json`
- `git diff --stat src/app/globals.css src/components/ui/ src/components/layout/ package.json package-lock.json` is empty
- The founder has signed off Task 3
</verification>

<success_criteria>
- A visitor sees a real monthly calendar and a real list of free slots, in Europe/Paris, without an
  account (AGD-02, D-09)
- Past, blocked, booked and holiday all render as one indistinguishable muted state (D-23, D-U3)
- The public legend is the three-state one (D-24)
- `/agenda` is still statically prerendered (D-06)
- No new npm dependency, no edit to the inherited design system
</success_criteria>

<output>
Create `.planning/phases/04-agenda-et-prise-de-rendez-vous/04-03-SUMMARY.md` when done.
</output>
