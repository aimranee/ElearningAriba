---
phase: 00-socle-technique-et-environnement
plan: 06
type: execute
wave: 5
depends_on: ["00-04", "00-05"]
files_modified:
  - vercel.json
  - README.md
  - docs/hebergement.md
autonomous: true
requirements: [SOCLE-01, SOCLE-02, SOCLE-03]
must_haves:
  truths:
    - "The repository carries the shape a hosting import consumes without manual guesswork — framework preset, build command, install command and Node version (SOCLE-03 local half)"
    - "A fresh clone reaches a running application with no undocumented step, including starting Docker Desktop and the local Supabase stack (SOCLE-01, D-12)"
    - "The Supabase CLI and the Vercel CLI are invoked through npx and are never assumed installed globally (D-02)"
    - "The hosted halves of SOCLE-02 and SOCLE-03 are declared as external dependencies with everything the party owning the cloud accounts needs to act (SOCLE-02, SOCLE-03)"
    - "The hosted Supabase project must be created in an EU region, and that constraint is written down before the project is created, because the region is permanent"
    - "Nothing internal appears anywhere in the repository — no day rate, no margin, no schedule, no out-of-scope lot (D-16)"
    - "Nothing was pushed, deployed or linked from this phase (D-15)"
  artifacts:
    - path: "README.md"
      provides: "git clone to running application, in French, with no undocumented step"
      min_lines: 40
    - path: "docs/hebergement.md"
      provides: "The external-dependency handover: hosted Supabase, Vercel connection, per-environment variables, branch protection"
      min_lines: 30
    - path: "vercel.json"
      provides: "Framework preset, build command and install command a hosting import consumes"
      contains: "nextjs"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "the documented copy-and-fill step"
      pattern: "\\.env\\.example"
    - from: "README.md"
      to: "npx supabase start"
      via: "the documented local database step, with the Docker Desktop prerequisite"
      pattern: "supabase start"
    - from: "vercel.json"
      to: "package.json scripts"
      via: "buildCommand npm run build, installCommand npm ci"
      pattern: "npm run build"
---

<objective>
Close the phase: give the repository the shape a hosting import consumes, write
the setup documentation that takes a fresh clone to a running application, and
declare in one place the hosted halves of SOCLE-02 and SOCLE-03 that another party
must provision.

Purpose: SOCLE-01 promises a *documented* setup. SOCLE-03's local half is the
repository configuration a Vercel import reads. Its hosted half — connecting the
repository, the per-branch preview over HTTPS, the production domain — is owned
outside this working session and must be declared, not silently skipped.

Output: `vercel.json`, `README.md`, `docs/hebergement.md`.

**No push, no deploy, no link.** No `git push`, no `npx vercel deploy`, no
`npx supabase link`, no `npx supabase db push`. This plan writes files and stops
(D-15).

**No paid-tier dependency.** Nothing documented here may require a paid Supabase
or Vercel plan; the plan tier is a decision made outside this phase.

**Nothing internal.** This repository belongs to the client. No day rate, no
margin, no internal calendar or dated schedule, no reference to anything outside
the signed lots 1–10, no named individual, and nothing framing the commercial
arrangement (D-16). `reference/` is read-only and is not modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-socle-technique-et-environnement/00-CONTEXT.md
@CLAUDE.md
@.planning/phases/00-socle-technique-et-environnement/00-04-SUMMARY.md
@.planning/phases/00-socle-technique-et-environnement/00-05-SUMMARY.md

<interfaces>
Everything this plan documents already exists and must be quoted accurately, not
reinvented:
- npm scripts `dev`, `build`, `start`, `lint`, `typecheck` (plan 00-01) and
  `db:types` (plan 00-05)
- `.env.example` with exactly four variable names and no values (plan 00-02)
- `.nvmrc` containing `22` (plan 00-04)
- `.github/workflows/ci.yml`, job `quality` (plan 00-04)
- `CONTRIBUTING.md` with the branch → pull request → gate → merge flow (plan 00-04)
- `supabase/config.toml` and one migration under `supabase/migrations/` (plan 00-03)
- Docker Desktop must be started by a human before `npx supabase start` (D-07)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give the repository the shape a hosting import consumes</name>
  <files>vercel.json</files>
  <read_first>
    - `package.json` — the exact script names and the `engines.node` field
    - `.nvmrc` as written by plan 00-04 — the single Node version pin
    - `.planning/phases/00-socle-technique-et-environnement/00-CONTEXT.md` — the `<external_dependencies>` table, SOCLE-03 row: this task delivers only "build command, Node version, framework preset, documented environment-variable names"
  </read_first>
  <action>
    Create `vercel.json` at the repository root declaring the framework preset as
    `nextjs`, the install command as `npm ci` (the lockfile is authoritative and
    must not be re-resolved on the build host), and the build command as
    `npm run build` — the same script the CI gate and a developer run, so a build
    that passes locally passes on the host.

    Do not declare a project id, an organisation id, a domain, a deployment region
    or a cron schedule. A project does not exist yet, the domain belongs to the
    client, and pinning a region or a cron in this file risks depending on a paid
    tier. Region guidance belongs in the handover document written by task 2.

    Do not create a `.vercel` directory and do not run `npx vercel link`,
    `npx vercel env` or `npx vercel deploy`. `.vercel` is already git-ignored by
    plan 00-02; confirm nothing under it exists.

    Confirm the Node version signal is coherent and singular: `.nvmrc` contains
    `22`, `package.json` `engines.node` allows 22, and `vercel.json` does not
    contradict either by declaring a different runtime.
  </action>
  <verify>
    <automated>test -f vercel.json && node -e "const c=require('./vercel.json');if(c.framework!=='nextjs')process.exit(1);if(c.buildCommand!=='npm run build')process.exit(1);if(c.installCommand!=='npm ci')process.exit(1);if(c.projectId||c.orgId||c.regions||c.crons)process.exit(1)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `vercel.json` exists at the repository root and parses as JSON
    - Its `framework` is `nextjs`, its `buildCommand` is `npm run build`, its `installCommand` is `npm ci`
    - It contains no `projectId`, no `orgId`, no `regions`, no `crons`, no `alias` and no domain string
    - `.nvmrc` contains `22` and `package.json` `engines.node` permits 22
    - No `.vercel` directory exists in the working tree
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>A hosting import reads the framework, install command, build command and Node version from the repository with nothing to guess, and no hosted resource was created or referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the external dependencies in a handover document</name>
  <files>docs/hebergement.md</files>
  <read_first>
    - `.planning/phases/00-socle-technique-et-environnement/00-CONTEXT.md` — the `<external_dependencies>` table in full, including the EU-region constraint and the paid-tier constraint
    - `.env.example` from plan 00-02 — the exact four variable names, copied verbatim
    - `CONTRIBUTING.md` from plan 00-04 — the branch-protection rule already declared there, to be cross-referenced rather than duplicated
    - `.planning/phases/00-socle-technique-et-environnement/00-03-SUMMARY.md` and `00-05-SUMMARY.md` — what the local halves already deliver
  </read_first>
  <action>
    Create `docs/hebergement.md`, in French, listing everything the party owning the
    cloud accounts must do, and nothing else. Structure it as a checklist so each
    item is either done or not done.

    Supabase (hosted half of SOCLE-02): create the project, **in an EU region**, and
    state explicitly and prominently that the region is permanent once chosen and
    cannot be moved afterwards — learner personal data outside the EU would
    undermine both the database-level isolation promised at Lot 3 and the RGPD
    compliance promised at Lot 5. Enable database, authentication and file storage.
    Then supply the project URL, the anon key and the service-role key as
    environment values. State that the repository's migrations under
    `supabase/migrations/` are applied to the hosted project by the party holding
    its credentials, and that no migration is applied to it from this phase.

    Vercel (hosted half of SOCLE-03): import this repository, confirm the framework
    preset, install command and build command the repository already declares in
    `vercel.json`, set the environment variables for the preview and production
    environments, confirm a branch push produces a preview deployment reachable
    over HTTPS, and attach the client's production domain. Note that placing the
    server region close to the Supabase EU region is preferable, and that this is a
    setting on the hosting side, not a file in this repository.

    Environment variables: reproduce the four names from `.env.example` verbatim, in
    a table with one column per environment — local, preview, production — showing
    only which environments need a value, never a value. Mark
    `SUPABASE_SERVICE_ROLE_KEY` as secret and server-only, and state that it must
    never carry a `NEXT_PUBLIC_` prefix and never be pasted into a repository file,
    a pull request or a workflow.

    Branch protection: cross-reference `CONTRIBUTING.md` rather than restating it —
    one sentence saying the `quality` job of the `CI` workflow must be required on
    `main`, and that this is the remaining half of SOCLE-05.

    Add a closing note that no step in this document may require a paid plan on
    either service.

    Write no day rate, no margin, no schedule, no date, no named individual, and no
    reference to a lot outside 1–10. Refer to the actor as "la partie qui détient
    les comptes d'hébergement" (D-16).
  </action>
  <verify>
    <automated>test -f docs/hebergement.md && grep -qi "région" docs/hebergement.md && grep -q "SUPABASE_SERVICE_ROLE_KEY" docs/hebergement.md && grep -ciE "TJM|taux journalier|marge|jours-homme|lot 1[1-8]" docs/hebergement.md | grep -qx 0 && grep -c "eyJ\|https://[a-z0-9]\{15,\}" docs/hebergement.md | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `docs/hebergement.md` exists and is written in French
    - It states that the Supabase project must be created in an EU region and that the region is permanent
    - It lists database, authentication and file storage as the three Supabase capabilities to provision
    - It lists all four variable names from `.env.example` verbatim, in a per-environment table, and contains no variable value
    - It contains no `eyJ` fragment and no long opaque URL that could be a real project URL
    - It marks `SUPABASE_SERVICE_ROLE_KEY` as secret and server-only
    - It cross-references `CONTRIBUTING.md` for the branch-protection rule and names the `quality` job
    - It states that no step may require a paid plan
    - It contains no day rate, no margin, no date, no schedule, no named individual and no reference to a lot outside 1–10
  </acceptance_criteria>
  <done>Everything this phase cannot do is written down in one place, actionable by the party holding the cloud accounts, with the irreversible EU-region constraint stated before the project is created.</done>
</task>

<task type="auto">
  <name>Task 3: Write the README that takes a fresh clone to a running application</name>
  <files>README.md</files>
  <read_first>
    - `package.json` — the exact script names, quoted verbatim, including `db:types`
    - `.env.example` from plan 00-02
    - `.nvmrc` from plan 00-04 and `CONTRIBUTING.md` from plan 00-04
    - `docs/hebergement.md` as written by task 2 — the README links to it rather than duplicating it
    - `.planning/phases/00-socle-technique-et-environnement/00-CONTEXT.md` — D-12, D-02, D-07, D-16
  </read_first>
  <action>
    Replace or create `README.md` at the repository root, in French. Its contract is
    D-12: someone who has just cloned this repository reaches a running application
    with no undocumented step.

    Cover, in order: what the project is in two sentences with no commercial
    framing; prerequisites — Node 22 as pinned in `.nvmrc`, npm, Docker Desktop
    installed **and started**, stating plainly that starting the Docker Desktop
    daemon is a manual step no command performs and that `npx supabase start` fails
    without it; then the sequence — clone, `npm ci`, copy `.env.example` to
    `.env.local`, `npx supabase start`, read the API URL and keys from
    `npx supabase status` and paste them into `.env.local`, `npm run db:types`, then
    `npm run dev`.

    State that the Supabase CLI is invoked through `npx` and is not installed
    globally (D-02).

    List every npm script with one line each: `dev`, `build`, `start`, `lint`,
    `typecheck`, `db:types`. Quote the names exactly as they appear in
    `package.json`.

    Add a short section on database migrations: new migrations are created with
    `npx supabase migration new`, applied locally with `npx supabase db reset`, and
    types are regenerated with `npm run db:types`. State that applying migrations to
    a hosted project is done by the party holding its credentials, per
    `docs/hebergement.md`, and is not done from a developer machine.

    Add a short section on environment variables pointing at `.env.example` as the
    authoritative list, stating that `.env.local` is never committed and that a
    missing variable stops the application at startup naming the variable — so an
    error mentioning a variable name means that variable is missing from
    `.env.local`, not that the application is broken.

    Link to `CONTRIBUTING.md` for the branch and pull-request flow, and to
    `docs/hebergement.md` for the hosting setup. Do not duplicate their content.

    Write no day rate, no margin, no schedule, no date, no roadmap, no phase or lot
    numbering, and no named individual (D-16).
  </action>
  <verify>
    <automated>test -f README.md && grep -q "npm ci" README.md && grep -q "\.env\.example" README.md && grep -q "supabase start" README.md && grep -q "db:types" README.md && grep -qi "docker" README.md && grep -ciE "TJM|taux journalier|marge|jours-homme|phase [0-9]|lot 1[1-8]" README.md | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at the repository root and is written in French
    - It names Node 22, npm and Docker Desktop as prerequisites and states that starting the Docker Desktop daemon is a manual step
    - It contains, in order, the commands `npm ci`, the copy of `.env.example` to `.env.local`, `npx supabase start`, `npx supabase status`, `npm run db:types` and `npm run dev`
    - It lists all six npm scripts by their exact names
    - It states that the Supabase CLI is used through `npx` and is not installed globally
    - It documents `npx supabase migration new` and `npx supabase db reset`, and contains no `supabase db push` instruction
    - It links to `CONTRIBUTING.md` and to `docs/hebergement.md` and duplicates neither
    - It contains no day rate, no margin, no date, no schedule, no phase or lot numbering, and no named individual
    - Following the README from a clean clone reaches `npm run dev` serving the application with no step invented along the way
  </acceptance_criteria>
  <done>A fresh clone reaches a running application by following the README alone, with the Docker prerequisite stated rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → client | Every file written here is delivered to the client who owns the repository |
| documentation → real credentials | A handover document is exactly where a real key gets pasted "just this once" |
| local phase → hosted infrastructure | The boundary this plan documents but must not cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-28 | Information disclosure | a real key or project URL pasted into `docs/hebergement.md` or `README.md` | mitigate | Both tasks forbid values; task 2 asserts the file contains no `eyJ` fragment and no long opaque URL, and reproduces only variable *names* in a per-environment table |
| T-00-29 | Information disclosure | internal commercial or scheduling detail in a client-owned repository | mitigate | Tasks 2 and 3 assert the absence of day rates, margins, dates, schedules, named individuals and out-of-scope lot references; the actor is named only as the party holding the hosting accounts (D-16) |
| T-00-30 | Spoofing | environment confusion — production credentials entered into the preview environment | mitigate | `docs/hebergement.md` splits the variable table by environment (local / preview / production) so each value is entered against a named target |
| T-00-31 | Elevation of privilege | `SUPABASE_SERVICE_ROLE_KEY` given a `NEXT_PUBLIC_` prefix during hosted setup | mitigate | The handover document marks it secret and server-only and states explicitly it must never carry the `NEXT_PUBLIC_` prefix |
| T-00-32 | Information disclosure | irreversible non-EU data residency | mitigate | The EU-region requirement and its permanence are stated prominently in `docs/hebergement.md` before the creation step, with the Lot 3 isolation and Lot 5 RGPD consequences named |
| T-00-33 | Tampering | an accidental deploy or link from this phase | mitigate | Task 1 forbids `vercel link`, `vercel env` and `vercel deploy`, asserts no `.vercel` directory exists, and asserts `vercel.json` carries no project or organisation id (D-15) |
| T-00-34 | Tampering | build host re-resolving dependencies | mitigate | `vercel.json` sets `installCommand` to `npm ci`, so the committed lockfile is authoritative on the build host as it is in CI |
| T-00-SC | Tampering | package installs | mitigate | This plan installs no package; the dependency tree is unchanged from plan 00-05 and remains pinned by `package-lock.json` |
</threat_model>

<verification>
- `vercel.json` parses and declares `nextjs`, `npm run build`, `npm ci`, with no project id, organisation id, region, cron or domain
- No `.vercel` directory exists
- `docs/hebergement.md` states the EU-region requirement and its permanence, lists the four variable names per environment, and contains no value, no `eyJ` fragment and no opaque URL
- `README.md` takes a clean clone through `npm ci`, `.env.local`, `npx supabase start`, `npx supabase status`, `npm run db:types` and `npm run dev`, and names the Docker Desktop prerequisite
- Neither document contains a day rate, a margin, a date, a schedule, a named individual or a lot outside 1–10
- `git status --porcelain` shows no change under `reference/`
- `git log` shows local commits only; no remote was pushed, no project linked, no deployment made
</verification>

<success_criteria>
1. The repository declares the framework preset, install command, build command and Node version a hosting import consumes
2. A fresh clone reaches a running application by following `README.md` alone
3. The hosted halves of SOCLE-02 and SOCLE-03 are declared in one actionable handover document, including the permanent EU-region constraint
4. No value, no secret and no internal detail appears in any committed file
5. Nothing was pushed, linked or deployed
</success_criteria>

<output>
Create `.planning/phases/00-socle-technique-et-environnement/00-06-SUMMARY.md` when done.
Record in it the full list of open external dependencies closing this phase:
hosted Supabase project in an EU region with database, authentication and file
storage; repository connected to the hosting side with per-branch HTTPS previews
and the production domain; branch protection making the `quality` job required on
`main`.
</output>
