---
phase: 02-site-public
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/components/sections/hero.tsx
  - src/components/sections/stats-band.tsx
  - src/components/motion/typewriter.tsx
  - src/components/motion/hero-spotlight.tsx
  - src/components/motion/magnetic.tsx
  - src/components/motion/count-up.tsx
  - src/app/page.tsx
autonomous: false
requirements: [PUB-01]
must_haves:
  truths:
    - "A visitor lands on / and reads the signed accroche, the sous-titre, both CTAs, the console visual and the ready video placeholder (PUB-01)"
    - "The H1 cycles the six signed competencies at 26 ms/char typing, 1050 ms hold, 15 ms/char erase, 140 ms between words (D-08)"
    - "The H1 never reflows while cycling — width locked to the widest word and re-locked on document.fonts.ready (D-09, AC-5)"
    - "After a tab switch the whole current word is restored, never a half-typed one (D-10)"
    - "A 600px violet radial spotlight follows the cursor inside the hero and only inside the hero (D-11)"
    - "The primary CTA translates toward the cursor by mx*.12 / my*.18-2 and snaps back on mouseleave (D-12)"
    - "The hero visual frame carries --shadow-4 at its call site (D-23, AC-2)"
    - "The figures band shows 5 modules, 17 h, 6 competences, 0 prerequisites — 17 computed by summing duree_heures from the database (D-30, PUB-13)"
    - "Under prefers-reduced-motion the H1 shows a complete word, the spotlight is absent and the magnet is inert (D-16, AC-7)"
    - "The hero and stats band read their copy from Supabase, not from landing.json (D-24, PUB-13)"
    - "No image is used anywhere in the hero — pictograms are inline SVG (D-41)"
  artifacts:
    - path: "src/components/motion/typewriter.tsx"
      provides: "width-locked, visibility-resynced H1 word cycler"
      min_lines: 60
    - path: "src/components/sections/hero.tsx"
      provides: "the maquette hero — eyebrow, H1, lead, two CTAs, chips, console frame, video placeholder"
    - path: "src/components/sections/stats-band.tsx"
      provides: "the animated figures band computed from module rows"
  key_links:
    - from: "src/components/sections/hero.tsx"
      to: "src/lib/content/queries.ts"
      via: "await getSection('hero')"
      pattern: "getSection"
    - from: "src/components/sections/hero.tsx"
      to: "src/app/globals.css"
      via: "hero visual frame shadow"
      pattern: "shadow-\\[var\\(--shadow-4\\)\\]"
---

<objective>
Deliver the first vertical slice of the landing page: the Hero and the figures band, read end
to end from Supabase, wearing the maquette's typewriter, spotlight and magnetic motion.

Purpose: this is the surface the founder judged in the maquette. If the hero does not read
as the maquette reads, nothing after it matters.
Output: `/` opens with the approved hero and the animated figures band, still statically
prerendered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-site-public/02-CONTEXT.md
@.planning/phases/02-site-public/02-UI-SPEC.md
@.planning/phases/02-site-public/02-PATTERNS.md
@.planning/phases/02-site-public/02-01-SUMMARY.md
@.planning/phases/02-site-public/02-02-SUMMARY.md
@.planning/phases/02-site-public/02-03-SUMMARY.md

<interfaces>
src/lib/content/queries.ts (plan 02-02) — `getSection(cle)`, `getSectionItems(sectionCle)`,
`getModules()`, each returning `{ ok: true; data } | { ok: false }`.
Section keys: `hero`, `competences`, `programme`.

src/lib/i18n/fr.ts — `formatNumber`, `formatHours` (added in 02-02). A hand-written " h"
suffix is forbidden (D-43).

src/components/motion/reveal.tsx (plan 02-03) — `Reveal` (server-safe wrapper, className
"reveal", optional data-d 1-5) and `RevealScope` (client, mounted in layout).

src/components/ui/button.tsx (plan 02-01) — polymorphic `render={<Link href="..." />}`;
`{...props}` spreads onto the primitive, so `data-magnetic="true"` reaches the DOM;
`data-slot="button"` is always present.

src/components/icons/pictograms.tsx — domain marks only, `pictograms` record and
`PictogramName` union. Generic marks (arrow, check, doc, play, calendar, shield) come from
`lucide-react` at the call site — the two-registry rule (D-41). No image file is permitted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typewriter, hero spotlight and magnetic islands</name>
  <read_first>
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 149-158: .hero-h1 / .tw / .caret / .tw-sizer / .sr-only; lines 145-147: #spot; lines 770-780: the hero mousemove; lines 798-868: the full typewriter and magnetic implementations)
    - src/components/motion/reveal.tsx (the island discipline from 02-03)
    - src/components/atmosphere/mesh-drift.tsx (the rAF-throttled listener pattern)
    - src/components/ui/button.tsx (the data-slot and props spread)
  </read_first>
  <action>
    Create `src/components/motion/typewriter.tsx` (`"use client"`). It takes a
    `words: readonly string[]` prop — the six signed competencies, passed down from the server
    component that read them from Supabase; the island must NOT hold a hardcoded word list. It
    renders an inline-block span with `white-space:nowrap`, containing the animated text span
    (carrying the gradient text treatment) and a 3px caret span blinking on a `steps(1)` 1.05s
    loop.

    Timings, verbatim (D-08): type 26 ms per character; hold 1050 ms at the full word; erase
    15 ms per character; 140 ms between words; the first erase fires 1400 ms after mount.
    Width lock (D-09): measure every word through a hidden absolutely-positioned sizer span,
    set the container width to `ceil(max) + 4` px, and re-run the lock on
    `document.fonts.ready` and on a 180 ms-debounced `resize`. Resync (D-10): on
    `visibilitychange` to visible, clear the pending timeout, set the text to the WHOLE current
    word, and restart the cycle from the 1050 ms hold — never resume mid-letter. Reduced motion
    (D-16): render the full current word with no typing and no caret animation. The accessible
    full sentence is always present in a visually hidden span so screen readers never read a
    partial word.

    Create `src/components/motion/hero-spotlight.tsx` (`"use client"`). It renders a 600x600
    absolutely-positioned circle, `border-radius:50%`,
    `background: radial-gradient(circle, rgba(99,91,255,.16), transparent 60%)`,
    `pointer-events:none`, `transform:translate(-50%,-50%)`, `opacity:0`, transitioning opacity
    `.4s var(--ease-brand)` (D-11). One `mousemove` listener attached to the HERO element only
    — resolved via a `data-slot="hero"` lookup or a forwarded ref, never `window` — setting
    left/top from the hero's bounding rect and opacity to 1, plus a `mouseleave` returning
    opacity to 0. Under reduced motion the component renders nothing at all (AC-7 requires the
    spotlight ABSENT, not merely still).

    Create `src/components/motion/magnetic.tsx` (`"use client"`). It renders `null` and, in one
    `useEffect`, selects `[data-slot="button"][data-magnetic="true"]` and attaches per element
    a `mousemove` setting
    `transform: translate((clientX - rect.left - rect.width/2) * 0.12 px,
    ((clientY - rect.top - rect.height/2) * 0.18 - 2) px)` plus a `mouseleave` clearing the
    inline transform (D-12). Do NOT create or target a `.btn-primary` class — it does not exist
    and adding one would fork the Button family (D-42, F-5). Early-return under reduced motion.
    Cleanup removes every listener.

    All three islands carry a `why` comment naming their decision ID. Each `useEffect` has an
    explicit cleanup. The motion budget allows exactly two global pointer listeners: the mesh
    drift (02-03) and this hero spotlight; the magnetic listener is element-scoped and attached
    only to buttons that opt in.
  </action>
  <acceptance_criteria>
    - `grep -n "26" src/components/motion/typewriter.tsx`, `grep -n "1050" ...`, `grep -n "15" ...` and `grep -n "140" ...` all match as timeout values.
    - `grep -n "document.fonts" src/components/motion/typewriter.tsx` matches and is followed by a call to the width-lock function.
    - `grep -n "visibilitychange" src/components/motion/typewriter.tsx` matches, and the handler assigns the FULL word (an assignment from `words[index]`, not a `slice`).
    - `grep -rn "SAP Ariba\|Procure-to-Pay\|Source-to-Pay" src/components/motion/typewriter.tsx` returns zero lines — the words arrive as a prop from the database read (D-25, PUB-13).
    - `grep -n "rgba(99,91,255,.16)" src/components/motion/hero-spotlight.tsx` matches and `grep -n "600" src/components/motion/hero-spotlight.tsx` matches the size.
    - `grep -n "window.addEventListener" src/components/motion/hero-spotlight.tsx` returns zero lines — the listener is on the hero element, not the window (D-11).
    - `grep -n "0.12\|\.12" src/components/motion/magnetic.tsx` and `grep -n "0.18\|\.18" src/components/motion/magnetic.tsx` both match; `grep -rn "btn-primary" src/` returns zero lines.
    - Each of the three files contains `matchMedia` and a cleanup: `for f in typewriter hero-spotlight magnetic; do grep -q matchMedia src/components/motion/$f.tsx || exit 1; done`.
    - `npm run lint` and `npx tsc --noEmit` exit 0 with zero `any`.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; ! grep -rn "btn-primary" src/ &amp;&amp; grep -n "visibilitychange" src/components/motion/typewriter.tsx &amp;&amp; ! grep -rn "Procure-to-Pay" src/components/motion/typewriter.tsx</automated>
  </verify>
  <done>Three motion islands exist with the measured timings, a width lock that survives font swap, a visibility resync, a hero-scoped spotlight and a data-attribute magnet — all inert under reduced motion.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the hero to the maquette, reading from Supabase</name>
  <read_first>
    - src/components/sections/hero.tsx (whole file — the Lot 1 hero being replaced, its per-section atmosphere mount at 31-35 and the Button render idiom at 51-57)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 386-469 — the complete hero markup: eyebrow, H1 with the sr-only full sentence, lead, two CTAs, three chips, the floating glass badges, the console frame with its bar / live row / three slots / video placeholder)
    - src/lib/content/queries.ts (the result shape)
    - src/components/ui/empty-state.tsx (the mandated error surface, D-32)
    - .planning/phases/02-site-public/02-PATTERNS.md (§2.9, §2.11)
  </read_first>
  <action>
    Rewrite `src/components/sections/hero.tsx` as an async Server Component. It calls
    `getSection("hero")` for the accroche and sous-titre and `getSectionItems("competences")`
    for the six words the typewriter cycles. On `{ ok: false }` it renders
    `EmptyState tone="error"` with `common.etats.erreurGenerique` — never a blank section
    (D-32). It must not import `src/locales/fr/landing.json` (D-24, PUB-13).

    Delete the per-section atmosphere mount entirely — the atmosphere is at the root now (D-20).

    Structure, from the maquette: a two-column grid `1.05fr .95fr` collapsing to one column
    below 1000px, section padding `clamp(7.5rem,13vw,10.5rem)` top. Left column: the `eyebrow`
    (« Formation live · SAP Ariba »), the H1 at `clamp(2.5rem,4.6vw,3.9rem)` weight 800
    `letter-spacing:-.032em` containing a visually hidden span with the complete signed
    accroche followed by an `aria-hidden` span with the `<Typewriter words={...} />` embedded
    in the sentence, the `lead`, a CTA row with the primary Button (« Démarrer ma formation »
    from `common.actions.demarrer`, `render={<Link href="/inscription" />}`,
    `data-magnetic="true"`) and the ghost Button (« Voir le programme »,
    `render={<Link href="/programme" />}`), then three chips.

    Right column: the console frame. Give the frame element `shadow-[var(--shadow-4)]` at its
    call site — one of the two required `--shadow-4` consumers (D-23, AC-2). It contains: a
    frame bar with three 10px dots and the url text `formation-sap-ariba.fr/reservation`; a
    live row naming a module read from the database (module 2, its title and its
    `duree_heures` formatted through `formatHours`); three static slot rows; and the dashed
    video placeholder carrying the signed « Emplacement vidéo » copy. The slots are
    presentational only — no booking logic, no selection state, no agenda (Lot 4 negative
    space). Add the two floating glass badges with the `bob` / `bob2` keyframes; declare those
    keyframes in globals.css with `var(--ease-brand)`, not the maquette's `ease-in-out`.

    Mount `<HeroSpotlight />` and `<Magnetic />` inside the hero, and give the hero root
    `data-slot="hero"` plus `position:relative; overflow:hidden` so the spotlight is clipped to
    it. Wrap the left-column children in `Reveal` with `data-d` 1 through 4 matching the
    maquette's stagger.

    No image, no `next/image`, no raster asset (D-41). No price anywhere (D-45).
  </action>
  <acceptance_criteria>
    - `grep -n "landing.json\|locales/fr/landing" src/components/sections/hero.tsx` returns zero lines, and `grep -n "getSection" src/components/sections/hero.tsx` matches.
    - `grep -n "shadow-\[var(--shadow-4)\]" src/components/sections/hero.tsx` matches on a line that also contains `className` — a rendered element, not a comment.
    - `grep -n "atmosphere\|mesh" src/components/sections/hero.tsx` returns zero lines (D-20).
    - `grep -n "EmptyState" src/components/sections/hero.tsx` matches — the failed-read path renders the mandated surface (D-32).
    - `grep -n 'data-slot="hero"' src/components/sections/hero.tsx` matches, and after `npm run build`, `grep -c 'data-magnetic="true"' .next/server/app/index.html` returns at least 1 — the hero's magnetic CTA reaches the landing page's prerendered HTML regardless of how the source expresses the flag.
    - `grep -n "HeroSpotlight" src/components/sections/hero.tsx` and `grep -n "Typewriter" src/components/sections/hero.tsx` both match.
    - `grep -n "use client" src/components/sections/hero.tsx` returns zero lines — the hero stays a Server Component (D-38).
    - `grep -n "next/image\|<img\|\.png\|\.jpg\|\.webp" src/components/sections/hero.tsx` returns zero lines (D-41).
    - `grep -n "€\|prix\|tarif" src/components/sections/hero.tsx` returns zero lines (D-45).
    - `npm run build` exits 0 and `/` is still listed as prerendered static.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; ! grep -n "landing.json" src/components/sections/hero.tsx &amp;&amp; grep -c 'shadow-\[var(--shadow-4)\]' .next/server/app/index.html &amp;&amp; ! grep -n "use client" src/components/sections/hero.tsx</automated>
  </verify>
  <done>The hero matches the maquette, reads its copy from Supabase, carries --shadow-4 on the console frame, and stays a statically rendered Server Component.</done>
</task>

<task type="auto">
  <name>Task 3: The figures band, computed from the module rows</name>
  <read_first>
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 190-195: .stats / .stat-num; lines 471-481: the four figures and their data-count / data-suffix; lines 870-886: the count-up IntersectionObserver)
    - src/lib/content/queries.ts (getModules and getSectionItems)
    - src/lib/i18n/fr.ts (formatNumber, formatHours)
    - src/app/page.tsx (the current Lot 1 composition — this task edits only the hero/band region)
  </read_first>
  <action>
    Create `src/components/sections/stats-band.tsx` as an async Server Component rendering a
    `Section tone="band"` with a four-column grid (two columns below 640px). The four figures
    (D-03, D-30): number of modules = `getModules()` row count; total hours =
    `sum(duree_heures)` over those rows, which must evaluate to 17 from the seeded 3+4+4+3+3
    and must be formatted through `formatHours` — never a hardcoded 17 and never a
    hand-written « h » (D-30, D-43, PUB-13); number of competences = `getSectionItems
    ("competences")` row count; and 0 prerequisites. Labels come from the database section row
    or from `common`, never from a literal in the component.

    Create the count-up as a small client island `src/components/motion/count-up.tsx`
    (`"use client"`): an `IntersectionObserver` at `threshold: 0.5` that, on first
    intersection, animates from 0 to the target over 1400 ms with the cubic ease-out
    `1 - (1 - p)^3`, then unobserves. Pass the final formatted string as a prop so the
    server-rendered HTML already contains the correct value for a crawler and for reduced
    motion. Under reduced motion, or a target of 0, render the final value immediately with no
    animation (D-16).

    In `src/app/page.tsx`, replace the Lot 1 hero region with `<Hero />` followed by
    `<StatsBand />`. Leave the remaining Lot 1 sections in place for now — plans 02-09 and
    02-10 replace them. Do not add `"use client"` to page.tsx. Set an explicit
    `export const revalidate` on the page so the route is ISR rather than dynamic (D-38).
  </action>
  <acceptance_criteria>
    - `grep -n "17" src/components/sections/stats-band.tsx` returns zero lines — the total is summed, not typed.
    - `grep -n "formatHours" src/components/sections/stats-band.tsx` matches; `grep -n "\" h\"\|' h'" src/components/sections/stats-band.tsx` returns zero lines.
    - `grep -n "getModules\|getSectionItems" src/components/sections/stats-band.tsx` matches at least twice.
    - `grep -n "threshold: 0.5" src/components/motion/count-up.tsx` and `grep -n "1400" src/components/motion/count-up.tsx` both match.
    - `grep -n "use client" src/app/page.tsx` returns zero lines; `grep -n "export const revalidate" src/app/page.tsx` matches.
    - PUB-13 proof: after `update app.content_item set publie = false where section_cle = 'programme' and cle = 'module-05';` and a rebuild, the modules figure reads 4 and the hours figure reads 14 h; restore the row afterwards.
    - `npm run build` exits 0 with `/` still prerendered.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; ! grep -n "17" src/components/sections/stats-band.tsx &amp;&amp; grep -n "formatHours" src/components/sections/stats-band.tsx &amp;&amp; ! grep -n "use client" src/app/page.tsx</automated>
  </verify>
  <done>The figures band counts up from real database rows, with the 17 h total derived by summation and formatted through the only permitted formatter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm the hero reads as the approved maquette</name>
  <read_first>
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (the whole file — open it in a browser next to the running site)
    - src/components/sections/hero.tsx (as built in Task 2)
  </read_first>
  <action>
    Pause for founder-facing review. Open the maquette HTML and the running site side by side
    and walk the reviewer through the checks below. Do not start the dev server — it is already
    running. Treat any "reads flatter than the maquette" report as a gap to fix, not a
    criterion to soften: the specific levers are the console frame shadow token
    (`--shadow-4`), the badge float offsets, the gradient on the H1's typed word, and the
    spotlight opacity. Record the reviewer's verdict verbatim in the SUMMARY, including any
    element still reported as timid.
  </action>
  <what-built>
    The hero and figures band rendered from Supabase, with the typewriter, hero spotlight,
    magnetic CTA, floating glass badges, --shadow-4 console frame and count-up figures.
  </what-built>
  <how-to-verify>
    1. Open the maquette HTML file in one browser window and `/` in another, side by side.
    2. Watch the H1 for a full cycle of all six words. The line below it must not move a single
       pixel — if the second line jumps when the word changes, the width lock failed.
    3. Switch to another tab for ten seconds, come back. The word must be complete, not
       half-typed.
    4. Move the cursor across the hero: a soft violet glow tracks it and stops at the hero's
       edges. Move the cursor over « Démarrer ma formation »: the button leans toward the
       cursor and snaps back when you leave.
    5. The console frame must sit on a deep, wide shadow — visibly deeper than the chips beside
       it. A flat frame is a failure.
    6. Scroll to the figures band: the four numbers count up once, and the second reads 17 h.
    7. Enable « Reduce motion » in the OS and reload: the H1 shows one complete word, there is
       NO glow following the cursor, the mesh does not drift, and everything is visible.
  </how-to-verify>
  <acceptance_criteria>
    - The reviewer confirms zero reflow of the H1 across a full six-word cycle.
    - The reviewer confirms the word is whole after a tab switch.
    - The reviewer confirms the console frame reads deeper than the surrounding surfaces.
    - The reviewer confirms that under reduced motion the spotlight is entirely absent, not merely still.
    - The reviewer's verdict is recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <resume-signal>Type "approved" or name what still reads flat or timid against the maquette.</resume-signal>
  <done>The hero is confirmed against the founder-approved maquette in a real browser.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; `/` prerendered static.
- `grep -rno "cubic-bezier([^)]*)" src/ | sed 's/.*cubic-bezier/cubic-bezier/' | sort -u | wc -l` returns 1.
- Exactly two global pointer listeners exist across `src/components`: mesh-drift and hero-spotlight.
</verification>

<success_criteria>
A visitor opening `/` sees the approved hero and a figures band computed from the database,
with all four hero motion behaviours at their measured values and a full reduced-motion bypass.
</success_criteria>

<output>
Create `.planning/phases/02-site-public/02-05-SUMMARY.md` when done.
</output>
