---
phase: 02-site-public
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/motion/scroll-progress.tsx
  - src/app/layout.tsx
autonomous: false
requirements: [PUB-12]
must_haves:
  truths:
    - "A visitor reaches every public page in at most two clicks from any page (PUB-12)"
    - "The header is blurred at rest and its blur, background and shadow deepen once the page is scrolled past 12px (D-18, AC-6)"
    - "A gradient scroll-progress bar tracks reading position across the top of the viewport (D-18)"
    - "The footer carries the four columns and every legal-page label from common.json (PUB-12)"
    - "The mobile menu opens and closes with correct aria-expanded and reachable focus (PUB-12)"
    - "Header links point at Lot 1 route shells only — no auth logic is added"
  artifacts:
    - path: "src/components/motion/scroll-progress.tsx"
      provides: "rAF-throttled scroll listener driving the progress bar and the header scrolled state"
    - path: "src/components/layout/header.tsx"
      provides: "blurred sticky header, desktop nav, mobile burger nav"
    - path: "src/components/layout/footer.tsx"
      provides: "four-column footer with legal labels"
  key_links:
    - from: "src/components/motion/scroll-progress.tsx"
      to: "src/components/layout/header.tsx"
      via: "data-scrolled attribute toggled on the header element"
      pattern: "data-scrolled"
---

<objective>
Deliver the site chrome: a header that is blurred at rest and deepens on scroll, a
scroll-progress indicator, and a complete footer — with navigation that puts every public
page within two clicks.

Purpose: PUB-12 is a payment-gate criterion and AC-6 / D-18 are visible in the maquette on
first paint.
Output: Chrome that works on every route, including the ones later plans render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-site-public/02-CONTEXT.md
@.planning/phases/02-site-public/02-UI-SPEC.md
@.planning/phases/02-site-public/02-PATTERNS.md
@.planning/phases/02-site-public/02-01-SUMMARY.md
@.planning/phases/02-site-public/02-03-SUMMARY.md

<interfaces>
src/locales/fr/common.json — `metadata`, `nav` (13 keys plus `menu.ouvrir` / `menu.fermer`
and `allerAuContenu`), `footer` (`baseline`, `colonnes.{formation,compte,informations}`,
`copyright` containing `{annee}`, five legal-page labels), `actions` (10 keys including
`demarrer` = « Démarrer ma formation » and `reserver` = « Réserver »), `etats`.
common.json is chrome, not content — it stays a build-time JSON import; D-24 scopes the
Supabase migration to public content only.

Placeholder substitution idiom — footer.tsx:109:
`common.footer.copyright.replace("{annee}", String(annee))`. There is no templating library.

FOCUS_RING was extracted to src/lib/utils.ts in plan 02-01; header.tsx and footer.tsx still
carry their own duplicated copies.

Existing route shells (Lot 1, no auth logic): /, /programme, /formation, /a-propos, /contact,
/connexion, /inscription, /agenda, /reservation, /espace, /paiement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scroll-progress island and header scrolled state</name>
  <read_first>
    - src/components/layout/header.tsx (whole file — the current markup, the duplicated FOCUS_RING at 5-6, the nav links at 43-51)
    - src/components/motion/reveal.tsx (the island pattern from plan 02-03)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 57-59 #progress; lines 109-135 header / header.scrolled / .hdr / .navlink / .burger / .mobile-nav; lines 757-768 the throttled scroll handler)
  </read_first>
  <action>
    Create `src/components/motion/scroll-progress.tsx` (`"use client"`, `why` comment naming
    D-18 and D-39). It renders a single fixed bar: `position:fixed; top:0; left:0; height:3px;
    width:0; z-index:60; background:linear-gradient(90deg, var(--violet), var(--blue),
    var(--mint)); box-shadow:0 1px 8px rgba(99,91,255,.5)`, `aria-hidden="true"`. In one
    `useEffect`, attach ONE `scroll` listener on `window` with `{ passive: true }`, throttled
    through `requestAnimationFrame` with a `ticking` boolean, which sets the bar width to
    `scrollY / (scrollHeight - innerHeight) * 100` percent and toggles `data-scrolled="true"`
    on the element matching `[data-slot="site-header"]` once `scrollY > 12`. Call the handler
    once on mount so a restored scroll position is correct. Cleanup removes the listener and
    cancels the pending frame. Under
    `matchMedia("(prefers-reduced-motion: reduce)").matches` the bar still tracks position
    (it is an indicator, not an animation) but its width change carries no transition.

    In `src/components/layout/header.tsx`, add `data-slot="site-header"` to the root element
    and restyle it to the maquette: `position:fixed; top:0; left:0; right:0; z-index:50`,
    `background: rgba(252,252,255,.55)`, `backdrop-filter: blur(10px) saturate(1.25)` (with
    the `-webkit-` prefix), and a transition on background, box-shadow and backdrop-filter at
    `.4s var(--ease-brand)`. Add the deepened state driven by the data attribute:
    `data-[scrolled=true]:` background `rgba(252,252,255,.82)`, `backdrop-filter: blur(20px)
    saturate(1.45)`, `box-shadow: 0 1px 0 rgba(10,37,64,.06), 0 12px 30px -24px
    rgba(10,37,64,.3)`. Header row height 76px, container `max-w-[1200px]` with 24px inline
    padding. Because the header becomes fixed, add `scroll-padding-top: 92px` and a top offset
    on the main content so nothing is hidden underneath.

    Mount `<ScrollProgress />` in `src/app/layout.tsx` beside the atmosphere and reveal
    islands. Replace header.tsx's local FOCUS_RING constant with the import from
    `@/lib/utils`.
  </action>
  <acceptance_criteria>
    - `grep -n 'data-slot="site-header"' src/components/layout/header.tsx` matches exactly once.
    - `grep -c "data-\[scrolled=true\]" src/components/layout/header.tsx` returns at least 3 (background, backdrop-filter, box-shadow), proving the header deepens rather than toggling a class with no visual delta.
    - `grep -n "backdrop-filter\|backdrop-blur" src/components/layout/header.tsx` matches on the resting state too — the header is blurred BEFORE any scroll (AC-6).
    - `grep -c "addEventListener(\"scroll\"\|addEventListener('scroll'" src/components/motion/scroll-progress.tsx` returns 1, with a matching `removeEventListener` in cleanup.
    - `grep -n "requestAnimationFrame" src/components/motion/scroll-progress.tsx` matches.
    - `grep -n "ScrollProgress" src/app/layout.tsx` matches exactly once.
    - `grep -n "FOCUS_RING" src/components/layout/header.tsx` resolves to an import from `@/lib/utils`, not a local const declaration.
    - `npm run build` exits 0 and every public route is still prerendered static.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; grep -n 'data-slot="site-header"' src/components/layout/header.tsx &amp;&amp; grep -c "data-\[scrolled=true\]" src/components/layout/header.tsx &amp;&amp; grep -n "removeEventListener" src/components/motion/scroll-progress.tsx</automated>
  </verify>
  <done>The header is blurred at rest, visibly deepens past 12px of scroll, and a gradient progress bar tracks reading position through one rAF-throttled listener.</done>
</task>

<task type="auto">
  <name>Task 2: Two-click navigation and the complete footer</name>
  <read_first>
    - src/components/layout/header.tsx (as modified in Task 1)
    - src/components/layout/footer.tsx (whole file — the existing columns and the copyright replace at line 109)
    - src/locales/fr/common.json (nav, footer, actions — the only permitted source of these strings)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 355-383 header markup; lines 705-744 footer markup; lines 904-919 the burger behaviour)
  </read_first>
  <action>
    Header desktop nav links, in order, using `common.nav` keys only (never a literal string —
    CLAUDE.md): Accueil `/`, Programme `/programme`, Formation `/formation`, À propos
    `/a-propos`, Contact `/contact`. Header actions: a ghost Button rendered as a Link to
    `/connexion` labelled from `common.nav`, and a primary Button rendered as a Link to
    `/inscription` labelled `common.actions.demarrer`, both using the polymorphic
    `render={<Link href="..." />}` idiom already used at hero.tsx:51-57. Add
    `data-magnetic="true"` to the primary header CTA so plan 02-05's magnetic island picks it
    up. Add no auth logic, no session read, no redirect — these are Lot 1 route shells (Lot 3
    owns auth).

    Add the mobile nav: a burger button below 1000px, `aria-label` from
    `common.nav.menu.ouvrir` / `.fermer` toggled with `aria-expanded`, revealing a stacked
    nav containing the five nav links plus Connexion. The toggle needs client state — put it
    in a small `"use client"` leaf beside header.tsx rather than making the whole header a
    client component. Closing the panel on link click and restoring focus to the burger are
    required.

    Footer: four columns matching `common.footer.colonnes` — the brand column with
    `footer.baseline`; « Formation » with Programme, Formation, À propos, Contact;
    « Mon compte » with Inscription `/inscription`, Connexion `/connexion`, Agenda `/agenda`,
    Mon espace `/espace`; « Informations » with the five legal-page labels from common.json.
    The five legal routes do not exist until Lot 5 — render them as non-navigating spans
    carrying the signed label, never as `href="#"` links that look clickable and go nowhere.
    Background `var(--ink)`, white text at the maquette's opacities, the bottom bar with the
    copyright through the existing `{annee}` replace and a « Prendre RDV » link to
    `/reservation`. Replace footer.tsx's local FOCUS_RING with the `@/lib/utils` import.

    Two-click reachability: from any public page, the header reaches `/`, `/programme`,
    `/formation`, `/a-propos`, `/contact`, `/connexion`, `/inscription` in one click, and the
    footer adds `/agenda`, `/espace`, `/reservation` in one click. Every public route is
    therefore one click away, satisfying PUB-12 with margin.
  </action>
  <acceptance_criteria>
    - `grep -rn "\"Accueil\"\|\"Programme\"\|\"Contact\"\|\"Connexion\"" src/components/layout/` returns zero lines — every visible string comes from `common.*`.
    - For each of `/programme /formation /a-propos /contact /connexion /inscription /agenda /espace /reservation`, `grep -rq "href=\"<route>\"" src/components/layout/` succeeds.
    - `grep -n "href=\"#\"" src/components/layout/footer.tsx` returns zero lines.
    - `grep -rc "aria-expanded" src/components/layout/` returns at least 1 and the same element carries an `aria-label` sourced from `common.nav.menu`.
    - `grep -n 'data-magnetic="true"' src/components/layout/header.tsx` matches on the primary CTA.
    - `grep -rn "createClient\|getSession\|getUser" src/components/layout/` returns zero lines — no auth logic was added.
    - `npm run lint` and `npx tsc --noEmit` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; ! grep -rn 'href="#"' src/components/layout/footer.tsx &amp;&amp; ! grep -rn "getSession\|getUser" src/components/layout/ &amp;&amp; for r in programme formation a-propos contact connexion inscription agenda espace reservation; do grep -rq "href=\"/$r\"" src/components/layout/ || exit 1; done</automated>
  </verify>
  <done>Every public route is one click from the chrome, the footer carries all four columns and the five legal labels, and the mobile menu is accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the header reads as blurred and deepening</name>
  <read_first>
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 109-116 — the two header states)
    - src/components/layout/header.tsx (as built in Tasks 1 and 2)
  </read_first>
  <action>
    Pause for founder-facing review. Present the running site at `/` and the maquette HTML
    side by side and walk the reviewer through the checks below. Do not start the dev server —
    it is already running. If the reviewer reports either header state is indistinguishable
    from the other, treat it as a gap: increase the delta between the resting values
    (`rgba(252,252,255,.55)`, `blur(10px) saturate(1.25)`, no shadow) and the scrolled values
    (`rgba(252,252,255,.82)`, `blur(20px) saturate(1.45)`, the two-part shadow) rather than
    recording the criterion as met. Record the reviewer's verdict verbatim in the SUMMARY.
  </action>
  <what-built>
    A fixed header blurred at rest, deepening its blur, background opacity and shadow past
    12px of scroll; a gradient scroll-progress bar; a four-column footer; a mobile burger nav.
  </what-built>
  <how-to-verify>
    1. Open the running site at `/`.
    2. At scroll position 0 the header must already be translucent and blurred: content behind
       it is visible but softened. A fully opaque or fully transparent header is a failure.
    3. Scroll 100px. The header must visibly change — more opaque, stronger blur, a shadow
       appearing under it. If you cannot tell the two states apart, it is a failure.
    4. Watch the top edge while scrolling: a 3px violet-to-blue-to-mint bar fills left to right.
    5. Narrow the window below 1000px: the desktop nav disappears, the burger appears, tapping
       it opens the stacked nav and `aria-expanded` flips.
    6. Compare against the maquette file rendered in a browser side by side.
  </how-to-verify>
  <acceptance_criteria>
    - The reviewer confirms the header is blurred at rest AND visibly deeper after scrolling — two distinguishable states, not one.
    - The progress bar reaches 100% at the bottom of the page.
    - The mobile nav opens, closes on link click, and the burger label switches between the two `common.nav.menu` strings.
    - The reviewer's verdict is recorded verbatim in the SUMMARY, including any section still reported as flat.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <resume-signal>Type "approved" or describe what still looks flat.</resume-signal>
  <done>The chrome is confirmed to match the maquette's two header states in a real browser, not only in a grep.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; all public routes prerendered static.
- `grep -rno "cubic-bezier([^)]*)" src/ | sed 's/.*cubic-bezier/cubic-bezier/' | sort -u | wc -l` returns 1.
</verification>

<success_criteria>
Every public page is reachable in one click from the chrome, the footer is complete, and the
header demonstrably has two states.
</success_criteria>

<output>
Create `.planning/phases/02-site-public/02-04-SUMMARY.md` when done.
</output>
