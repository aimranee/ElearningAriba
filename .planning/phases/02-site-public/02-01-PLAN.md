---
phase: 02-site-public
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/ui/card.tsx
  - src/components/ui/button.tsx
  - src/components/ui/empty-state.tsx
  - src/components/sections/section.tsx
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, PUB-06, PUB-07]
must_haves:
  truths:
    - "Every surface on the site draws from the 26academy hex palette, violet #635BFF as accent (D-01, D-02, D-05)"
    - "A card rendered with variant=raised has no visible border, sits on --shadow-3 at rest, and lifts to --shadow-4 with translateY(-7px) on hover (D-19, D-23)"
    - "The error/empty surface renders as a floating card on public routes, not a bordered flat one (D-32, resolves contradiction 2)"
    - "A section can be rendered as a --lav2 tinted band or transparent, and its header opens eyebrow -> two-sentence H2 -> lead (D-21, D-22)"
    - "grep for cubic-bezier across src/ returns exactly one distinct value; no bare transition-all remains in card.tsx or button.tsx (D-15, resolves contradiction 6)"
    - "Fonts still come from next/font; no CDN font link is introduced (D-40)"
    - "No second Button or Card implementation exists (D-42)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "26academy palette, five shadow tiers including --shadow-brand, single easing curve"
      contains: "--shadow-brand"
    - path: "src/components/ui/card.tsx"
      provides: "raised variant with hover escalation to --shadow-4"
      contains: "hover:shadow-[var(--shadow-4)]"
    - path: "src/components/sections/section.tsx"
      provides: "band tone alternation and eyebrow-carrying SectionHeader"
      contains: "eyebrow"
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "src/app/globals.css"
      via: "shadow token consumption"
      pattern: "shadow-\\[var\\(--shadow-4\\)\\]"
    - from: "src/components/ui/empty-state.tsx"
      to: "src/components/ui/card.tsx"
      via: "Card variant"
      pattern: "variant=\\{?\"raised\""
---

<objective>
Replace the Lot 1 blue/green oklch palette with the founder-approved 26academy hex token
set, add the missing `--shadow-brand` tier, and make the Lot 1 Card / Button / Section /
EmptyState families capable of the floating, banded, eyebrow-led composition the maquette
demands. This is the shared design-system foundation — the one legitimate horizontal
exception in an MVP-mode phase, because every later vertical slice renders through it.

Purpose: Lot 1's maquettes were rejected because cards sat bordered and flat on
`--shadow-1` and no section carried an eyebrow. Nothing downstream can look right until the
component families can express the maquette.
Output: A design system that makes at least eleven floating cards and zero bordered-flat
cards mechanically possible on every public page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-site-public/02-CONTEXT.md
@.planning/phases/02-site-public/02-UI-SPEC.md
@.planning/phases/02-site-public/02-PATTERNS.md
@CLAUDE.md

<interfaces>
Current exports the executor must extend, never fork (D-42):

src/components/ui/card.tsx
  cardVariants (cva) — variants.variant: default | raised | outline | success | muted
  Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, CardSkeleton
  base string contains bare `transition-all` (card.tsx:6) — a second cubic-bezier

src/components/ui/button.tsx
  buttonVariants (cva) — variants.variant: default | success | ghost | outline | ...
  Button renders @base-ui/react/button with data-slot="button", spreads {...props}
  base string contains bare `transition-all` (button.tsx:7) — a second cubic-bezier

src/components/ui/empty-state.tsx
  "use client"; EmptyState({ tone: "neutral" | "error", size }) — hardcodes Card variant="outline"
  EmptyStateTitle, EmptyStateDescription (error tone delegates to Message)

src/components/sections/section.tsx
  Section({ tone: "default" | "muted" | "atmosphere" }) — mounts atmosphere-blob per section
  SectionHeader({ title, lead }) — no eyebrow prop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the palette and complete the shadow ladder</name>
  <read_first>
    - src/app/globals.css (whole file — the :root block at 77-120 and @theme inline at 7-75)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 7-24: the authoritative :root token block)
    - .planning/phases/02-site-public/02-PATTERNS.md (§2.4 and F-11: the --success / --primary consumer inventory)
  </read_first>
  <action>
    In `src/app/globals.css`, replace the `:root` colour values with the maquette's hex set.
    Add these raw tokens verbatim to `:root`: `--ink:#0A2540; --ink-soft:#28374B;
    --muted-ink:#5C6B82; --muted2:#8A95A8; --violet:#635BFF; --violet-l:#8B85FF;
    --deep:#4F46E5; --indigo:#4338CA; --blue:#3B82F6; --sky:#38BDF8; --mint:#1FC79B;
    --amber:#FFB444; --lav:#F1F0FF; --lav2:#F6F5FF; --soft:#FBFBFE; --paper:#FCFCFF;
    --border2:#F0F2F8;` (D-05; `--muted-ink` avoids colliding with shadcn's existing
    `--muted` surface token, which the `@theme inline` `--color-muted` alias consumes).

    Then re-map the existing shadcn aliases onto the new palette so their ~15 live consumers
    keep working (F-11): `--background: var(--paper)`, `--foreground: var(--ink)`,
    `--card: #FFFFFF`, `--card-foreground: var(--ink)`, `--primary: var(--violet)`,
    `--primary-foreground: #FFFFFF`, `--secondary: var(--lav)`, `--secondary-foreground:
    var(--deep)`, `--muted: var(--lav2)`, `--muted-foreground: var(--muted-ink)`,
    `--accent: var(--lav)`, `--accent-foreground: var(--deep)`, `--border: #E9ECF3`,
    `--input: #E9ECF3`, `--ring: var(--violet)`, `--success: var(--mint)`,
    `--success-foreground: #FFFFFF`, `--success-muted: #E6FAF3`, `--destructive: #EF4444`.
    Keep `--radius: 0.625rem`. Delete the five `--chart-*` and the eight `--sidebar-*`
    values only if nothing consumes them; otherwise re-map them the same way.

    Replace the two shadow colour components with the maquette's rgba pairs so all four
    tiers match the file exactly: `--shadow-1: 0 1px 2px rgba(10,37,64,.04), 0 4px 14px -8px
    rgba(10,37,64,.18)`, `--shadow-2: 0 1px 2px rgba(10,37,64,.04), 0 24px 50px -28px
    rgba(10,37,64,.35)`, `--shadow-3: 0 1px 2px rgba(10,37,64,.05), 0 36px 74px -34px
    rgba(10,37,64,.34)`, `--shadow-4: 0 1px 2px rgba(10,37,64,.05), 0 50px 100px -50px
    rgba(10,37,64,.45)`. Declare these four in `@theme inline` where they already live and
    drop the now-unused `--shadow-contact` / `--shadow-soft` indirection if nothing else
    reads it.

    Add the missing fifth tier to `@theme inline` (F-2, D-06): `--shadow-brand: 0 30px 60px
    -24px rgba(99,91,255,.45)`.

    Keep `--ease-brand: cubic-bezier(0.16, 1, 0.3, 1)` under its current name (F-1 — the
    brief spells it `--ease`, the code is authoritative; renaming would fork the curve).
    Keep `--duration-base`, `--spacing`, and the four `--text-*` fluid type tokens untouched
    (D-07, UI-SPEC §2 "the Lot 1 fluid type scale and spacing scale stay").

    Add a display letter-spacing rule so headings match the maquette: any element using
    `font-heading` carries `letter-spacing: -0.028em`. Do not add any `@import` or `<link>`
    for a font — `next/font` is already wired in layout.tsx (D-40).

    Add a `why` comment naming D-02 and recording that `#635BFF` departs from CADR-05's
    « palette bleu / blanc / vert », that the conflict was raised and priced, and that the
    founder reaffirmed violet.
  </action>
  <acceptance_criteria>
    - `grep -c "635BFF" src/app/globals.css` returns at least 2 (the `--violet` declaration and the `--shadow-brand` tier), and `grep -n "shadow-brand" src/app/globals.css` shows the value `0 30px 60px -24px rgba(99,91,255,.45)`.
    - `grep -n "oklch" src/app/globals.css` returns zero lines inside the `:root` colour block — every colour value is hex or a `var()` onto one.
    - `grep -rn "ease-brand" src/app/globals.css` still matches; `grep -rn -- "--ease:" src/` returns nothing (the token was NOT renamed).
    - The four `--text-*` tokens and `--spacing: 0.25rem` are still present verbatim in `@theme inline`.
    - `grep -rn "fonts.googleapis\|fonts.gstatic\|@import url(" src/` returns zero lines.
    - `npx tsc --noEmit` and `npm run build` both exit 0 — proving no alias was dropped out from under a live consumer.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; grep -n "shadow-brand" src/app/globals.css &amp;&amp; ! grep -rn "fonts.googleapis" src/</automated>
  </verify>
  <done>globals.css carries the maquette palette, five shadow tiers including --shadow-brand, the untouched type/spacing scale and the single --ease-brand curve; the build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Make cards float and remove the second easing curve</name>
  <read_first>
    - src/components/ui/card.tsx (whole file — cardVariants at 6-18, CardSkeleton at 99-114)
    - src/components/ui/button.tsx (whole file — buttonVariants base at 7, default variant at 15-16)
    - .planning/phases/02-site-public/02-PATTERNS.md (§2.1, §2.2, §3.5, F-4, F-5)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 87-100: .card / .card-raised / .chip; lines 69-86: .btn / .btn-primary)
  </read_first>
  <action>
    In `src/components/ui/card.tsx`, extend the `raised` variant string in place (do not add a
    new variant, do not fork the file — D-42). `raised` becomes: transparent border,
    `bg-background`, `rounded-[22px]`, `shadow-[var(--shadow-3)]`,
    `hover:shadow-[var(--shadow-4)]`, `hover:-translate-y-[7px]`. Put the lift on `raised`
    unconditionally, NOT behind the existing `data-[interactive=true]` gate — D-19 requires
    every floating card to lift, and the base comment's "a static card must never look
    clickable" rationale is superseded here. Replace that comment with a `why` naming D-19.

    In the same file, replace the bare `transition-all` in the base cva string (card.tsx:6)
    with `transition-[transform,box-shadow,border-color,background-color]
    duration-[var(--duration-base)] ease-[var(--ease-brand)]`. Bare `transition-all` compiles
    Tailwind's default `cubic-bezier(0.4,0,0.2,1)` — a second distinct curve that fails the
    one-curve rule (D-15, resolves contradiction 6).

    In `src/components/ui/button.tsx`, apply the identical replacement to the bare
    `transition-all` at button.tsx:7. Then give the `default` variant the accent shadow from
    the maquette: `shadow-[var(--shadow-brand)]` at rest (replacing `shadow-[var(--shadow-2)]`),
    escalating on hover, and `rounded-full` to match the maquette's `border-radius:999px`
    pill buttons. Add a `data-magnetic` pass-through: no new prop is needed because
    `{...props}` already spreads onto `ButtonPrimitive`, so a call site can write
    `data-magnetic="true"` and the D-12 island will select
    `[data-slot="button"][data-magnetic="true"]`. Do NOT introduce a `.btn-primary` class —
    it does not exist and adding one would be the forked second button implementation D-42
    forbids (F-5).

    Every transition or animation you touch in either file names
    `ease-[var(--ease-brand)]` explicitly.
  </action>
  <acceptance_criteria>
    - `grep -n "transition-all" src/components/ui/card.tsx src/components/ui/button.tsx` returns zero lines.
    - `grep -rn "transition-" src/components/ui/ | grep -v "ease-\[var(--ease-brand)\]"` returns zero lines with a `transition-` utility that lacks the token.
    - `grep -rn "ease-in\|ease-out\|ease-linear\|duration-\[[0-9]" src/components/` returns zero lines.
    - `grep -n "shadow-\[var(--shadow-4)\]" src/components/ui/card.tsx` matches inside the `raised` variant string, and the same line also contains `-translate-y-\[7px\]` and is NOT prefixed by `data-\[interactive=true\]`.
    - `grep -n "shadow-\[var(--shadow-brand)\]" src/components/ui/button.tsx` matches inside the `default` variant.
    - `grep -rn "btn-primary" src/` returns zero lines.
    - Exactly one file under `src/` declares a `cardVariants` and exactly one declares a `buttonVariants`: `grep -rln "cardVariants\s*=\s*cva\|buttonVariants\s*=\s*cva" src/ | wc -l` returns 2.
  </acceptance_criteria>
  <verify>
    <automated>! grep -n "transition-all" src/components/ui/card.tsx src/components/ui/button.tsx &amp;&amp; grep -n "shadow-\[var(--shadow-4)\]" src/components/ui/card.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>`variant="raised"` renders borderless on --shadow-3 and lifts 7px to --shadow-4 on hover; both Lot 1 components name --ease-brand explicitly and no bare transition-all survives.</done>
</task>

<task type="auto">
  <name>Task 3: Add band alternation, the eyebrow, and a floating surface state</name>
  <read_first>
    - src/components/sections/section.tsx (whole file — Section at 16-44, SectionHeader at 57-70 and its doc comment at 52-56)
    - src/components/ui/empty-state.tsx (whole file — the hardcoded Card variant="outline" at line 44)
    - .planning/phases/02-site-public/02-PATTERNS.md (§2.3 and §5 contradiction 1, §2.9)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 61-67 .eyebrow/.h-sec/.grad-text/.lead; lines 137-142 .sec/.sec-band/.sec-head)
  </read_first>
  <action>
    In `src/components/sections/section.tsx`:

    Replace the `tone` union with `"default" | "band"`. `default` is fully transparent so the
    root atmosphere layer reads through it. `band` applies the maquette's gradient band:
    `linear-gradient(180deg, rgba(241,240,255,0), var(--lav2) 18%, var(--lav2) 82%,
    rgba(241,240,255,0))` (D-21). Delete the `atmosphere` tone and both per-section mounts
    (`atmosphere-blob`, `atmosphere-grain`) entirely — the atmosphere moves to the root in
    plan 02-03 (D-20, AC-3). Set the section padding to `clamp(4.5rem,9vw,7.5rem)` block and
    keep the `max-w-6xl` inner container.

    Add an `eyebrow` prop to `SectionHeader` (D-22, AC-8). The eyebrow renders as an
    inline-flex span at `0.72rem`, weight 700, `letter-spacing: 0.18em`, uppercase, colour
    `var(--deep)`, preceded by a 22px x 2px gradient rule
    (`linear-gradient(90deg, var(--violet), var(--blue))`) as a `::before`-equivalent span.
    Keep `title` as a single string in a single `<h2>` — the existing doc comment's reason
    (splitting on "." breaks on abbreviations) still holds. Add an optional
    `titleAccent` string prop rendered as a trailing `<span>` inside the same `<h2>` carrying
    the gradient text treatment (`linear-gradient(100deg, var(--violet) 0%, var(--deep) 42%,
    var(--blue) 100%)` with `background-clip:text` and transparent colour) — this is the
    maquette's "second half carries the emotional payload and takes the gradient" device.
    `lead` stays optional, `max-w-[62ch]`, colour `var(--muted-ink)`.

    In `src/components/ui/empty-state.tsx`, replace the hardcoded `variant="outline"` with
    `variant="raised"` (resolves contradiction 2). A failed database read must render a
    floating surface, because AC-1 forbids a bordered `shadow-none` card anywhere on a public
    page while D-32 mandates this exact component for the error state. Extend in place — do
    not fork a second empty-state family. Keep `tone`, `size`, the `role="alert"` on error
    tone, and the `Message` delegation in `EmptyStateDescription` unchanged. Add a `why`
    comment naming D-32 and D-19 together.

    Add `src/lib/utils.ts` export `FOCUS_RING` with the string currently duplicated in
    header.tsx:5-6 and footer.tsx:4-5, since this plan creates a third consumer class of
    interactive non-Button elements (§3.3). Do not yet rewrite header/footer to import it —
    plan 02-04 owns those files.
  </action>
  <acceptance_criteria>
    - `grep -n "atmosphere-blob\|atmosphere-grain\|tone === \"atmosphere\"" src/components/sections/section.tsx` returns zero lines.
    - `grep -n "eyebrow" src/components/sections/section.tsx` matches both a prop declaration and a rendered element.
    - `grep -n "lav2" src/components/sections/section.tsx` matches the `band` tone string.
    - `grep -n "variant=\"outline\"" src/components/ui/empty-state.tsx` returns zero lines and `grep -n "variant=\"raised\"" src/components/ui/empty-state.tsx` returns exactly one line.
    - `grep -rn "FOCUS_RING" src/lib/utils.ts` matches an exported const.
    - `npx tsc --noEmit` exits 0 — any existing call site that passed `tone="atmosphere"` or `tone="muted"` is updated in the same commit, not left dangling.
  </acceptance_criteria>
  <verify>
    <automated>! grep -n "atmosphere-blob" src/components/sections/section.tsx &amp;&amp; ! grep -n 'variant="outline"' src/components/ui/empty-state.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <done>Sections alternate transparent / --lav2 band, SectionHeader carries eyebrow + gradient accent, the atmosphere is out of Section, and the mandated error surface floats instead of sitting bordered and flat.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with the eleven existing routes still prerendered static.
- `npx tsc --noEmit` exits 0, `npm run lint` exits 0.
- `grep -rno "cubic-bezier([^)]*)" src/ | sed 's/.*cubic-bezier/cubic-bezier/' | sort -u | wc -l` returns 1.
- No new file named `*button*.tsx` or `*card*.tsx` exists under `src/components/` beyond the two Lot 1 files.
</verification>

<success_criteria>
The design system can express the maquette: floating cards that lift to --shadow-4, banded
alternating sections, eyebrow-led two-sentence headers with a gradient second half, a violet
accent carrying --shadow-brand, and exactly one easing curve in the whole codebase.
</success_criteria>

<output>
Create `.planning/phases/02-site-public/02-01-SUMMARY.md` when done.
</output>
