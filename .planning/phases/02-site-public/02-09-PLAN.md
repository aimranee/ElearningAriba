---
phase: 02-site-public
plan: 09
type: execute
wave: 4
depends_on: ["02-05", "02-06"]
files_modified:
  - src/components/sections/pour-qui.tsx
  - src/components/sections/competences.tsx
  - src/components/sections/programme-accordion.tsx
  - src/app/page.tsx
autonomous: false
requirements: [PUB-02, PUB-03, PUB-04, PUB-13]
must_haves:
  truths:
    - "A visitor sees « Pour qui est cette formation ? » with the five profiles, their pictograms and the « même sans expérience SAP » reassurance (PUB-02)"
    - "A visitor sees « Ce que vous allez apprendre » with the six competencies (PUB-03)"
    - "A visitor sees « Programme détaillé » — five modules in an accordion with durations and a working PDF button (PUB-04)"
    - "The five profil cards render borderless on --shadow-3 and lift to --shadow-4 on hover (D-19, AC-1)"
    - "Each section opens eyebrow -> two-sentence H2 -> lead, the second sentence taking the gradient (D-22, AC-8)"
    - "The three sections alternate transparent / --lav2 band against the root atmosphere (D-21)"
    - "The competency pictogram comes from a picto column, not a positional array in page.tsx (D-25)"
    - "page.tsx no longer imports landing.json for these sections (D-24, PUB-13)"
    - "Pictograms are inline SVG; no raster asset is added (D-41)"
    - "The Lot 1 accordion is reused, not forked (D-42)"
  artifacts:
    - path: "src/components/sections/pour-qui.tsx"
      provides: "five floating profil tiles from app.content_item"
    - path: "src/components/sections/competences.tsx"
      provides: "six numbered competency rows on a tinted band"
    - path: "src/components/sections/programme-accordion.tsx"
      provides: "five-module accordion plus PDF and RDV CTAs"
  key_links:
    - from: "src/components/sections/programme-accordion.tsx"
      to: "src/app/programme.pdf/route.ts"
      via: "download link"
      pattern: "programme\\.pdf"
    - from: "src/components/sections/pour-qui.tsx"
      to: "src/lib/content/queries.ts"
      via: "await getSectionItems('pour-qui')"
      pattern: "getSectionItems"
---

<objective>
Deliver landing sections two, three and four as one vertical slice: each reads its own rows
from Supabase and renders through the floating-card, banded, eyebrow-led composition.

Purpose: PUB-02, PUB-03 and PUB-04 are gate requirements, and five of the eleven required
floating cards live here.
Output: `/` gains the three sections between the figures band and the remaining Lot 1 markup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-site-public/02-CONTEXT.md
@.planning/phases/02-site-public/02-UI-SPEC.md
@.planning/phases/02-site-public/02-PATTERNS.md
@.planning/phases/02-site-public/02-05-SUMMARY.md
@.planning/phases/02-site-public/02-06-SUMMARY.md

<interfaces>
src/lib/content/queries.ts — getSection(cle), getSectionItems(sectionCle), getModules().
Section keys: `pour-qui`, `competences`, `programme`.
content_item columns: cle, titre, description, picto, duree_heures, statut, position, publie, donnees.

src/components/sections/section.tsx — Section({ tone: "default" | "band" }),
SectionHeader({ eyebrow, title, titleAccent, lead }).
src/components/ui/card.tsx — Card variant="raised": borderless, --shadow-3, lifts to
--shadow-4 with translateY(-7px) on hover.
src/components/ui/accordion.tsx — the Lot 1 accordion; --ease-brand on the chevron rotation
and the --accordion-panel-height transition. Reuse it; do not build a second one.
src/components/icons/pictograms.tsx — `pictograms` record + `PictogramName` union. The five
profil pictos exist (acheteur, category-manager, supply-chain, consultant, etudiant) and the
six competency pictos exist (ecosysteme-ariba, procure-to-pay, source-to-pay, rfq-rfp,
gestion-catalogues, contrats-workflows). Plan 02-02 moved the positional COMPETENCE_PICTOS
mapping out of page.tsx into a real `picto` column.
src/lib/i18n/fr.ts — formatHours, formatNumber.
src/components/motion/reveal.tsx — server-safe Reveal with data-d 1-5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: « Pour qui » — five floating profil tiles</name>
  <read_first>
    - src/app/page.tsx (the Lot 1 pourQui block at 45-66 and the bordered Card call sites at 54 and 74 that AC-1 requires to reach zero)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 197-211 .profils/.tile/.tile-ic; lines 483-519 the section markup and its 3+2 asymmetric grid)
    - src/components/sections/section.tsx, src/components/ui/card.tsx, src/components/icons/pictograms.tsx
  </read_first>
  <action>
    Create `src/components/sections/pour-qui.tsx`, an async Server Component rendering
    `Section tone="default"` (transparent, so the root atmosphere reads through it).
    SectionHeader takes eyebrow « Pour qui », the first sentence of the signed H2 in `title`,
    the second in `titleAccent`, and the « Même sans expérience SAP » reassurance as `lead` —
    all four from the `pour-qui` section row (D-22).

    Five profils from `getSectionItems("pour-qui")`, ordered by `position`, in the maquette's
    asymmetric six-column grid: items 1-3 span two columns each, items 4-5 span three each;
    two equal columns below 1000px, one below 640px. Each item is a `Card variant="raised"`
    at 1.7rem padding with a 52x52 rounded-16px icon tile on
    `linear-gradient(135deg, var(--violet), var(--deep))`, the white `pictograms[item.picto]`
    mark at 24px, `box-shadow: 0 12px 24px -10px rgba(99,91,255,.6)`, scaling to 1.08 and
    rotating -4deg on card hover over `.5s var(--ease-brand)`; the title at 1.08rem weight 700
    `letter-spacing:-.02em`; the description at 0.94rem in `var(--muted-ink)`.

    These five cards are five of the eleven floating surfaces AC-1 counts. Passing
    `variant="raised"` is sufficient because plan 02-01 put the lift on that variant; passing
    no variant, or `outline`, is exactly the Lot 1 failure.

    Wrap each item in `Reveal` with `data-d` from its index, 1..5. `{ ok: false }` renders
    `EmptyState tone="error"` (D-32). No image (D-41), no `any`, no `"use client"`.
  </action>
  <acceptance_criteria>
    - `grep -cE 'variant=("raised"|\{[^}]+\})' src/components/sections/pour-qui.tsx` returns at least 1; `grep -n 'variant="outline"\|variant="default"' src/components/sections/pour-qui.tsx` returns zero lines, and no `<Card>` appears without a variant.
    - `grep -n "getSectionItems" src/components/sections/pour-qui.tsx` matches; `grep -n "landing.json" src/components/sections/pour-qui.tsx` returns zero lines.
    - `grep -n "eyebrow=" src/components/sections/pour-qui.tsx` and `grep -n "titleAccent=" src/components/sections/pour-qui.tsx` both match.
    - `grep -n "pictograms\[" src/components/sections/pour-qui.tsx` matches with a subscript reading the row's `picto` field, not an array index.
    - `grep -n "shadow-none\|border-border" src/components/sections/pour-qui.tsx` returns zero lines.
    - `grep -n "use client" src/components/sections/pour-qui.tsx` returns zero lines.
    - Rendered proof after Task 3 mounts it: `curl -s http://localhost:3000 | grep -c 'data-slot="card"'` increases by exactly 5.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -cE 'variant=("raised"|\{[^}]+\})' src/components/sections/pour-qui.tsx &amp;&amp; ! grep -n 'variant="outline"' src/components/sections/pour-qui.tsx</automated>
  </verify>
  <done>Five borderless, lifting profil tiles render from database rows with pictograms resolved from a column, under an eyebrow-led two-sentence header.</done>
</task>

<task type="auto">
  <name>Task 2: « Ce que vous allez apprendre » and « Programme détaillé »</name>
  <read_first>
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 213-219 .comp/.comp-n; 521-537 the competences section; 221-237 .acc-mod/.acc-dur/.acc-plus; 539-573 the programme section and its two CTAs)
    - src/components/ui/accordion.tsx (whole file — the family to reuse, its data-slot hooks and --ease-brand transitions)
    - src/lib/i18n/fr.ts, src/components/sections/section.tsx
  </read_first>
  <action>
    Create `src/components/sections/competences.tsx`, async Server Component rendering
    `Section tone="band"` — the `--lav2` tinted band alternating against the transparent
    section above it (D-21). SectionHeader gets eyebrow « Ce que vous allez apprendre », the
    first sentence in `title`, the second in `titleAccent`. Six items from
    `getSectionItems("competences")` in a three-column grid (two below 1000px, one below
    640px). Each row: 1.15rem/1.35rem padding, `border-radius:18px`, white background,
    `shadow-[var(--shadow-2)]`, lifting `translateY(-5px)` to `--shadow-3` on hover over
    `.42s var(--ease-brand)`; a 32x32 `var(--lav)` badge in `var(--deep)` with
    `font-variant-numeric: tabular-nums` carrying the zero-padded position; the competency
    text at 0.97rem weight 600; and the row's `picto` mark from the `pictograms` record where
    one is present.

    Create `src/components/sections/programme-accordion.tsx`, async Server Component
    rendering `Section tone="default"`. SectionHeader gets eyebrow « Programme détaillé » plus
    the split two-sentence H2. Five modules from `getModules()`, rendered through the Lot 1
    `Accordion` family — no second accordion, no raw `<details>` (D-42). Each trigger: a 34x34
    gradient module badge with the zero-padded position, the title at 1.02rem weight 700, and
    a `var(--lav)` duration pill produced by `formatHours(duree_heures)`, hidden below 640px
    as the maquette does. Each panel carries the module `resume` from `donnees`. The first
    module opens by default.

    Below the accordion, a CTA row: a ghost Button rendered as a link to `/programme.pdf`
    labelled from the database `telechargerPdf` value — no longer disabled, since plan 02-08
    built the route — and a primary Button rendered as a link to `/reservation` with
    `data-magnetic="true"`.

    Both sections render `EmptyState tone="error"` on a failed read (D-32). No price (D-45),
    no image (D-41), no `any`, no `"use client"`.
  </action>
  <acceptance_criteria>
    - `grep -nE 'tone=("band"|\{[^}]+\})' src/components/sections/competences.tsx` matches and the same pattern on programme-accordion.tsx returns zero lines — the two alternate (D-21).
    - `grep -l "eyebrow=" src/components/sections/competences.tsx src/components/sections/programme-accordion.tsx` lists both files, and so does the same grep for `titleAccent=`.
    - `grep -n "details\|summary" src/components/sections/programme-accordion.tsx` returns zero JSX element lines, and `grep -n "Accordion" src/components/sections/programme-accordion.tsx` matches (D-42).
    - `grep -n "formatHours" src/components/sections/programme-accordion.tsx` matches; `grep -rn "\" h\"\|' h'" src/components/sections/` returns zero lines (D-43).
    - `grep -n "programme.pdf" src/components/sections/programme-accordion.tsx` matches on an `href`; `grep -n "disabled" src/components/sections/programme-accordion.tsx` returns zero lines.
    - `grep -nE 'data-magnetic=("true"|\{[^}]+\})' src/components/sections/programme-accordion.tsx` matches on the primary CTA (D-12).
    - `grep -rn "landing.json" src/components/sections/competences.tsx src/components/sections/programme-accordion.tsx` returns zero lines.
    - `grep -rn "transition-" src/components/sections/competences.tsx | grep -v "ease-\[var(--ease-brand)\]"` returns zero lines (D-15).
    - `grep -l "use client" src/components/sections/competences.tsx src/components/sections/programme-accordion.tsx` lists no file.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; grep -n "programme.pdf" src/components/sections/programme-accordion.tsx &amp;&amp; grep -n "Accordion" src/components/sections/programme-accordion.tsx</automated>
  </verify>
  <done>Six competencies render on a tinted band and five modules render in the reused Lot 1 accordion with formatted durations and a live PDF download.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mount the three sections and confirm the cards float</name>
  <read_first>
    - src/app/page.tsx (whole file — the Lot 1 sections being replaced and the ones left for plan 02-10)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 483-573 — the three sections in approved order and band alternation)
  </read_first>
  <action>
    In `src/app/page.tsx`, replace the Lot 1 pourQui, competences and programme blocks with
    `<PourQui />`, `<Competences />` and `<ProgrammeAccordion />`, in that order, immediately
    after `<StatsBand />`. Delete the now-unused `landing.json` reads for those three sections
    and the positional `COMPETENCE_PICTOS` array — the `picto` column replaced it. Leave the
    format, confiance and CTA/FAQ blocks in place; plan 02-10 replaces them. page.tsx stays a
    Server Component with its `export const revalidate`.
  </action>
  <what-built>
    Sections two, three and four of the landing page, rendered from Supabase, on alternating
    bands, with five floating profil cards.
  </what-built>
  <how-to-verify>
    1. Open `/` beside the maquette HTML file in a browser.
    2. Hover each of the five profil cards. Each must lift noticeably and its shadow deepen. A
       card that does not move, or that shows a hairline border, is a failure.
    3. « Ce que vous allez apprendre » sits on a faint lavender band while the sections above
       and below are transparent, with the mesh blobs visible drifting behind them.
    4. Each of the three sections opens with a small uppercase eyebrow preceded by a short
       gradient rule, then a two-sentence heading whose SECOND sentence is in gradient colour.
    5. Open the first accordion module: it expands smoothly and its pill reads « 3 h ».
    6. Click « Télécharger le programme en PDF »: a PDF opens or downloads.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -n "COMPETENCE_PICTOS" src/app/page.tsx` returns zero lines.
    - `grep -n "PourQui\|Competences\|ProgrammeAccordion" src/app/page.tsx` matches all three, in the maquette's order.
    - `curl -s http://localhost:3000 | grep -c 'data-slot="card"'` returns at least 5.
    - The reviewer confirms all five profil cards lift on hover and none shows a border.
    - The reviewer confirms exactly one of the three sections is tinted.
    - `npm run build` exits 0 with `/` still prerendered static.
    - After `npm run build`: `grep -c 'data-tone="band"' .next/server/app/index.html` returns at least 1 and `grep -c 'data-tone="default"' .next/server/app/index.html` returns at least 1 — the three mounted sections alternate banded and transparent (D-21).
    - `grep -c 'shadow-\[var(--shadow-3)\]' .next/server/app/index.html` and `grep -c 'rounded-\[22px\]' .next/server/app/index.html` each return at least 5 — the five profil tiles this plan contributes (Task 1: "These five cards are five of the eleven floating surfaces AC-1 counts").
    - `grep -c 'data-magnetic="true"' .next/server/app/index.html` returns at least 1 — the programme accordion's primary CTA (Task 2: "a primary Button rendered as a link to /reservation with data-magnetic=\"true\"").
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; ! grep -n "COMPETENCE_PICTOS" src/app/page.tsx &amp;&amp; grep -n "ProgrammeAccordion" src/app/page.tsx &amp;&amp; test "$(grep -c 'data-tone="band"' .next/server/app/index.html)" -ge 1 &amp;&amp; test "$(grep -c 'data-tone="default"' .next/server/app/index.html)" -ge 1 &amp;&amp; test "$(grep -c 'shadow-\[var(--shadow-3)\]' .next/server/app/index.html)" -ge 5 &amp;&amp; test "$(grep -c 'rounded-\[22px\]' .next/server/app/index.html)" -ge 5 &amp;&amp; test "$(grep -c 'data-magnetic="true"' .next/server/app/index.html)" -ge 1</automated>
  </verify>
  <resume-signal>Type "approved" or name the cards that still read flat.</resume-signal>
  <done>Sections two through four are mounted in order, sourced from Supabase, with five confirmed floating cards and confirmed band alternation.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; `/` prerendered static.
- `grep -n "landing.json" src/app/page.tsx` no longer matches the pourQui, competences or programme keys.
- `grep -rno "cubic-bezier([^)]*)" src/ | sed 's/.*cubic-bezier/cubic-bezier/' | sort -u | wc -l` returns 1.
</verification>

<success_criteria>
PUB-02, PUB-03 and PUB-04 are visible on the landing page, sourced from the database, with
five floating cards and a working PDF download.
</success_criteria>

<output>
Create `.planning/phases/02-site-public/02-09-SUMMARY.md` when done.
</output>
