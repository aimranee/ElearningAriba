---
phase: 02-site-public
plan: 06
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/app/programme/page.tsx
  - src/app/formation/page.tsx
  - src/app/a-propos/page.tsx
autonomous: true
requirements: [PUB-08, PUB-09, PUB-10, PUB-13]
must_haves:
  truths:
    - "A visitor reads the Programme page and finds the five modules with their pedagogical objectives, content and duration (PUB-08)"
    - "A visitor reads the Formation page and finds the format, the modalities, the concrete run of a live session and what is provided (PUB-09)"
    - "A visitor reads the À propos page and finds the trainer, their background and their SAP Ariba legitimacy (PUB-10)"
    - "None of the three pages imports a locale JSON at runtime — every string comes from Supabase (D-24, PUB-13)"
    - "Each page opens eyebrow -> two-sentence H2 -> lead through SectionHeader (D-22, AC-8)"
    - "Sections alternate transparent and --lav2 band so the root atmosphere reads against them (D-21)"
    - "A failed database read renders the error surface, never a blank page (D-32)"
    - "Every card on these pages floats — no bordered card on --shadow-1 survives (D-19, AC-1)"
    - "Durations are formatted through src/lib/i18n/fr.ts, never with a hand-written h (D-43)"
  artifacts:
    - path: "src/app/programme/page.tsx"
      provides: "the five modules developed, from app.content_item rows"
    - path: "src/app/formation/page.tsx"
      provides: "format, modalities, session run, what is provided"
    - path: "src/app/a-propos/page.tsx"
      provides: "trainer, background, legitimacy"
  key_links:
    - from: "src/app/programme/page.tsx"
      to: "src/lib/content/queries.ts"
      via: "await getSection / getSectionItems"
      pattern: "getSection"
---

<objective>
Rebuild the three internal pages as vertical slices: each reads its own content rows from
Supabase and renders them through the Section / SectionHeader / floating Card families.

Purpose: PUB-08, PUB-09 and PUB-10 are three of the thirteen gate requirements, and PUB-13
("no module, price or asset hardcoded") is only provable if these pages stop importing JSON.
Output: Three pages that change when a database row changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-site-public/02-CONTEXT.md
@.planning/phases/02-site-public/02-UI-SPEC.md
@.planning/phases/02-site-public/02-PATTERNS.md
@.planning/phases/02-site-public/02-01-SUMMARY.md
@.planning/phases/02-site-public/02-02-SUMMARY.md

<interfaces>
src/lib/content/queries.ts — `getSection(cle)`, `getSectionItems(sectionCle)`, `getModules()`,
returning `{ ok: true; data } | { ok: false }`.
Section keys seeded for these pages: `page-programme`, `page-formation`, `page-a-propos`.
The per-page arrays (`objectifs`, `contenu`, `deroule`, `fourni`) live in the `donnees` jsonb
column and are validated with Zod at the read boundary.

src/components/sections/section.tsx (plan 02-01) — `Section({ tone: "default" | "band" })`,
`SectionHeader({ eyebrow, title, titleAccent, lead })`.
src/components/ui/card.tsx — `Card variant="raised"` floats and lifts to --shadow-4 on hover.
src/components/ui/accordion.tsx — Lot 1 accordion, `--ease-brand` throughout.
src/components/ui/empty-state.tsx — `EmptyState tone="error"`, now on `variant="raised"`.
src/lib/i18n/fr.ts — `formatHours` (added in 02-02).
src/components/motion/reveal.tsx — server-safe `Reveal` wrapper with `data-d` 1-5.

Current state of these three files: each reproduces the section rhythm inline with a comment
saying Section/SectionHeader did not exist yet. That reason has expired — refactor onto the
shared shell and remove the `sr-only` h2 workaround at programme/page.tsx:33.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Programme page from the module rows</name>
  <read_first>
    - src/app/programme/page.tsx (whole file — the inline section rhythm, the Badge duration call site at line 41, the sr-only h2 at line 33)
    - src/lib/content/queries.ts
    - src/components/sections/section.tsx (the eyebrow-carrying SectionHeader)
    - src/locales/fr/programme.json (the shape now seeded into app.content_item.donnees — read for structure only, never imported by the page)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 221-237: the accordion treatment; lines 540-573: the programme section composition)
  </read_first>
  <action>
    Rewrite `src/app/programme/page.tsx` as an async Server Component. Delete the
    `import programme from "@/locales/fr/programme.json"` line — the page reads
    `getSection("page-programme")` for the eyebrow / title / titleAccent / lead and
    `getSectionItems("page-programme")` for the five modules, each carrying `titre`,
    `duree_heures`, and `donnees.objectifs[]` / `donnees.contenu[]`.

    Render one `Section tone="default"` with a `SectionHeader` whose `title` is the signed
    two-sentence H2 split at its natural break — first sentence in `title`, second in
    `titleAccent` so the second half takes the gradient (D-22). Below it, the five modules as
    `Card variant="raised"` blocks, each with a numbered badge, the module title, the
    duration rendered through `formatHours(duree_heures)`, an « Objectifs » list and a
    « Contenu » list. Wrap each card in `Reveal` with `data-d` cycling 1..5.

    Add a second `Section tone="band"` at the foot carrying the download CTA — a ghost Button
    rendered as a link to `/programme.pdf` labelled from the database `telechargerPdf` value —
    and a primary Button to `/reservation` with `data-magnetic="true"`. The PDF route is built
    in plan 02-08; link to it now, it is a plain href.

    On `{ ok: false }` from either query, render `EmptyState tone="error"` with the signed
    `common.etats.erreurGenerique` string (D-32). Never render an empty page.

    Set `export const revalidate` so the route is static/ISR (D-38). Remove the `sr-only`
    heading workaround. No price (D-45), no image (D-41), no `any` (CLAUDE.md).
  </action>
  <acceptance_criteria>
    - `grep -n "locales/fr/programme.json" src/app/programme/page.tsx` returns zero lines.
    - `grep -c "getSection" src/app/programme/page.tsx` returns at least 2.
    - `grep -n "formatHours" src/app/programme/page.tsx` matches; `grep -rn "\" h\"\|' h'\|>h<" src/app/programme/page.tsx` returns zero lines.
    - After `npm run build`, `grep -c 'shadow-\[var(--shadow-3)\]' .next/server/app/programme.html` and `grep -c 'rounded-\[22px\]' .next/server/app/programme.html` both return at least 1 — a raised card reached the prerendered page regardless of how the source expresses the variant — and `grep -n 'variant="outline"\|<Card>' src/app/programme/page.tsx` returns zero lines — no bordered-flat card (AC-1).
    - `grep -n "eyebrow=" src/app/programme/page.tsx` and `grep -n "titleAccent=" src/app/programme/page.tsx` both match (D-22).
    - `grep -n "sr-only" src/app/programme/page.tsx` returns zero lines.
    - `grep -n "EmptyState" src/app/programme/page.tsx` matches.
    - `grep -n "use client" src/app/programme/page.tsx` returns zero lines; `grep -n "export const revalidate" src/app/programme/page.tsx` matches.
    - PUB-13 proof: setting `publie = false` on one `page-programme` item and rebuilding drops that module from the rendered page.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; ! grep -n "locales/fr/programme.json" src/app/programme/page.tsx &amp;&amp; grep -n "titleAccent=" src/app/programme/page.tsx &amp;&amp; ! grep -n 'variant="outline"' src/app/programme/page.tsx</automated>
  </verify>
  <done>The Programme page renders five modules with objectives, content and formatted durations straight from Supabase, on floating cards under an eyebrow-led two-sentence header.</done>
</task>

<task type="auto">
  <name>Task 2: Formation page from the format rows</name>
  <read_first>
    - src/app/formation/page.tsx (whole file)
    - src/locales/fr/formation.json (structure reference only: titre, intro, deroule[], fourni[], prerequis, dureeAcces, modalites[] with optional statut)
    - src/components/sections/section.tsx
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 239-249: .mod-item / .mod-ic / .mod-item.futur / .tag-futur; lines 575-627: the format section)
  </read_first>
  <action>
    Rewrite `src/app/formation/page.tsx` as an async Server Component reading
    `getSection("page-formation")` and `getSectionItems("page-formation")`. Drop the
    `formation.json` import.

    Compose three sections alternating tone (D-21): a `default` section with the
    eyebrow-led header plus the modalités as a two-column grid of floating
    `Card variant="raised"` items, each with a lucide mark in a `--lav` rounded tile; a
    `band` section with « Le déroulé d'une session » as an ordered list from
    `donnees.deroule`; and a `default` section with « Ce qui est fourni » from
    `donnees.fourni`, plus the prérequis and durée d'accès lines.

    Items whose `statut` is `futur` (the « Vidéos à venir » modalité) render the maquette's
    amber « À venir » tag — `#B4771A` on `#FFF4E3`, 0.68rem, uppercase, letter-spacing .06em —
    and an amber icon tile. This is an honest status marker, not a claim; do not present a
    future feature as delivered.

    `{ ok: false }` renders `EmptyState tone="error"`. `export const revalidate` is set. No
    price, no image, no `any`, no literal French string outside the database read.
  </action>
  <acceptance_criteria>
    - `grep -n "locales/fr/formation.json" src/app/formation/page.tsx` returns zero lines.
    - After `npm run build`, `grep -c 'data-tone="band"' .next/server/app/formation.html` returns at least 1 and `grep -c 'data-tone="default"' .next/server/app/formation.html` returns at least 1 — the page alternates (D-21).
    - After `npm run build`, `grep -c 'shadow-\[var(--shadow-3)\]' .next/server/app/formation.html` and `grep -c 'rounded-\[22px\]' .next/server/app/formation.html` both return at least 1; `grep -n 'variant="outline"\|<Card>' src/app/formation/page.tsx` returns zero lines.
    - `grep -n "futur" src/app/formation/page.tsx` matches a conditional on the `statut` column, and `grep -n "B4771A\|FFF4E3" src/app/formation/page.tsx` matches the amber tag treatment.
    - `grep -n "eyebrow=" src/app/formation/page.tsx` matches.
    - `grep -n "EmptyState" src/app/formation/page.tsx` matches; `grep -n "use client" src/app/formation/page.tsx` returns zero lines.
    - `npm run build` exits 0 with `/formation` prerendered static.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; ! grep -n "locales/fr/formation.json" src/app/formation/page.tsx &amp;&amp; grep -n "eyebrow=" src/app/formation/page.tsx &amp;&amp; ! grep -n 'variant="outline"' src/app/formation/page.tsx</automated>
  </verify>
  <done>The Formation page renders the format, the modalities with their honest future marker, the session run and what is provided, all from Supabase across alternating bands.</done>
</task>

<task type="auto">
  <name>Task 3: À propos page from the trainer row</name>
  <read_first>
    - src/app/a-propos/page.tsx (whole file)
    - src/locales/fr/a-propos.json (structure reference only: titre, intro, parcours, legitimite, approche)
    - src/components/sections/section.tsx
    - src/components/icons/pictograms.tsx (the domain-mark contract — add a mark here only if the design genuinely needs a domain pictogram; generic marks come from lucide-react at the call site)
  </read_first>
  <action>
    Rewrite `src/app/a-propos/page.tsx` as an async Server Component reading
    `getSection("page-a-propos")` and `getSectionItems("page-a-propos")` (the three narrative
    blocks — parcours, légitimité, approche — seeded as items). Drop the `a-propos.json`
    import.

    Compose a `default` section with the eyebrow-led two-sentence header and the intro as
    lead, then a `band` section with the three blocks as floating
    `Card variant="raised"` surfaces, each with a lucide mark, a title from the row and the
    narrative text. Wrap in `Reveal` with `data-d` 1..3.

    Make no claim the database rows do not carry — no invented credential, no invented year
    count, no invented client name (D-25). `{ ok: false }` renders `EmptyState tone="error"`.
    `export const revalidate` is set. No image, no price, no `any`.
  </action>
  <acceptance_criteria>
    - `grep -n "locales/fr/a-propos.json" src/app/a-propos/page.tsx` returns zero lines.
    - After `npm run build`, `grep -c 'shadow-\[var(--shadow-3)\]' .next/server/app/a-propos.html` and `grep -c 'rounded-\[22px\]' .next/server/app/a-propos.html` both return at least 1; `grep -n 'variant="outline"\|<Card>' src/app/a-propos/page.tsx` returns zero lines.
    - `grep -n "eyebrow=" src/app/a-propos/page.tsx` matches.
    - The file contains no French sentence outside a JSX expression reading a database field: `grep -n "[a-zA-ZÀ-ÿ]\{4,\} [a-zA-ZÀ-ÿ]\{4,\} [a-zA-ZÀ-ÿ]\{4,\}" src/app/a-propos/page.tsx | grep -v "^\s*[0-9]*:\s*[/*]"` returns zero lines outside comments.
    - `grep -n "use client" src/app/a-propos/page.tsx` returns zero lines.
    - `npm run build` exits 0 with `/a-propos` prerendered static; `npm run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; ! grep -n "locales/fr/a-propos.json" src/app/a-propos/page.tsx &amp;&amp; ! grep -n 'variant="outline"' src/app/a-propos/page.tsx</automated>
  </verify>
  <done>The À propos page presents the trainer, their background and their legitimacy from database rows, on floating cards, with no invented credential.</done>
</task>

</tasks>

<verification>
- `grep -rn "locales/fr/\(landing\|programme\|formation\|a-propos\)\.json" src/app/` returns zero lines.
- `npm run build` exits 0; `/programme`, `/formation`, `/a-propos` all prerendered static.
- `grep -rn 'variant="outline"' src/app/` returns zero lines on public routes (AC-1).
</verification>

<success_criteria>
The three internal pages are readable, complete against PUB-08 / PUB-09 / PUB-10, sourced
entirely from Supabase, and composed of floating cards under eyebrow-led headers.
</success_criteria>

<output>
Create `.planning/phases/02-site-public/02-06-SUMMARY.md` when done.
</output>
