---
phase: 02-site-public
plan: 11
type: execute
wave: 6
depends_on: ["02-04", "02-07", "02-08", "02-10"]
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, PUB-06, PUB-07, PUB-08, PUB-09, PUB-10, PUB-11, PUB-12, PUB-13]
must_haves:
  truths:
    - "The founder-approved maquette is the authority the finished site is judged against, and where any document disagreed with it the file won (D-04)"
    - "tsc exits 0, eslint exits 0, next build exits 0, and the mock guard still exits 1 on an unresolved client-dependent key (D-48)"
    - "A codebase search for cubic-bezier returns exactly one distinct value, and no transition or animation uses ease-in, ease-out or linear (D-15, AC-4)"
    - "--shadow-4 is consumed at two rendered call sites — the hero visual frame and the final CTA — not merely declared (D-23, AC-2, resolves contradiction 5)"
    - "Zero bordered-flat cards appear on any public page, and at least eleven floating cards appear on the landing page (D-19, AC-1)"
    - "No payment provider is named anywhere in the codebase and no price appears on the public site (D-45)"
    - "PUB-13 is proven by unpublishing a row and observing the page change (AC-10)"
    - "Every public page is reachable in two clicks and the footer is complete (PUB-12, AC-12)"
    - "Work is committed atomically on the current branch with STATE.md updated, and nothing was pushed (D-46, D-47)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "phase position, decisions and metrics updated for Lot 2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PUB-01..PUB-13 marked complete in the traceability table"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/02-site-public/"
      via: "traceability table status"
      pattern: "PUB-1[0-3] \\| Phase 2"
---

<objective>
Prove the phase against its own acceptance criteria — as applied outcomes, not presence
checks — then close the payment gate cleanly.

Purpose: Lot 1 shipped twelve green plans whose maquettes were rejected on sight because every
design criterion was a presence check. This plan exists so that cannot happen twice.
Output: A verified, green, committed phase with an honest record of what was and was not
delivered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-site-public/02-CONTEXT.md
@.planning/phases/02-site-public/02-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the applied-outcome audit</name>
  <read_first>
    - .planning/phases/02-site-public/02-CONTEXT.md (the thirteen acceptance criteria in `<specifics>`)
    - .planning/phases/02-site-public/02-UI-SPEC.md (§7)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-les-maquettes-lot-1-sont-insuffisantes.md (why these criteria are written as applied outcomes)
    - src/components/ui/card.tsx, src/components/ui/button.tsx, src/app/globals.css
  </read_first>
  <action>
    Run each check below and record the literal command output in the SUMMARY. Do not soften a
    failing check into a presence check; a failing check is a gap to fix, not a criterion to
    reword.

    Single curve (AC-4, D-15): `grep -rno "cubic-bezier([^)]*)" src/ | sed 's/.*cubic-bezier/cubic-bezier/' | sort -u`
    must print exactly one line. Then `grep -rn "ease-in\|ease-out\|ease-linear\|transition-timing-function: *linear" src/`
    must return zero lines, and every `transition-` utility must be accompanied by
    `ease-[var(--ease-brand)]`.

    --shadow-4 consumed (AC-2): `grep -rn "shadow-\[var(--shadow-4)" src/` must return at least
    three lines — `src/components/sections/hero.tsx` (the console frame),
    `src/components/sections/cta-final.tsx` (the final CTA) and `src/components/ui/card.tsx`
    (the raised hover). Verify each of the first two matches sits on a line containing
    `className`, so the token is at a rendered call site rather than in a comment. Checking the
    compiled CSS bundle is NOT sufficient: Tailwind v4 `@theme inline` emits declared variables
    regardless of use, so the bundle check passes with zero call sites.

    Zero bordered-flat cards, eleven floating (AC-1): for every public route — `/`,
    `/programme`, `/formation`, `/a-propos`, `/contact` — `curl -s <route> | grep -c "shadow-none"`
    must return 0, and `curl -s http://localhost:3000 | grep -c 'data-slot="card"'` must return
    at least 11. Also `grep -rn 'variant="outline"' src/app/ src/components/sections/` must
    return zero lines.

    Atmosphere at the root (AC-3): `grep -rln "bg-fixed" src/` must return exactly one file, and
    `grep -n "AtmosphereLayer" src/app/layout.tsx` must match — the layer is mounted once, so it
    is behind all seven sections by construction.

    Eyebrow-led sections (AC-8): every file under `src/components/sections/` that renders a
    `SectionHeader` must pass `eyebrow=`. `grep -L "eyebrow=" $(grep -rl "SectionHeader" src/components/sections/)`
    must list no file.

    No price, no provider (D-45): `grep -rin "stripe\|paypal\|mollie\|paddle\|adyen\|braintree\|checkout.com" src/ supabase/`
    must return zero lines, and `grep -rn "€" src/components/sections/ src/app/` must return
    zero lines.

    No negative-space work: `grep -rn "disponibilite\|availability\|reservation_slot\|session_groupe\|attendance\|facture\|invoice" src/ supabase/migrations/20260830090000_public_content.sql`
    must return zero lines — Lots 4, 6, 7 and 9 were not started.

    Green gate (AC-13, D-48): `npx tsc --noEmit` exits 0, `npm run lint` exits 0,
    `npm run build` exits 0, and `npm run content:check` exits 1 naming at least one unresolved
    key.

    PUB-13 proof (AC-10): `grep -rn "locales/fr/landing.json" src/` returns zero lines. Then
    unpublish one row per public surface — a `pour-qui` item, a `programme` module, a
    `format-modalites` item and a `faq` item — rebuild, confirm each disappears from the
    rendered HTML and from `/programme.pdf`, then restore all four rows and rebuild again.

    Two-click navigation (AC-12): for each of `/programme /formation /a-propos /contact
    /connexion /inscription /agenda /espace /reservation`, `grep -rq "href=\"<route>\"" src/components/layout/`
    must succeed.
  </action>
  <acceptance_criteria>
    - The distinct-`cubic-bezier` count is exactly 1 and the output is recorded verbatim in the SUMMARY.
    - `grep -rn "shadow-\[var(--shadow-4)" src/` lists hero.tsx and cta-final.tsx, each on a `className` line.
    - `curl -s http://localhost:3000 | grep -c 'data-slot="card"'` returns at least 11 and `grep -c "shadow-none"` returns 0 on all five public routes.
    - `grep -rln "bg-fixed" src/` returns exactly 1.
    - `grep -L "eyebrow=" $(grep -rl "SectionHeader" src/components/sections/)` lists no file.
    - The provider, price and negative-space greps all return zero lines.
    - `npx tsc --noEmit` 0, `npm run lint` 0, `npm run build` 0, `npm run content:check` 1.
    - The four unpublish/restore round trips each produced a visible change and were reverted.
    - Any check that failed is recorded in the SUMMARY as a named gap with the failing command output — never quietly reworded.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; test "$(grep -rno 'cubic-bezier([^)]*)' src/ | sed 's/.*cubic-bezier/cubic-bezier/' | sort -u | wc -l)" = "1" &amp;&amp; ! grep -rin "stripe\|paypal\|mollie\|paddle" src/ supabase/ &amp;&amp; ! npm run content:check</automated>
  </verify>
  <done>Every one of the thirteen acceptance criteria has been checked as an applied outcome, with the literal command output recorded and any failure named as a gap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Founder-facing recette of the whole public site</name>
  <read_first>
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (the visual contract — where it and any other document disagree, this file wins, D-04)
    - .planning/phases/02-site-public/02-CONTEXT.md (the thirteen acceptance criteria)
  </read_first>
  <action>
    Pause for the founder-facing recette that closes the « à la recette des pages » payment
    gate. Open the maquette HTML beside the running site — do not start the dev server, it is
    already running — and walk the reviewer through every check in the verification steps
    below, in order. Have a real inbox open for step 4 and browser devtools open for step 5.

    Treat every "reads flatter than the maquette" report as a named gap with an owning fix, not
    as a criterion to soften. Record the reviewer's verdict verbatim in the SUMMARY, section by
    section, including anything reported as timid and how it was resolved. If the reviewer
    rejects any section, stop before Task 3 — do not update tracking or commit a phase the
    founder has not accepted.
  </action>
  <what-built>
    The complete Lot 2 public site: the landing page in its seven signed sections plus the two
    approved bands, the three internal pages, the contact form with its two emails and
    anti-spam, `/programme.pdf`, and navigation reaching every page in one click.
  </what-built>
  <how-to-verify>
    1. Open the maquette HTML and the running site side by side. Scroll both fully. The site
       must not read flatter or more timid than the maquette at any point. Where it does, name
       the section — that is the Lot 1 failure recurring. The maquette is the authority (D-04).
    2. Landing: all seven sections present in order, atmosphere visible behind every one of
       them, cards lifting on hover, the H1 cycling with zero reflow, the header deepening on
       scroll, the progress bar tracking.
    3. Visit `/programme`, `/formation`, `/a-propos`. Each is readable, complete and floating.
    4. Send a real message from `/contact` with your own address. Confirm the form shows the
       submitting state, then the success message; confirm the trainer address receives the
       notification and your address receives the acknowledgement; confirm the row exists in
       `app.contact_message`.
    5. Submit again with the hidden `societe` field filled via devtools. Confirm no new row and
       no new email.
    6. Download `/programme.pdf`. It opens, lists five modules with durations and totals 17 h.
    7. Enable OS reduce-motion and reload the landing page: the H1 shows a complete word, no
       spotlight follows the cursor, the mesh is static, every section is visible.
    8. Narrow to a phone width. Every section is usable, the burger menu works.
    9. Confirm no price and no payment provider name appears anywhere.
  </how-to-verify>
  <acceptance_criteria>
    - The reviewer confirms the site does not read flatter than the maquette in any of the seven sections.
    - A real contact submission produced exactly two emails and one stored row.
    - A honeypot submission produced zero emails and zero rows.
    - `/programme.pdf` opens and totals 17 h.
    - Under reduce-motion the spotlight is absent, not merely still.
    - The reviewer confirms no price and no provider name is visible.
    - The verdict is recorded verbatim in the SUMMARY, section by section.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <resume-signal>Type "recette OK" or list, section by section, what still reads flat.</resume-signal>
  <done>The payment gate « à la recette des pages » is demonstrably met, confirmed against the maquette in a real browser and a real inbox.</done>
</task>

<task type="auto">
  <name>Task 3: Close the phase — tracking, commits, no push</name>
  <read_first>
    - .planning/STATE.md (whole file — current position, accumulated decisions, metrics tables)
    - .planning/REQUIREMENTS.md (the traceability table rows for PUB-01..PUB-13)
    - CLAUDE.md (commit format `#<type>: <one sentence>`, no AI or phase references in messages)
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md`: mark PUB-01 through PUB-13 as delivered in the v1
    requirements checklist and set their traceability rows to Complete. Do not touch CADR or
    any other category.

    Update `.planning/STATE.md` (D-46): current position to Phase 02 complete, `stopped_at`,
    `last_activity`, the progress counters, and add to Accumulated Context the decisions this
    phase actually made — the violet accent superseding CADR-05's written palette for this lot;
    the cookieless public Supabase client existing beside the session-aware one; the
    content-table naming and the `donnees` jsonb escape hatch; `RESEND_API_KEY` optional at boot
    and strictly required at send time; Resend called over `fetch` rather than the SDK, so no
    dependency was added; the dependency-free PDF writer; the in-process rate limiter being
    per-instance and best-effort. Record as carried concerns that the two contact email bodies
    remain unsigned client-dependent copy blocked on CADR-03, and that the RGPD deletion and
    export path for `app.contact_message` is owed by Lot 5 (D-37).

    Commit atomically on the CURRENT branch using `#<type>: <one sentence>` — no AI reference,
    no phase or plan number in the message. Split unrelated work into separate commits.

    ⛔ Never run `git push` (D-47). Publishing `main` in this repository is a Vercel production
    deploy and belongs to the CIO alone. Verify before finishing that no push occurred.
  </action>
  <acceptance_criteria>
    - `grep -c "PUB-.. | Phase 2 | Complete" .planning/REQUIREMENTS.md` returns 13.
    - `.planning/STATE.md` names Phase 02 as complete and lists at least six new entries under Accumulated Context, including the unsigned contact-email copy and the Lot 5 RGPD debt.
    - `git log --format=%s -20 | grep -ic "phase\|plan\|claude"` returns 0, and every new subject matches `^#(feat|fix|refactor|chore|docs): `.
    - `git status --porcelain` is empty.
    - `git reflog --date=iso | grep -c "push"` returns 0 (D-47).
    - `git rev-parse --abbrev-ref HEAD` is not `main`.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain)" &amp;&amp; test "$(git log --format=%s -20 | grep -ic 'phase\|plan\|claude')" = "0" &amp;&amp; test "$(git rev-parse --abbrev-ref HEAD)" != "main"</automated>
  </verify>
  <done>Requirements and STATE reflect what shipped and what is owed, everything is committed atomically on the working branch, and nothing was pushed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 0, `npm run lint` 0, `npm run build` 0, `npm run content:check` 1.
- Exactly one distinct `cubic-bezier` value across `src/`.
- `--shadow-4` consumed at the hero frame and the final CTA call sites.
- Zero `shadow-none` cards on any public route; at least eleven floating cards on `/`.
- No `git push` in the reflog.
</verification>

<success_criteria>
The phase is provably complete against all thirteen applied-outcome criteria, confirmed against
the maquette in a browser and an inbox, committed atomically on the working branch, with the
unsigned email copy and the Lot 5 RGPD debt recorded rather than hidden.
</success_criteria>

<output>
Create `.planning/phases/02-site-public/02-11-SUMMARY.md` when done.
</output>
