---
phase: 02-site-public
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/globals.css
  - src/components/atmosphere/atmosphere-layer.tsx
  - src/components/atmosphere/mesh-drift.tsx
  - src/components/motion/reveal.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, PUB-06, PUB-07]
must_haves:
  truths:
    - "One atmosphere layer is mounted at the root and is visible behind all seven landing sections (D-20)"
    - "Four blurred blobs at blur(84px) with mix-blend-mode multiply drift on 24-32 s loops (D-13)"
    - "The whole mesh layer drifts +/-34 px with the cursor through exactly one rAF-throttled pointer listener (D-13, D-39)"
    - "Elements marked reveal rise from translateY(34px) over 1 s, staggered .09s per data-d step (D-14)"
    - "Under prefers-reduced-motion: reduce the mesh is static and every revealed element is visible (D-16)"
    - "No per-section cursor-reactive background exists anywhere (D-17)"
    - "The landing route is still prerendered static despite the motion (D-38, D-39)"
  artifacts:
    - path: "src/components/atmosphere/atmosphere-layer.tsx"
      provides: "the single root-mounted mesh + grid-fade atmosphere"
      min_lines: 30
    - path: "src/components/motion/reveal.tsx"
      provides: "IntersectionObserver reveal island"
    - path: "src/app/layout.tsx"
      provides: "the one and only atmosphere mount point"
      contains: "AtmosphereLayer"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/atmosphere/atmosphere-layer.tsx"
      via: "first child of body"
      pattern: "<AtmosphereLayer"
---

<objective>
Mount the atmosphere once at the root, behind everything, and build the two motion
primitives every section depends on: the mesh drift and the scroll reveal. Lot 1's
atmosphere reached 2 of 8 sections because it was mounted per section; this plan makes that
structurally impossible.

Purpose: D-20 / AC-3 is a composition rule, not an effect — the atmosphere must be a single
fixed layer the whole document reads against.
Output: A root atmosphere layer, a reveal island, and the CSS keyframes both run on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-site-public/02-CONTEXT.md
@.planning/phases/02-site-public/02-UI-SPEC.md
@.planning/phases/02-site-public/02-PATTERNS.md
@.planning/phases/02-site-public/02-01-SUMMARY.md

<interfaces>
src/app/layout.tsx — server component using Next 16's generated `LayoutProps<"/">`.
`<body className="min-h-full flex flex-col">` contains: skip link, `<Header />`,
`<main id="contenu-principal" className="flex-1">{children}</main>`, `<Footer />`.
next/font is wired at lines 8-12 (Inter -> --font-body, Plus_Jakarta_Sans -> --font-heading-face).

Client-island discipline precedent — src/components/ui/accordion.tsx:1-3: `"use client"`
plus a `why` comment; every route stays a server component, only the interactive leaf is a
client component. NO precedent exists for useEffect, requestAnimationFrame, addEventListener
cleanup, matchMedia or IntersectionObserver — zero occurrences repo-wide.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the atmosphere and reveal CSS to the maquette's values</name>
  <read_first>
    - src/app/globals.css (the @layer components block at 139-215, as left by plan 02-01)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 43-59: .bg-fixed / .mesh-layer / .mesh / @keyframes m1-m4 / .grid-fade / #progress; lines 102-107: .reveal and the data-d delays; lines 307-313: the reduced-motion block)
    - .planning/phases/02-site-public/02-PATTERNS.md (§2.4 — what currently exists and why it is insufficient)
  </read_first>
  <action>
    In `src/app/globals.css`, delete `.atmosphere-wash`, `.atmosphere-blob`,
    `.atmosphere-grain`, `.reveal-rise`, `@keyframes reveal-rise` and the three
    `.reveal-delay-*` classes. They are superseded: `blur(60px)` instead of 84, no
    `mix-blend-mode`, no drift keyframes, `translateY(20px)` instead of 34, and three
    hardcoded delays instead of a `data-d` stagger.

    Add, transcribed from the maquette:
    - `.bg-fixed` — `position:fixed; inset:0; z-index:-5; pointer-events:none;
      overflow:hidden; clip-path:inset(0)`.
    - `.mesh-layer` — `position:absolute; inset:-10%; will-change:transform;
      transition:transform 1.2s var(--ease-brand)`.
    - `.mesh` — `position:absolute; border-radius:50%; filter:blur(84px); opacity:.55;
      mix-blend-mode:multiply; will-change:transform`.
    - `@keyframes m1` through `@keyframes m4`, verbatim from maquette lines 47-50.
    - `.grid-fade` — `position:fixed; inset:0; z-index:-4; pointer-events:none`, the two
      1px `rgba(99,91,255,.045)` gradient lines at `background-size:60px 60px`, masked with
      `radial-gradient(ellipse 90% 55% at 50% 0%, #000 30%, transparent 72%)` (both the
      `-webkit-mask-image` and `mask-image` properties).
    - `.reveal` — `opacity:0; transform:translateY(34px); transition:opacity 1s
      var(--ease-brand), transform 1s var(--ease-brand)`; `.reveal.in` — `opacity:1;
      transform:none`; and five `.reveal[data-d="N"]` rules with `transition-delay` .09s,
      .18s, .27s, .36s, .45s.

    The `m1`-`m4` animations are declared at the call site in the component with an
    `ease-in-out` timing function in the maquette — replace that with `var(--ease-brand)` in
    every one of the four, because AC-4 forbids a second curve and a bare `ease-in-out`
    keyword resolves to `cubic-bezier(0.42,0,0.58,1)`.

    Extend the existing `@media (prefers-reduced-motion: reduce)` block so it also sets
    `.reveal { opacity:1; transform:none }` and `.mesh { animation:none }` — the blunt
    `animation-duration: 0.01ms` sweep alone does not satisfy AC-7 (D-16).
  </action>
  <acceptance_criteria>
    - `grep -n "blur(84px)" src/app/globals.css` matches and `grep -n "blur(60px)" src/app/globals.css` returns zero lines.
    - `grep -n "mix-blend-mode: *multiply" src/app/globals.css` matches inside the `.mesh` rule.
    - `grep -n "translateY(34px)" src/app/globals.css` matches inside `.reveal`; `grep -n "translateY(20px)" src/app/globals.css` returns zero lines.
    - `grep -c 'reveal\[data-d=' src/app/globals.css` returns 5.
    - `grep -n "ease-in-out\|ease-in \|ease-out\| linear" src/app/globals.css` returns zero lines; every `animation` and `transition` in the file names `var(--ease-brand)`.
    - The reduced-motion block contains both `.reveal` and `.mesh` overrides: `grep -A12 "prefers-reduced-motion" src/app/globals.css | grep -c "reveal\|mesh"` returns at least 2.
    - `grep -n "atmosphere-blob\|atmosphere-wash\|reveal-delay-" src/app/globals.css` returns zero lines.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "blur(84px)" src/app/globals.css &amp;&amp; ! grep -n "ease-in-out" src/app/globals.css &amp;&amp; ! grep -rn "reveal-rise\|atmosphere-blob\|reveal-delay-" src/ &amp;&amp; npm run build</automated>
  </verify>
  <done>globals.css carries the maquette's mesh, grid-fade and 34px reveal primitives, all on the single curve, with a real reduced-motion bypass.</done>
</task>

<task type="auto">
  <name>Task 2: Build the root atmosphere layer and its cursor drift island</name>
  <read_first>
    - src/app/globals.css (the classes written in Task 1)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 316-326: the .bg-fixed markup with the four inline-styled meshes; lines 782-796: the throttled mesh drift listener)
    - src/components/ui/accordion.tsx (lines 1-20: the "use client" + why-comment discipline)
  </read_first>
  <action>
    Create `src/components/atmosphere/atmosphere-layer.tsx` as a SERVER component (no
    `"use client"`). It renders, all `aria-hidden="true"`: a `div.bg-fixed` containing a
    `div.mesh-layer` with the four `.mesh` spans at the maquette's exact geometry —
    560x560 `#CFC9FF` at `top:-8%; left:-6%` on `m1 24s`; 520x520 `#C5E4FF` at `top:12%;
    right:-9%` on `m2 28s`; 440x440 `#CFF6E7` at `bottom:-6%; left:8%` on `m3 30s`;
    480x480 `#E7DBFF` at `bottom:4%; right:2%` on `m4 32s` — followed by a sibling
    `div.grid-fade`. Each animation is declared `infinite` with `var(--ease-brand)`.
    Give the mesh-layer element a stable hook: `data-slot="mesh-layer"`.

    Create `src/components/atmosphere/mesh-drift.tsx` as the client island (`"use client"`)
    with a `why` comment naming D-13 and D-39. It renders nothing (returns `null`). In a
    single `useEffect`: early-return if
    `window.matchMedia("(prefers-reduced-motion: reduce)").matches` (D-16); otherwise select
    `[data-slot="mesh-layer"]`, attach ONE `mousemove` listener on `window` with
    `{ passive: true }` that computes `mx = (clientX / innerWidth - 0.5) * 34` and
    `my = (clientY / innerHeight - 0.5) * 34`, and applies
    `transform: translate(mx px, my px)` inside a `requestAnimationFrame` guarded by a
    `pending` boolean so at most one frame is queued. Return a cleanup that removes the
    listener and cancels any pending frame. This is one of exactly two pointer listeners the
    motion budget allows (the other is the hero spotlight in plan 02-05) — do not add a
    third, and do not add any per-section cursor-reactive background (D-17).

    Mount both in `src/app/layout.tsx` as the FIRST children of `<body>`, before the skip
    link: `<AtmosphereLayer />` then `<MeshDrift />`. Because `<body>` is
    `min-h-full flex flex-col`, the layer must be `position:fixed` (it already is, via
    `.bg-fixed`) so it sits behind all seven sections rather than participating in the flex
    column (D-20). Keep `LayoutProps<"/">` — do not hand-write layout props. Do not add
    `"use client"` to layout.tsx.

    No section, page or component anywhere else may mount an atmosphere element — this is
    the single mount point (AC-3).
  </action>
  <acceptance_criteria>
    - `grep -rln "bg-fixed" src/` returns exactly one file: `src/components/atmosphere/atmosphere-layer.tsx`.
    - `grep -rln "mesh-layer\|class=\"mesh\|className=\"mesh" src/components src/app | wc -l` returns at most 2 (the layer and the drift island).
    - `grep -c "use client" src/components/atmosphere/atmosphere-layer.tsx` returns 0 and `grep -c "use client" src/components/atmosphere/mesh-drift.tsx` returns 1.
    - `grep -n "AtmosphereLayer" src/app/layout.tsx` matches and the mount appears before the skip-link anchor in the file: `grep -n "AtmosphereLayer\|contenu-principal" src/app/layout.tsx` lists AtmosphereLayer on a lower line number.
    - `grep -c "matchMedia" src/components/atmosphere/mesh-drift.tsx` returns at least 1 and the effect early-returns on a match.
    - `grep -rc "addEventListener(\"mousemove\"\|addEventListener('mousemove'" src/components/atmosphere/mesh-drift.tsx` returns 1, and the same file contains a matching `removeEventListener` in the effect cleanup.
    - `grep -c "requestAnimationFrame" src/components/atmosphere/mesh-drift.tsx` returns at least 1.
    - `npm run build` exits 0 and the build output still marks `/` and the other public routes as static (`○` / prerendered), not dynamic.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; test "$(grep -rln 'bg-fixed' src/ | wc -l)" = "1" &amp;&amp; grep -n "AtmosphereLayer" src/app/layout.tsx &amp;&amp; grep -n "removeEventListener" src/components/atmosphere/mesh-drift.tsx</automated>
  </verify>
  <done>Exactly one atmosphere layer exists, mounted at the root behind every section, drifting with the cursor through a single rAF-throttled listener that stops dead under reduced motion.</done>
</task>

<task type="auto">
  <name>Task 3: Build the reveal-on-scroll island</name>
  <read_first>
    - src/app/globals.css (the .reveal / .reveal.in / data-d rules from Task 1)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 751-755: the IntersectionObserver with threshold 0.12 and rootMargin "0px 0px -8% 0px")
    - src/components/atmosphere/mesh-drift.tsx (the island pattern established in Task 2)
  </read_first>
  <action>
    Create `src/components/motion/reveal.tsx` (`"use client"`, `why` comment naming D-14 and
    D-16). Export a `RevealScope` island that renders `null` and, in one `useEffect`,
    observes every `.reveal` in the document with an `IntersectionObserver` configured
    `{ threshold: 0.12, rootMargin: "0px 0px -8% 0px" }`, adding the class `in` on
    intersection and unobserving that element immediately. Under
    `matchMedia("(prefers-reduced-motion: reduce)").matches`, skip the observer entirely and
    add `in` to every `.reveal` synchronously so nothing stays invisible (D-16, AC-7). Return
    a cleanup that disconnects the observer.

    Also export a `Reveal` server-safe wrapper component that is NOT a client component: a
    plain function returning a `<div>` (or the element named by an `as` prop) carrying
    `className="reveal"` and an optional `data-d` in 1-5. Sections use `Reveal` freely inside
    Server Components; only `RevealScope` is client. Mount `<RevealScope />` once in
    `src/app/layout.tsx` beside `<MeshDrift />`.

    Do not turn any page or section into a client component to get reveals — that is the
    D-38 violation the island pattern exists to prevent.
  </action>
  <acceptance_criteria>
    - `grep -c "use client" src/components/motion/reveal.tsx` returns 1, and the exported `Reveal` wrapper contains no hook call: `grep -n "useEffect\|useState" src/components/motion/reveal.tsx` matches only inside `RevealScope`.
    - `grep -n "threshold: 0.12" src/components/motion/reveal.tsx` and `grep -n '"0px 0px -8% 0px"' src/components/motion/reveal.tsx` both match.
    - `grep -n "matchMedia" src/components/motion/reveal.tsx` matches and the reduced-motion branch adds `in` to every `.reveal`.
    - `grep -n "disconnect()" src/components/motion/reveal.tsx` matches inside the effect cleanup.
    - `grep -n "RevealScope" src/app/layout.tsx` matches exactly once.
    - `grep -rln "use client" src/app/*/page.tsx src/app/page.tsx` returns zero files.
    - `npm run build` exits 0 and every public route is still listed as prerendered static.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; grep -n "threshold: 0.12" src/components/motion/reveal.tsx &amp;&amp; ! grep -rln "use client" src/app/page.tsx src/app/*/page.tsx</automated>
  </verify>
  <done>Elements marked reveal rise 34px on scroll with a .09s per-step stagger, are simply visible under reduced motion, and no route became a client component to get it.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; `/`, `/programme`, `/formation`, `/a-propos`, `/contact` remain statically prerendered.
- `grep -rno "cubic-bezier([^)]*)" src/ | sed 's/.*cubic-bezier/cubic-bezier/' | sort -u | wc -l` returns 1.
- Exactly two `mousemove` listeners will exist in the finished phase; after this plan there is exactly one: `grep -rc "mousemove" src/components | grep -v ":0"` lists only mesh-drift.tsx.
</verification>

<success_criteria>
One fixed atmosphere layer reads behind the whole document, drifting with the cursor at
one rAF-throttled listener, with a real reduced-motion bypass and no route turned client.
</success_criteria>

<output>
Create `.planning/phases/02-site-public/02-03-SUMMARY.md` when done.
</output>
