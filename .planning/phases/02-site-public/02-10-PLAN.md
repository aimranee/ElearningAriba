---
phase: 02-site-public
plan: 10
type: execute
wave: 5
depends_on: ["02-09"]
files_modified:
  - src/components/sections/format-modalites.tsx
  - src/components/sections/confiance.tsx
  - src/components/sections/cta-final.tsx
  - src/components/sections/faq.tsx
  - src/app/page.tsx
autonomous: false
requirements: [PUB-05, PUB-06, PUB-07, PUB-13]
must_haves:
  truths:
    - "A visitor sees « Format et modalités » — the six repères including live training, PDF supports, videos to come, access duration, end-of-module practical cases and prerequisites (PUB-05)"
    - "A visitor sees « Confiance et sécurité » — the three faits plus marked placeholders for témoignages and logos (PUB-06)"
    - "A visitor sees a final CTA and a FAQ of seven questions (PUB-07)"
    - "Témoignages and logos render as marked placeholders, never as invented reviews or fake logos (D-31)"
    - "The final CTA carries --shadow-4 at its call site — the second required consumer (D-23, AC-2)"
    - "The three faits and the six format items render as floating cards, bringing the landing total to at least eleven (D-19, AC-1)"
    - "All seven landing sections now open eyebrow -> two-sentence H2 -> lead (D-22, AC-8)"
    - "page.tsx imports no locale JSON for landing content — every string comes from Supabase (D-24, PUB-13)"
    - "No price and no payment provider name appears anywhere on the landing page (D-45)"
  artifacts:
    - path: "src/components/sections/confiance.tsx"
      provides: "three floating trust facts plus two honest placeholders"
    - path: "src/components/sections/cta-final.tsx"
      provides: "the gradient final CTA on --shadow-4 + --shadow-brand"
    - path: "src/components/sections/faq.tsx"
      provides: "seven FAQ entries in the Lot 1 accordion"
  key_links:
    - from: "src/components/sections/cta-final.tsx"
      to: "src/app/globals.css"
      via: "final CTA shadow call site"
      pattern: "shadow-\\[var\\(--shadow-4\\)\\]"
    - from: "src/app/page.tsx"
      to: "src/lib/content/queries.ts"
      via: "seven section reads"
      pattern: "getSection"
---

<objective>
Complete the landing page with sections five, six and seven — closing the seven signed
sections, the eleven-card floor and the second `--shadow-4` call site.

Purpose: PUB-05, PUB-06 and PUB-07 are the last three landing gate requirements, and this is
where AC-1 (eleven floating cards), AC-2 (`--shadow-4` consumed at two call sites) and AC-8
(all seven sections eyebrow-led) become provable.
Output: `/` renders the seven signed sections in order, entirely from Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-site-public/02-CONTEXT.md
@.planning/phases/02-site-public/02-UI-SPEC.md
@.planning/phases/02-site-public/02-PATTERNS.md
@.planning/phases/02-site-public/02-09-SUMMARY.md

<interfaces>
src/lib/content/queries.ts — getSection(cle), getSectionItems(sectionCle).
Section keys: `format-modalites`, `confiance`, `cta-final`, `faq`.
Placeholder rows for témoignages and logos are seeded under `confiance` with
`statut = 'placeholder'`; FAQ items carry `question` and `reponse` in `donnees`.

src/components/sections/section.tsx — Section({ tone: "default" | "band" }),
SectionHeader({ eyebrow, title, titleAccent, lead }).
src/components/ui/card.tsx — Card variant="raised".
src/components/ui/accordion.tsx — the Lot 1 accordion, reused for the FAQ (D-42).
src/components/motion/reveal.tsx — server-safe Reveal with data-d 1-5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: « Format et modalités » and « Confiance et sécurité »</name>
  <read_first>
    - src/app/page.tsx (the Lot 1 format and confiance blocks at 110-174, including the bordered Card call sites at 114 and 135 that AC-1 requires to reach zero)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 239-255 .mod-item/.mod-ic/.mod-item.futur/.tag-futur/.ph/.ph-lbl; lines 575-664 the two sections)
    - src/components/sections/pour-qui.tsx (the tile pattern established in plan 02-09)
  </read_first>
  <action>
    Create `src/components/sections/format-modalites.tsx`, async Server Component rendering
    `Section tone="band"` (the `--lav2` band, alternating after the transparent programme
    section — D-21). SectionHeader gets eyebrow « Format et modalités » with the two-sentence
    H2 split across `title` and `titleAccent`. Six items from
    `getSectionItems("format-modalites")` in a two-column grid collapsing to one below 640px.
    Each item is a `Card variant="raised"` at 1.4rem/1.5rem padding with a 42x42 rounded-13px
    `var(--lav)` icon tile in `var(--deep)` holding a lucide mark, a title at 1.02rem
    `letter-spacing:-.02em`, and a description at 0.94rem in `var(--muted-ink)`.

    Items whose `statut` is `futur` (« Vidéos à venir ») get the maquette's honest amber
    treatment: the icon tile background `#FFF4E3` with `#B4771A` foreground, and an inline
    « À venir » tag at 0.68rem weight 700 `letter-spacing:.06em` uppercase in `#B4771A` on
    `#FFF4E3`. Never present a future item as delivered.

    Create `src/components/sections/confiance.tsx`, async Server Component rendering
    `Section tone="default"`. Three faits from the non-placeholder
    `getSectionItems("confiance")` rows as `Card variant="raised"` tiles with the same
    52x52 gradient icon tile as the profil tiles. Below them, the two rows with
    `statut = 'placeholder'` render as the maquette's honest placeholder blocks: a
    1.5px dashed `#D6D3F0` border, `border-radius:18px`, 2rem/1.5rem padding, centred, on
    `rgba(255,255,255,.5)`, with an uppercase violet label at 0.68rem `letter-spacing:.14em`
    and the signed placeholder sentence below it (D-31). These two are DELIBERATELY not
    floating cards — a dashed placeholder must not be mistaken for content. Do not invent a
    testimonial, a name, a company, a star rating or a partner logo.

    Together with plan 02-09's five profil tiles, this task brings the landing page to at
    least fourteen floating cards, clearing the AC-1 floor of eleven.

    Both sections render `EmptyState tone="error"` on a failed read (D-32). No image (D-41),
    no price (D-45), no `any`, no `"use client"`.
  </action>
  <acceptance_criteria>
    - `grep -cE 'variant=("raised"|\{[^}]+\})' src/components/sections/format-modalites.tsx` and the same on confiance.tsx both return at least 1; `grep -n 'variant="outline"\|variant="default"' src/components/sections/format-modalites.tsx src/components/sections/confiance.tsx` returns zero lines.
    - `grep -nE 'tone=("band"|\{[^}]+\})' src/components/sections/format-modalites.tsx` matches; the same pattern on confiance.tsx returns zero lines (D-21).
    - `grep -n "B4771A\|FFF4E3" src/components/sections/format-modalites.tsx` matches — the future item wears the honest amber marker.
    - `grep -n "D6D3F0\|dashed" src/components/sections/confiance.tsx` matches, and the placeholder branch keys off `statut === "placeholder"`, not off an item index.
    - `grep -rin "avis\|étoiles\|note sur\|5/5\|témoignage de \|logo-\|\.svg\"\|\.png" src/components/sections/confiance.tsx` returns zero lines — no invented review and no partner logo asset (D-31, D-41).
    - `grep -l "eyebrow=" src/components/sections/format-modalites.tsx src/components/sections/confiance.tsx` lists both files; so does the same grep for `titleAccent=`.
    - `grep -l "use client" src/components/sections/format-modalites.tsx src/components/sections/confiance.tsx` lists no file.
    - `npx tsc --noEmit` and `npm run lint` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; grep -cE 'variant=("raised"|\{[^}]+\})' src/components/sections/confiance.tsx &amp;&amp; grep -n "dashed" src/components/sections/confiance.tsx &amp;&amp; ! grep -n 'variant="outline"' src/components/sections/format-modalites.tsx src/components/sections/confiance.tsx</automated>
  </verify>
  <done>Six format repères and three trust facts render as floating cards, and the témoignages and logos slots render as honest dashed placeholders carrying only signed placeholder copy.</done>
</task>

<task type="auto">
  <name>Task 2: The final CTA and the FAQ</name>
  <read_first>
    - src/app/page.tsx (the Lot 1 ctaFinal and faq blocks at 161-190)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (lines 257-271 .cta-final/.btn-white/.btn-outline-w; lines 666-702 the CTA block and the seven FAQ entries)
    - src/components/ui/accordion.tsx (the family the FAQ reuses)
    - src/components/ui/button.tsx (the render-as-Link idiom and data-magnetic)
  </read_first>
  <action>
    Create `src/components/sections/cta-final.tsx`, async Server Component reading
    `getSection("cta-final")`. It renders a `Section tone="default"` containing a single
    rounded-34px panel with `overflow:hidden`, padding `clamp(3rem,6vw,5rem)` block and
    `clamp(1.5rem,5vw,4rem)` inline, background
    `linear-gradient(125deg, var(--indigo), var(--deep) 45%, var(--violet))`, white text,
    centred, and — this is the second required `--shadow-4` call site (D-23, AC-2) —
    `shadow-[var(--shadow-4),var(--shadow-brand)]` applied at the element, not merely
    declared in CSS.

    Inside: a masked 52px grid overlay at `rgba(255,255,255,.06)` with the radial mask from
    the maquette; a white eyebrow « Votre prochaine étape »; the signed two-sentence H2 at
    `clamp(2rem,4vw,3rem)` weight 800; the support line at 1.1rem in `rgba(255,255,255,.82)`;
    two buttons — a white Button rendered as a link to `/reservation` with
    `data-magnetic="true"` and a translucent outlined Button rendered as a link to
    `/programme`; and the closing support line at 0.87rem. Every string comes from the
    `cta-final` row. No price, no formula, no payment provider name (D-45).

    Create `src/components/sections/faq.tsx`, async Server Component reading
    `getSectionItems("faq")` — seven entries, each with `donnees.question` and
    `donnees.reponse`. Render them through the Lot 1 `Accordion` family inside a
    `Section tone="band"` with a centred eyebrow-led header, container `max-w-[880px]`. No
    second accordion, no raw `<details>` (D-42). Wrap each entry in `Reveal` with `data-d`
    cycling 1..3 as the maquette does.

    `EmptyState tone="error"` on a failed read (D-32). No `any`, no `"use client"`.
  </action>
  <acceptance_criteria>
    - `grep -n "shadow-\[var(--shadow-4)" src/components/sections/cta-final.tsx` matches on a line that also contains `className` — a rendered call site, not a comment or a declaration (AC-2, resolves contradiction 5).
    - `grep -n "shadow-brand" src/components/sections/cta-final.tsx` matches (D-06).
    - Across `src/`, `--shadow-4` is consumed at both required call sites plus its existing declaration: `grep -rln "shadow-\[var(--shadow-4)" src/components/ src/app/ | wc -l` returns at least 3 (hero.tsx, cta-final.tsx, card.tsx's raised hover — a legitimate additional consumer is not a failure).
    - `grep -nE 'data-magnetic=("true"|\{[^}]+\})' src/components/sections/cta-final.tsx` matches (D-12).
    - `grep -rin "stripe\|paypal\|mollie\|paddle\|€\|prix\|tarif\|forfait" src/components/sections/cta-final.tsx` returns zero lines (D-45).
    - `grep -n "Accordion" src/components/sections/faq.tsx` matches; `grep -n "details\|summary" src/components/sections/faq.tsx` returns zero JSX element lines (D-42).
    - `grep -l "eyebrow=" src/components/sections/cta-final.tsx src/components/sections/faq.tsx` lists both files.
    - `grep -l "use client" src/components/sections/cta-final.tsx src/components/sections/faq.tsx` lists no file.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -n "shadow-\[var(--shadow-4)" src/components/sections/cta-final.tsx &amp;&amp; ! grep -rin "stripe\|paypal\|€" src/components/sections/cta-final.tsx &amp;&amp; grep -n "Accordion" src/components/sections/faq.tsx</automated>
  </verify>
  <done>The final CTA wears the gradient panel on --shadow-4 plus --shadow-brand with a magnetic white CTA, and seven FAQ entries render in the reused accordion, all from Supabase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mount the seven sections and confirm the landing page against the maquette</name>
  <read_first>
    - src/app/page.tsx (whole file — every remaining Lot 1 block and every remaining locale import)
    - C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-29-maquette-lot2-landing-validee.html (the whole file — open it in a browser beside the running site)
  </read_first>
  <action>
    In `src/app/page.tsx`, replace the remaining Lot 1 blocks with `<FormatModalites />`,
    `<Confiance />`, `<CtaFinal />` and `<Faq />`, so the final composition reads, in order:
    `<Hero />`, `<StatsBand />`, `<PourQui />`, `<Competences />`, `<ProgrammeAccordion />`,
    `<FormatModalites />`, `<Confiance />`, `<CtaFinal />`, `<Faq />`. Delete every remaining
    `landing.json` import from page.tsx — after this task the file imports no locale bundle
    for landing content (D-24, PUB-13). Keep `export const revalidate` and keep page.tsx a
    Server Component (D-38).
  </action>
  <what-built>
    The complete landing page: the seven signed sections plus the two founder-approved
    26academy bands, rendered end to end from Supabase, on the root atmosphere.
  </what-built>
  <how-to-verify>
    1. Open `/` beside the maquette HTML file and scroll both from top to bottom.
    2. Count the sections: Hero, figures band, Pour qui, Compétences, Programme, Format,
       Confiance, CTA final, FAQ. All must be present, in that order.
    3. The atmosphere blobs must be visible behind EVERY transparent section, not only the
       hero — scroll to the FAQ and confirm the coloured haze is still there.
    4. The final CTA panel must sit on a deep violet-tinted shadow, clearly lifted off the
       page. A flat gradient rectangle is a failure.
    5. Count the floating cards: five profils, six format items, three faits — at least
       fourteen surfaces with no border that lift on hover.
    6. The témoignages and logos blocks must read as dashed, labelled placeholders. If either
       shows a name, a quote or a logo, stop — that is invented content.
    7. Every section opens with an eyebrow, a two-sentence heading whose second sentence is in
       gradient, and a lead.
    8. Confirm no price and no payment provider name appears anywhere on the page.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -n "landing.json" src/app/page.tsx` returns zero lines (D-24, PUB-13).
    - `grep -c "<Hero\|<StatsBand\|<PourQui\|<Competences\|<ProgrammeAccordion\|<FormatModalites\|<Confiance\|<CtaFinal\|<Faq" src/app/page.tsx` returns 9, in that source order.
    - `curl -s http://localhost:3000 | grep -c 'data-slot="card"'` returns at least 14.
    - `curl -s http://localhost:3000 | grep -c 'shadow-none'` returns 0.
    - `curl -s http://localhost:3000 | grep -ci "stripe\|paypal\|€"` returns 0 (D-45).
    - PUB-13 proof: `update app.content_item set publie = false where section_cle = 'pour-qui' and position = 1;`, rebuild, reload — the first profil card is gone. Restore the row.
    - The reviewer confirms the atmosphere is visible behind the FAQ section, not only the hero (AC-3).
    - The reviewer confirms the final CTA reads as lifted, and that the two placeholder blocks carry no invented content.
    - `npm run build` exits 0 with `/` still prerendered static.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; ! grep -n "landing.json" src/app/page.tsx &amp;&amp; grep -n "CtaFinal" src/app/page.tsx</automated>
  </verify>
  <resume-signal>Type "approved" or name the sections that still read flat or timid against the maquette.</resume-signal>
  <done>The seven signed sections plus the two approved bands render in order from the database, with at least fourteen floating cards, a lifted final CTA, honest placeholders and the atmosphere behind all of them.</done>
</task>

</tasks>

<verification>
- `grep -rn "locales/fr/landing.json" src/` returns zero lines (AC-10).
- `npm run build` exits 0; `/` prerendered static.
- `grep -rno "cubic-bezier([^)]*)" src/ | sed 's/.*cubic-bezier/cubic-bezier/' | sort -u | wc -l` returns 1.
</verification>

<success_criteria>
The landing page delivers all seven signed sections plus the approved bands, from the
database, at or above the eleven-floating-card floor, with --shadow-4 consumed at both
required call sites.
</success_criteria>

<output>
Create `.planning/phases/02-site-public/02-10-SUMMARY.md` when done.
</output>
