---
phase: 01-cadrage-contenus-et-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design-exploration/README.md
  - design-exploration/direction-01-clarte.html
  - design-exploration/direction-02-profondeur.html
  - design-exploration/direction-03-editorial.html
autonomous: true
requirements: [CADR-05]

must_haves:
  truths:
    - "Three visual directions can be opened in a browser and compared side by side (D-03, D-33)"
    - "Each direction shows a real hero and a real card/CTA cluster with real typefaces and real motion"
    - "Each direction proposes an exact bleu/blanc/vert palette in oklch, an easing curve and a shadow ladder (D-16, D-17, D-18)"
    - "Nothing from this plan lands inside src/ or public/ (D-03)"
    - "A decision sheet exists that the founder fills in and Wave 2 reads mechanically (D-34)"
  artifacts:
    - path: "design-exploration/README.md"
      provides: "Comparison table + the empty « Direction retenue » block Wave 2 reads"
      contains: "Direction retenue"
    - path: "design-exploration/direction-01-clarte.html"
      provides: "Direction 1 — clarté"
      min_lines: 120
    - path: "design-exploration/direction-02-profondeur.html"
      provides: "Direction 2 — profondeur"
      min_lines: 120
    - path: "design-exploration/direction-03-editorial.html"
      provides: "Direction 3 — éditorial"
      min_lines: 120
  key_links:
    - from: "design-exploration/README.md"
      to: "src/app/globals.css"
      via: "token names in the comparison table match the Wave 2 custom-property names verbatim"
      pattern: "--primary|--success|--ease-brand|--shadow-1"
---

<objective>
Wave 1 of the locked wave shape (D-33). Produce two-or-three throwaway visual
directions as static HTML **outside `src/`**, so the founder can pick one before
any token is written into the application.

Purpose: D-01 + D-03 create a gap no headless run can cross — the founder must
choose. A silent guess here propagates a palette and a typeface into eleven
full-fidelity maquettes. This plan makes the choice cheap and reversible: two of
three directions cost a deletion.

Output: `design-exploration/` at the repository root — three self-contained HTML
files plus a decision sheet. **Typography is settled here** (D-03).

⛔ **Execution STOPS at the end of this plan** (D-33). The next step is a manager
session, not a run (D-34).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md
@.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md
@CLAUDE.md

**Hard prohibitions that apply to every task in this plan:**
- ⛔ Do not create any file under `src/`, `public/`, `supabase/`, or `reference/`.
- ⛔ Do not run `npm install`, `npx shadcn add`, or any package-manager install.
  This phase installs nothing; `@base-ui/react@1.7.0`, `lucide-react`,
  `class-variance-authority`, `clsx`, `tailwind-merge` and `tw-animate-css` are
  already dependencies.
- ⛔ Do not `git push`, do not create a PR, do not run `vercel` (D-38).
- ⛔ Do not build a generative hero — no JS-computed Bézier paths, no
  `stroke-dasharray` draw-on, no comets, no `animateMotion` (D-20).
- ⛔ No video player frame, no play button, no « vidéo à venir » caption (D-13).
- ⛔ The strings « Apprenez à votre rythme » and « Accès 24/7 » must not appear
  anywhere (D-15).
- ⛔ No payment provider is named anywhere (D-04).
</context>

<execution_order>
D-39 — **commit before verifying.** Commit each task's files with
`#<type>: <one sentence>` as soon as that task's acceptance criteria hold, before
starting the next task. Do not background any command.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Decision sheet and Direction 1 — clarté</name>
  <files>design-exploration/README.md, design-exploration/direction-01-clarte.html</files>
  <read_first>
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-03, D-13…D-21, D-25, D-33, D-34
    - `.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md` §2 (token structure) and §4 (`next/font` fork)
    - `src/app/globals.css` — the `@theme inline` and `:root` block whose names the directions must mirror
  </read_first>
  <action>
Create `design-exploration/README.md` in French. It carries, in this order:
(1) a « Comment regarder » block — open each `.html` directly in a browser, no
build, no server; (2) a « Ce qui se décide ici » block naming exactly three
decisions: la palette, la typographie, la courbe d'animation; (3) an empty
comparison table with one row per direction and the columns `Direction`,
`Police titres`, `Police texte`, `--primary`, `--success`, `--foreground`,
`--ease-brand`, `Parti pris` — left blank, filled in Task 3; (4) a
`## Direction retenue` section containing a fill-in block with the literal
keys `direction:`, `police-titres:`, `police-texte:`, `primary:`, `success:`,
`foreground:`, `ease-brand:`, each followed by ` À REMPLIR PAR LE FONDATEUR`.
Wave 2 reads this block verbatim, so the key names are a contract — do not
rename them.

Create `design-exploration/direction-01-clarte.html` — a single self-contained
file: `<!doctype html>`, `<html lang="fr">`, one `<style>` in `<head>`, no
external JS, no build step. Typeface loaded by a `<link>` to Google Fonts —
`Inter` for both headings and body. Restrict every direction to the Google
Fonts catalogue: `next/font/google` is already the repo pattern
(`src/app/layout.tsx:2`) and a non-catalogue face would require
`next/font/local` plus committed font files, which do not exist and are not
budgeted.

Declare the palette in `:root` using **the exact custom-property names Wave 2
will use**: `--background`, `--foreground`, `--primary`, `--primary-foreground`,
`--success`, `--success-foreground`, `--success-muted`, `--muted`,
`--muted-foreground`, `--border`, `--ring`, `--destructive`,
`--shadow-contact`, `--shadow-soft`, `--radius`, `--ease-brand`,
`--duration-base`, `--shadow-1`, `--shadow-2`, `--shadow-3`, `--shadow-4`.
All colours in `oklch()`. `--foreground` must carry **non-zero chroma** tinted
toward the accent — ink is never pure black (D-16). `--primary` is the bleu,
`--success` the vert, `--background` white. `--ease-brand` is a single
`cubic-bezier(...)` used by **every** transition in the file — no `ease`, no
`linear`, no second curve (D-17). The four `--shadow-*` tiers are each a paired
shadow: a 1px contact shadow plus a large soft one, both built from
`--shadow-contact` / `--shadow-soft` (D-18).

The parti pris of direction 1 is **clarté**: white ground, very generous
whitespace, blue used sparingly as pure accent, near-flat surfaces relying on
`--shadow-1`/`--shadow-2` only, a barely-there linear gradient wash behind the
hero, no blobs, no grain.

Render two blocks. **Hero:** an `<h1>` written with the D-25 copy device — two
short sentences with a full stop between them, a claim then a turn — a
supporting paragraph, a primary CTA « Démarrer ma formation » and a secondary
CTA « Voir le programme ». **Card/CTA cluster:** three cards, each with an
inline `<svg>` pictogram inheriting `currentColor`, a title, a body line and a
green success badge on one of them.

Motion, all CSS, all on `--ease-brand`: a rise-and-fade reveal on load via
`@keyframes` and `animation`, plus hover and `:focus-visible` transitions on
both CTAs and on the cards. Every focusable element must have a visible
`:focus-visible` ring — the reference site ships none and doing better is free
(D-23).

Every French string in these files is mocked client content. Put an HTML
comment `<!-- contenu factice — remplacé par les réponses client -->` above each
mocked block. The marker is a comment, never visible text.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('design-exploration/direction-01-clarte.html','utf8');const r=fs.readFileSync('design-exploration/README.md','utf8');const need=['--primary','--success','--foreground','--ease-brand','--shadow-1','--shadow-4','oklch(','focus-visible','@keyframes'];const miss=need.filter(t=>!h.includes(t));if(miss.length)throw new Error('direction-01 missing: '+miss);if((h.match(/cubic-bezier\(/g)||[]).length!==1)throw new Error('direction-01 must declare exactly one cubic-bezier');if(/animateMotion|stroke-dasharray|&lt;video|Apprenez a votre rythme|Acces 24\/7/.test(h))throw new Error('prohibited construct in direction-01');for(const k of ['direction:','police-titres:','police-texte:','primary:','success:','foreground:','ease-brand:','Direction retenue'])if(!r.includes(k))throw new Error('README missing key: '+k);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `design-exploration/README.md` exists and contains the literal heading `## Direction retenue` and the seven fill-in keys `direction:`, `police-titres:`, `police-texte:`, `primary:`, `success:`, `foreground:`, `ease-brand:`
    - `design-exploration/direction-01-clarte.html` declares all of `--background`, `--foreground`, `--primary`, `--primary-foreground`, `--success`, `--success-foreground`, `--success-muted`, `--muted`, `--muted-foreground`, `--border`, `--ring`, `--destructive`, `--shadow-contact`, `--shadow-soft`, `--radius`, `--ease-brand`, `--duration-base`, `--shadow-1`, `--shadow-2`, `--shadow-3`, `--shadow-4` in a `:root` block
    - Exactly one `cubic-bezier(` literal appears in the file (D-17)
    - `--foreground` is an `oklch()` value whose second component (chroma) is not `0`
    - Each of `--shadow-1` … `--shadow-4` contains two comma-separated shadow layers
    - The file contains `focus-visible` and at least one `@keyframes` rule
    - The file contains the strings `Démarrer ma formation` and `Voir le programme`
    - The file contains no `<video`, no `animateMotion`, no `stroke-dasharray`, no « Apprenez à votre rythme », no « Accès 24/7 », and no payment-provider name
    - No file was created under `src/`, `public/`, `supabase/` or `reference/`
  </acceptance_criteria>
  <done>The founder can open direction 1 in a browser and see a finished hero and card cluster, and the README tells them what they are deciding.</done>
</task>

<task type="auto">
  <name>Task 2: Direction 2 — profondeur, and Direction 3 — éditorial</name>
  <files>design-exploration/direction-02-profondeur.html, design-exploration/direction-03-editorial.html</files>
  <read_first>
    - `design-exploration/direction-01-clarte.html` — the structure, the block order and the `:root` token names to reproduce exactly
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-16 … D-21
  </read_first>
  <action>
Create the two remaining directions. Each is a self-contained HTML file with
**the same block order, the same markup skeleton and the same `:root` token
names** as direction 1 — only the values and the visual parti pris change. That
sameness is what makes the comparison honest and the transfer to Wave 2
mechanical.

`direction-02-profondeur.html` — parti pris **profondeur**: a deep blue-tinted
hero built from a CSS radial/linear gradient plus two blurred colour blobs
(`filter: blur(...)` on absolutely-positioned divs) and a grain overlay
produced by an inline SVG `feTurbulence` data-URI background, white cards
floating on it with `--shadow-3`/`--shadow-4`, high contrast. All CSS and inline
SVG, zero raster images (D-21). Typeface: `Manrope` for headings, `Inter` for
body.

`direction-03-editorial.html` — parti pris **éditorial**: a serif display face
against a grotesque body, a large type scale, hairline rules between sections,
green used as an editorial marker (a rule, a small caps label) rather than only
as a success colour. Typeface: `Fraunces` for headings, `Inter` for body.

Both files keep the single `--ease-brand` curve, the paired four-tier shadow
ladder, the non-zero-chroma `--foreground`, the visible `:focus-visible` ring on
every focusable element, and the `<!-- contenu factice … -->` comments. Both
propose their own oklch values for the bleu, the blanc and the vert — the point
of the round is that the founder compares three concrete palettes, not one.

⛔ No generative hero in either file (D-20): the blobs are static CSS, the grain
is a static overlay, nothing is computed in JS and nothing rebuilds on resize.
There must be no `<script>` element in either file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');for(const f of ['design-exploration/direction-02-profondeur.html','design-exploration/direction-03-editorial.html']){const h=fs.readFileSync(f,'utf8');const need=['--primary','--success','--foreground','--ease-brand','--shadow-1','--shadow-4','oklch(','focus-visible','@keyframes','Démarrer ma formation','Voir le programme'];const miss=need.filter(t=>!h.includes(t));if(miss.length)throw new Error(f+' missing: '+miss);if((h.match(/cubic-bezier\(/g)||[]).length!==1)throw new Error(f+': exactly one cubic-bezier required');if(/<script|animateMotion|stroke-dasharray|<video/.test(h))throw new Error(f+': prohibited construct');}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and each declares the same `:root` custom-property names as `direction-01-clarte.html`
    - Each file contains exactly one `cubic-bezier(` literal
    - Neither file contains a `<script` element, `animateMotion`, `stroke-dasharray`, or `<video`
    - Neither file references a raster image: no `.png`, `.jpg`, `.jpeg` or `.webp` URL
    - `direction-02-profondeur.html` contains `blur(` and a `feTurbulence` data URI
    - `direction-03-editorial.html` loads a serif display face (`Fraunces`) distinct from its body face (`Inter`)
    - Both files contain `focus-visible` and at least one `@keyframes`
    - Both files carry `<!-- contenu factice` comments above mocked copy blocks
  </acceptance_criteria>
  <done>Three directions exist, structurally identical and visually distinct, openable with a double-click.</done>
</task>

<task type="auto">
  <name>Task 3: Fill the comparison table and close Wave 1</name>
  <files>design-exploration/README.md</files>
  <read_first>
    - `design-exploration/direction-01-clarte.html`, `design-exploration/direction-02-profondeur.html`, `design-exploration/direction-03-editorial.html` — read the `:root` blocks and copy the literal values
  </read_first>
  <action>
Fill the comparison table in `design-exploration/README.md` with the **literal
values read out of the three files** — the two font family names, and the exact
`oklch(...)` strings for `--primary`, `--success` and `--foreground`, and the
exact `cubic-bezier(...)` string for `--ease-brand`, plus a one-line « Parti
pris ». No paraphrase: Wave 2 copies these strings into
`src/app/globals.css`, so a transcription error is a wrong palette in eleven
maquettes.

Add a short `## Après la décision` section stating: the retained direction's
values go into `src/app/globals.css` and `src/app/layout.tsx` in Wave 2, and
`design-exploration/` is deleted once the maquettes are validated.

Leave `## Direction retenue` **unfilled** — that block is the founder's, and
filling it here would be exactly the silent guess D-33/D-34 exist to prevent.

Then stop. Do not start any Wave 2 work.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('design-exploration/README.md','utf8');const files=['design-exploration/direction-01-clarte.html','design-exploration/direction-02-profondeur.html','design-exploration/direction-03-editorial.html'];for(const f of files){const c=fs.readFileSync(f,'utf8');for(const v of ['--primary','--success','--ease-brand']){const m=c.match(new RegExp(v.replace(/-/g,'\\\\-')+':\\\\s*([^;]+);'));if(!m)throw new Error(f+': cannot read '+v);if(!r.includes(m[1].trim()))throw new Error('README table missing literal '+v+' value from '+f+': '+m[1].trim());}}if(!/À REMPLIR PAR LE FONDATEUR/.test(r))throw new Error('Direction retenue must stay unfilled');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The README comparison table contains one row per direction with the literal `oklch(...)` strings for `--primary`, `--success` and `--foreground` copied verbatim from each HTML file
    - The table contains the literal `cubic-bezier(...)` string of each direction
    - The table names both typefaces per direction
    - `## Direction retenue` still contains ` À REMPLIR PAR LE FONDATEUR` on every one of its seven keys
    - A `## Après la décision` section exists and names `src/app/globals.css` and `src/app/layout.tsx`
    - No file exists under `src/`, `public/` or `supabase/` that was not there before this plan
  </acceptance_criteria>
  <done>Wave 1 is complete and committed. Execution stops; the founder's pick is a manager session (D-34).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → Vercel production build | anything under `src/` or `public/` is served publicly; `design-exploration/` is not |
| mocked copy → client review | the founder and later the client read these directions as if the words were real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | exploration HTML | mitigate | Files live only in `design-exploration/` at the repo root — never `src/`, never `public/`; acceptance criteria assert no file was added under either, so Next.js cannot serve them |
| T-01-02 | Spoofing | mocked French copy | mitigate | Every mocked block carries an `<!-- contenu factice — remplacé par les réponses client -->` comment; the copy in this plan never enters the locale bundle, so it cannot leak into Lot 2 |
| T-01-03 | Tampering | third-party font CDN `<link>` | accept | Google Fonts `<link>` is used only in throwaway files opened locally by the founder; the application itself self-hosts through `next/font` (D-19) and this directory is deleted after validation |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No package-manager install task exists in this plan or in this phase.** Every primitive needed is already a dependency (`@base-ui/react@1.7.0`, `lucide-react`, `cva`, `clsx`, `tailwind-merge`, `tw-animate-css`). If an executor believes an install is required, it must stop and report rather than install |
| T-01-04 | Elevation of Privilege | authenticated surfaces | accept | This plan ships no route, no auth surface and no runtime authorization — Lot 1 has none by design (D-11, D-41). Nothing here changes the production attack surface |
</threat_model>

<verification>
Manager-session checks after the plan is committed (D-39):

1. `git status --porcelain src public supabase` returns empty — nothing leaked into the app.
2. Open all three `design-exploration/*.html` files in a browser; each shows a finished hero and card cluster, animates on load, and shows a visible focus ring when tabbing.
3. `## Direction retenue` in the README is still unfilled.
4. ⏸ **Checkpoint D-34** — show the founder the three directions, record their pick and the chosen typography in `## Direction retenue`, then relaunch for Waves 2–4.
</verification>

<success_criteria>
- Three self-contained directions exist under `design-exploration/`, structurally identical, visually distinct
- Each proposes an exact bleu/blanc/vert oklch palette, one easing curve and a four-tier paired shadow ladder
- Typography is settled to three concrete Google-catalogue proposals
- The decision sheet is filled except the founder's block
- Nothing was added under `src/`, `public/` or `supabase/`
- Everything is committed; nothing was pushed
</success_criteria>

<output>
Create `.planning/phases/01-cadrage-contenus-et-design/01-01-SUMMARY.md` when done.
</output>
