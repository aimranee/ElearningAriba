---
phase: 01-cadrage-contenus-et-design
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements: [CADR-05]

must_haves:
  truths:
    - "The application renders in the bleu / blanc / vert palette the founder chose, not shadcn greys (D-16)"
    - "Every transition on the site uses one single easing curve (D-17)"
    - "Four paired shadow tiers exist as tokens and are the only shadows components use (D-18)"
    - "The chosen typefaces are self-hosted through next/font and html actually renders in them (D-19)"
    - "A CSS-only atmosphere layer exists — gradient wash, blurred colour blobs, grain — with no raster image (D-21)"
    - "A compact density mode exists as a data-attribute contract both surfaces can use (D-24)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design tokens, atmosphere layer, motion primitives, density contract"
      contains: "--ease-brand"
    - path: "src/app/layout.tsx"
      provides: "next/font wiring for the chosen typefaces"
      contains: "next/font/google"
  key_links:
    - from: "src/app/globals.css"
      to: "src/app/layout.tsx"
      via: "--font-sans / --font-heading resolve to the next/font CSS variable names"
      pattern: "--font-sans:\\s*var\\(--font-body\\)"
    - from: "design-exploration/README.md"
      to: "src/app/globals.css"
      via: "the founder's retained oklch values are transcribed verbatim"
      pattern: "oklch\\("
---

<objective>
Wave 2 of the locked wave shape (D-35), first half: turn the founder's retained
direction into the real design-system foundation — tokens, typography, the
atmosphere layer, motion primitives and the density contract.

Purpose: `CADR-05` names « palette, typographie, échelle d'espacement ». Everything
downstream — the five component families, the eleven maquettes, and every later
lot — reads these tokens. The whole palette lives in one CSS file, so a rejected
direction stays one commit (verified: no `tailwind.config.*` exists).

Output: a rewritten token layer in `src/app/globals.css` and correct `next/font`
wiring in `src/app/layout.tsx`, with the three Phase-0 placeholder comments
deleted.

**This plan consumes the founder's chosen direction and typography**, read from
`design-exploration/README.md` § `Direction retenue`. It cannot start before
plan 01-01 has landed and that block has been filled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md
@.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md
@CLAUDE.md

<interfaces>
Tokens `src/components/ui/button.tsx` already depends on and which must keep
resolving after this plan: `--color-primary`, `--color-primary-foreground`,
`--color-secondary`, `--color-secondary-foreground`, `--color-destructive`,
`--color-ring`, `--color-border`, `--color-input`, `--color-muted`,
`--color-background`, `--color-foreground`, `--radius-md`.
Deleting or renaming any of them breaks the only existing component.

Current font wiring, which is **broken** and this plan fixes:
`src/app/globals.css:13` declares `--font-sans: var(--font-sans)` — self-referential
and never defined in `:root` — while `src/app/layout.tsx:10` exposes the face as
`--font-geist-sans`. So `html { @apply font-sans }` (`globals.css:131`) resolves
to nothing and the app silently renders in the browser default.
</interfaces>

**Hard prohibitions:**
- ⛔ Do not touch `supabase/`, any migration, any auth surface, or `src/types/database.types.ts` (D-11).
- ⛔ Do not run `npm install` or `npx shadcn add`. Nothing is installed in this phase.
- ⛔ Do not `git push`, create a PR, or run `vercel` (D-38).
- ⛔ Do not add an i18n library or a locale switcher (D-28).
- ⛔ Do not touch `next.config.ts` — its Lot 5 placeholder comment stays.
- ⛔ Do not build a generative hero; the atmosphere is static CSS only (D-20, D-21).
</context>

<execution_order>
D-39 — **commit before verifying.** Commit each task as soon as its acceptance
criteria hold. Run `npm run typecheck` only as the final task's verification,
after the earlier tasks are already committed.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Replace the token layer with the retained palette, type scale, easing and shadow ladder</name>
  <files>src/app/globals.css</files>
  <read_first>
    - `design-exploration/README.md` — the `## Direction retenue` block; the seven values there are the source of truth for this task
    - the retained `design-exploration/direction-0N-*.html` — its full `:root` block, for the values the README block does not carry
    - `src/app/globals.css` — all 133 lines, before editing
    - `src/components/ui/button.tsx` — every token it consumes must survive
  </read_first>
  <action>
Rewrite the token layer of `src/app/globals.css`, keeping lines 1–5
(`@import "tailwindcss"`, `@import "tw-animate-css"`, `@import "shadcn/tailwind.css"`,
`@custom-variant dark`) unchanged — `tw-animate-css` is the motion dependency
Task 3 builds on.

**Delete** the placeholder comment currently at lines 7–9 (« untouched shadcn/ui
library defaults … after client validation »). That comment is this phase's own
to-do item and leaving it behind is a verification failure.

**`:root`** — replace the 33 chroma-zero `oklch(… 0 0)` values with the retained
direction's palette. Transcribe the founder's values verbatim from
`## Direction retenue`; where that block is silent, take the value from the
retained direction's HTML `:root`. `--primary` is the bleu and is the primary
action colour, `--background` is white, `--foreground` is ink with **non-zero
chroma tinted toward the accent — never pure black** (D-16). Set `--ring` to the
same value as `--primary` so the focus ring is the accent. Keep `--radius:
0.625rem` as the single knob the seven `--radius-*` steps derive from.

**Add** to `:root`, with the retained direction's values: `--success`,
`--success-foreground`, `--success-muted` (vert — confirmation and success:
a slot booked, a payment through), `--shadow-contact` and `--shadow-soft`
(the two colour components of the ladder).

**`@theme inline`** — keep every existing `--color-*` → `var(--*)` mapping
(button.tsx depends on them) and add:
- `--color-success: var(--success)`, `--color-success-foreground: var(--success-foreground)`, `--color-success-muted: var(--success-muted)`
- `--ease-brand: <the retained cubic-bezier>` and `--duration-base: 220ms` — **the only easing curve on the site** (D-17)
- `--shadow-1` … `--shadow-4`, four tiers, **each a pair**: a 1px contact shadow built from `var(--shadow-contact)` plus a large soft one built from `var(--shadow-soft)`, growing in offset and blur across the tiers (D-18)
- `--spacing: 0.25rem` — the spacing scale `CADR-05` names, given an explicit home rather than Tailwind's implicit default
- the type scale, as `--text-display`, `--text-title`, `--text-section`, `--text-lead`, each with its `--text-*--line-height` companion; the display and title steps use `clamp()` so they are fluid on mobile
- `--font-sans: var(--font-body)` and `--font-heading: var(--font-heading-face)` — Task 2 defines those two variables in `layout.tsx`. Replace the self-referential `--font-sans: var(--font-sans)` on line 13; leave `--font-mono` mapped to a system monospace stack, since Task 2 removes Geist_Mono.

**`.dark`** — delete the whole block (currently lines 89–121). This is an
explicit call, not an oversight: no dark-mode toggle exists anywhere in the app,
none is in signed scope, and keeping it means 33 colour values the founder never
chose. Keep the `@custom-variant dark` declaration on line 5 so the `dark:`
utilities already written in `button.tsx` keep compiling — they simply never
match. Write a one-line `/* why */` comment saying so.

Keep the `@layer base` block at the end intact.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/app/globals.css','utf8');const need=['--color-success:','--color-success-foreground:','--color-success-muted:','--ease-brand:','--duration-base:','--shadow-1:','--shadow-2:','--shadow-3:','--shadow-4:','--spacing:','--text-display','--text-title','--text-section','--text-lead','--success:','--shadow-contact:','--shadow-soft:'];const miss=need.filter(t=>!c.includes(t));if(miss.length)throw new Error('missing tokens: '+miss);if(c.includes('untouched shadcn/ui library defaults'))throw new Error('Phase 0 placeholder comment still present');if(/^\.dark\s*\{/m.test(c))throw new Error('.dark block must be deleted');if(c.includes('--font-sans: var(--font-sans)'))throw new Error('self-referential --font-sans still present');for(const t of ['--color-primary:','--color-destructive:','--color-ring:','--color-border:','--color-input:','--color-muted:','--color-secondary:','--radius-md:'])if(!c.includes(t))throw new Error('button.tsx dependency removed: '+t);const fg=c.match(/--foreground:\s*oklch\(([^)]*)\)/);if(!fg)throw new Error('--foreground must be oklch');const parts=fg[1].trim().split(/\s+/);if(parseFloat(parts[1])===0)throw new Error('--foreground chroma is 0 — ink must be tinted (D-16)');for(let i=1;i<=4;i++){const m=c.match(new RegExp('--shadow-'+i+':\\\\s*([^;]+);'));if(!m||m[1].split('),').length<2&&(m[1].match(/,/g)||[]).length<4)throw new Error('--shadow-'+i+' must be a paired shadow');}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` no longer contains the string `untouched shadcn/ui library defaults`
    - No `.dark {` block remains; the `@custom-variant dark (&:is(.dark *));` line on line 5 remains
    - `:root` defines `--success`, `--success-foreground`, `--success-muted`, `--shadow-contact`, `--shadow-soft`
    - `--foreground` is an `oklch()` value whose chroma component is strictly greater than 0
    - `@theme inline` defines `--color-success`, `--color-success-foreground`, `--color-success-muted`, `--ease-brand`, `--duration-base`, `--shadow-1` … `--shadow-4`, `--spacing`, `--text-display`, `--text-title`, `--text-section`, `--text-lead`
    - Each of `--shadow-1` … `--shadow-4` declares two shadow layers, one 1px contact and one large soft
    - Exactly one `cubic-bezier(` literal appears in the whole file
    - `--font-sans` resolves to `var(--font-body)` and `--font-heading` to `var(--font-heading-face)`
    - All of `--color-primary`, `--color-primary-foreground`, `--color-secondary`, `--color-secondary-foreground`, `--color-destructive`, `--color-ring`, `--color-border`, `--color-input`, `--color-muted`, `--color-background`, `--color-foreground`, `--radius-md` are still defined
    - The `oklch(...)` values for `--primary`, `--success` and `--foreground` are byte-identical to the retained direction's values in `design-exploration/README.md`
  </acceptance_criteria>
  <done>The application's token layer is the founder's chosen palette, with one easing curve, one shadow ladder, one spacing scale and one radius knob.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the chosen typefaces through next/font and fix the broken font chain</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - `design-exploration/README.md` — `## Direction retenue`, keys `police-titres:` and `police-texte:`
    - `src/app/layout.tsx` — all 33 lines
    - `src/app/globals.css` — the `--font-sans` / `--font-heading` / `--font-mono` mappings written in Task 1
    - `.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md` §4 — the `next/font` fork and the `LayoutProps<"/">` typed-routes rule
  </read_first>
  <action>
Replace the Geist placeholder wiring in `src/app/layout.tsx` with the retained
typefaces.

Import the two chosen faces from `next/font/google` — the same module the file
already uses, which fetches at build and self-hosts, satisfying D-19 and the
`GOL-02` Lighthouse ≥ 90 budget with zero CLS. Declare them following the
existing one-const-per-face pattern:
`const bodyFont = <BodyFace>({ variable: "--font-body", subsets: ["latin"] });`
and `const headingFont = <HeadingFace>({ variable: "--font-heading-face", subsets: ["latin"] });`.
The variable names `--font-body` and `--font-heading-face` are a contract with
Task 1's `@theme` mappings — do not rename them.

Remove both `Geist` and `Geist_Mono` imports and **delete the placeholder
comment at lines 6–8** (« Geist is the untouched create-next-app library
default … »). That comment is the third Phase-0 placeholder this phase closes;
`CONTEXT.md` names only two, so record the third in the summary.

Compose both font variables onto `<html className>` alongside the existing
`h-full antialiased`, keeping the template-literal composition already there.
Keep `lang="fr"` and keep `<body className="min-h-full flex flex-col">` — the
route pages depend on that flex contract via `flex-1`.

Keep the `metadata` export reading from `common.metadata`, and keep the
`LayoutProps<"/">` props type — it is a Next 16 typed-routes global generated by
`next typegen`. Do not hand-write a props interface and do not use `any`.

If the founder's chosen face is not in the Google Fonts catalogue, **stop and
report** rather than improvise: `next/font/local` requires committed font files
under a directory that does not exist and is not budgeted.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/app/layout.tsx','utf8');if(/Geist/.test(c))throw new Error('Geist still referenced');if(c.includes('untouched create-next-app library default'))throw new Error('Phase 0 placeholder comment still present');for(const t of ['next/font/google','--font-body','--font-heading-face','lang=\"fr\"','min-h-full flex flex-col','LayoutProps<\"/\">'])if(!c.includes(t))throw new Error('missing: '+t);if(/:\s*any\b/.test(c))throw new Error('any type present');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains no occurrence of `Geist`
    - It declares exactly two `next/font/google` faces, with `variable: "--font-body"` and `variable: "--font-heading-face"`
    - Both variables appear in the `<html>` `className`
    - `lang="fr"` and `className="min-h-full flex flex-col"` on `<body>` are unchanged
    - The props type is still `LayoutProps<"/">`; no hand-written interface, no `any`
    - The comment block « Geist is the untouched create-next-app library default » is gone
    - `--font-body` and `--font-heading-face` match the names consumed by `@theme` in `src/app/globals.css`
  </acceptance_criteria>
  <done>`html { @apply font-sans }` now resolves to the founder's body face, and `--font-heading` is available for the D-25 display headings.</done>
</task>

<task type="auto">
  <name>Task 3: Atmosphere layer, motion primitives and the density contract</name>
  <files>src/app/globals.css</files>
  <read_first>
    - `src/app/globals.css` — as rewritten by Task 1
    - the retained `design-exploration/direction-0N-*.html` — its gradient, blob and grain CSS, to be promoted into reusable utilities
    - `src/components/ui/button.tsx:7` — the `in-data-[slot=…]` idiom the density contract mirrors
  </read_first>
  <action>
Append, below the existing `@layer base` block, a `@layer components` block
carrying the reusable atmosphere and motion primitives. Everything is CSS and
inline SVG — no raster image, no image request (D-21). ⛔ No generative hero:
no JS, no computed paths, no resize handler (D-20).

**Atmosphere utilities**, named exactly:
- `.atmosphere-wash` — the section-level gradient ground built from `--primary`, `--success` and `--background` via `color-mix(in oklch, …)`
- `.atmosphere-blob` — a positioned, `filter: blur(...)` colour blob, `pointer-events: none`, `aria-hidden` at the call site
- `.atmosphere-grain` — a full-bleed overlay whose `background-image` is an inline `data:image/svg+xml` `feTurbulence`, at low opacity, `pointer-events: none`

**Motion primitives**, every one of them using `var(--ease-brand)` and
`var(--duration-base)` — no `ease`, no `linear`, no second curve anywhere
(D-17):
- `.reveal-rise` — the opacity + translate-Y entrance, defined with `@keyframes reveal-rise`
- `.reveal-delay-1`, `.reveal-delay-2`, `.reveal-delay-3` — stagger steps
- a `@media (prefers-reduced-motion: reduce)` block that neutralises every animation and transition duration to `0.01ms`. This is not optional: it is the cheapest accessibility win in the lot and the reference site has none.

**Density contract (D-24)** — the back-office and the learner surface share one
component set at two densities. Define it as a data attribute on a surface root,
`[data-density="compact"]`, and document in a `/* why */` comment that components
react to it with the `in-data-[density=compact]:` variant, mirroring the
`in-data-[slot=button-group]:` idiom already in `button.tsx:25-32`. Set the
compact overrides that belong at the layer level here — a tighter `--spacing`
and a reduced `--radius` inside the compact subtree — so component files only
need to adjust heights and type sizes. A back-office discovered at Lot 4 to need
a different density is a re-do of validated maquettes; this is the cheap moment.

Do not add any component class for the five signed families — those are TSX
files owned by plans 01-05 and 01-06.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` defines `.atmosphere-wash`, `.atmosphere-blob`, `.atmosphere-grain`, `.reveal-rise`, `.reveal-delay-1`, `.reveal-delay-2`, `.reveal-delay-3`
    - `@keyframes reveal-rise` is defined
    - A `@media (prefers-reduced-motion: reduce)` block exists and sets animation and transition durations to `0.01ms`
    - `[data-density="compact"]` is defined and carries a `/* why */` comment naming the `in-data-[density=compact]:` variant
    - `.atmosphere-grain` uses a `data:image/svg+xml` background with `feTurbulence`; no `.png`, `.jpg`, `.jpeg` or `.webp` reference exists anywhere in the file
    - The whole file still contains exactly one `cubic-bezier(` literal, and every `transition-timing-function` / `animation-timing-function` uses `var(--ease-brand)`
    - No `<script>`, no JS, no `@import` of an external stylesheet was added
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Components and maquettes can compose atmosphere, reveals and density without inventing their own CSS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → Vercel production build | `src/**` is served publicly on `elearning-ariba.vercel.app` |
| build-time font fetch → self-hosted asset | `next/font/google` fetches at build and inlines; no runtime third-party request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | third-party font CDN at runtime | mitigate | `next/font/google` self-hosts at build time — no runtime request to `fonts.googleapis.com`, so no visitor IP leaks and no render-blocking third party (also the D-19 / `GOL-02` requirement) |
| T-01-06 | Denial of Service | Lighthouse budget | mitigate | Atmosphere is CSS and one inline SVG data URI; acceptance criteria forbid any raster reference, keeping the `GOL-02` mobile budget reachable |
| T-01-07 | Tampering | token removal breaking `button.tsx` | mitigate | Acceptance criteria assert every token `button.tsx` consumes still resolves after the rewrite; `npm run typecheck` is the gate |
| T-01-NOINDEX | Information Disclosure | production is publicly reachable and holds unfinished design work | accept | Production carries `X-Robots-Tag: noindex, nofollow` (shipped in Phase 0) and holds no real data. ⛔ No task in this plan may modify `next.config.ts` or `vercel.json`; removing that header is what would make this exploitable |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No install task exists.** Every dependency is already present at a pinned version. An executor that believes an install is needed must stop and report |
| T-01-08 | Elevation of Privilege | runtime authorization | accept | Lot 1 ships none by design (D-11, D-41): no auth surface, no session, no RLS, no guard. Lot 3 adds the gate. This plan touches no `supabase/` file and no `src/types/database.types.ts` |
</threat_model>

<verification>
Manager-session checks after commit (D-39):

1. `npm run typecheck` exits 0 and `npm run build` succeeds.
2. `npm run dev` and load `/` — the page renders in the chosen body face, not the browser default.
3. `grep -c 'cubic-bezier(' src/app/globals.css` returns `1`.
4. Toggle OS reduced-motion and confirm reveals are neutralised.
</verification>

<success_criteria>
- The bleu / blanc / vert palette, type scale, spacing scale, one easing curve and a four-tier paired shadow ladder are defined in one file
- The chosen typefaces are self-hosted and actually applied
- The three Phase-0 placeholder comments in `globals.css` and `layout.tsx` are deleted
- The atmosphere layer and motion primitives exist, CSS-only
- The compact density contract exists for the back-office surface
- `npm run typecheck` passes; nothing pushed
</success_criteria>

<output>
Create `.planning/phases/01-cadrage-contenus-et-design/01-02-SUMMARY.md` when done.
</output>
