---
phase: 01-cadrage-contenus-et-design
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/locales/fr/common.json
  - src/locales/fr/landing.json
  - src/locales/fr/programme.json
  - src/locales/fr/formation.json
  - src/locales/fr/a-propos.json
  - src/locales/fr/contact.json
  - src/locales/fr/_mocks.public.json
autonomous: true
requirements: [CADR-03]

must_haves:
  truths:
    - "Every French string the five public screens need exists in the locale bundle, never inline in JSX (D-29)"
    - "Section headings read as two short sentences with a full stop between them (D-25)"
    - "« Format et modalités » leads with formations live; vidéos à venir is a listed future item, never an included modality (D-14)"
    - "Every client-dependent value is listed in exactly one place with the requirement it blocks (D-07)"
    - "No mock marker is visible on screen — the marker lives in the data, not the rendering (D-07)"
  artifacts:
    - path: "src/locales/fr/landing.json"
      provides: "Copy for the seven landing sections"
      contains: "formatModalites"
    - path: "src/locales/fr/_mocks.public.json"
      provides: "Registry of the client-dependent keys of the public screens"
      contains: "CADR-03"
    - path: "src/locales/fr/common.json"
      provides: "Navigation, footer and shared action labels"
      contains: "nav"
  key_links:
    - from: "src/locales/fr/_mocks.public.json"
      to: "src/locales/fr/landing.json"
      via: "every registered key path resolves in its named bundle"
      pattern: "\"file\""
---

<objective>
Wave 3 of the locked wave shape (D-36), first half: write all French copy for the
five public screens — landing, programme, formation, à propos, contact — plus the
shared navigation, footer and action labels, into the locale bundle.

Purpose: `CADR-03` requires « tous les textes en français ». D-29 makes the
bundle the only home for a string, which is what turns the client's real answers
into a one-place swap. This plan authors **every key the Wave 4 maquettes will
consume**, so no maquette plan invents a key and four parallel plans never fight
over the same file.

Output: six locale files plus a mock registry. No component, no route, no CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md
@.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
The whole content layer today is nine lines, `src/locales/fr/common.json`:
`metadata.title`, `metadata.description`, `home.placeholder`.

Access pattern, confirmed by code and by D-28 — **no i18n library, no barrel,
no registry**: `import landing from "@/locales/fr/landing.json";` then plain
property access. Enabled by `tsconfig.json:13` `resolveJsonModule` and
`tsconfig.json:21-23` `"@/*": ["./src/*"]`. TypeScript infers the literal shape
per file, so a missing key is already a compile error — do **not** create an
index or barrel file, which would erase that inference.

`home.placeholder` stays in place in this plan. Plan 01-09 deletes it in the
same commit that rewrites `src/app/page.tsx`, so the repository never sits in a
broken state between waves.
</interfaces>

**Hard prohibitions:**
- ⛔ No payment provider is named anywhere (D-04).
- ⛔ « Apprenez à votre rythme » and « Accès 24/7 » stay struck — they are not in the signed offer and do not come back through this door (D-15).
- ⛔ No « vidéo à venir » caption, no play-button label, no video-player copy above the fold (D-13).
- ⛔ No SEO metadata, structured data, legal page or RGPD text — that is Lot 5 (D-41).
- ⛔ Do not add an i18n library or a locale switcher (D-28).
- ⛔ Do not create `src/locales/fr/index.ts` or any barrel.
- ⛔ Do not hand-write a price string such as `"1 500 €"` in a value — prices format through `numberFormatter` (D-30). Prices are numeric fields plus a separate label key.
- ⛔ Do not `git push`, create a PR, or run `vercel` (D-38).
- ⛔ `[ASSUMPTION — unverified]` SAP may have renamed the Ariba Network to "SAP Business Network" (D-26). It is **not verified**. Prefer wording that does not depend on which name is current; do not write either brand name as an assertion of fact.
</context>

<execution_order>
D-39 — **commit before verifying.** Commit each task as soon as its acceptance
criteria hold.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Shared chrome copy and the seven landing sections</name>
  <files>src/locales/fr/common.json, src/locales/fr/landing.json</files>
  <read_first>
    - `src/locales/fr/common.json` — the existing nine lines and their shape
    - `.planning/REQUIREMENTS.md` lines 39–52 — `PUB-01` … `PUB-13`, which name the landing sections verbatim
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-13, D-14, D-15, D-25, D-26, D-29, D-30
    - `src/lib/i18n/fr.ts` — the formatter contract prices must respect
  </read_first>
  <action>
Extend `src/locales/fr/common.json` — keep `metadata` and `home.placeholder`
untouched — adding these top-level namespaces:
- `nav` — the labels for the ten French routes (`/programme`, `/formation`,
  `/a-propos`, `/contact`, `/inscription`, `/connexion`, `/agenda`,
  `/reservation`, `/paiement`, `/espace`) plus `accueil`, plus `menu.ouvrir`
  and `menu.fermer` for the mobile toggle
- `footer` — `baseline`, column headings, a `copyright` line, and the labels for
  the five Lot 5 legal pages as **link labels only** (no legal text — D-41)
- `actions` — the shared button labels: `demarrer`, `voirProgramme`,
  `reserver`, `sInscrire`, `seConnecter`, `envoyer`, `telecharger`, `reessayer`,
  `annuler`, `continuer`
- `etats` — the shared state strings the components need: `chargement`,
  `erreurGenerique`, `champRequis`, `aucunResultat`

Create `src/locales/fr/landing.json` with one namespace per landing section, in
the signed order:
`hero`, `pourQui`, `competences`, `programme`, `formatModalites`, `confiance`,
`faq`, `ctaFinal`.

- `hero` — `titre` and `sousTitre`. The title uses the D-25 copy device: **two
  short sentences with a full stop between them, a claim then a turn** — it
  produces the display-type line break and carries the payload in the second
  half. CTA labels come from `common.actions.demarrer` and
  `common.actions.voirProgramme`; do not duplicate them here. ⛔ No video key of
  any kind in `hero` (D-13).
- `pourQui` — heading in the D-25 device, plus an array of the **five** target
  profiles, each `{ titre, description, picto }` where `picto` is the pictogram
  name plan 01-07 will export; plus the reassurance line « même sans expérience
  SAP ».
- `competences` — heading plus the **six** acquired competencies of `PUB-03`:
  écosystème Ariba, Procure-to-Pay, Source-to-Pay, appels d'offres RFQ/RFP,
  catalogues, contrats et workflows, et la préparation à la certification.
- `programme` — heading plus the **five** modules as `{ titre, duree,
  resume }`, feeding the accordion; plus a `telechargerPdf` label. `duree` is a
  **number of hours**, not a formatted string.
- `formatModalites` — ⛔ **keep the signed order: « formations live » leads**
  (D-14). The list is: formations live, supports PDF, *vidéos à venir* — listed
  explicitly as a future item and **never** as an included modality — durée
  d'accès, cas pratiques en fin de module, prérequis.
- `confiance` — protection des données, experts SAP Ariba certifiés, contenus
  régulièrement mis à jour, plus `temoignages.placeholder` and
  `logos.placeholder` labels for the ready placeholders of `PUB-06`.
- `faq` — heading plus **between six and eight** `{ question, reponse }` entries
  answering the most frequent objections.
- `ctaFinal` — heading in the D-25 device plus a supporting line.

Every string is French, uses French typography (`« … »` for quotes, non-breaking
space before `:` `?` `!` where the sentence needs it) and never contains the
struck phrases « Apprenez à votre rythme » or « Accès 24/7 ».

Client-dependent values — the positioning, the commercial promise, the five
module titles and durations, the FAQ answers, the trust proofs — are written as
**plausible, finished-looking French placeholder copy**. They must look finished:
the client reviews these maquettes and a visible `[MOCK]` badge destroys the
fidelity the founder paid for (D-07). The marking happens in Task 3, in a
separate file, never here and never in the rendering.
  </action>
  <verify>
    <automated>node -e "const c=require('./src/locales/fr/common.json');const l=require('./src/locales/fr/landing.json');for(const k of ['metadata','home','nav','footer','actions','etats'])if(!c[k])throw new Error('common missing '+k);for(const k of ['hero','pourQui','competences','programme','formatModalites','confiance','faq','ctaFinal'])if(!l[k])throw new Error('landing missing '+k);const s=JSON.stringify(l)+JSON.stringify(c);if(/Apprenez à votre rythme|Accès 24\/7/.test(s))throw new Error('struck phrase present (D-15)');if(/vidéo à venir|Vidéo à venir/.test(JSON.stringify(l.hero)))throw new Error('video caption in hero (D-13)');if(/video|Video|vidéo/i.test(JSON.stringify(l.hero)))throw new Error('hero must carry no video key or copy (D-13)');const fm=JSON.stringify(l.formatModalites);if(fm.indexOf('live')<0)throw new Error('formatModalites must name formations live');const keys=Object.keys(l.formatModalites);if(l.pourQui.profils.length!==5)throw new Error('pourQui needs exactly 5 profiles');if(l.competences.items.length!==6)throw new Error('competences needs exactly 6 items');if(l.programme.modules.length!==5)throw new Error('programme needs exactly 5 modules');if(l.faq.items.length<6||l.faq.items.length>8)throw new Error('faq needs 6 to 8 entries');for(const m of l.programme.modules)if(typeof m.duree!=='number')throw new Error('module duree must be numeric (D-30)');if(/\d\s?€/.test(s))throw new Error('hand-written price string found (D-30)');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/locales/fr/common.json` keeps `metadata` and `home.placeholder` and adds `nav`, `footer`, `actions`, `etats`
    - `common.nav` carries a label for each of the ten French routes plus `accueil`
    - `src/locales/fr/landing.json` has exactly the namespaces `hero`, `pourQui`, `competences`, `programme`, `formatModalites`, `confiance`, `faq`, `ctaFinal`
    - `landing.pourQui.profils` has length 5; `landing.competences.items` has length 6; `landing.programme.modules` has length 5; `landing.faq.items` has length between 6 and 8
    - Every entry of `landing.programme.modules` has a numeric `duree`
    - `landing.formatModalites` lists formations live first, and names vidéos à venir as a future item
    - `landing.hero` contains no key or value matching `/vid[ée]o/i`
    - `landing.hero.titre` and `landing.ctaFinal.titre` each contain at least two sentences separated by a full stop (D-25)
    - No value anywhere matches `/\d\s?€/` — prices are numeric fields, not formatted strings
    - The strings « Apprenez à votre rythme » and « Accès 24/7 » appear nowhere
    - No payment provider name appears anywhere
    - Both files are valid JSON and no barrel or index file was created
  </acceptance_criteria>
  <done>Every string the landing maquette and the shared chrome need exists in the bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Copy for programme, formation, à propos and contact</name>
  <files>src/locales/fr/programme.json, src/locales/fr/formation.json, src/locales/fr/a-propos.json, src/locales/fr/contact.json</files>
  <read_first>
    - `src/locales/fr/landing.json` — as written in Task 1; the module titles must be identical across the two files
    - `.planning/REQUIREMENTS.md` lines 46–49 — `PUB-08`, `PUB-09`, `PUB-10`, `PUB-11`
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-25, D-26, D-29, D-41
  </read_first>
  <action>
Create four locale files, each with a `titre` and `intro` at the top using the
D-25 two-sentence device.

`programme.json` — the five modules **developed**: for each, `titre` (identical
string to the matching `landing.programme.modules[n].titre`), `objectifs` as an
array of pedagogical objectives, `contenu` as an array of covered points, and a
numeric `duree` matching the landing value. Plus the `telechargerPdf` label.

`formation.json` — format et modalités developed: `deroule` describing the
concrete run of a live session step by step, `fourni` listing what is provided,
`prerequis`, `dureeAcces`. Same D-14 constraint: live leads, vidéos à venir is a
future item and never an included modality.

`a-propos.json` — the trainer: `parcours`, `legitimite` (their SAP Ariba
legitimacy), `approche`. This is the most client-dependent file in the plan —
every value here is a placeholder awaiting the client questionnaire.

`contact.json` — the form **labels only**, no submission logic and no backend
(D-41, Lot 2 owns `PUB-11`): `champs.nom`, `champs.email`, `champs.telephone`,
`champs.profil`, `champs.message`, the `profil` option labels (acheteur,
consultant, étudiant, entreprise), `aideParChamp` hints, per-field validation
messages under `erreurs` including a **server-side rejection** message
(`erreurs.rejetServeur`) that plan 01-06's field error state and plan 01-11's
inscription maquette both consume, plus `succes.titre` and `succes.message` and
`coordonnees` for the direct contact block.

Apply D-26 throughout: prefer wording that does not depend on whether the
network is currently called « Ariba Network » or « SAP Business Network ».
  </action>
  <verify>
    <automated>node -e "const l=require('./src/locales/fr/landing.json');const p=require('./src/locales/fr/programme.json');const f=require('./src/locales/fr/formation.json');const a=require('./src/locales/fr/a-propos.json');const c=require('./src/locales/fr/contact.json');for(const [n,o] of [['programme',p],['formation',f],['a-propos',a],['contact',c]]){if(!o.titre||!o.intro)throw new Error(n+' missing titre/intro');}if(p.modules.length!==5)throw new Error('programme needs 5 modules');l.programme.modules.forEach((m,i)=>{if(p.modules[i].titre!==m.titre)throw new Error('module title drift at '+i);if(p.modules[i].duree!==m.duree)throw new Error('module duree drift at '+i);});for(const k of ['nom','email','telephone','profil','message'])if(!c.champs[k])throw new Error('contact.champs missing '+k);if(!c.erreurs||!c.erreurs.rejetServeur)throw new Error('contact.erreurs.rejetServeur required');const s=JSON.stringify([p,f,a,c]);if(/Apprenez à votre rythme|Accès 24\/7/.test(s))throw new Error('struck phrase present');if(/\d\s?€/.test(s))throw new Error('hand-written price string');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist, are valid JSON, and each has `titre` and `intro`
    - `programme.modules` has length 5, and each entry's `titre` and numeric `duree` are byte-identical to the matching entry in `landing.programme.modules`
    - `formation.json` lists live delivery before any mention of vidéos, and marks vidéos as à venir
    - `contact.champs` carries `nom`, `email`, `telephone`, `profil`, `message`
    - `contact.erreurs.rejetServeur` exists — it is the string the D-23 server-rejected field state renders
    - No file contains submission logic, an endpoint URL, or an API key — labels only
    - No struck phrase, no hand-written price string, no payment provider name
    - No SEO metadata, structured data, legal text or RGPD copy in any file
  </acceptance_criteria>
  <done>The four internal public screens have all their copy.</done>
</task>

<task type="auto">
  <name>Task 3: Register every client-dependent key of the public screens</name>
  <files>src/locales/fr/_mocks.public.json</files>
  <read_first>
    - `src/locales/fr/landing.json`, `src/locales/fr/programme.json`, `src/locales/fr/formation.json`, `src/locales/fr/a-propos.json`, `src/locales/fr/contact.json`, `src/locales/fr/common.json` — as written in Tasks 1 and 2
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-01, D-02, D-07, D-08, D-09
  </read_first>
  <action>
Create `src/locales/fr/_mocks.public.json` — the registry that marks the mocked
copy **in the data, invisibly** (D-07). Shape:

A top-level object with a `mocks` array. Each entry is an object with exactly
these fields: `file` (the bundle filename, e.g. `landing.json`), `key` (the
dotted path inside it, e.g. `hero.titre`, array elements addressed as
`programme.modules.0.titre`), `blocks` (the requirement ID the mock blocks —
`CADR-03` for copy, `CADR-01` for the framing values), and `awaiting` (the
literal string `Cadrage_Formation_SAP_Ariba_Questions_Client.docx`).

Register **every** client-dependent value written in Tasks 1 and 2: the
positioning and the commercial promise (`landing.hero.*`, `landing.ctaFinal.*`),
the five module titles, durations, objectives and contents in **both**
`landing.json` and `programme.json`, the six competencies, the five target
profiles, the FAQ answers, the trust proofs, and the whole of `a-propos.json`.
Labels that are ours and not the client's — navigation, footer link labels,
button labels, form field labels, error messages, state strings — are **not**
registered. Keeping the two sets structurally separable is the point: swapping in
the client's answers must be a deletion from one place, not a hunt (D-09).

⛔ Do not add any marker inside the bundles themselves, do not wrap values in
`{ value, mock }` objects, and do not add a visible badge string anywhere. The
rendering layer must stay untouched.

⛔ Do not create the guard script and do not add an npm script here — plan 01-08
owns `scripts/check-mock-content.mjs`, the `content:check` entry, and the
`_mocks.emails.json` registry. This plan only produces the registry file for the
public screens.

Add a short header comment field `_pourquoi` at the top of the JSON explaining
in French why the file exists and that deleting it is the gate-close action —
JSON has no comments, so this is a documented data field the guard ignores.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const reg=require('./src/locales/fr/_mocks.public.json');if(!reg._pourquoi)throw new Error('_pourquoi required');if(!Array.isArray(reg.mocks)||reg.mocks.length===0)throw new Error('mocks array must be non-empty');const get=(o,p)=>p.split('.').reduce((a,k)=>a===undefined?undefined:a[k],o);const cache={};for(const m of reg.mocks){for(const f of ['file','key','blocks','awaiting'])if(!m[f])throw new Error('entry missing '+f+': '+JSON.stringify(m));if(!['CADR-01','CADR-03'].includes(m.blocks))throw new Error('unexpected blocks id '+m.blocks);if(m.awaiting!=='Cadrage_Formation_SAP_Ariba_Questions_Client.docx')throw new Error('unexpected awaiting value');cache[m.file]=cache[m.file]||JSON.parse(fs.readFileSync('src/locales/fr/'+m.file,'utf8'));if(get(cache[m.file],m.key)===undefined)throw new Error('unresolvable key '+m.file+'#'+m.key);}const files=new Set(reg.mocks.map(m=>m.file));for(const f of ['landing.json','programme.json','a-propos.json'])if(!files.has(f))throw new Error('no mock registered for '+f);const s=fs.readFileSync('src/locales/fr/landing.json','utf8')+fs.readFileSync('src/locales/fr/a-propos.json','utf8');if(/\[MOCK\]|\[FACTICE\]|__mock/i.test(s))throw new Error('visible marker leaked into a bundle (D-07)');console.log('ok — '+reg.mocks.length+' mocked keys registered')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/locales/fr/_mocks.public.json` exists, is valid JSON, has a `_pourquoi` string and a non-empty `mocks` array
    - Every entry has exactly the fields `file`, `key`, `blocks`, `awaiting`
    - Every `blocks` value is `CADR-01` or `CADR-03`; every `awaiting` value is `Cadrage_Formation_SAP_Ariba_Questions_Client.docx`
    - Every registered `file`#`key` resolves to a defined value in that bundle
    - At minimum `landing.json`, `programme.json` and `a-propos.json` each have registered entries
    - `landing.hero.titre`, `landing.ctaFinal.titre` and every `programme.modules.N.titre` are registered
    - No navigation, footer, action or form-label key is registered
    - No bundle file contains `[MOCK]`, `[FACTICE]` or `__mock` — the marker is invisible on screen
    - No `{ "value": …, "mock": … }` wrapper was introduced in any bundle
    - No npm script was added and `scripts/check-mock-content.mjs` was not created
  </acceptance_criteria>
  <done>Every mocked public string is marked once, in the data, invisibly — and the whole registry is one deletable file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mocked copy → client review → Lot 2 | full-fidelity maquettes make mocked copy indistinguishable from real by looking at it |
| locale bundle → public production build | every string here is served publicly once a route consumes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Spoofing | mocked French copy reaching the client as real | mitigate | This is the phase's real threat (D-01). `_mocks.public.json` registers every client-dependent key with the requirement it blocks; plan 01-08's `content:check` exits non-zero while any entry remains. Acceptance criteria assert every registered key resolves, so drift between registry and bundle fails the check |
| T-01-10 | Repudiation | "which strings were ours and which were the client's?" | mitigate | Each entry records `blocks` and `awaiting`, naming the requirement and the outstanding questionnaire, so the gate-close swap is auditable |
| T-01-11 | Information Disclosure | copy leaking internal commercial data | mitigate | ⛔ No day rate, margin, internal calendar or unsold-lot reference may appear in any value — the client owns this repository (CLAUDE.md). No lot number and no phase reference in any client-facing string |
| T-01-12 | Tampering | a visible `[MOCK]` badge destroying review fidelity | mitigate | Acceptance criteria assert no bundle contains `[MOCK]`, `[FACTICE]` or `__mock`; the marker lives only in the sidecar registry |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No install task exists.** This plan writes JSON only |
| T-01-08 | Elevation of Privilege | runtime authorization | accept | No route, no auth surface, no data access in this plan. Lot 1 ships no authorization by design (D-11, D-41) |
</threat_model>

<verification>
Manager-session checks after commit (D-39):

1. `npm run typecheck` exits 0 — the bundles are valid JSON and nothing references a missing key.
2. Read `landing.formatModalites` aloud and confirm live leads (D-14).
3. `grep -ri "apprenez à votre rythme\|accès 24/7" src/locales` returns nothing (D-15).
4. Spot-check five entries of `_mocks.public.json` against the bundles.
</verification>

<success_criteria>
- Six locale files carry every string the five public screens need
- Section headings use the two-sentence copy device
- The signed « format et modalités » order is preserved and no video copy sits in the hero
- Every client-dependent value is registered once, invisibly, with the requirement it blocks
- No barrel, no i18n library, no hand-written price, no provider name
- Committed; nothing pushed
</success_criteria>

<output>
Create `.planning/phases/01-cadrage-contenus-et-design/01-03-SUMMARY.md` when done.
</output>
