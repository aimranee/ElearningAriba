---
phase: 01-cadrage-contenus-et-design
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/locales/fr/inscription.json
  - src/locales/fr/connexion.json
  - src/locales/fr/agenda.json
  - src/locales/fr/reservation.json
  - src/locales/fr/paiement.json
  - src/locales/fr/espace.json
  - src/locales/fr/_mocks.journey.json
autonomous: true
requirements: [CADR-03]

must_haves:
  truths:
    - "Every French string the six journey screens need exists in the locale bundle, never inline in JSX (D-29)"
    - "The four D-23 surface states have their copy: agenda with no slot, espace with no booking, field rejected server-side, payment handoff failed"
    - "The paiement copy names no payment provider and stays provider-neutral (D-04, D-32)"
    - "No price is written as a formatted string — prices are numeric and format through numberFormatter (D-30)"
    - "Every client-dependent journey value is registered once, invisibly, in a deletable sidecar (D-07)"
  artifacts:
    - path: "src/locales/fr/paiement.json"
      provides: "Provider-neutral payment handoff copy incl. the failed-handoff state"
      contains: "echecHandoff"
    - path: "src/locales/fr/espace.json"
      provides: "Learner space copy incl. the no-booking empty state"
      contains: "aucuneReservation"
    - path: "src/locales/fr/agenda.json"
      provides: "Agenda copy incl. the no-available-slot empty state"
      contains: "aucunCreneau"
    - path: "src/locales/fr/_mocks.journey.json"
      provides: "Registry of the client-dependent keys of the journey screens"
      contains: "CADR-03"
  key_links:
    - from: "src/locales/fr/_mocks.journey.json"
      to: "src/locales/fr/paiement.json"
      via: "every registered key path resolves in its named bundle"
      pattern: "\"file\""
---

<objective>
Wave 3 of the locked wave shape (D-36), second half: write all French copy for
the six journey screens — inscription, connexion, agenda, réservation, paiement,
espace apprenant — including the copy for the four surface-level states D-23
names as a deliverable.

Purpose: `CADR-03` covers « libellés de formulaires » and the journey screens
carry most of them. D-29 makes the bundle the only home for a string. Authoring
every key here means the four parallel Wave 4 maquette plans consume and never
invent, so they never collide on a locale file.

Output: six locale files plus the journey mock registry. No component, no route,
no form logic.

Runs in parallel with plan 01-02 — disjoint files, no shared dependency beyond
the founder's Wave 1 pick.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md
@.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
Access pattern — no i18n library, no barrel, no registry (D-28):
`import paiement from "@/locales/fr/paiement.json";` then plain property access.
`tsconfig.json:13` `resolveJsonModule`, `tsconfig.json:21-23` `"@/*": ["./src/*"]`.
TypeScript infers the literal shape per file, so a missing key is already a
compile error. ⛔ Do not create an index or barrel file — it would erase that
inference.

Formatters that own every date, time and number — `src/lib/i18n/fr.ts`:
`LOCALE = "fr-FR"`, `TIME_ZONE = "Europe/Paris"`, `dateFormatter`,
`dateTimeFormatter`, `numberFormatter`, `formatDate`, `formatDateTime`,
`formatNumber`. ⚠ `numberFormatter` has **no currency style**, so
`formatNumber(1500)` yields `"1 500"` with no « € ». Plan 01-12 adds a
`currencyFormatter` and a `timeFormatter` to that file. This plan therefore
stores prices as **numbers** and durations as **numbers of minutes**, never as
formatted strings.
</interfaces>

**Hard prohibitions:**
- ⛔ **No payment provider is named anywhere** (D-04). The paiement copy is provider-neutral by design (D-32): an order summary, the formula and price, the consent line, and a primary action that hands off to an external payment page. Nothing on that screen changes when the provider is finally named.
- ⛔ No account, session, role, RLS policy or Google sign-in copy that implies working auth — labels only, no logic (D-41, Lot 3).
- ⛔ No availability, booking or agenda **logic**; no slot data source; no `.ics`; no reminder copy (D-41, Lot 4).
- ⛔ No invoice, receipt, promotional code or refund-policy text (D-41, Lot 7 / Lot 5).
- ⛔ No tenancy concept — no organisation, workspace or tenant string anywhere (D-42).
- ⛔ « Apprenez à votre rythme » and « Accès 24/7 » stay struck (D-15).
- ⛔ Do not hand-write a price string such as `"1 500 €"` (D-30).
- ⛔ Do not add an i18n library or a locale switcher (D-28).
- ⛔ Do not `git push`, create a PR, or run `vercel` (D-38).
- ⛔ `[ASSUMPTION — unverified]` the Ariba Network may now be « SAP Business Network » (D-26). Prefer wording independent of which name is current.
</context>

<execution_order>
D-39 — **commit before verifying.** Commit each task as soon as its acceptance
criteria hold.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Account copy — inscription, connexion, espace apprenant</name>
  <files>src/locales/fr/inscription.json, src/locales/fr/connexion.json, src/locales/fr/espace.json</files>
  <read_first>
    - `src/locales/fr/common.json` — the `actions` and `etats` namespaces plan 01-03 authored; do not duplicate a label that already lives there
    - `src/locales/fr/contact.json` — the `erreurs` shape, including `erreurs.rejetServeur`; reuse the same field-error vocabulary
    - `.planning/REQUIREMENTS.md` lines 55–63 — `CPT-01` … `CPT-09`, which name the learner-space surfaces
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-23, D-25, D-29, D-41, D-42
  </read_first>
  <action>
Create three locale files. Each opens with `titre` and `intro`, the `titre`
using the D-25 device — two short sentences with a full stop between them.

`inscription.json` — the account-creation form **labels only**:
`champs.prenom`, `champs.nom`, `champs.email`, `champs.motDePasse`,
`champs.confirmation`, `champs.profil` with its four option labels (acheteur,
consultant, étudiant, entreprise), a `conditions` consent line pointing at the
Lot 5 legal pages **by label only**, an `aideParChamp` hint per field, and an
`erreurs` block carrying `requis`, `emailInvalide`, `motDePasseFaible`,
`confirmationDifferente` and **`rejetServeur`** — the string the D-23
server-rejected field state renders. Plus `succes.titre` / `succes.message`
describing the email-verification step in future tense, and a
`dejaInscrit` link label toward `/connexion`.

`connexion.json` — `champs.email`, `champs.motDePasse`, a `motDePasseOublie`
link label, a `google` button label for the one-click Google sign-in of `CPT-02`
(**label only** — no OAuth copy, no scope description, no consent-screen text),
an `erreurs` block with `identifiantsInvalides`, `rejetServeur` and
`tropDeTentatives`, and a `pasDeCompte` link label toward `/inscription`.

`espace.json` — the learner space of `CPT-06`, with one namespace per surface:
`rendezVous`, `sessions`, `historique`, `documents`, `factures`, `avancement`.
Each surface carries a `titre` and — this is the D-23 deliverable — an **honest
empty state** `{ titre, message, action }`. The top-level empty state, when the
learner has booked nothing at all, is the key **`aucuneReservation`** with
`titre`, `message` and an `action` label pointing at `/agenda`. A maquette
validated without its empty state is a Lot 2 re-do, so these strings are not
optional. Also add `bienvenue` with a `{prenom}` placeholder token written
literally as `{prenom}` — interpolation is the maquette's concern, not an i18n
library's (D-28).

Every string is French. ⛔ No organisation, workspace, tenant or team string —
single-tenant by construction (D-42). ⛔ No invoice number format, no certificate
copy, no attendance copy — those are Lots 7 and 9.
  </action>
  <verify>
    <automated>node -e "const i=require('./src/locales/fr/inscription.json');const c=require('./src/locales/fr/connexion.json');const e=require('./src/locales/fr/espace.json');for(const [n,o] of [['inscription',i],['connexion',c],['espace',e]]){if(!o.titre||!o.intro)throw new Error(n+' missing titre/intro');if(!/\.\s/.test(o.titre.trim())&&o.titre.split('.').filter(Boolean).length<2)throw new Error(n+'.titre must be two sentences (D-25)');}for(const k of ['prenom','nom','email','motDePasse','confirmation','profil'])if(!i.champs[k])throw new Error('inscription.champs missing '+k);for(const k of ['requis','emailInvalide','motDePasseFaible','confirmationDifferente','rejetServeur'])if(!i.erreurs[k])throw new Error('inscription.erreurs missing '+k);for(const k of ['identifiantsInvalides','rejetServeur','tropDeTentatives'])if(!c.erreurs[k])throw new Error('connexion.erreurs missing '+k);if(!c.champs.email||!c.champs.motDePasse||!c.motDePasseOublie||!c.google)throw new Error('connexion missing keys');if(!e.aucuneReservation||!e.aucuneReservation.titre||!e.aucuneReservation.message||!e.aucuneReservation.action)throw new Error('espace.aucuneReservation must carry titre/message/action (D-23)');for(const s of ['rendezVous','sessions','historique','documents','factures','avancement']){if(!e[s])throw new Error('espace missing surface '+s);if(!e[s].vide||!e[s].vide.titre||!e[s].vide.message)throw new Error('espace.'+s+' missing honest empty state');}if(!/\{prenom\}/.test(JSON.stringify(e)))throw new Error('espace.bienvenue must carry the {prenom} token');const s=JSON.stringify([i,c,e]);if(/organisation|workspace|tenant|équipe/i.test(s))throw new Error('tenancy concept present (D-42)');if(/\d\s?€/.test(s))throw new Error('hand-written price string (D-30)');if(/Apprenez à votre rythme|Accès 24\/7/.test(s))throw new Error('struck phrase present (D-15)');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The three files exist, are valid JSON, and each has `titre` and `intro` with a two-sentence `titre`
    - `inscription.champs` carries `prenom`, `nom`, `email`, `motDePasse`, `confirmation`, `profil` with four option labels
    - `inscription.erreurs` carries `requis`, `emailInvalide`, `motDePasseFaible`, `confirmationDifferente`, `rejetServeur`
    - `connexion` carries `champs.email`, `champs.motDePasse`, `motDePasseOublie`, `google`, and `erreurs.identifiantsInvalides` / `erreurs.rejetServeur` / `erreurs.tropDeTentatives`
    - `espace` carries the six surfaces `rendezVous`, `sessions`, `historique`, `documents`, `factures`, `avancement`, each with a `vide` object having `titre` and `message`
    - `espace.aucuneReservation` exists with `titre`, `message` and `action`
    - `espace` contains the literal token `{prenom}`
    - No value matches `/organisation|workspace|tenant|équipe/i`
    - No value matches `/\d\s?€/`; no struck phrase; no payment provider name
    - No submission endpoint, OAuth scope, RLS rule or session copy appears — labels only
  </acceptance_criteria>
  <done>Inscription, connexion and the learner space have every string, including their honest empty states.</done>
</task>

<task type="auto">
  <name>Task 2: Booking and payment copy — agenda, réservation, paiement</name>
  <files>src/locales/fr/agenda.json, src/locales/fr/reservation.json, src/locales/fr/paiement.json</files>
  <read_first>
    - `src/lib/i18n/fr.ts` — all 43 lines; the formatter contract that forbids formatted date, time and price strings in the bundle
    - `src/locales/fr/landing.json` — `programme.modules`, whose titles the réservation summary reuses
    - `.planning/REQUIREMENTS.md` lines 67–75 and 100–109 — `AGD-*` and `PAY-*`, for the vocabulary only
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-04, D-23, D-30, D-32, D-41
  </read_first>
  <action>
Create three locale files, each with `titre` and `intro` in the D-25 device.

`agenda.json` — the public calendar and slot list of `AGD-02`. Keys:
`moisPrecedent` / `moisSuivant` navigation labels, `legende` with the labels for
libre / réservé / passé / bloqué, `typesRendezVous` as an array of
`{ id, libelle, dureeMinutes, prix }` covering the 30-minute discovery call
(price `0`) and the one-hour individual session — `dureeMinutes` and `prix` are
**numbers**, never formatted strings (D-30) — and the D-23 empty state
**`aucunCreneau`** with `titre`, `message` and an `action` label. « Aucun
créneau disponible » is the state a maquette validated without it turns into a
Lot 2 re-do, so it is a named deliverable here. ⛔ No availability rule, no
recurrence, no timezone string, no `.ics` copy — Lot 4 owns those (D-41).

`reservation.json` — the confirmation step of `AGD-04`. Keys: `etapes` naming
the three steps (type de rendez-vous, créneau, confirmation), a `recapitulatif`
block with the labels for type, date, heure, durée and prix — **labels only, the
values are formatted at render through `formatDateTime` and the currency
formatter** — a `conditions` consent line, and `erreurs.creneauIndisponible`
for the case where the slot was taken while the learner hesitated.

`paiement.json` — **provider-neutral by design** (D-32). Lot 7 is contractually
hosted payment, and redirect-to-a-hosted-page is the shape every hosted provider
shares, so nothing on this screen changes when the provider is finally named.
Keys: `recapitulatif` with labels for la formule, le contenu inclus and le
montant; `formules` as an array of the **four sale formulas** written as
`{ id, libelle, description, prix, inclus }` where `prix` is a **number** and
`inclus` a list of strings — no price in this lot is a commitment, the four
formulas are Lot 7's contract; a `consentement` line; `action.payer` as the
primary action label describing a handoff to an **external secure payment page**
without naming who runs it; a `securite` reassurance line saying no bank data
transits or is stored on the site; and the D-23 state **`echecHandoff`** with
`titre`, `message` and a `reessayer` action for a payment handoff that fails.

⛔ Do not write the word Stripe, PayPal, Mollie, Adyen, Checkout, Lemon Squeezy,
Paddle, or any other provider name, in any file, in any casing (D-04). ⛔ Do not
write an order status vocabulary, an invoice label, a promotional-code field or
a refund clause — Lot 7 and Lot 5 own those (D-41).
  </action>
  <verify>
    <automated>node -e "const a=require('./src/locales/fr/agenda.json');const r=require('./src/locales/fr/reservation.json');const p=require('./src/locales/fr/paiement.json');for(const [n,o] of [['agenda',a],['reservation',r],['paiement',p]])if(!o.titre||!o.intro)throw new Error(n+' missing titre/intro');if(!a.aucunCreneau||!a.aucunCreneau.titre||!a.aucunCreneau.message||!a.aucunCreneau.action)throw new Error('agenda.aucunCreneau must carry titre/message/action (D-23)');if(!Array.isArray(a.typesRendezVous)||a.typesRendezVous.length<2)throw new Error('agenda.typesRendezVous needs the discovery call and the individual session');for(const t of a.typesRendezVous){if(typeof t.dureeMinutes!=='number'||typeof t.prix!=='number')throw new Error('typesRendezVous dureeMinutes and prix must be numbers (D-30)');}if(!a.typesRendezVous.some(t=>t.prix===0))throw new Error('the 30-minute discovery call must be free');if(!r.etapes||!r.recapitulatif||!r.erreurs||!r.erreurs.creneauIndisponible)throw new Error('reservation missing keys');if(!Array.isArray(p.formules)||p.formules.length!==4)throw new Error('paiement.formules needs exactly 4 sale formulas');for(const f of p.formules){if(typeof f.prix!=='number')throw new Error('formule prix must be numeric (D-30)');if(!Array.isArray(f.inclus))throw new Error('formule inclus must be a list');}if(!p.echecHandoff||!p.echecHandoff.titre||!p.echecHandoff.message||!p.echecHandoff.reessayer)throw new Error('paiement.echecHandoff must carry titre/message/reessayer (D-23)');if(!p.consentement||!p.securite||!p.action||!p.action.payer)throw new Error('paiement missing consent/securite/action.payer');const s=JSON.stringify([a,r,p]);if(/stripe|paypal|mollie|adyen|checkout\.com|lemon ?squeezy|paddle|braintree|payplug/i.test(s))throw new Error('payment provider named (D-04)');if(/\d\s?€/.test(s))throw new Error('hand-written price string (D-30)');if(/facture|remboursement|code promo/i.test(s))throw new Error('Lot 7 / Lot 5 copy present (D-41)');if(/organisation|workspace|tenant/i.test(s))throw new Error('tenancy concept present (D-42)');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The three files exist, are valid JSON, and each has `titre` and `intro`
    - `agenda.aucunCreneau` carries `titre`, `message`, `action`
    - `agenda.typesRendezVous` has at least two entries; every `dureeMinutes` and `prix` is a number; at least one entry has `prix: 0`
    - `agenda.legende` carries labels for libre, réservé, passé and bloqué
    - `reservation.etapes` names the three booking steps; `reservation.recapitulatif` carries labels only; `reservation.erreurs.creneauIndisponible` exists
    - `paiement.formules` has exactly four entries, each with a numeric `prix` and an array `inclus`
    - `paiement.echecHandoff` carries `titre`, `message`, `reessayer`
    - `paiement` carries `consentement`, `securite` and `action.payer`
    - No file matches `/stripe|paypal|mollie|adyen|checkout\.com|lemon ?squeezy|paddle|braintree|payplug/i` in any casing
    - No file matches `/\d\s?€/`, `/facture|remboursement|code promo/i`, or `/organisation|workspace|tenant/i`
    - No `.ics`, recurrence rule, timezone string or availability rule appears
  </acceptance_criteria>
  <done>Agenda, réservation and a provider-neutral paiement have every string, including their two failure states.</done>
</task>

<task type="auto">
  <name>Task 3: Register every client-dependent key of the journey screens</name>
  <files>src/locales/fr/_mocks.journey.json</files>
  <read_first>
    - `src/locales/fr/_mocks.public.json` — the registry shape plan 01-03 established; this file must match it field for field
    - `src/locales/fr/inscription.json`, `connexion.json`, `espace.json`, `agenda.json`, `reservation.json`, `paiement.json` — as written in Tasks 1 and 2
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-07, D-08, D-09
  </read_first>
  <action>
Create `src/locales/fr/_mocks.journey.json` with **exactly the same shape** as
`src/locales/fr/_mocks.public.json`: a `_pourquoi` string and a `mocks` array
whose entries carry exactly `file`, `key`, `blocks`, `awaiting`. Plan 01-08's
guard discovers registries by the `_mocks.*.json` filename pattern, so the shape
is a contract — do not invent a variant.

Register every value the client owns and we guessed: the four sale formulas in
`paiement.formules` (each `libelle`, `description`, `prix`, `inclus`), the
appointment types and their prices in `agenda.typesRendezVous`, and any
commercial promise wording in the journey `intro` lines. `blocks` is `CADR-01`
for the sale formulas and the appointment prices — those are framing-workshop
output — and `CADR-03` for copy. `awaiting` is the literal string
`Cadrage_Formation_SAP_Ariba_Questions_Client.docx`.

Do **not** register form field labels, error messages, navigation labels, step
names, legend labels or empty-state copy: those are ours, not the client's, and
keeping the two sets structurally separable is what makes the gate-close swap a
deletion from one place rather than a hunt (D-09).

⛔ Do not add any marker inside the bundles, do not wrap values in
`{ value, mock }`, and do not add a visible badge string. ⛔ Do not create the
guard script and do not add an npm script — plan 01-08 owns
`scripts/check-mock-content.mjs` and the `content:check` entry.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const pub=require('./src/locales/fr/_mocks.public.json');const reg=require('./src/locales/fr/_mocks.journey.json');if(!reg._pourquoi)throw new Error('_pourquoi required');if(!Array.isArray(reg.mocks)||reg.mocks.length===0)throw new Error('mocks array must be non-empty');const pubFields=Object.keys(pub.mocks[0]).sort().join(',');for(const m of reg.mocks){if(Object.keys(m).sort().join(',')!==pubFields)throw new Error('entry shape differs from _mocks.public.json: '+JSON.stringify(m));if(!['CADR-01','CADR-03'].includes(m.blocks))throw new Error('unexpected blocks id '+m.blocks);if(m.awaiting!=='Cadrage_Formation_SAP_Ariba_Questions_Client.docx')throw new Error('unexpected awaiting value');}const get=(o,p)=>p.split('.').reduce((x,k)=>x===undefined?undefined:x[k],o);const cache={};for(const m of reg.mocks){cache[m.file]=cache[m.file]||JSON.parse(fs.readFileSync('src/locales/fr/'+m.file,'utf8'));if(get(cache[m.file],m.key)===undefined)throw new Error('unresolvable key '+m.file+'#'+m.key);}const files=new Set(reg.mocks.map(m=>m.file));for(const f of ['paiement.json','agenda.json'])if(!files.has(f))throw new Error('no mock registered for '+f);for(let i=0;i<4;i++)if(!reg.mocks.some(m=>m.file==='paiement.json'&&m.key.startsWith('formules.'+i)))throw new Error('sale formula '+i+' not registered');const blob=['inscription','connexion','espace','agenda','reservation','paiement'].map(n=>fs.readFileSync('src/locales/fr/'+n+'.json','utf8')).join('');if(/\[MOCK\]|\[FACTICE\]|__mock/i.test(blob))throw new Error('visible marker leaked into a bundle (D-07)');console.log('ok — '+reg.mocks.length+' mocked journey keys registered')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/locales/fr/_mocks.journey.json` exists, is valid JSON, has `_pourquoi` and a non-empty `mocks` array
    - Every entry has exactly the same field set as the entries of `src/locales/fr/_mocks.public.json`
    - Every `blocks` value is `CADR-01` or `CADR-03`; every `awaiting` value is `Cadrage_Formation_SAP_Ariba_Questions_Client.docx`
    - Every registered `file`#`key` resolves to a defined value in that bundle
    - All four `paiement.formules.N` entries are registered, and `agenda.typesRendezVous` prices are registered
    - No form label, error message, step name, legend label or empty-state key is registered
    - No journey bundle contains `[MOCK]`, `[FACTICE]` or `__mock`
    - No npm script was added and `scripts/check-mock-content.mjs` was not created
  </acceptance_criteria>
  <done>Every mocked journey string is marked once, in the data, invisibly — in a second deletable file matching the public one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mocked copy → client review → Lot 2 | full-fidelity maquettes make mocked copy indistinguishable from real by looking at it |
| locale bundle → public production build | every journey string is served publicly once a route consumes it |
| paiement screen → external hosted payment page | the handoff boundary Lot 7 will implement; nothing crosses it in Lot 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Spoofing | mocked sale formulas and prices reaching the client as committed | mitigate | `_mocks.journey.json` registers all four `paiement.formules.N` entries and the appointment prices under `blocks: CADR-01`; plan 01-08's `content:check` exits non-zero while any remain. Prices are numeric fields, so no formatted price string can be mistaken for a signed figure |
| T-01-14 | Information Disclosure | a provider name leaking into client-facing copy before the choice is made | mitigate | Acceptance criteria assert no file matches the provider-name pattern in any casing (D-04). The screen is provider-neutral by construction (D-32), so nothing changes when Lot 7 names one |
| T-01-15 | Tampering | bank data implied to transit the site | mitigate | `paiement.securite` states plainly that no bank data transits or is stored on the site, matching the contractual hosted-payment shape of `PAY-01`. No card field label exists in any bundle |
| T-01-12 | Tampering | a visible `[MOCK]` badge destroying review fidelity | mitigate | Acceptance criteria assert no journey bundle contains `[MOCK]`, `[FACTICE]` or `__mock`; the marker lives only in the sidecar registry |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No install task exists.** This plan writes JSON only |
| T-01-08 | Elevation of Privilege | runtime authorization on `/espace` | accept | Lot 1 ships no authorization by design (D-11, D-41): this plan writes labels, no route, no session, no guard. `/espace` becomes an unguarded route in plan 01-11, safe only because production carries `X-Robots-Tag: noindex, nofollow` and holds no real data; Lot 3 adds the gate |
</threat_model>

<verification>
Manager-session checks after commit (D-39):

1. `npm run typecheck` exits 0 — the six bundles are valid JSON.
2. `grep -rniE "stripe|paypal|mollie|adyen|paddle" src/locales` returns nothing (D-04).
3. `grep -rn "€" src/locales` returns nothing outside a standalone currency label.
4. Read `espace.aucuneReservation` and `agenda.aucunCreneau` and confirm both are honest, not apologetic placeholders.
5. Spot-check five entries of `_mocks.journey.json` against the bundles.
</verification>

<success_criteria>
- Six locale files carry every string the six journey screens need
- The four D-23 surface states have their copy: `agenda.aucunCreneau`, `espace.aucuneReservation`, `*.erreurs.rejetServeur`, `paiement.echecHandoff`
- The paiement screen is provider-neutral and names no provider
- All prices and durations are numeric; no formatted price string exists
- Every client-dependent journey value is registered once, invisibly, in a deletable sidecar matching the public registry shape
- Committed; nothing pushed
</success_criteria>

<output>
Create `.planning/phases/01-cadrage-contenus-et-design/01-04-SUMMARY.md` when done.
</output>
