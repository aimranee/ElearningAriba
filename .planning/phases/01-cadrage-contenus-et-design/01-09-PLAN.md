---
phase: 01-cadrage-contenus-et-design
plan: 09
type: execute
wave: 5
depends_on: ["01-03", "01-05", "01-06", "01-07"]
files_modified:
  - src/app/page.tsx
  - src/locales/fr/common.json
  - src/components/sections/hero.tsx
  - src/components/sections/section.tsx
autonomous: true
requirements: [CADR-06]

must_haves:
  truths:
    - "The landing maquette exists at / as a real Next.js route on the real design system (D-05)"
    - "All seven signed landing sections are present, in the signed order"
    - "The hero accepts a video later without the visitor seeing a hole today (D-13)"
    - "« Format et modalités » leads with formations live (D-14)"
    - "The Phase 0 home placeholder is gone from both the route and the locale bundle"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "The landing maquette"
      min_lines: 60
    - path: "src/components/sections/hero.tsx"
      provides: "The hero with the prepared, invisible-until-filled video slot"
      exports: ["Hero"]
    - path: "src/components/sections/section.tsx"
      provides: "The shared section shell every landing block and internal page uses"
      exports: ["Section", "SectionHeader"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/locales/fr/landing.json"
      via: "direct JSON import, zero literal French in JSX"
      pattern: "@/locales/fr/landing.json"
    - from: "src/app/page.tsx"
      to: "src/components/icons/pictograms.tsx"
      via: "picto names resolved through the registry"
      pattern: "pictograms"
---

<objective>
Wave 4 of the locked wave shape (D-37), screen 1 of 11: the **landing** maquette,
built as a real Next.js route on the real design system, consuming the real
content layer.

Purpose: D-05 makes the maquettes full fidelity and real, so a validated maquette
*is* the next lot's page. That is a fidelity-and-medium choice, not a scope
change — `CADR-06` already names the eleven screens and they are signed.

Output: the landing route, the hero, and the shared section shell the other
public screens reuse.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md
@.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md
@CLAUDE.md

<interfaces>
**The route template** — `src/app/page.tsx` today, nine lines:
`import common from "@/locales/fr/common.json";` then
`export default function Home() { return (<div className="flex flex-1 …"><p>{common.home.placeholder}</p></div>); }`.
Rules it establishes and this plan keeps:
- **server component, no `"use client"`**; only interactive leaves carry it
- `export default function <PascalName>()` — synchronous, **not** `async`, no data fetching (D-41)
- **no `metadata` export on the page** — only `layout.tsx` exports it; per-route metadata is `GOL-01`, Lot 5 (D-41)
- the layout owns the chrome: plan 01-07 wrapped `{children}` in `<main className="flex-1">`, so a page no longer needs its own `flex-1`
- typed routes: a page taking no params omits props entirely; if props are ever needed the type is the generated `PageProps<"/route">` global — **never a hand-written interface**

**Available from earlier plans:**
- `@/components/ui/button` → `Button`, `buttonVariants` (variants incl. `success`; `data-loading`; `in-data-[density=compact]:`)
- `@/components/ui/card` → `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`, `CardSkeleton`
- `@/components/ui/badge` → `Badge`
- `@/components/ui/message` → `Message`
- `@/components/ui/accordion` → `Accordion`, `AccordionItem`, `AccordionHeader`, `AccordionTrigger`, `AccordionPanel` (**client component** — importing it into a server page is fine; it is the leaf that is client)
- `@/components/icons/pictograms` → `pictograms` registry and the `PictogramName` type
- `@/components/layout/header`, `@/components/layout/footer` — already mounted in the root layout; **do not render them again**
- `src/app/globals.css` → `.atmosphere-wash`, `.atmosphere-blob`, `.atmosphere-grain`, `.reveal-rise`, `.reveal-delay-1..3`, the `--shadow-N` ladder, the `--text-*` scale, `--ease-brand`
- `@/locales/fr/landing.json` → `hero`, `pourQui`, `competences`, `programme`, `formatModalites`, `confiance`, `faq`, `ctaFinal`
- `@/locales/fr/common.json` → `nav`, `footer`, `actions`, `etats`
- `@/lib/i18n/fr.ts` → `formatNumber`, `formatDate`, `formatDateTime`
</interfaces>

**Hard prohibitions:**
- ⛔ **No hero video player frame, no play button, no « vidéo à venir » caption above the fold** (D-13). *Ready* means the code accepts a video later, not that the visitor sees a hole.
- ⛔ In « Format et modalités », keep the **signed order — formations live leads**; *vidéos à venir* stays a listed future item and is **never** presented as an included modality (D-14).
- ⛔ « Apprenez à votre rythme » and « Accès 24/7 » stay struck (D-15).
- ⛔ **Do not build a generative hero** — no JS-computed Bézier paths, no `stroke-dasharray` draw-on, no comets, no `animateMotion`, no resize handler (D-20).
- ⛔ No page logic, no data fetching, no `async` page, no form submission, no contact-form backend (D-41).
- ⛔ No per-route `metadata`, Open Graph, Twitter Card, structured data, `sitemap.xml` or `robots.txt` (D-41, Lot 5).
- ⛔ No French string literal in JSX — every word reads from the locale bundle (D-29). If a key is missing, **stop and report**: plan 01-03 owns the copy and inventing a key here creates a same-wave conflict.
- ⛔ No hand-written price string; prices format through `formatNumber` (D-30).
- ⛔ No payment provider named (D-04).
- ⛔ No raster asset, nothing under `public/` (D-06, D-21).
- ⛔ Do not modify any file under `src/components/ui/`, `src/components/icons/`, `src/components/layout/`, `src/app/globals.css`, `src/app/layout.tsx`, or any locale file other than removing `home.placeholder` from `common.json`.
- ⛔ Do not run `npm install`. Do not `git push`, create a PR, or run `vercel` (D-38).
</context>

<execution_order>
D-39 — **commit before verifying.** Commit each task as soon as its acceptance
criteria hold. `npm run typecheck` runs once, as the last task's verification.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: The shared section shell and the hero</name>
  <files>src/components/sections/section.tsx, src/components/sections/hero.tsx</files>
  <read_first>
    - `src/app/globals.css` — `.atmosphere-wash`, `.atmosphere-blob`, `.atmosphere-grain`, `.reveal-*`, the `--text-*` scale
    - `src/components/ui/button.tsx` — the CTA API and its variants
    - `src/locales/fr/landing.json` — `hero`, and the shape every section header shares
    - `src/app/layout.tsx` — the `<main className="flex-1">` wrapper plan 01-07 added, so sections do not fight it
  </read_first>
  <action>
Create `src/components/sections/` — it does not exist yet.

`section.tsx` exports `Section` and `SectionHeader`, both server components.
`Section` is the shell every landing block and every internal page reuses:
a `<section>` with `data-slot="section"`, consistent vertical rhythm from the
spacing scale, a max-width container, and a `tone` variant of `default`,
`muted` and `atmosphere` — the last composing `.atmosphere-wash` plus one
`.atmosphere-blob`, both `aria-hidden`. `SectionHeader` renders an `<h2>` in the
`--font-heading` face at the `--text-title` step and an optional lead paragraph
at `--text-lead`. Both take their text as props; ⛔ no French literal.

Because the copy device is two short sentences with a full stop between them
(D-25), `SectionHeader` must render the title so the break can land: keep the
title in one element and let the display type wrap naturally — do not split the
string on `.` in JavaScript, which would break on any abbreviation.

`hero.tsx` exports `Hero`, a server component rendering `landing.hero.titre` at
the `--text-display` step, `landing.hero.sousTitre`, the primary CTA
(`common.actions.demarrer` → `/inscription`) and the secondary CTA
(`common.actions.voirProgramme` → `/programme`), both as `next/link`-wrapped
`Button`s with typed hrefs.

**The video slot** (D-13, and `PUB-01`'s « emplacement vidéo prêt ») is the
delicate part. Give `Hero` an optional `video` prop typed as a React node.
When it is absent — which is the whole of Lot 1 — the hero renders **the visual
composition and nothing else**: no player frame, no play button, no aspect-ratio
box, no caption, no dashed placeholder, no « vidéo à venir » text. *Ready* means
the component accepts a video later, not that the visitor sees a hole. Write a
`/* why */` comment saying exactly that, because the next reader's instinct will
be to add a placeholder.

The one strong idea we can afford (D-20): compose the hero from the atmosphere
utilities and a `.reveal-rise` entrance staggered across the title, subtitle and
CTAs with `.reveal-delay-1..3`. ⛔ No `<script>`, no `useEffect`, no
`requestAnimationFrame`, no `stroke-dasharray`, no `animateMotion`, no resize
listener. The reference's generative centrepiece is roughly a day on its own and
the whole lot is two and a half.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/sections/section.tsx','utf8');const h=fs.readFileSync('src/components/sections/hero.tsx','utf8');for(const [n,c] of [['section',s],['hero',h]]){if(/'use client'|\"use client\"/.test(c))throw new Error(n+': must stay a server component');if(/:\s*any\b/.test(c))throw new Error(n+': any type');if(/export default/.test(c))throw new Error(n+': default export forbidden');if(/dangerouslySetInnerHTML/.test(c))throw new Error(n+': forbidden');const st=c.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/.*/g,'');if(/>[^<>{}]*[à-ÿ]{2,}[^<>{}]*</.test(st))throw new Error(n+': hardcoded French string in JSX (D-29)');if(/\.(png|jpe?g|webp|avif)\b/.test(c))throw new Error(n+': raster asset forbidden');}if(/<video|play|Play|animateMotion|stroke-dasharray|requestAnimationFrame|useEffect|addEventListener/.test(h))throw new Error('hero: prohibited construct (D-13/D-20)');if(/vid[ée]o à venir/i.test(h))throw new Error('hero: video caption forbidden (D-13)');if(!/atmosphere-/.test(h))throw new Error('hero must compose the atmosphere layer');if(!/reveal-/.test(h))throw new Error('hero must use the reveal primitives');if(!/data-slot=\"section\"/.test(s))throw new Error('Section needs data-slot');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/section.tsx` exports `Section` and `SectionHeader`; `Section` carries `data-slot="section"` and a `tone` variant of `default` / `muted` / `atmosphere`
    - `src/components/sections/hero.tsx` exports `Hero` with an optional `video` React-node prop and a `/* why */` comment explaining that absent means *render nothing*, not *render a placeholder*
    - When `video` is absent the rendered output contains no `<video>`, no play control, no aspect-ratio placeholder box and no caption
    - `hero.tsx` contains none of `animateMotion`, `stroke-dasharray`, `requestAnimationFrame`, `useEffect`, `addEventListener`, `<script`
    - `hero.tsx` composes at least one `.atmosphere-*` utility and the `.reveal-*` primitives
    - Both files are server components, have no default export, no `: any`, no `dangerouslySetInnerHTML`, no French string literal in JSX, no raster reference
    - Both CTAs are `next/link` elements with typed hrefs `/inscription` and `/programme`
  </acceptance_criteria>
  <done>The hero is finished-looking without a video, and every later section has one shell to sit in.</done>
</task>

<task type="auto">
  <name>Task 2: The seven landing sections</name>
  <files>src/app/page.tsx, src/locales/fr/common.json</files>
  <read_first>
    - `src/app/page.tsx` — the current nine lines and the route template they establish
    - `src/locales/fr/landing.json` — every namespace, read in full; the JSX must consume it, not paraphrase it
    - `src/components/icons/pictograms.tsx` — the `pictograms` registry and the `PictogramName` type
    - `src/components/ui/accordion.tsx`, `card.tsx`, `badge.tsx` — the APIs the sections compose
    - `src/lib/i18n/fr.ts` — `formatNumber`, the only legal way to render a duration or a number
  </read_first>
  <action>
Rewrite `src/app/page.tsx` as the landing maquette. Keep it a **synchronous
server component** with a default export named `Home` and **no `metadata`
export**.

Render, in the signed order: `Hero`, then one `Section` per block —
`pourQui`, `competences`, `programme`, `formatModalites`, `confiance`, `faq`,
`ctaFinal`. Do not render `Header` or `Footer`; plan 01-07 mounted them in the
root layout.

- `pourQui` — the five profiles as `Card`s, each with its pictogram looked up in
  the `pictograms` registry by the copy's `picto` string, plus the « même sans
  expérience SAP » reassurance line from the bundle.
- `competences` — the six competencies, `Card variant="outline"`, pictogram +
  title + line.
- `programme` — the five modules in the `Accordion`, each trigger showing the
  module title and a `Badge` with the duration rendered through `formatNumber`
  and the unit label from the bundle. ⛔ Never concatenate a number and a unit by
  hand in JSX. Add the « télécharger le programme » `Button` as a **link with no
  handler** — the PDF is Lot 2 (D-41); use the bundle's label and a `disabled`
  or non-navigating variant rather than inventing a route that does not exist.
- `formatModalites` — ⛔ **formations live first**. Render the list in exactly the
  order the bundle declares it; do not sort, reverse, filter or re-group. *Vidéos
  à venir* renders as a listed future item, visually marked as such with a
  `Badge variant="muted"`, and **never** styled like an included modality (D-14).
- `confiance` — the trust items plus the two ready placeholders for testimonials
  and company logos, rendered as `Card variant="muted"` with the bundle's
  placeholder labels. These are legitimately visible « à venir » surfaces; the
  hero video is the one that must stay invisible.
- `faq` — the six-to-eight questions in a second `Accordion`.
- `ctaFinal` — `Section tone="atmosphere"` with the title and the primary CTA.

Every section is wrapped in `Section` and introduced by `SectionHeader`. Stagger
entrances with the `.reveal-*` primitives. Give each `Section` a stable `id`
matching its namespace so the header and future anchors can target it.

Then remove `home.placeholder` from `src/locales/fr/common.json` **in the same
commit** — it is one of exactly two artifacts this phase replaces, and deleting
it separately would leave the repository broken between waves. The `metadata`
namespace stays.

⛔ Do not add a key to any locale file. If a needed string is missing, stop and
report.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=fs.readFileSync('src/app/page.tsx','utf8');const c=require('./src/locales/fr/common.json');if(c.home!==undefined)throw new Error('home.placeholder must be removed from common.json');if(!c.metadata)throw new Error('metadata namespace must stay');if(/'use client'|\"use client\"/.test(p))throw new Error('page must stay a server component');if(/export const metadata|export async function|export default async/.test(p))throw new Error('no page metadata and no async page (D-41)');if(/:\s*any\b/.test(p))throw new Error('any type');if(/fetch\(|useState|useEffect|onSubmit/.test(p))throw new Error('page logic forbidden (D-41)');const st=p.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/.*/g,'');if(/>[^<>{}]*[à-ÿ]{2,}[^<>{}]*</.test(st))throw new Error('hardcoded French string in JSX (D-29)');if(/Apprenez à votre rythme|Accès 24\/7/.test(p))throw new Error('struck phrase (D-15)');if(/\d\s?€|\}\s*€|€\s*\{/.test(p))throw new Error('hand-written price/unit concatenation (D-30)');if(/<Header|<Footer/.test(p))throw new Error('chrome is mounted in the root layout, not the page');if(/\.reverse\(|\.sort\(/.test(p))throw new Error('do not reorder the signed modality list (D-14)');for(const ns of ['hero','pourQui','competences','programme','formatModalites','confiance','faq','ctaFinal'])if(!p.includes(ns))throw new Error('landing section missing: '+ns);if(!/pictograms/.test(p))throw new Error('pictograms registry must be used');if(!/formatNumber/.test(p))throw new Error('durations must format through formatNumber');if(/\.(png|jpe?g|webp|avif)\b/.test(p))throw new Error('raster asset forbidden');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` is a synchronous server component with a default `Home` export and no `metadata` export
    - All eight blocks appear in the signed order: `hero`, `pourQui`, `competences`, `programme`, `formatModalites`, `confiance`, `faq`, `ctaFinal`
    - Each `Section` has a stable `id` matching its namespace
    - The five profiles, six competencies and five modules are rendered by mapping over the bundle arrays — no hardcoded list
    - Pictograms resolve through the `pictograms` registry; module durations render through `formatNumber` with the unit label read from the bundle
    - The modality list is rendered in bundle order with no `.sort(` and no `.reverse(`; *vidéos à venir* carries a `Badge variant="muted"`
    - `src/locales/fr/common.json` no longer has a `home` namespace and still has `metadata`
    - The file contains no `"use client"`, no `async`, no `fetch(`, no `useState`, no `useEffect`, no `onSubmit`, no `: any`
    - No French string literal in JSX, no struck phrase, no hand-written price or unit concatenation, no raster reference, no `<Header` / `<Footer`
  </acceptance_criteria>
  <done>The landing maquette renders the seven signed sections on the real design system, and the Phase 0 placeholder is gone from route and bundle together.</done>
</task>

<task type="auto">
  <name>Task 3: Responsive and keyboard pass on the landing route</name>
  <files>src/app/page.tsx, src/components/sections/hero.tsx, src/components/sections/section.tsx</files>
  <read_first>
    - `src/app/page.tsx`, `src/components/sections/hero.tsx`, `src/components/sections/section.tsx` — as written in Tasks 1 and 2
    - `src/app/globals.css` — the `--text-*` clamp steps and the `prefers-reduced-motion` block
  </read_first>
  <action>
Make the landing route hold up at the three widths `GOL-03` will be measured at,
and under keyboard-only use, while the maquette is still cheap to change — a
maquette validated at desktop width and found broken on mobile at Lot 2 is a
re-do of a validated screen.

Adjust only the layout classes in the three files: single column below the `sm`
breakpoint, two columns at `md`, the signed grid at `lg`. The display type
already uses `clamp()` steps from the token layer, so **do not add a
width-specific font size** — if a heading overflows, fix the step in the token
layer's remit, not with a one-off `text-[Npx]` here.

Verify the heading outline is coherent: exactly one `<h1>` on the page — the
hero title — and every `SectionHeader` an `<h2>`, with card titles at `<h3>`.
A broken outline is invisible until Lot 5 measures it and then costs a re-do
across eleven screens.

Confirm the keyboard path end to end: the skip link from the root layout, then
the header, then every accordion trigger, then both CTAs — each with the
three-class focus ring, none reachable only by mouse. The accordion panel content
must not be focusable while collapsed.

⛔ Do not add a media query to `globals.css` — plan 01-02 owns it and this plan
runs in a later wave but must not reopen that file. Use Tailwind responsive
variants in the components.

Then run the plan's single type gate.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` renders exactly one `<h1>`; every `SectionHeader` renders an `<h2>`; card titles render `<h3>`
    - Responsive behaviour is expressed with Tailwind `sm:` / `md:` / `lg:` variants in the three files; `src/app/globals.css` is unmodified by this plan
    - No `text-[Npx]` or other ad-hoc font size appears in any of the three files
    - Every interactive element on the route is reachable by keyboard and shows the three-class `focus-visible` ring
    - Collapsed accordion panels are not focusable
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The landing maquette is validated-ready at mobile, tablet and desktop, and operable without a mouse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale bundle → rendered public page | mocked strings become visible to the client here, at full fidelity |
| repository → Vercel production | this route is publicly reachable at `/` as soon as it merges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-32 | Spoofing | mocked landing copy read as final by the client | mitigate | Every string comes from the bundle registered in `_mocks.public.json`; `npm run content:check` lists them and exits non-zero (plan 01-08). Acceptance criteria forbid any French literal in JSX, so no string can escape the registry by being inlined |
| T-01-33 | Tampering | the signed modality order silently reordered in the view | mitigate | Acceptance criteria forbid `.sort(` and `.reverse(` on the modality list and require bundle order (D-14); *vidéos à venir* is visually marked as a future item |
| T-01-34 | Information Disclosure | scope drift — a video player implying delivered video | mitigate | The `video` prop renders nothing when absent, asserted by acceptance criteria that forbid `<video`, a play control and a placeholder box (D-13) |
| T-01-16 | Denial of Service | keyboard-only users and reduced-motion users | mitigate | One `<h1>`, a coherent outline, focus rings on every control, non-focusable collapsed panels, and the `prefers-reduced-motion` block from plan 01-02 |
| T-01-24 | Denial of Service | `GOL-02` Lighthouse ≥ 90 mobile | mitigate | No raster asset, no `<script>`, no client component beyond the accordion leaf, atmosphere done in CSS |
| T-01-NOINDEX | Information Disclosure | an unfinished public landing page indexed | accept | Production carries `X-Robots-Tag: noindex, nofollow` from Phase 0 and holds no real data. ⛔ No task here may modify `next.config.ts` or `vercel.json`; Lot 5 removes the header only after the site is finished |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No install task exists.** Every component and icon is already in the repository |
| T-01-08 | Elevation of Privilege | runtime authorization | accept | A public marketing page with no data access and no session. Lot 1 ships no authorization by design (D-11, D-41) |
</threat_model>

<verification>
Manager-session checks after commit (D-39):

1. `npm run typecheck` and `npm run build` pass.
2. Load `/` at 375px, 768px and 1440px — no horizontal scroll, no overlapping type.
3. Confirm nothing above the fold hints at a video.
4. Read « Format et modalités » and confirm live leads.
5. Tab the whole page; confirm the skip link, the focus rings and the accordion behaviour.
6. `npm run content:check` still exits 1 and still lists the landing keys.
</verification>

<success_criteria>
- The landing maquette exists at `/` as a real route on the real design system
- Seven signed sections in the signed order, all copy from the bundle
- The hero is finished-looking with no video hole, and the video slot is a prop away
- The Phase 0 placeholder is gone from both `page.tsx` and `common.json`
- Responsive at three widths, operable by keyboard, one `<h1>`
- `npm run typecheck` passes; nothing pushed
</success_criteria>

<output>
Create `.planning/phases/01-cadrage-contenus-et-design/01-09-SUMMARY.md` when done.
</output>
