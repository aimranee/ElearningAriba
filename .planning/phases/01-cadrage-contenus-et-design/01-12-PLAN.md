---
phase: 01-cadrage-contenus-et-design
plan: 12
type: execute
wave: 5
depends_on: ["01-04", "01-05", "01-06", "01-07"]
files_modified:
  - src/lib/i18n/fr.ts
  - src/app/agenda/page.tsx
  - src/app/reservation/page.tsx
  - src/app/paiement/page.tsx
autonomous: true
requirements: [CADR-06]

must_haves:
  truths:
    - "The last three of the eleven signed screens exist as real Next.js routes at the French slugs (D-05, D-31)"
    - "The agenda shows its « aucun créneau disponible » state, not only a populated calendar (D-23)"
    - "The paiement screen names no provider and hands off to an external page (D-04, D-32)"
    - "The paiement screen shows the failed-handoff state (D-23)"
    - "Every price and time renders through a formatter in src/lib/i18n/fr.ts, never hand-written (D-30)"
  artifacts:
    - path: "src/lib/i18n/fr.ts"
      provides: "currencyFormatter and timeFormatter, the two formatters the journey needs"
      exports: ["currencyFormatter", "timeFormatter", "formatCurrency", "formatTime"]
    - path: "src/app/agenda/page.tsx"
      provides: "The agenda maquette incl. the no-slot empty state"
      min_lines: 50
    - path: "src/app/reservation/page.tsx"
      provides: "The réservation maquette, three steps"
      min_lines: 40
    - path: "src/app/paiement/page.tsx"
      provides: "The provider-neutral paiement maquette incl. the failed handoff"
      min_lines: 40
  key_links:
    - from: "src/app/paiement/page.tsx"
      to: "src/lib/i18n/fr.ts"
      via: "prices render through formatCurrency, never a literal string"
      pattern: "formatCurrency"
    - from: "src/app/agenda/page.tsx"
      to: "src/components/ui/empty-state.tsx"
      via: "agenda.aucunCreneau renders as EmptyState tone=waiting"
      pattern: "aucunCreneau"
---

<objective>
Wave 4 of the locked wave shape (D-37), screens 9–11 of 11: **agenda,
réservation, paiement**, as real Next.js routes at the French slugs. This plan
completes `CADR-06`.

Purpose: these three screens are where the offer's money and time live, and where
a maquette validated without its failure states costs the most later. D-32 makes
the paiement screen provider-neutral by design: Lot 7 is contractually hosted
payment, and redirect-to-a-hosted-page is the shape every hosted provider shares,
so nothing on this screen changes when the provider is finally named.

Output: two new formatters and three route files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md
@.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md
@CLAUDE.md

<interfaces>
**`src/lib/i18n/fr.ts` as it stands** — 43 lines, every export explicitly
annotated:
`LOCALE = "fr-FR" as const` (line 8), `TIME_ZONE = "Europe/Paris" as const`
(line 9), `dateFormatter` (`dateStyle: "long"`), `dateTimeFormatter`
(`dateStyle: "long"`, `timeStyle: "short"`), `numberFormatter =
new Intl.NumberFormat(LOCALE)`, then the thin wrappers `formatDate`,
`formatDateTime`, `formatNumber`. Locale and timezone are **pinned, not
inferred**, because the app renders on a UTC host and inferring would silently
produce the wrong day for an agenda slot.

⚠ **Two real gaps this plan closes.** `numberFormatter` has **no currency
style**, so `formatNumber(1500)` yields `"1 500"` with **no « € »** — the euro
symbol is produced by no existing code. And **no time-only formatter exists**;
only long date and long-date-plus-short-time. The agenda and réservation screens
need slot times and the paiement screen needs a price, so both gaps must be
closed here rather than papered over in JSX.

Pattern to follow exactly: a module-scope singleton `Intl.*` instance with an
explicit type annotation, then a thin `formatX(value): string` wrapper.

**The route template** — `src/app/page.tsx`: server component, `export default
function <PascalName>()`, synchronous, **not** `async`, **no `metadata` export on
the page**. Chrome lives in `src/app/layout.tsx` (plan 01-07). Typed routes: a
page with no params omits props; the type, if needed, is the generated
`PageProps<"/route">` global — never hand-written.

**Available from earlier plans:**
- `@/components/sections/section` → `Section`, `SectionHeader`
- `@/components/ui/{button,card,badge,message,accordion,input,field,empty-state}`
- `@/components/icons/pictograms` → the `pictograms` registry
- `@/locales/fr/{agenda,reservation,paiement,common}.json`

**Copy already written** (plan 01-04): `agenda.legende`,
`agenda.typesRendezVous` as `{ id, libelle, dureeMinutes, prix }` with **numeric**
`dureeMinutes` and `prix`, `agenda.aucunCreneau` with `titre` / `message` /
`action`; `reservation.etapes`, `reservation.recapitulatif`,
`reservation.erreurs.creneauIndisponible`; `paiement.recapitulatif`,
`paiement.formules` as four entries with **numeric** `prix` and an `inclus`
list, `paiement.consentement`, `paiement.securite`, `paiement.action.payer`,
`paiement.echecHandoff` with `titre` / `message` / `reessayer`.

**Slugs (D-31):** `/agenda`, `/reservation`, `/paiement`. Directory names must
match exactly — `reservation` and `paiement` without accents.
</interfaces>

**Hard prohibitions:**
- ⛔ **No payment provider is named anywhere, in any casing** (D-04). No SDK, no redirect URL, no publishable key, no `<script>` from a provider domain, no card field, no iframe. The primary action is a **maquette** of a handoff to an external secure payment page (D-32).
- ⛔ No availability logic, no recurrence rule, no booking transaction, no lock, no `.ics`, no conflict check. `AGD-*` is Lot 4 (D-41).
- ⛔ No order, no order status vocabulary, no invoice, no receipt, no promotional code, no refund clause. `PAY-*` is Lot 7 (D-41).
- ⛔ No Google Calendar reference of any kind (D-41, Lot 8).
- ⛔ No form submission, no `action`, no `onSubmit`, no `"use server"`, no `fetch`, no `zod`.
- ⛔ Do not touch `supabase/`, any migration, any auth surface, or `src/types/database.types.ts` (D-11). Do not import `@/lib/supabase/*`.
- ⛔ No page logic, no data fetching, no `async` page.
- ⛔ No per-route `metadata`, Open Graph or structured data (D-41, Lot 5).
- ⛔ No French string literal in JSX (D-29). If a key is missing, **stop and report** — plan 01-04 owns the copy.
- ⛔ **No hand-written price, duration or time string.** Everything renders through `src/lib/i18n/fr.ts` (D-30). No price in this lot is a commitment.
- ⛔ No tenancy concept (D-42). No raster asset (D-06, D-21).
- ⛔ Do not modify any file under `src/components/`, `src/locales/`, `src/app/globals.css` or `src/app/layout.tsx`.
- ⛔ Do not run `npm install`. Do not `git push`, create a PR, or run `vercel` (D-38).
</context>

<execution_order>
D-39 — **commit before verifying.** Commit each task as soon as its acceptance
criteria hold. `npm run typecheck` runs once, as the last task's verification.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Close the two formatter gaps</name>
  <files>src/lib/i18n/fr.ts</files>
  <read_first>
    - `src/lib/i18n/fr.ts` — all 43 lines; the two-step singleton-plus-wrapper pattern and the explicit type annotations on every export
    - `src/locales/fr/paiement.json` — the numeric `prix` values the currency formatter renders
    - `src/locales/fr/agenda.json` — the numeric `dureeMinutes` values
  </read_first>
  <action>
Add two formatters to `src/lib/i18n/fr.ts`, following the file's existing shape
exactly: a module-scope `Intl.*` singleton with an explicit type annotation,
then a thin wrapper with an explicit `string` return type.

`currencyFormatter: Intl.NumberFormat` — `new Intl.NumberFormat(LOCALE, { style:
"currency", currency: "EUR" })`, with `formatCurrency(value: number): string`.
This closes the real gap the existing `numberFormatter` leaves: `formatNumber`
produces no « € » at all, and letting a maquette hand-write `"1 500 €"` in JSX
would violate D-29 and D-30 at once. Add a short `/* why */` comment saying that,
so the next reader does not "simplify" it away.

`timeFormatter: Intl.DateTimeFormat` — `new Intl.DateTimeFormat(LOCALE, {
timeZone: TIME_ZONE, timeStyle: "short" })`, with `formatTime(date: Date):
string`. The agenda renders slot times and there is no time-only formatter today;
the pinned `TIME_ZONE` is what stops a UTC host from showing the wrong hour.

⛔ Do not change `LOCALE`, `TIME_ZONE`, or any existing formatter or wrapper —
they are pinned and other code depends on them. ⛔ Do not add a duration
formatter that concatenates a number and a unit label: durations are a number
through `formatNumber` plus a unit **from the locale bundle**, composed at the
call site.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/lib/i18n/fr.ts','utf8');for(const t of ['currencyFormatter','formatCurrency','timeFormatter','formatTime'])if(!c.includes(t))throw new Error('missing export: '+t);if(!/style:\s*\"currency\"/.test(c)||!/currency:\s*\"EUR\"/.test(c))throw new Error('currencyFormatter must use style currency and EUR');if(!/timeStyle:\s*\"short\"/.test(c))throw new Error('timeFormatter must use timeStyle short');if(!/timeZone:\s*TIME_ZONE/.test(c))throw new Error('timeFormatter must pin the timezone');if(!/LOCALE = \"fr-FR\" as const/.test(c)||!/TIME_ZONE = \"Europe\/Paris\" as const/.test(c))throw new Error('pinned constants must not change');for(const t of ['dateFormatter','dateTimeFormatter','numberFormatter','formatDate','formatDateTime','formatNumber'])if(!c.includes(t))throw new Error('existing export removed: '+t);if(/:\s*any\b/.test(c))throw new Error('any type');if(!/export const currencyFormatter: Intl.NumberFormat/.test(c))throw new Error('explicit type annotation required, matching the file style');if(!/export const timeFormatter: Intl.DateTimeFormat/.test(c))throw new Error('explicit type annotation required, matching the file style');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/i18n/fr.ts` exports `currencyFormatter: Intl.NumberFormat` built with `style: "currency"` and `currency: "EUR"`, plus `formatCurrency(value: number): string`
    - It exports `timeFormatter: Intl.DateTimeFormat` built with `timeZone: TIME_ZONE` and `timeStyle: "short"`, plus `formatTime(date: Date): string`
    - `LOCALE = "fr-FR" as const` and `TIME_ZONE = "Europe/Paris" as const` are unchanged
    - Every pre-existing export — `dateFormatter`, `dateTimeFormatter`, `numberFormatter`, `formatDate`, `formatDateTime`, `formatNumber` — is still present and unchanged
    - Every new export carries an explicit type annotation, matching the file's style
    - A `/* why */` comment explains why `numberFormatter` is not sufficient for prices
    - No duration formatter that concatenates a number and a unit was added; no `: any`
  </acceptance_criteria>
  <done>Prices and slot times have a legal way to render, so no maquette needs to hand-write one.</done>
</task>

<task type="auto">
  <name>Task 2: /agenda and /reservation</name>
  <files>src/app/agenda/page.tsx, src/app/reservation/page.tsx</files>
  <read_first>
    - `src/locales/fr/agenda.json` and `src/locales/fr/reservation.json` — read in full
    - `src/lib/i18n/fr.ts` — as extended in Task 1; `formatTime`, `formatDate`, `formatCurrency`, `formatNumber`
    - `src/components/ui/empty-state.tsx` — the `tone` variants and the parts
    - `src/components/ui/card.tsx`, `badge.tsx`, `button.tsx` — the surfaces and controls
    - `src/app/espace/page.tsx` — the empty-state composition plan 01-11 established; match it
  </read_first>
  <action>
Create `src/app/agenda/page.tsx` — directory `agenda`, default export `Agenda`.
Render `agenda.titre` / `agenda.intro`, the appointment-type chooser built from
`agenda.typesRendezVous` — each a `Card` showing the `libelle`, the duration as
`formatNumber(dureeMinutes)` plus the unit label from the bundle, and the price
as `formatCurrency(prix)`; the free 30-minute discovery call shows a
`Badge variant="success"` from the bundle rather than a zero price. Then a
month grid with `agenda.moisPrecedent` / `agenda.moisSuivant` controls that do
nothing, and the `agenda.legende` marks for libre, réservé, passé and bloqué.

The calendar is a **static maquette**: build the grid from a small local constant
of illustrative days, clearly commented as maquette-only data that Lot 4
replaces. ⛔ No `Date` arithmetic that pretends to be real availability, no
recurrence rule, no conflict check, no timezone conversion beyond
`formatDate` / `formatTime` (D-41).

**Render the empty state, because it is the deliverable** (D-23). Below the
populated grid, render a clearly-labelled second view showing
`agenda.aucunCreneau` as `EmptyState tone="waiting"` with its `titre`, `message`
and an `EmptyStateAction` from its `action` label. « Aucun créneau disponible »
is the state a maquette validated without it turns into a Lot 2 re-do, and this
is the screen where the client can approve it.

Create `src/app/reservation/page.tsx` — directory `reservation`, default export
`Reservation`. Render `reservation.titre` / `reservation.intro`, the three
`reservation.etapes` as a static stepper with the third step active, and the
`reservation.recapitulatif` card: type, date through `formatDate`, heure through
`formatTime`, durée through `formatNumber` plus the bundle unit, and prix through
`formatCurrency` — all values from the same maquette-only constant, commented as
such. Then `reservation.conditions` and the primary `Button` from
`common.actions.reserver`, which does nothing.

Render `reservation.erreurs.creneauIndisponible` as a visible `Message
variant="error"` state in a labelled demonstration block — the slot taken while
the learner hesitated is the failure Lot 4 must handle and the client should see
it now.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const A=fs.readFileSync('src/app/agenda/page.tsx','utf8');const R=fs.readFileSync('src/app/reservation/page.tsx','utf8');for(const [n,c] of [['agenda',A],['reservation',R]]){if(/export const metadata|export default async|export async function/.test(c))throw new Error(n+': no page metadata, no async page');if(/fetch\(|\"use server\"|onSubmit=|action=\{|from \"zod\"|@\/lib\/supabase|createClient|cookies\(\)/.test(c))throw new Error(n+': logic forbidden (D-11/D-41)');if(/:\s*any\b/.test(c))throw new Error(n+': any type');const st=c.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/.*/g,'');if(/>[^<>{}]*[à-ÿ]{2,}[^<>{}]*</.test(st))throw new Error(n+': hardcoded French string in JSX (D-29)');if(/\d\s?€|€\s*\{|\}\s*€/.test(c))throw new Error(n+': hand-written price (D-30)');if(/ics|RRULE|googleapis|calendar\.google/i.test(c))throw new Error(n+': Lot 4/Lot 8 construct forbidden');if(/organisation|workspace|tenant/i.test(c))throw new Error(n+': tenancy forbidden (D-42)');if((c.match(/<h1/g)||[]).length!==1)throw new Error(n+': exactly one h1 required');if(/<Header|<Footer/.test(c))throw new Error(n+': chrome is in the root layout');if(/\.(png|jpe?g|webp|avif)\b|<img|next\/image/.test(c))throw new Error(n+': image forbidden');}if(!/aucunCreneau/.test(A))throw new Error('agenda must render the aucunCreneau empty state (D-23)');if(!/@\/components\/ui\/empty-state/.test(A))throw new Error('agenda must compose EmptyState');if(!/formatCurrency/.test(A))throw new Error('agenda prices must use formatCurrency');if(!/legende/.test(A))throw new Error('agenda must render the legend');if(!/creneauIndisponible/.test(R))throw new Error('reservation must render the slot-taken error state');for(const t of ['formatDate','formatTime','formatCurrency'])if(!R.includes(t))throw new Error('reservation must format through '+t);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/agenda/page.tsx` and `src/app/reservation/page.tsx` exist at exactly those directory names and export default `Agenda` and `Reservation`
    - Neither contains `fetch(`, `"use server"`, `onSubmit=`, an `action={` prop, a `zod` import, `@/lib/supabase`, `createClient` or `cookies()`
    - Neither matches `/ics|RRULE|googleapis|calendar\.google/i`
    - `/agenda` renders the appointment types from `agenda.typesRendezVous` with prices through `formatCurrency` and durations through `formatNumber` plus a bundle unit label
    - `/agenda` renders `agenda.legende` and a clearly-labelled `agenda.aucunCreneau` view as `EmptyState tone="waiting"` with an action
    - The calendar grid is built from a local constant explicitly commented as maquette-only data; no availability computation exists
    - `/reservation` renders the three `reservation.etapes`, the recapitulatif using `formatDate`, `formatTime`, `formatNumber` and `formatCurrency`, and a demonstration of `reservation.erreurs.creneauIndisponible` as a `Message variant="error"`
    - Neither file matches `/\d\s?€/`, `/€\s*\{/` or `/\}\s*€/`
    - Each page has exactly one `<h1>`; no French literal in JSX; no image; no `: any`; no tenancy term; no `<Header` / `<Footer`
  </acceptance_criteria>
  <done>Two more screens are validated-ready, with the no-slot state and the slot-taken failure both visible.</done>
</task>

<task type="auto">
  <name>Task 3: /paiement, provider-neutral, and the closing pass</name>
  <files>src/app/paiement/page.tsx, src/app/agenda/page.tsx, src/app/reservation/page.tsx</files>
  <read_first>
    - `src/locales/fr/paiement.json` — read in full: `recapitulatif`, the four `formules`, `consentement`, `securite`, `action.payer`, `echecHandoff`
    - `src/lib/i18n/fr.ts` — `formatCurrency`
    - `src/components/ui/empty-state.tsx` and `src/components/ui/message.tsx` — the `error` tone and the alert message
    - `src/app/agenda/page.tsx`, `src/app/reservation/page.tsx` — as written in Task 2, for the closing pass
  </read_first>
  <action>
Create `src/app/paiement/page.tsx` — directory `paiement`, default export
`Paiement`. **Provider-neutral by design** (D-32): Lot 7 is contractually hosted
payment, and redirect-to-a-hosted-page is the shape every hosted provider shares,
so nothing on this screen changes when the provider is finally named.

Render `paiement.titre` / `paiement.intro`, then:
- the four `paiement.formules` as `Card`s — `libelle`, `description`, the
  `inclus` list, and `formatCurrency(prix)`. ⛔ Never a hand-written price. Mark
  one as the recommended formula with a `Badge` label from the bundle
- the `paiement.recapitulatif` order summary: the chosen formula, what it
  includes, and the amount through `formatCurrency`, from a local maquette-only
  constant commented as such
- the `paiement.consentement` consent line
- the primary `Button` from `paiement.action.payer`, describing a handoff to an
  external secure payment page. ⛔ It navigates nowhere: no provider SDK, no
  redirect URL, no publishable key, no `<script>` from an external domain, no
  iframe, no card field, no CVV, no expiry input
- the `paiement.securite` reassurance line stating no bank data transits or is
  stored on the site — which is the contractual shape of `PAY-01`

Then render `paiement.echecHandoff` in a clearly-labelled demonstration block as
`EmptyState tone="error"` with its `titre`, `message` and a `reessayer` action.
A payment handoff that fails is one of the four D-23 surface states and it is the
one the client is most likely to hit in real life.

**Closing pass across all three routes.** Directories must be exactly `agenda`,
`reservation`, `paiement` — no accents (D-31). Exactly one `<h1>` per route,
`SectionHeader` at `<h2>`, card titles at `<h3>`, no level skipped. Responsive
with Tailwind `sm:` / `md:` / `lg:` variants in the route files; ⛔ do not add a
media query to `src/app/globals.css`. Every control keyboard reachable with the
three-class focus ring, the formula cards tabbable in visual order, the
demonstration blocks reachable without trapping focus.

Then run the plan's single type gate, which is also the gate that completes
`CADR-06`: all eleven screens must build.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/paiement/page.tsx` exists at exactly that directory name and exports default `Paiement`
    - It renders all four `paiement.formules` with prices through `formatCurrency`, the `recapitulatif`, `consentement`, `action.payer` and `securite`
    - It renders `paiement.echecHandoff` as `EmptyState tone="error"` with a `reessayer` action
    - It matches no payment-provider name in any casing, contains no external `<script`, no `iframe`, no redirect URL, no publishable key, and no card, CVV or expiry field
    - It contains no order status vocabulary, no invoice, no receipt, no promotional code and no refund clause
    - Neither `/paiement`, `/agenda` nor `/reservation` matches `/\d\s?€/`, `/€\s*\{/` or `/\}\s*€/`
    - The directories `src/app/agenda`, `src/app/reservation`, `src/app/paiement` exist with exactly those names, no accents
    - Each of the three routes renders exactly one `<h1>` and skips no heading level
    - Responsive behaviour uses Tailwind variants in the route files; `src/app/globals.css` is unmodified by this plan
    - Every interactive element is keyboard reachable with the focus ring
    - `npm run typecheck` exits 0, and all eleven signed screens build
  </acceptance_criteria>
  <done>All eleven screens of CADR-06 exist as real routes, and the payment screen is one provider name away from being Lot 7's page — a name it does not need.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learner → external hosted payment page | the handoff boundary Lot 7 implements; in Lot 1 nothing crosses it |
| displayed price → commercial commitment | a price on a validated maquette can be read as an offer |
| anonymous visitor → `/agenda`, `/reservation`, `/paiement` | all three ship unguarded and publicly reachable by URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-42 | Information Disclosure | card data entered on a page that stores or transmits it | mitigate | ⛔ No card, CVV or expiry field exists — acceptance criteria assert their absence, along with any external `<script`, `iframe`, redirect URL or publishable key. `PAY-01` is contractually hosted payment: no bank data transits or is stored on the site, and `paiement.securite` says so on screen |
| T-01-14 | Information Disclosure | a provider name leaking before the decision is made | mitigate | Acceptance criteria assert the provider-name pattern matches nothing in any casing (D-04). The screen is provider-neutral by construction (D-32) |
| T-01-13 | Spoofing | mocked prices and formulas read as a commercial commitment | mitigate | All four `paiement.formules` and the appointment prices are registered in `_mocks.journey.json` under `blocks: CADR-01`; `npm run content:check` lists them and exits non-zero. Every price renders through `formatCurrency`, so none can be hand-written into JSX and escape the registry |
| T-01-43 | Tampering | a fake calendar read as real availability | mitigate | The grid is built from a local constant explicitly commented as maquette-only; acceptance criteria forbid any availability computation, recurrence rule, `.ics` or conflict check (D-41) |
| T-01-44 | Repudiation | a failed payment handoff that leaves the learner with nothing | mitigate | `paiement.echecHandoff` renders as `EmptyState tone="error"` with `role="alert"` and a retry action — the pattern Lot 7 inherits already validated by the client |
| T-01-45 | Tampering | wrong slot time shown to a Paris learner from a UTC host | mitigate | `timeFormatter` pins `timeZone: TIME_ZONE`; acceptance criteria assert it, and no route may format a time any other way (D-30) |
| T-01-NOINDEX | Information Disclosure | unguarded journey routes publicly reachable | accept | Lot 1 ships no runtime authorization by design (D-11, D-41). Safe only because production carries `X-Robots-Tag: noindex, nofollow` from Phase 0 and the routes hold no real data. ⛔ No task here may modify `next.config.ts` or `vercel.json`; Lot 3 adds the gate |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No install task exists.** No payment SDK, no calendar library, no date library — `Intl` is a platform builtin. An executor that believes an install is required must stop and report |
| T-01-41 | Tampering | schema or auth surface touched to make a screen "work" | mitigate | ⛔ D-11 is absolute: no `supabase/` file, no migration, no RLS policy, no `src/types/database.types.ts` change, no `@/lib/supabase` import |
</threat_model>

<verification>
Manager-session checks after commit (D-39):

1. `npm run typecheck` and `npm run build` pass — all eleven screens build.
2. `git status --porcelain supabase src/types` returns empty.
3. Load `/agenda`, `/reservation`, `/paiement` at 375px, 768px and 1440px.
4. `grep -rniE "stripe|paypal|mollie|adyen|paddle|braintree|payplug" src/` returns nothing.
5. `grep -rn "€" src/app/` returns nothing — every euro comes from `formatCurrency`.
6. Confirm the four states are visible and approvable: no available slot, slot taken, failed handoff, and the free discovery call badge.
7. `npm run content:check` still exits 1 and lists the four sale formulas.
</verification>

<success_criteria>
- The last three of the eleven signed screens exist as real routes at the French slugs — `CADR-06` is structurally complete
- `currencyFormatter` and `timeFormatter` close the two real gaps in `src/lib/i18n/fr.ts`
- No euro symbol, time or duration is hand-written anywhere
- The paiement screen names no provider and shows the failed-handoff state
- The agenda shows its no-slot state and invents no availability
- `npm run typecheck` passes; nothing pushed
</success_criteria>

<output>
Create `.planning/phases/01-cadrage-contenus-et-design/01-12-SUMMARY.md` when done.
</output>
