---
phase: 01-cadrage-contenus-et-design
plan: 08
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified:
  - src/locales/fr/emails.json
  - src/locales/fr/_mocks.emails.json
  - scripts/check-mock-content.mjs
  - package.json
  - docs/cadrage-lot-1.md
autonomous: true
requirements: [CADR-01, CADR-02, CADR-03]

must_haves:
  truths:
    - "The four automatic email texts are written in the locale bundle (D-27, CADR-03)"
    - "One command answers « is anything still mocked? » in one run, lists every remaining placeholder with its key and the requirement it blocks, and exits non-zero while any remain (D-08)"
    - "That command is provably unreachable from CI and from the Vercel build (D-09)"
    - "The framing workshop output is recorded in French — positioning, commercial promise, sale formulas, page hierarchy, priorities (CADR-01)"
    - "CADR-02 is recorded as deferred to Lot 7, closing unmet, with no payment provider named (D-04)"
  artifacts:
    - path: "src/locales/fr/emails.json"
      provides: "Copy for the four automatic emails — written, never sent"
      contains: "confirmationInscription"
    - path: "scripts/check-mock-content.mjs"
      provides: "The mock-content guard"
      min_lines: 40
    - path: "docs/cadrage-lot-1.md"
      provides: "The CADR-01 framing record and the CADR-02 deferral"
      contains: "CADR-02"
    - path: "package.json"
      provides: "The content:check script entry"
      contains: "content:check"
  key_links:
    - from: "scripts/check-mock-content.mjs"
      to: "src/locales/fr/_mocks.public.json"
      via: "the guard discovers every _mocks.*.json registry and resolves each key in its bundle"
      pattern: "_mocks\\."
    - from: "package.json"
      to: "scripts/check-mock-content.mjs"
      via: "the content:check npm script"
      pattern: "content:check"
---

<objective>
Wave 3 of the locked wave shape (D-36), closing half: write the automatic email
copy, **build the mock-content guard**, and record the framing output.

Purpose: D-01 creates a specific, dated failure — mocked French copy that looks
finished, surviving into Lot 2 and reaching the client as real. The maquettes are
deliberately full-fidelity, so nothing about *looking* at them reveals which
strings are real. This plan is the guard. It also closes `CADR-01` by writing the
framing record, and records `CADR-02` as deferred without naming a provider.

Output: the email bundle, its mock registry, `scripts/check-mock-content.mjs`,
one `package.json` script entry, and `docs/cadrage-lot-1.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md
@.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
**The guard's analog is `scripts/gen-db-types.mjs`** (49 lines) — a node CLI
invoked from `package.json` that inspects something and exits non-zero on
failure. Conventions to copy exactly:
- `.mjs`, ESM, `node:`-prefixed builtins (`node:fs`, `node:path`, `node:url`)
- a leading comment stating **why** the script exists, not what it does
- path resolution from the module, never from `cwd`:
  `join(dirname(dirname(fileURLToPath(import.meta.url))), "src/locales/fr")`
- failure discipline: `console.error("check-mock-content: <message>")` then
  `process.exit(1)`; success logs one `console.log` line
- `tsconfig.json:25-32` includes `**/*.ts`, `**/*.tsx`, `**/*.mts` — **not
  `.mjs`** — so a `.mjs` guard is not type-checked, matching the analog. It
  **will** be linted: `npm run lint` is bare `eslint` and `eslint.config.mjs`
  ignores only `.next/**`, `out/**`, `build/**`, `next-env.d.ts`. Keep it
  lint-clean.

**D-09 evidence — the guard must stay unreachable from CI, and provably so.**
`.github/workflows/ci.yml` is the only file under `.github/`. Its single job runs
three npm scripts: `lint` (line 55–56), `typecheck` (58–59), `build` (61–62).
Second reachable surface: `vercel.json` sets `"installCommand": "npm ci"` and
`"buildCommand": "npm run build"`. There is no `postinstall`. So a script named
anything other than `lint`, `typecheck`, `build`, or a `pre*`/`post*` hook on
those three, is unreachable from both. `content:check` satisfies D-08 and D-09
simultaneously.

**Existing `package.json` scripts** (lines 8–15): `dev`, `build`, `start`,
`lint`, `typecheck`, `db:types`. The new entry follows `db:types` exactly.

**The registries the guard reads**, written by plans 01-03 and 01-04:
`src/locales/fr/_mocks.public.json` and `src/locales/fr/_mocks.journey.json` —
each `{ "_pourquoi": string, "mocks": [{ file, key, blocks, awaiting }] }`, where
`key` is a dotted path and array elements are addressed numerically
(`programme.modules.0.titre`).

**Formatters** — `src/lib/i18n/fr.ts`: `LOCALE = "fr-FR"`,
`TIME_ZONE = "Europe/Paris"`, `formatDate`, `formatDateTime`, `formatNumber`.
Email copy stores no formatted date and no formatted price; it carries
interpolation tokens written literally, e.g. `{prenom}`, `{dateHeure}`,
`{montant}` (D-28 — no i18n library, so interpolation is the consumer's job).
</interfaces>

**Hard prohibitions:**
- ⛔ **The guard must NOT be wired into the PR gate** — not into `lint`, `typecheck`, `build`, or any CI workflow (D-09). Do not add a `prebuild`, `postbuild`, `prelint`, `pretypecheck`, `postinstall` or any other lifecycle hook. Do not create or edit any file under `.github/`. Do not edit `vercel.json`. It is *designed to fail* for the whole of Lot 1; adding it to CI would block every merge in the lot and the pressure would be to delete the guard.
- ⛔ **It sends nothing and wires no provider** (D-27): no transactional email service, no SDK, no domain authentication, no webhook, no PDF, no template renderer. Lot 1 **writes** the email copy, full stop.
- ⛔ **No payment provider is named anywhere** (D-04), including in `docs/cadrage-lot-1.md`.
- ⛔ Do not run `npm install`. An executor that believes an install is required must stop and report.
- ⛔ Do not touch `src/locales/fr/_mocks.public.json`, `_mocks.journey.json`, or any bundle plans 01-03 and 01-04 own — this plan only **reads** them.
- ⛔ Do not touch `next.config.ts` — its Lot 5 placeholder comment stays.
- ⛔ Do not hand-write a price string such as `"1 500 €"` (D-30).
- ⛔ **The client owns this repository.** ⛔ Nothing internal goes in `docs/cadrage-lot-1.md`: no day rate, no margin, no internal calendar, no reference to unsold lots, no phase number (CLAUDE.md).
- ⛔ `reference/` and the offer PDFs are read-only input (D-12).
- ⛔ Do not `git push`, create a PR, or run `vercel` (D-38).
</context>

<execution_order>
D-39 — **commit before verifying.** Commit each task as soon as its acceptance
criteria hold. The guard **is expected to exit 1** at the end of this plan — that
is success, not failure, and it must not be run as a blocking gate.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: The four automatic email texts</name>
  <files>src/locales/fr/emails.json, src/locales/fr/_mocks.emails.json</files>
  <read_first>
    - `src/locales/fr/_mocks.public.json` — the registry shape to reproduce field for field
    - `src/locales/fr/inscription.json`, `src/locales/fr/reservation.json` — the vocabulary the emails must match, so the same event is not named two ways
    - `src/lib/i18n/fr.ts` — why no formatted date or price is stored in the bundle
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-27, D-29, D-30
  </read_first>
  <action>
Create `src/locales/fr/emails.json` with exactly four namespaces, the four
`CADR-03` / D-27 emails: `confirmationInscription`, `confirmationReservation`,
`rappel`, `reinitialisationMotDePasse`.

Each namespace carries: `objet` (the subject line), `preheader` (the one-line
preview text), `salutation`, `corps` as an array of paragraphs, `action` with
`libelle` and a `contexte` sentence explaining what the button does, `signature`,
and `pied` for the transactional footer line. Interpolation tokens are written
literally and never formatted here — `{prenom}`, `{dateHeure}`, `{duree}`,
`{typeRendezVous}`, `{montant}`, `{lien}` — because the consumer formats through
`src/lib/i18n/fr.ts` and there is no i18n library (D-28, D-30).

`rappel` is the pre-session reminder text. ⚠ Note honestly in the summary that
the offer excludes a **J-1 / H-1 reminder sequence** — this plan writes the
reminder *text* that `CADR-03` requires, and no scheduling, no sequence and no
trigger.

`reinitialisationMotDePasse` says the link expires, without naming a duration —
that is Lot 3's implementation detail.

⛔ No HTML, no MJML, no `<table>`, no inline style, no template syntax. Plain
strings only: the rendering medium is Lot 5's decision and hardcoding a template
here would have to be undone. ⛔ No provider name, no unsubscribe mechanics, no
tracking pixel, no SPF/DKIM copy.

Then create `src/locales/fr/_mocks.emails.json` with **exactly the same shape**
as `src/locales/fr/_mocks.public.json`: a `_pourquoi` string and a `mocks` array
of `{ file, key, blocks, awaiting }` entries. Register the client-dependent
values only — the signature block, and any wording that asserts something about
the trainer or the offer. Subject lines, action labels and transactional
boilerplate are ours and are **not** registered. `blocks` is `CADR-03`;
`awaiting` is `Cadrage_Formation_SAP_Ariba_Questions_Client.docx`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const e=require('./src/locales/fr/emails.json');const pub=require('./src/locales/fr/_mocks.public.json');const reg=require('./src/locales/fr/_mocks.emails.json');const ns=['confirmationInscription','confirmationReservation','rappel','reinitialisationMotDePasse'];if(Object.keys(e).sort().join(',')!==ns.slice().sort().join(','))throw new Error('emails.json must have exactly the four namespaces, got: '+Object.keys(e));for(const n of ns){const o=e[n];for(const k of ['objet','preheader','salutation','corps','action','signature','pied'])if(o[k]===undefined)throw new Error(n+' missing '+k);if(!Array.isArray(o.corps)||!o.corps.length)throw new Error(n+'.corps must be a non-empty array');if(!o.action.libelle||!o.action.contexte)throw new Error(n+'.action needs libelle and contexte');}const s=JSON.stringify(e);if(/<[a-z]+[ >]|mjml|<table/i.test(s))throw new Error('HTML or MJML forbidden in the email bundle');if(/stripe|paypal|mollie|adyen|paddle|sendgrid|mailgun|postmark|resend|brevo/i.test(s))throw new Error('provider named (D-04, D-27)');if(/\d\s?€/.test(s))throw new Error('hand-written price string (D-30)');if(!/\{prenom\}/.test(s))throw new Error('interpolation tokens must be present');const f=Object.keys(pub.mocks[0]).sort().join(',');if(!reg._pourquoi||!Array.isArray(reg.mocks)||!reg.mocks.length)throw new Error('_mocks.emails.json malformed');for(const m of reg.mocks){if(Object.keys(m).sort().join(',')!==f)throw new Error('entry shape differs from _mocks.public.json');if(m.blocks!=='CADR-03')throw new Error('emails mocks block CADR-03');}const get=(o,p)=>p.split('.').reduce((x,k)=>x===undefined?undefined:x[k],o);for(const m of reg.mocks){const b=JSON.parse(fs.readFileSync('src/locales/fr/'+m.file,'utf8'));if(get(b,m.key)===undefined)throw new Error('unresolvable key '+m.file+'#'+m.key);}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/locales/fr/emails.json` has exactly the four namespaces `confirmationInscription`, `confirmationReservation`, `rappel`, `reinitialisationMotDePasse`
    - Each namespace carries `objet`, `preheader`, `salutation`, a non-empty `corps` array, `action.libelle`, `action.contexte`, `signature`, `pied`
    - Interpolation tokens appear literally (at minimum `{prenom}`); no formatted date and no formatted price is stored
    - The file contains no HTML tag, no MJML, no `<table>`, no inline style, no template syntax
    - The file names no payment provider and no email service provider
    - `src/locales/fr/_mocks.emails.json` matches the field set of `_mocks.public.json` exactly; every entry has `blocks: "CADR-03"` and the standard `awaiting` value; every registered key resolves
    - No SDK import, no webhook, no domain-authentication copy, no PDF
  </acceptance_criteria>
  <done>The automatic email copy `CADR-03` requires exists in the bundle, written and unsent.</done>
</task>

<task type="auto">
  <name>Task 2: The mock-content guard</name>
  <files>scripts/check-mock-content.mjs, package.json</files>
  <read_first>
    - `scripts/gen-db-types.mjs` — all 49 lines; the ESM style, the path-resolution idiom, the failure discipline, the Windows EOL note
    - `package.json` — the existing `scripts` block, lines 8–15
    - `.github/workflows/ci.yml` — confirm with your own eyes which npm scripts CI runs, before naming the new one
    - `vercel.json` — confirm `buildCommand` is `npm run build` and there is no `postinstall`
    - `src/locales/fr/_mocks.public.json`, `_mocks.journey.json`, `_mocks.emails.json` — the registry contract the guard consumes
    - `eslint.config.mjs` — the rules the script must satisfy, since `npm run lint` is bare `eslint`
  </read_first>
  <action>
Create `scripts/check-mock-content.mjs`. Open it with a comment explaining
**why** it exists and — critically — **why it is not in CI**: it is designed to
fail for the whole of Lot 1, so wiring it into `lint`, `typecheck` or `build`
would block every merge in the lot and the pressure would be to delete the guard.
Write that down in the file or a later run will "helpfully" add it.

Behaviour, satisfying D-08 in **one run**:
1. Resolve `src/locales/fr` from the module, not from `cwd`, with the
   `dirname(dirname(fileURLToPath(import.meta.url)))` idiom.
2. Discover every registry by filename pattern `_mocks.*.json` — the pattern is
   the contract, so a new bundle namespace only needs a new sidecar and no change
   here.
3. If **no** registry file is found, that is an error, not cleanliness:
   `console.error` and `process.exit(1)`. Deleting the registries must not be a
   way to fake a green result.
4. For every entry: resolve `key` as a dotted path inside
   `src/locales/fr/<file>`, addressing array elements numerically. An
   **unresolvable** key is a hard error — the registry has drifted from the
   bundle — reported distinctly from a still-mocked key.
5. Print one line per remaining placeholder, prefixed with the script name, in
   the form
   `check-mock-content: <file>#<key> — bloque <blocks> — en attente de <awaiting>`.
   Group by `blocks` and print a per-requirement count, so the reader sees at a
   glance which signed requirement is still open.
6. Exit **non-zero while any placeholder remains**. Exit `0` with a single
   confirmation line only when every registry's `mocks` array is empty.

Keep it dependency-free: `node:fs`, `node:path`, `node:url` only. No `glob`, no
`chalk`, no argument parser. Keep it lint-clean under bare `eslint`. It is
read-only, so no EOL normalisation is needed — but if you ever make it write,
normalise line endings the way `gen-db-types.mjs:44-47` does, because Windows is
a live concern in this repo.

Then add **exactly one** line to `package.json` `scripts`, following `db:types`
verbatim in shape:
`"content:check": "node scripts/check-mock-content.mjs"`.

⛔ Add nothing else to `package.json`. ⛔ Do not add `prebuild`, `postbuild`,
`prelint`, `pretypecheck`, `postinstall` or any lifecycle hook. ⛔ Do not create
or edit any file under `.github/`. ⛔ Do not edit `vercel.json`. Those four
prohibitions are D-09 and they are the reason the guard survives the lot.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8'));if(p.scripts['content:check']!=='node scripts/check-mock-content.mjs')throw new Error('content:check entry wrong or missing');const allowed=['dev','build','start','lint','typecheck','db:types','content:check'];const extra=Object.keys(p.scripts).filter(k=>!allowed.includes(k));if(extra.length)throw new Error('unexpected npm scripts added (D-09): '+extra);for(const k of Object.keys(p.scripts))if(/^(pre|post)(build|lint|typecheck|install)$/.test(k))throw new Error('lifecycle hook forbidden (D-09): '+k);const s=fs.readFileSync('scripts/check-mock-content.mjs','utf8');if(!/^\/\/|^\/\*/.test(s.trim()))throw new Error('script must open with a why comment');if(!/_mocks\./.test(s))throw new Error('guard must discover _mocks.*.json registries');if(!/process.exit\(1\)/.test(s))throw new Error('guard must exit non-zero');if(!/node:fs|node:path|node:url/.test(s))throw new Error('use node: prefixed builtins');if(/require\(|from ['\\\"](?!node:)[a-z@]/.test(s.replace(/from ['\\\"]node:[a-z]+['\\\"]/g,'')))throw new Error('guard must be dependency-free');const ci=fs.readFileSync('.github/workflows/ci.yml','utf8');if(/content:check/.test(ci))throw new Error('guard must NOT be in CI (D-09)');const v=fs.readFileSync('vercel.json','utf8');if(/content:check/.test(v))throw new Error('guard must NOT be in the Vercel build (D-09)');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-mock-content.mjs` exists, is ESM `.mjs`, opens with a comment explaining why it exists **and why it is not in CI**
    - It imports only `node:`-prefixed builtins; no third-party import, no `require(`
    - It resolves `src/locales/fr` from the module path, not from `cwd`
    - It discovers registries by the `_mocks.*.json` filename pattern and errors with exit 1 when none is found
    - It reports each remaining placeholder as `check-mock-content: <file>#<key> — bloque <blocks> — en attente de <awaiting>` and prints a per-requirement count
    - It reports an unresolvable registry key as a distinct hard error
    - It exits non-zero while any placeholder remains and exits 0 with one confirmation line when all registries are empty
    - `package.json` `scripts` contains exactly `dev`, `build`, `start`, `lint`, `typecheck`, `db:types`, `content:check` — no lifecycle hook of any kind
    - `.github/workflows/ci.yml` and `vercel.json` are unmodified and contain no reference to `content:check`
    - `npm run lint` reports no error for the new file
  </acceptance_criteria>
  <done>One command answers « is anything still mocked? », lists every placeholder with the requirement it blocks, exits non-zero — and cannot be reached by CI or by the Vercel build.</done>
</task>

<task type="auto">
  <name>Task 3: The framing record, and the CADR-02 deferral</name>
  <files>docs/cadrage-lot-1.md</files>
  <read_first>
    - `docs/hebergement.md` — the only existing doc; match its French register and heading style
    - `src/locales/fr/landing.json` and `src/locales/fr/paiement.json` — the positioning, the commercial promise and the four sale formulas already written; the record must agree with the bundle, not restate it differently
    - `.planning/REQUIREMENTS.md` lines 30–35 — `CADR-01` … `CADR-06` verbatim
    - `.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md` — D-04, D-06, D-31, and the Deferred Ideas section
  </read_first>
  <action>
Create `docs/cadrage-lot-1.md`, in French, as the recorded output `CADR-01`
requires: « positionnement, promesse commerciale, formules de vente, hiérarchie
des pages, priorités ».

Sections, in this order:
1. `## Positionnement` — who the training is for and what it is not.
2. `## Promesse commerciale` — the promise the landing page makes, agreeing
   word-for-word in substance with `landing.hero` and `landing.ctaFinal`.
3. `## Formules de vente` — the four formulas, each with what it includes and its
   price. Prices are written as **numerals with the currency named in prose**,
   and each carries the note that no price in this lot is a commitment — the four
   formulas are the next lot's contract.
4. `## Hiérarchie des pages` — the eleven screens and the ten French slugs
   (`/programme`, `/formation`, `/a-propos`, `/contact`, `/inscription`,
   `/connexion`, `/agenda`, `/reservation`, `/paiement`, `/espace`), with a line
   saying why the slugs are French: the audience and the search keywords are
   French, and nothing is indexed yet, so this is the cheap moment to fix them.
5. `## Priorités` — what is validated first and why.
6. `## Contenus en attente du client` — a table of every value still mocked,
   generated by reading the three `_mocks.*.json` registries, with the
   questionnaire name and a line saying the real answers replace the mocks
   **before this lot's validation closes**. State plainly that
   `npm run content:check` lists them at any time and exits non-zero while any
   remain.
7. `## Visuels` — the pictograms are delivered; the professional photography and
   the interface captures wait on the client's image library and are an
   enhancement rather than a blocker.
8. `## Choix du prestataire de paiement — reporté` — record, honestly and
   briefly, that this decision is **deferred to the payment lot** and therefore
   that the requirement closes **unmet** in this lot. ⛔ **Name no provider, no
   shortlist, no candidate, in any casing** (D-04). Say what the deferral means
   concretely: provider account verification has its own lead time and starts
   later than it could. Do not editorialise beyond that.

⛔ **The client owns this repository.** No day rate, no margin, no internal
calendar, no reference to unsold or future offers, no lot numbering beyond what
the signed offer itself uses, no phase number, no planning-tool reference, and no
mention of AI or of how the work was produced.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/cadrage-lot-1.md','utf8');for(const h of ['## Positionnement','## Promesse commerciale','## Formules de vente','## Hiérarchie des pages','## Priorités','## Contenus en attente du client','## Visuels','reporté'])if(!d.includes(h))throw new Error('missing section: '+h);if(!/CADR-02/.test(d))throw new Error('CADR-02 must be named as deferred');if(!/CADR-01/.test(d))throw new Error('CADR-01 must be named');if(/stripe|paypal|mollie|adyen|paddle|braintree|payplug|lemon ?squeezy|checkout\.com/i.test(d))throw new Error('payment provider named (D-04)');if(/taux journalier|marge|TJM|Business|Premium|lot 1[1-8]|Phase \d/i.test(d))throw new Error('internal or unsold-scope reference (CLAUDE.md)');if(/\bIA\b|intelligence artificielle|Claude|GSD/i.test(d))throw new Error('no mention of how the work was produced');for(const s of ['/programme','/formation','/a-propos','/contact','/inscription','/connexion','/agenda','/reservation','/paiement','/espace'])if(!d.includes(s))throw new Error('missing slug: '+s);if(!/content:check/.test(d))throw new Error('the record must name the content:check command');const regs=fs.readdirSync('src/locales/fr').filter(f=>/^_mocks\..*\.json$/.test(f));if(regs.length<3)throw new Error('expected three registries, found '+regs.length);let n=0;for(const r of regs)n+=JSON.parse(fs.readFileSync('src/locales/fr/'+r,'utf8')).mocks.length;if(n===0)throw new Error('no mocks registered — check plans 01-03/01-04');console.log('ok — '+n+' mocked keys outstanding across '+regs.length+' registries')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/cadrage-lot-1.md` exists, is in French, and carries the sections Positionnement, Promesse commerciale, Formules de vente, Hiérarchie des pages, Priorités, Contenus en attente du client, Visuels, and the deferred payment-provider section
    - It names `CADR-01` as recorded and `CADR-02` as **deferred and closing unmet**
    - It matches no payment-provider name in any casing
    - It lists all ten French slugs and explains why they are French
    - The « Contenus en attente du client » table lists the outstanding mocked values and names the questionnaire and the `content:check` command
    - The « Visuels » section states that the pictograms ship and that photography and interface captures wait on the client's library
    - It contains no day rate, no margin, no internal calendar, no reference to unsold offers or lots beyond the signed ten, no phase number, and no mention of how the work was produced
    - The four formulas' prices agree with the numeric values in `src/locales/fr/paiement.json`
  </acceptance_criteria>
  <done>`CADR-01` is recorded, the outstanding client content is enumerated in one place, and `CADR-02`'s deferral is on the record without a provider being named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mocked copy → Lot 2 → the client | the phase's primary threat: full-fidelity maquettes hide which strings are real |
| repository → the client | the client owns this repo; internal commercial data must never enter it |
| npm script namespace → CI and Vercel | the only two surfaces that can accidentally make the guard blocking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-25 | Spoofing | mocked French copy reaching the client as real | mitigate | `content:check` reads all three `_mocks.*.json` registries, lists every remaining placeholder with its key and the blocking requirement, and exits non-zero while any remain (D-08). `docs/cadrage-lot-1.md` restates the outstanding set in prose so the gate conversation cannot skip it |
| T-01-26 | Tampering | the guard neutralised by deleting its registries | mitigate | Finding zero registry files is a hard error with exit 1, not a clean result — deleting the sidecars cannot fake green |
| T-01-27 | Tampering | the guard drifting out of sync with the bundles | mitigate | An unresolvable registry key is reported as a distinct hard error, so renaming a copy key without updating the registry fails the check rather than silently passing |
| T-01-28 | Denial of Service | the guard wired into CI, blocking every merge in the lot | mitigate | This is D-09 and it is enforced twice: acceptance criteria assert the exact allowed `scripts` key set with no `pre*`/`post*` hook, and assert `ci.yml` and `vercel.json` are unmodified and contain no `content:check` reference. The script's own header comment records why, so a later run does not "helpfully" add it |
| T-01-29 | Information Disclosure | internal commercial data leaking into a client-owned repository | mitigate | Acceptance criteria assert `docs/cadrage-lot-1.md` matches no day rate, margin, internal calendar, unsold-offer reference, phase number, or mention of how the work was produced |
| T-01-30 | Information Disclosure | a payment provider named before the decision is made | mitigate | Acceptance criteria assert the provider-name pattern matches nothing in the record; `CADR-02` is written as deferred with the commercial consequence stated plainly and no shortlist |
| T-01-31 | Tampering | email copy that implies sending is wired | mitigate | Plain strings only — acceptance criteria forbid HTML, MJML, template syntax, any SDK import, any webhook and any domain-authentication copy. Lot 1 writes; nothing sends (D-27) |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No install task exists.** The guard is dependency-free and uses only `node:` builtins; acceptance criteria assert the absence of any third-party import |
| T-01-08 | Elevation of Privilege | runtime authorization | accept | This plan ships no route, no auth surface and no data access. Lot 1 ships no authorization by design (D-11, D-41); Lot 3 adds the gate |
</threat_model>

<verification>
Manager-session checks after commit (D-39):

1. `npm run content:check` — **expected to exit 1** and print one line per outstanding placeholder with its blocking requirement. A zero exit here would mean the registries are empty, which at this point is a bug.
2. `npm run lint`, `npm run typecheck` and `npm run build` all still pass, and none of them runs the guard.
3. `git diff --stat .github vercel.json` is empty.
4. `node -e "const p=require('./package.json');console.log(Object.keys(p.scripts).join(' '))"` prints exactly `dev build start lint typecheck db:types content:check`.
5. Read `docs/cadrage-lot-1.md` as the client would and confirm nothing internal is in it.
</verification>

<success_criteria>
- The four automatic email texts exist in the bundle, plain strings, unsent, provider-free
- `npm run content:check` answers the mock question in one run and exits non-zero while any placeholder remains
- The guard is provably unreachable from `ci.yml` and from the Vercel build, and says in its own header why
- `CADR-01` is recorded in French in `docs/cadrage-lot-1.md`, agreeing with the locale bundle
- `CADR-02` is recorded as deferred and closing unmet, with no provider named anywhere
- Committed; nothing pushed
</success_criteria>

<output>
Create `.planning/phases/01-cadrage-contenus-et-design/01-08-SUMMARY.md` when done.
</output>
