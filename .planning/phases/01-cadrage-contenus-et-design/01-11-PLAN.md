---
phase: 01-cadrage-contenus-et-design
plan: 11
type: execute
wave: 5
depends_on: ["01-04", "01-05", "01-06", "01-07"]
files_modified:
  - src/app/inscription/page.tsx
  - src/app/connexion/page.tsx
  - src/app/espace/page.tsx
autonomous: true
requirements: [CADR-06]

must_haves:
  truths:
    - "Three of the eleven signed screens exist as real Next.js routes at the French slugs (D-05, D-31)"
    - "The espace apprenant shows honest empty states for every surface later lots will fill (D-23)"
    - "The inscription screen shows the server-rejected field state, not only browser validation (D-23)"
    - "No account, session, role, RLS policy or Google sign-in behaviour ships — labels and layout only (D-11, D-41)"
  artifacts:
    - path: "src/app/inscription/page.tsx"
      provides: "The inscription maquette with the full field state set"
      min_lines: 40
    - path: "src/app/connexion/page.tsx"
      provides: "The connexion maquette"
      min_lines: 30
    - path: "src/app/espace/page.tsx"
      provides: "The espace apprenant maquette with six honest empty states"
      min_lines: 50
  key_links:
    - from: "src/app/espace/page.tsx"
      to: "src/components/ui/empty-state.tsx"
      via: "every surface renders EmptyState with copy from espace.json"
      pattern: "@/components/ui/empty-state"
    - from: "src/app/inscription/page.tsx"
      to: "src/components/ui/field.tsx"
      via: "the form is composed from the signed field family incl. data-rejected=server"
      pattern: "data-rejected"
---

<objective>
Wave 4 of the locked wave shape (D-37), screens 6–8 of 11: **inscription,
connexion, espace apprenant**, as real Next.js routes at the French slugs.

Purpose: `CADR-06` names every screen of the full journey, and these three are
where the client sees what having an account looks like. D-05 makes them full
fidelity on the real design system, so a validated maquette *is* the Lot 3 page.

The espace apprenant is the most important empty-state screen in the phase:
`CPT-06` promises « des états vides honnêtes pour les surfaces remplies par les
lots suivants », and a maquette validated without them is a Lot 2 re-do.

Output: three route files. No new component, no new copy key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cadrage-contenus-et-design/01-CONTEXT.md
@.planning/phases/01-cadrage-contenus-et-design/01-PATTERNS.md
@CLAUDE.md

<interfaces>
**The route template** — `src/app/page.tsx`: server component, `export default
function <PascalName>()`, synchronous, **not** `async`, **no `metadata` export on
the page**. The chrome lives in `src/app/layout.tsx` (plan 01-07): `Header`,
`<main className="flex-1">`, `Footer`. Typed routes: a page with no params omits
props; the type, if ever needed, is the generated `PageProps<"/route">` global —
never hand-written.

**Available from earlier plans:**
- `@/components/sections/section` → `Section`, `SectionHeader` (plan 01-09)
- `@/components/ui/{button,card,badge,message,accordion,input,field,empty-state}`
- `@/components/icons/pictograms` → the `pictograms` registry, `PictogramName`
- `@/locales/fr/{inscription,connexion,espace,common}.json`
- `@/lib/i18n/fr.ts` → `formatDate`, `formatDateTime`, `formatNumber`

**Field family** (plan 01-06): `Field`, `FieldLabel`, `FieldControl`,
`FieldDescription`, `FieldError`. `FieldError` renders `Message variant="error"`
with `role="alert"`. The server-rejected state is `data-rejected="server"` on
`Field` — a named deliverable distinct from client-side `aria-invalid`.

**Empty-state family** (plan 01-06): `EmptyState`, `EmptyStateIcon`,
`EmptyStateTitle`, `EmptyStateDescription`, `EmptyStateAction`, with a `tone` of
`neutral` / `waiting` / `error`. Every string is a prop.

**Copy already written** (plan 01-04): `inscription.champs`, `inscription.erreurs`
(incl. `rejetServeur`), `inscription.succes`, `inscription.dejaInscrit`;
`connexion.champs`, `connexion.motDePasseOublie`, `connexion.google`,
`connexion.erreurs` (`identifiantsInvalides`, `rejetServeur`,
`tropDeTentatives`), `connexion.pasDeCompte`; `espace.bienvenue` carrying the
literal `{prenom}` token, `espace.aucuneReservation` with `titre` / `message` /
`action`, and the six surfaces `rendezVous`, `sessions`, `historique`,
`documents`, `factures`, `avancement`, each with a `vide` object.

**Slugs (D-31):** `/inscription`, `/connexion`, `/espace`. Directory names must
match exactly.
</interfaces>

**Hard prohibitions:**
- ⛔ **Do not touch `supabase/`, any migration, any auth surface, any RLS policy, or `src/types/database.types.ts`** (D-11). Lot 1 ships no authenticated behaviour. **No schema push task belongs in this phase.**
- ⛔ Do not import from `@/lib/supabase/*`, do not create a session, do not read a cookie, do not add middleware, do not add a route guard or a redirect. `CPT-01` … `CPT-09` are Lot 3 (D-41).
- ⛔ No Google OAuth call, no consent screen, no scope declaration — the `connexion.google` button is a **label on an inert control** (D-41).
- ⛔ No form submission, no `action`, no `onSubmit`, no `"use server"`, no `fetch`, no `zod` schema, no password-strength function (D-41).
- ⛔ No page logic, no data fetching, no `async` page.
- ⛔ No per-route `metadata`, Open Graph, Twitter Card or structured data (D-41, Lot 5).
- ⛔ **No tenancy concept** — no organisation, workspace, tenant or team anywhere. Single-tenant by construction (D-42).
- ⛔ No attendance, progression percentage computation, certificate or invoice **logic** — `avancement` and `factures` render their empty states only (D-41, Lots 7 and 9).
- ⛔ No French string literal in JSX (D-29). If a key is missing, **stop and report** — plan 01-04 owns the copy.
- ⛔ No raster asset, nothing under `public/` (D-06, D-21).
- ⛔ Do not modify any file under `src/components/`, `src/locales/`, `src/app/globals.css` or `src/app/layout.tsx`.
- ⛔ Do not run `npm install`. Do not `git push`, create a PR, or run `vercel` (D-38).
</context>

<execution_order>
D-39 — **commit before verifying.** Commit each task as soon as its acceptance
criteria hold. `npm run typecheck` runs once, as the last task's verification.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: /inscription and /connexion</name>
  <files>src/app/inscription/page.tsx, src/app/connexion/page.tsx</files>
  <read_first>
    - `src/locales/fr/inscription.json` and `src/locales/fr/connexion.json` — read in full
    - `src/components/ui/field.tsx`, `src/components/ui/input.tsx`, `src/components/ui/message.tsx` — the field family API and the `data-rejected="server"` state
    - `src/app/contact/page.tsx` — the form composition plan 01-10 established; these two must match it, not diverge
    - `src/components/sections/section.tsx` — the shell
  </read_first>
  <action>
Create `src/app/inscription/page.tsx` — directory `inscription`, default export
`Inscription`. Render `inscription.titre` / `inscription.intro`, then the
account-creation form in a centred `Card`: `prenom`, `nom`, `email`,
`motDePasse`, `confirmation`, `profil` with its four option labels — each as
`Field` → `FieldLabel` → `FieldControl`/`Input` → `FieldDescription`, labels and
hints from `inscription.champs` and `inscription.aideParChamp`. Below the fields,
the `inscription.conditions` consent line and the submit `Button` reading
`common.actions.sInscrire`, then the `inscription.dejaInscrit` link to
`/connexion`.

The form is a **maquette of a form**: the submit `Button` does nothing. ⛔ No
`action`, no `onSubmit`, no `"use server"`, no `fetch`, no `zod`, no
password-strength meter, no confirmation-match check in JavaScript.

**Show the states, because that is the deliverable** (D-23). Render a « états »
demonstration block with a stable `id` and a heading from the bundle, presenting
the field family in each named state: default, focus-visible, disabled, loading
via `data-loading`, client-side invalid via `aria-invalid` with
`inscription.erreurs.emailInvalide`, and the **server-rejected** state via
`data-rejected="server"` rendering `inscription.erreurs.rejetServeur`. The two
error states must be visually distinguishable — an email already taken is not the
same event as a malformed address, and Lot 3 must not have to invent the
difference.

Also render the `inscription.succes` state — the « vérifiez votre boîte mail »
confirmation — as a second `Card` in the demonstration block, so the client
validates the whole outcome and not just the input.

Create `src/app/connexion/page.tsx` — directory `connexion`, default export
`Connexion`. Render `connexion.titre` / `connexion.intro`, the `email` and
`motDePasse` fields, the `connexion.motDePasseOublie` link label, the submit
`Button` from `common.actions.seConnecter`, a separator, the
`connexion.google` `Button variant="outline"` with a lucide icon, and the
`connexion.pasDeCompte` link to `/inscription`. Render the three
`connexion.erreurs` states in a small demonstration block on the same page.

⛔ The Google button is inert. No OAuth URL, no provider client, no
`@/lib/supabase` import, no redirect (D-11, D-41).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const I=fs.readFileSync('src/app/inscription/page.tsx','utf8');const C=fs.readFileSync('src/app/connexion/page.tsx','utf8');for(const [n,c] of [['inscription',I],['connexion',C]]){if(/export const metadata|export default async|export async function/.test(c))throw new Error(n+': no page metadata, no async page');if(/fetch\(|\"use server\"|onSubmit=|action=\{|from \"zod\"|@\/lib\/supabase|createClient|cookies\(\)|redirect\(/.test(c))throw new Error(n+': auth or submission logic forbidden (D-11/D-41)');if(/:\s*any\b/.test(c))throw new Error(n+': any type');const st=c.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/.*/g,'');if(/>[^<>{}]*[à-ÿ]{2,}[^<>{}]*</.test(st))throw new Error(n+': hardcoded French string in JSX (D-29)');if(/organisation|workspace|tenant/i.test(c))throw new Error(n+': tenancy concept forbidden (D-42)');if(/\.(png|jpe?g|webp|avif)\b|<img|next\/image/.test(c))throw new Error(n+': image forbidden');if((c.match(/<h1/g)||[]).length!==1)throw new Error(n+': exactly one h1 required');if(/<Header|<Footer/.test(c))throw new Error(n+': chrome is in the root layout');if(!/@\/components\/ui\/field/.test(c))throw new Error(n+': must compose the signed field family');}for(const t of ['data-rejected','data-loading','aria-invalid','FieldError','rejetServeur'])if(!I.includes(t))throw new Error('inscription: missing state demonstration: '+t);if(!/succes/.test(I))throw new Error('inscription must render the succes state');for(const t of ['identifiantsInvalides','tropDeTentatives','rejetServeur'])if(!C.includes(t))throw new Error('connexion: missing error state: '+t);if(/accounts.google|oauth2|OAuth/.test(C))throw new Error('connexion: no OAuth wiring allowed');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/inscription/page.tsx` and `src/app/connexion/page.tsx` exist at exactly those directory names and export default `Inscription` and `Connexion`
    - Neither contains `fetch(`, `"use server"`, `onSubmit=`, an `action={` prop, a `zod` import, `@/lib/supabase`, `createClient`, `cookies()` or `redirect(`
    - Both compose `Field`, `FieldLabel`, `FieldControl`, `FieldDescription`, `FieldError` from `@/components/ui/field`
    - `/inscription` renders the six fields `prenom`, `nom`, `email`, `motDePasse`, `confirmation`, `profil` and a states block containing `data-loading`, `aria-invalid` and `data-rejected="server"` examples, the last rendering `inscription.erreurs.rejetServeur`
    - The client-side invalid state and the server-rejected state are visually distinguishable in the rendered output
    - `/inscription` also renders the `inscription.succes` confirmation state
    - `/connexion` renders all three of `connexion.erreurs.identifiantsInvalides`, `.rejetServeur`, `.tropDeTentatives`, and an inert Google button with no OAuth URL
    - Neither file matches `/organisation|workspace|tenant/i`
    - Each page has exactly one `<h1>`; no French literal in JSX; no image; no `: any`; no `<Header` / `<Footer`
  </acceptance_criteria>
  <done>The two account entry screens are validated-ready, with both error kinds visible and nothing wired.</done>
</task>

<task type="auto">
  <name>Task 2: /espace — the learner space and its six honest empty states</name>
  <files>src/app/espace/page.tsx</files>
  <read_first>
    - `src/locales/fr/espace.json` — read in full: `bienvenue` with its `{prenom}` token, `aucuneReservation`, and the six surfaces with their `vide` objects
    - `src/components/ui/empty-state.tsx` — the `EmptyState` parts and the `tone` variants
    - `src/components/ui/card.tsx`, `badge.tsx` — the surfaces the space is built from
    - `src/lib/i18n/fr.ts` — the formatters, for the surface that would show a date
    - `.planning/REQUIREMENTS.md` line 60 — `CPT-06` verbatim
  </read_first>
  <action>
Create `src/app/espace/page.tsx` — directory `espace`, default export `Espace`.
This is the screen `CPT-06` describes: « mes prochains rendez-vous et sessions,
mon historique, mes documents, mes factures, mon avancement », **with honest
empty states for the surfaces filled by later lots**.

Render the `espace.bienvenue` line. The copy carries the literal `{prenom}`
token; substitute it with a plain `String.prototype.replace` against a local
placeholder constant — there is no i18n library and none may be added (D-28).
Write a `/* why */` comment naming the constant as maquette-only data that Lot 3
replaces with the session's first name.

Render the six surfaces — `rendezVous`, `sessions`, `historique`, `documents`,
`factures`, `avancement` — each as a `Card` with its title as `<h2>` or `<h3>`
consistent with the outline, and each containing an `EmptyState` built from that
surface's `vide` object. Tone: `neutral` for the surfaces that are simply empty
(`historique`, `documents`, `factures`, `avancement`), `waiting` for the ones
where something is expected soon (`rendezVous`, `sessions`).

Render `espace.aucuneReservation` as the **top-level** empty state, shown when
the learner has booked nothing at all: `EmptyState tone="waiting"` with its
`titre`, `message`, and an `EmptyStateAction` holding a `Button` linked to
`/agenda`.

**Honest means honest.** The empty states say what is not there and what to do
next; they do not apologise, do not promise a date, and do not describe a feature
as « bientôt disponible » in a way the signed offer does not support. ⛔ Do not
render fabricated sample rows — no fake appointment, no fake invoice, no fake
progress percentage. A maquette that shows invented data is a maquette the client
validates against a promise nobody made.

`avancement` renders its empty state only: ⛔ no progress bar with a made-up
number, no attendance count, no certificate mention — `SUI-*` is a later lot
(D-41).

Because the back-office reuses this component set at a tighter density, wrap the
surface grid in an element carrying `data-density` set from a local constant, and
leave it at the default; the point is that the contract exists and the compact
value is one attribute away (D-24).

⛔ No session, no cookie, no `@/lib/supabase` import, no guard, no redirect. This
route ships **unguarded** — that is the design (D-11), and it is safe only
because production carries `X-Robots-Tag: noindex, nofollow` and holds no real
data.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const E=fs.readFileSync('src/app/espace/page.tsx','utf8');const j=require('./src/locales/fr/espace.json');if(/export const metadata|export default async|export async function/.test(E))throw new Error('no page metadata, no async page');if(/fetch\(|@\/lib\/supabase|createClient|cookies\(\)|redirect\(|\"use server\"|middleware/.test(E))throw new Error('auth or data logic forbidden (D-11/D-41)');if(/:\s*any\b/.test(E))throw new Error('any type');const st=E.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/.*/g,'');if(/>[^<>{}]*[à-ÿ]{2,}[^<>{}]*</.test(st))throw new Error('hardcoded French string in JSX (D-29)');if(/organisation|workspace|tenant/i.test(E))throw new Error('tenancy concept forbidden (D-42)');if(!/@\/components\/ui\/empty-state/.test(E))throw new Error('must compose the EmptyState family');for(const s of ['rendezVous','sessions','historique','documents','factures','avancement'])if(!E.includes(s))throw new Error('surface missing: '+s);if(!/aucuneReservation/.test(E))throw new Error('top-level empty state missing');if(!/bienvenue/.test(E)||!/replace\(/.test(E))throw new Error('bienvenue must interpolate {prenom} with a plain replace');if(!/data-density/.test(E))throw new Error('density contract must be present (D-24)');if(/\d+\s?%/.test(E))throw new Error('fabricated progress percentage forbidden');if(/<progress|role=\"progressbar\"/.test(E))throw new Error('no progress bar with invented data (D-41)');if((E.match(/<h1/g)||[]).length!==1)throw new Error('exactly one h1 required');if(/<Header|<Footer/.test(E))throw new Error('chrome is in the root layout');if(/\.(png|jpe?g|webp|avif)\b|<img|next\/image/.test(E))throw new Error('image forbidden');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/espace/page.tsx` exists at exactly that directory name and exports default `Espace`
    - It contains no `fetch(`, `@/lib/supabase`, `createClient`, `cookies()`, `redirect(`, `"use server"` or middleware reference
    - It composes `EmptyState` from `@/components/ui/empty-state` and renders an empty state for all six surfaces `rendezVous`, `sessions`, `historique`, `documents`, `factures`, `avancement`
    - `espace.aucuneReservation` renders as the top-level empty state with an action linking to `/agenda`
    - `espace.bienvenue` is interpolated by a plain `replace(` against a local maquette-only constant, with a `/* why */` comment; no i18n library is imported
    - A `data-density` attribute is present on the surface grid
    - No fabricated data: the file matches no `\d+\s?%`, no `<progress`, no `role="progressbar"`, and renders no sample appointment, invoice or history row
    - Exactly one `<h1>`; no French literal in JSX; no image; no `: any`; no `<Header` / `<Footer`; no tenancy term
  </acceptance_criteria>
  <done>The learner space is validated-ready with six honest empty states and no invented data.</done>
</task>

<task type="auto">
  <name>Task 3: Slug, outline and keyboard pass across the three routes</name>
  <files>src/app/inscription/page.tsx, src/app/connexion/page.tsx, src/app/espace/page.tsx</files>
  <read_first>
    - the three route files as written in Tasks 1 and 2
    - `src/components/layout/header.tsx` — the `/inscription` and `/connexion` hrefs it already renders
  </read_first>
  <action>
Close the three things that stop being cheap after this phase.

**Slugs (D-31).** The directories must be exactly `inscription`, `connexion`,
`espace`. Every header href must resolve under Next 16 typed routes, so a
mismatch is a build failure rather than a dead link discovered at the gate.

**Heading outline.** Exactly one `<h1>` per route, `SectionHeader` at `<h2>`,
card and surface titles at `<h3>`, no level skipped.

**Responsive and keyboard.** Single column below `sm`, two at `md`, the full grid
at `lg`, expressed with Tailwind responsive variants in the route files. ⛔ Do not
add a media query to `src/app/globals.css`. Every control keyboard reachable with
the three-class focus ring; both forms tabbable in visual order; the states
demonstration blocks reachable without trapping focus; each `EmptyStateAction`
button reachable.

One extra check specific to this plan: confirm `/espace` renders without an
authenticated session, because there is none — if any part of the page assumes
data exists, it will throw at build or render, and that is the signal that
something forbidden crept in.

Then run the plan's single type gate.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The directories `src/app/inscription`, `src/app/connexion`, `src/app/espace` exist with exactly those names
    - Every header href resolves to an existing route, proven by `npm run typecheck` under typed routes
    - Each of the three routes renders exactly one `<h1>` and skips no heading level
    - Responsive behaviour uses Tailwind `sm:` / `md:` / `lg:` variants in the route files; `src/app/globals.css` is unmodified by this plan
    - No `text-[Npx]` or ad-hoc font size in any of the three files
    - Every interactive element is keyboard reachable with the focus ring; both forms tab in visual order
    - `/espace` renders with no session and no data source
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Three more of the eleven screens are validated-ready, at French slugs the type system enforces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → `/espace` | this route ships **unguarded** and is publicly reachable by URL; Lot 3 adds the gate |
| user credentials → inscription and connexion forms | password fields are rendered; in Lot 1 they submit nowhere |
| locale bundle → rendered page | mocked strings become visible at full fidelity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-NOINDEX | Information Disclosure | `/espace` ships unguarded and publicly reachable | accept | **This is the phase's honest second threat.** Lot 1 ships no runtime authorization by design (D-11, D-41). It is safe only because production carries the repo-wide `X-Robots-Tag: noindex, nofollow` shipped in Phase 0 **and** the route renders no real data — it has no session, no query and no data source, asserted by acceptance criteria. ⛔ No task here may modify `next.config.ts` or `vercel.json`. Lot 3 adds the gate; the noindex header must not be removed before it does |
| T-01-38 | Information Disclosure | credentials posted somewhere unintended | mitigate | Acceptance criteria forbid `action=`, `onSubmit=`, `"use server"` and `fetch(` in both forms, so no password can leave the page. The `Input` for `motDePasse` is a maquette control |
| T-01-39 | Spoofing | fabricated appointments, invoices or a progress percentage read as real learner data | mitigate | Acceptance criteria forbid any `\d+%`, `<progress`, `role="progressbar"` and any sample row; every surface renders its empty state only |
| T-01-40 | Elevation of Privilege | a Google sign-in button that appears functional | mitigate | The control is inert with no OAuth URL, no provider client and no redirect; acceptance criteria assert the absence of `@/lib/supabase`, `createClient` and `redirect(` |
| T-01-20 | Repudiation | a server-side rejection with no visible feedback | mitigate | `/inscription` renders `data-rejected="server"` with `FieldError` → `Message role="alert"`, visually distinct from client-side `aria-invalid`, so Lot 3 inherits the pattern already validated |
| T-01-16 | Denial of Service | keyboard-only users unable to complete registration | mitigate | Tab order in visual order, focus ring on every control, no focus trap, reachable empty-state actions |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No install task exists.** Every component is already in the repository |
| T-01-41 | Tampering | schema or auth surface touched under pressure to make the page "work" | mitigate | ⛔ D-11 is absolute: acceptance criteria assert no `supabase/` file, no migration, no RLS policy and no `src/types/database.types.ts` change. **No schema push task belongs in this phase.** An executor tempted to add one must stop and report |
</threat_model>

<verification>
Manager-session checks after commit (D-39):

1. `npm run typecheck` and `npm run build` pass.
2. `git status --porcelain supabase src/types` returns empty.
3. Load `/inscription`, `/connexion`, `/espace` at 375px, 768px and 1440px.
4. Submit both forms and confirm nothing happens and no network request is made.
5. On `/espace`, confirm all six surfaces show an empty state and that no number, date or row is invented.
6. Confirm the client-side invalid and server-rejected states on `/inscription` look different.
7. `curl -I https://elearning-ariba.vercel.app` still returns `X-Robots-Tag: noindex, nofollow`.
</verification>

<success_criteria>
- Three more of the eleven signed screens exist as real routes at the French slugs
- `/espace` shows six honest empty states and invents no data
- `/inscription` shows the full field state set including the server-rejected state
- Nothing authenticated ships: no session, no cookie, no guard, no OAuth, no schema change
- `npm run typecheck` passes; nothing pushed
</success_criteria>

<output>
Create `.planning/phases/01-cadrage-contenus-et-design/01-11-SUMMARY.md` when done.
</output>
