---
phase: 3
slug: comptes-connexion-et-espace-apprenant
status: draft
shadcn_initialized: true
preset: "none — components.json present, components hand-authored on @base-ui/react; no registry block is installed in this phase"
created: 2026-08-31
revised: 2026-08-31
mode: autonomous
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Lot 3 (`CPT-01`..`CPT-09`). Generated by gsd-ui-researcher
> in autonomous mode, verified by gsd-ui-checker.

**Authority.** The design system shipped by Lots 1–2 is the authority. This phase **consumes**
it and never modifies it (`03-CONTEXT.md` D-04). Where this document and
`src/app/globals.css` / `src/components/ui/*` disagree, **the code wins** — this file must
then be corrected, never the code.

**Write-forbidden for the whole phase (D-04):** `src/app/globals.css`,
`src/components/ui/*` (the eight existing components), `src/components/layout/*`.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn CLI present (`components.json`, `shadcn@^4.19.0` devDependency) — **not used in this phase** |
| Preset | not applicable — no registry block is installed |
| Component library | **`@base-ui/react` `^1.7.0`** (not Radix, not shadcn primitives) |
| Icon library | `lucide-react` `^1.34.0` |
| Font | body **Inter** (`--font-body`), headings **Plus Jakarta Sans** (`--font-heading-face`), both via `next/font/google`. No CDN font. |
| Styling | Tailwind v4, tokens in `@theme inline` + `:root` of `globals.css` |
| New dependency budget | **zero** (D-19) |

### Components consumed as-is (read-only)

| Component | Parts / variants Lot 3 uses |
|---|---|
| `button` | `variant`: `default` (primary pill), `outline` (Google), `ghost` (déconnexion), `destructive` (suppression de compte), `link`. `data-loading="true"` is the pending API. |
| `card` | `Card` (`variant` `default` \| `raised` \| `muted`), `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter` |
| `field` | `Field` (+ `data-rejected="server"`), `FieldLabel`, `FieldControl` (incl. `render={<select>}`), `FieldDescription`, `FieldError` |
| `input` | via `FieldControl` only — no bare `Input` on a labelled form |
| `message` | `variant` `error` \| `success` \| `info` — form-level banners |
| `empty-state` | `EmptyState` (`tone` `neutral` \| `waiting` \| `error`), `…Title`, `…Description`, `…Action` |
| `badge` | `variant` `default` (support disponible), `muted` |
| `accordion` | not used in this phase |

### New components this phase creates (alongside, never by modifying)

| File | Why the eight cannot express it |
|---|---|
| `src/components/ui/checkbox.tsx` | Communication preferences (`CPT-05`) and the deletion acknowledgement (`CPT-09`) need a checkbox. `inputVariants` is `h-8 w-full` — wrong shape. Built on `@base-ui/react/checkbox` (already installed), same `cva` + `data-slot` + `in-data-[density=compact]:` idiom as its neighbours. |
| `src/components/compte/auth-shell.tsx` | The centred card shell (`mx-auto max-w-md/2xl px-4 py-16`, title block, card, footer link) is repeated on five surfaces. Composition only — no new token, no new variant. |
| `src/components/compte/submit-button.tsx` | Client island wrapping `Button` with `useFormStatus` to drive `data-loading` and `disabled`. `Button` already exposes the attribute; nothing drives it today. |
| `src/components/espace/espace-nav.tsx` | `src/components/layout/header.tsx` is read-only and has **no session awareness** — so `/espace/profil`, `/espace/donnees` and **Se déconnecter** have no home in the global chrome. They live in a local nav band rendered by `src/app/espace/layout.tsx`. |
| `src/components/espace/documents-list.tsx` | `CPT-07` grant rows rendered as `Card` + `Badge` + download `Button`; falls back to the existing `espace.documents.vide` empty state. |

**No dialog / modal component is created.** See "Decisions taken autonomously" D-A4.

---

## Spacing Scale

Inherited from `globals.css` (`--spacing: 0.25rem` → Tailwind's 4px step). Lot 3 declares no
new spacing token.

| Token | Value | Tailwind | Usage in Lot 3 |
|-------|-------|----------|----------------|
| xs | 4px | `gap-1` | icon-to-label inside a button/badge |
| sm | 8px | `gap-2` | checkbox to its label, footer link row |
| md | 16px | `gap-4` / `px-4` | card internal padding, page gutter (`px-4`) |
| lg | 24px | `gap-6` / `py-6` | grid gap of the six `/espace` cards, gap between form groups |
| xl | 32px | `gap-8` | espace nav band to page content |
| 2xl | 48px | `gap-12` | title block to card on every auth surface |
| 3xl | 64px | `py-16` | page top/bottom padding on every Lot 3 surface |

**Exceptions (inherited, not authored by this phase):**
- `gap-5` (20px) — the intra-form field rhythm already used by `inscription/page.tsx:35` and
  `connexion/page.tsx:26`. A multiple of 4. Lot 3 reuses it verbatim so the replaced pages keep
  their approved rhythm.
- `gap-1.5` (6px) — hard-coded inside the read-only `field.tsx:8` and `card.tsx:64`. **Not a
  multiple of 4, and not a Lot 3 debt.** See the merge-back item below.
- **Touch target:** `Button` `size="default"` is `h-8` (32px). Every **primary submit** and every
  **destructive** button in Lot 3 takes `className="h-11"` (44px) at the call site — the same
  override `header.tsx:100` already uses. A call-site class is not a component modification.

### Merge-back item for Lot 2 — `gap-1.5` (do not raise this against Lot 3)

`field.tsx:8` (`flex flex-col gap-1.5`) and `card.tsx:64` (`CardHeader … gap-1.5`) put a 6px
value into a 4px scale. Both files are **write-forbidden for the entire phase (D-04)** — the
founder is editing them in the parallel Lot 2 session — so Lot 3 can neither fix nor override
them without breaking the unidirectional Lot 2 → Lot 3 merge.

**Owner: Lot 2.** **Disposition: carry, do not fix here.** If the deviation is to be resolved,
the change is `gap-1.5` → `gap-2` inside those two read-only files, made on the Lot 2 side.
Recorded here so it is not re-discovered as a Lot 3 finding at recette.

---

## Typography

Inherited scale. Lot 3 authors no new size.

| Role | Size | Weight | Line Height | Where |
|------|------|--------|-------------|-------|
| Body | 16px (`text-base`, Inter) | 400 | 1.5 | page intro, card body, empty-state description |
| Label / meta | 14px (`text-sm`) | 500 (inherited) | 1.43 | `FieldLabel`, `FieldDescription`, `FieldError`, `Message`, footer links |
| Section title | 24px (`--text-section`, Plus Jakarta Sans) | 600 | 1.3 | `CardTitle` — the six `/espace` cards, profile sections |
| Page heading | 30px (`text-3xl`, Plus Jakarta Sans, `.font-heading` `-0.028em`) | 600 | 1.2 | the `h1` of every Lot 3 surface |

**Weights authored by this phase: exactly two — 400 (body) and 600 (headings).**
Weight 500 appears only through the read-only `field.tsx` / `button.tsx` / `badge.tsx`
(`font-medium`); this phase may not change it and must not introduce it by hand.

`--text-display` and `--text-title` are landing-page tokens — **not used on any Lot 3 surface.**

---

## Color

Inherited `:root` palette. Lot 3 declares no new colour.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--paper` `#FCFCFF` (`--background`) | page background under the root atmosphere layer |
| Secondary (30%) | `#FFFFFF` (`--card`) + `--lav2` `#F6F5FF` (`--muted`) | every `Card`, the espace nav band |
| Accent (10%) | `--violet` `#635BFF` (`--primary`, `--ring`) | see reserved list below |
| Destructive | `#EF4444` (`--destructive`) | see reserved list below |
| Success | `--mint` `#1FC79B` (`--success`, `--success-muted` `#E6FAF3`) | see reserved list below |

**Accent `#635BFF` reserved for, and nothing else:**
1. The single primary submit button of each form (S'inscrire, Se connecter, Envoyer le lien,
   Enregistrer mon nouveau mot de passe, Enregistrer mes informations, Télécharger mes données).
2. The focus ring (`--ring`) on every focusable element.
3. Inline text links inside a card footer or under a form — *Déjà inscrit ? Connectez-vous.*,
   *Pas encore de compte ? Inscrivez-vous.*, *Mot de passe oublié ?*, *Mes données*.
4. `Badge variant="default"` on an available support in *Mes documents*.

Never on: card borders, the espace nav band, section headings, empty-state icons, the Google
button (`variant="outline"`), or the *Se déconnecter* control (`variant="ghost"`).

**Destructive `#EF4444` reserved for:** `FieldError` text, the `data-rejected="server"` field
treatment, the form-level `Message variant="error"`, and the account-deletion confirm button
(`Button variant="destructive"` — a tinted `destructive/10` fill, never a solid red block).

**Success `#1FC79B` reserved for:** the "Vos informations ont été enregistrées." and
"Votre mot de passe a été modifié." confirmations (`Message variant="success"`). Never as a
surface fill on an auth form.

---

## Surfaces and their states

Every surface below is a **static page shell (server component) + a client island for the form**,
so `/inscription` and `/connexion` **stay statically rendered** (D-16, hard constraint 5). No
Lot 3 page reads cookies at render on a public route.

| Route | Req | Status | Rendering |
|---|---|---|---|
| `/inscription` | CPT-01 | replaces the 240-line shell | static shell + client form island |
| `/connexion` | CPT-02, CPT-03 | replaces the 144-line shell | static shell + client form island |
| `/mot-de-passe-oublie` | CPT-03 | **new** | static shell + client form island |
| `/nouveau-mot-de-passe` | CPT-03 | **new** | static shell + client form island |
| `/espace` | CPT-06 | replaces the 82-line shell | dynamic (session) |
| `/espace/profil` | CPT-05 | **new** | dynamic (session) |
| `/espace/donnees` | CPT-09 | **new** | dynamic (session) |
| `/espace` documents download | CPT-07 | **new**, no page — an action on the *Mes documents* card | route handler → signed URL redirect |
| déconnexion | CPT-08 | **new**, no page — a `POST` form in the espace nav | route handler → redirect `/connexion` |
| — | **CPT-04** | **no surface — deliberate.** See below. | not applicable |

**No `/admin` route and no administration screen exists in this contract (D-02).**

### `CPT-04` has no Lot 3 UI — and that is deliberate, not an omission

`CPT-04` — *"An account is required for every reservation, including the free discovery call —
one identity, with the learner's whole history attached to their record"* — is a **data-model and
gating invariant, not a screen.** It is satisfied in Lot 3 by the profile table being the single
identity anchor keyed on `auth.uid()`, with RLS proving the isolation (`CPT-08`, D-09, D-18).

**Nothing in Lot 3 may render a reservation entry point.** The flow that would *enforce* the
invariant at the point of booking is Lot 4 (`AGD-02`, `AGD-04`, `AGD-05`), and
`03-CONTEXT.md` § Deferred already sends every reservation surface there — including the
`reservation` table itself (D-03). An executor finding no `CPT-04` surface in the tables above has
found the intended state.

The one visible trace `CPT-04` leaves on a Lot 3 surface already exists and is not re-authored:
the unconditional `EmptyState tone="waiting"` on `/espace` — *« Vous n'avez encore rien
réservé. »* with its *« Voir les créneaux disponibles »* action pointing at `/agenda`. That link
is a Lot 1 route shell and stays one.

### State matrix — every surface renders all five

| State | Rendered as |
|---|---|
| Idle | the form / the card, untouched |
| Pending | `submit-button.tsx` sets `data-loading="true"` on the primary `Button` (opacity 0.7, pointer-events none) and `disabled` on every `FieldControl` |
| Field-level error | `FieldError` under the field + `aria-invalid="true"` on the control |
| Server rejection | `Field data-rejected="server"` (label + border + ring go destructive) **and/or** a form-level `Message variant="error"` above the submit button |
| Success | either a `Message variant="success"` in place (profil, nouveau mot de passe) or the card body is replaced by a `Card` + `CardHeader`/`CardTitle`/`CardDescription` confirmation block — the shape `inscription/page.tsx:231-236` already establishes |

### Focal point — what the eye must land on first

One primary anchor per surface. Everything else is subordinate by size, weight or surface tier.

| Surface | Focal point (primary) | Subordinate |
|---|---|---|
| `/inscription`, `/connexion`, `/mot-de-passe-oublie`, `/nouveau-mot-de-passe` | the form card, and within it the single accent submit button (`h-11`) | the `h1` above it, the footer link below it |
| **`/espace`** | **the `EmptyState tone="waiting"` — *« Vous n'avez encore rien réservé. »*.** It is the page's only actionable element and carries its only accent button, and it sits above the fold, ahead of the grid. It is rendered as a bare `EmptyState`, exactly as `espace/page.tsx:37-47` already does — **it is not wrapped in a `Card`, and no surface tier is changed to promote it.** | the `h1` and intro read as a page header, not a destination; the `h2` greeting is a section label; the **six-card grid is a flat, uniform field** — `variant="default"` (niveau 2), identical treatment on all six, no card promoted, no card tinted, no accent inside any of them. A grid of six equal empty states must never out-shout the one thing the learner can act on. |
| `/espace/profil` | the form card and its single accent submit button | the four `sections.*` headings, which are `CardTitle`-weight labels inside one card — not four competing cards |
| **`/espace/donnees`** | **the export card — *Récupérer mes données* — first in DOM order, `Card variant="default"`, carrying the page's only accent button.** Routine, reversible, safe to click. | **the deletion card is second and deliberately quieter: `Card variant="muted"` (`--muted` fill, `shadow-none`), its trigger a `Button variant="outline"`, not accent and not red.** The revealed confirmation panel nested inside it is `Card variant="default"` — white on `--muted`, so the new step has a visible boundary at the moment it matters most (never `muted` inside `muted`). Destructive colour appears **only after** that panel is revealed, and only on the final confirm button. The irreversible action must never be the visually loudest thing on the page. |

### `/espace` — the six surfaces stay honest (D-03, D-27, hard constraint 3)

The existing composition is preserved exactly: `h1` → intro → the unconditional
`EmptyState tone="waiting"` *« Vous n'avez encore rien réservé. »* → `h2` greeting → the
six-card grid at `data-density="default"`.

**The only change to the overview is the source of the first name:**
`PRENOM_MAQUETTE = "…"` (`espace/page.tsx:16`) is replaced by the session's real value.
**The copy is not touched** (D-28).

| Card | Lot 3 behaviour |
|---|---|
| Mes prochains rendez-vous | always the existing empty state — no `reservation` table exists (D-03) |
| Mes sessions | always the existing empty state |
| Mon historique | always the existing empty state |
| **Mes documents** | the **only** card that can be non-empty: ≥1 grant → a list of rows (title + `Badge` + `Télécharger`); 0 grants → the existing empty state |
| Mes factures | always the existing empty state — Lot 7 |
| Mon avancement | always the existing empty state — Lot 9 |

No placeholder data, no fake row, no skeleton standing in for content that does not exist.

### Auth-failure behaviour (CPT-08 / D-27, hard constraint 4)

A session refresh failure or an unauthenticated hit on `/espace*` **redirects to `/connexion`**
before any markup is produced. A half-authenticated page is never rendered — no greeting with an
empty name, no card grid behind a spinner. The `/connexion` page then shows a form-level
`Message variant="error"` carrying the **existing** `connexion.erreurs.rejetServeur`.
**No new error string is invented for this path.**

---

## Copywriting Contract

**All strings live in `src/locales/fr/*.json`. No literal text in any component** (`CLAUDE.md`).
French only, LTR, *vouvoiement*, full sentences, no technical vocabulary exposed to the learner
(D-11, D-25). Tone reference: `espace.json` and `inscription.json`.

`CPT-04` authors no copy — it has no surface (see above), and the one string that touches it,
`espace.aucuneReservation.*`, already exists and is reused verbatim.

### Reused verbatim — never re-authored

| Key | Used by |
|---|---|
| `inscription.*` (champs, aideParChamp, conditions, erreurs, succes, dejaInscrit) | `/inscription` |
| `inscription.aideParChamp.motDePasse` — *« Huit caractères minimum, avec au moins un chiffre. »* | `/inscription`, `/nouveau-mot-de-passe`. **Signed copy — the configuration moves to match it (D-12), never the reverse.** |
| `connexion.*` incl. `google`, `motDePasseOublie`, `erreurs.identifiantsInvalides`, `erreurs.rejetServeur`, `erreurs.tropDeTentatives`, `pasDeCompte` | `/connexion` |
| `espace.*` — the six titles and their six empty states, plus `aucuneReservation.*` | `/espace` |
| `common.actions.sInscrire` / `seConnecter` / `telecharger` / `annuler` / `reessayer` | every surface |
| `emails.confirmationInscription`, `emails.reinitialisationMotDePasse` | transactional email templates |

`connexion.motDePasseOublie` is today an **inert `<button type="button">`**
(`connexion/page.tsx:53-58`). Lot 3 turns it into a `Link href="/mot-de-passe-oublie"` keeping the
same class string and the same label.

### New copy this phase authors

Three new locale files. Structure mirrors the existing files (`titre` / `intro` / `champs` /
`aideParChamp` / `erreurs` / `succes`).

**`src/locales/fr/mot-de-passe.json`** — `CPT-03`

| Key | Copy |
|---|---|
| `demande.titre` | Mot de passe oublié ? Nous vous envoyons un lien. |
| `demande.intro` | Indiquez l'adresse email de votre compte. Vous recevrez un lien pour choisir un nouveau mot de passe. |
| `demande.action` | Envoyer le lien |
| `demande.succes.titre` | Vérifiez votre boîte mail. Un lien de réinitialisation vous attend. |
| `demande.succes.message` | Si un compte existe pour cette adresse, le lien arrive dans quelques instants. Il expire après un délai de sécurité. |
| `demande.retourConnexion` | Revenir à la connexion. |
| `nouveau.titre` | Choisissez votre nouveau mot de passe. |
| `nouveau.intro` | Saisissez-le deux fois : la seconde saisie évite toute erreur de frappe. |
| `nouveau.champs.motDePasse` | Nouveau mot de passe |
| `nouveau.champs.confirmation` | Confirmez le nouveau mot de passe |
| `nouveau.action` | Enregistrer mon nouveau mot de passe |
| `nouveau.succes.titre` | Votre mot de passe a été modifié. |
| `nouveau.succes.message` | Vous pouvez dès maintenant vous connecter avec votre nouveau mot de passe. |
| `erreurs.lienExpire` | Ce lien n'est plus valide. Demandez-en un nouveau. |
| `erreurs.rejetServeur` | Votre demande n'a pas pu être traitée. Réessayez dans quelques instants. |

> `demande.succes` is shown **whether or not the address exists** — the copy is written so it
> stays true either way and never confirms that an account exists.
> `connexion.erreurs.tropDeTentatives` covers the throttled case (D-08) — no new string.

**`src/locales/fr/profil.json`** — `CPT-05`

| Key | Copy |
|---|---|
| `titre` | Votre profil. Ce que le formateur sait de vous, et rien de plus. |
| `intro` | Ces informations personnalisent vos échanges, vos attestations et vos factures. Vous pouvez les modifier à tout moment. |
| `sections.identite` | Identité |
| `sections.contact` | Coordonnées |
| `sections.professionnel` | Profil professionnel |
| `sections.preferences` | Préférences de communication |
| `champs.telephone` | Téléphone |
| `aideParChamp.telephone` | Utilisé uniquement si le formateur doit vous joindre avant une session. |
| `emailNonModifiable` | Votre adresse email est votre identifiant de connexion. Écrivez au formateur pour en changer. |
| `preferences.rappels.libelle` | Recevoir un rappel avant chaque rendez-vous. |
| `preferences.rappels.aide` | Un seul email, envoyé la veille. |
| `preferences.actualites.libelle` | Recevoir les nouveautés de la formation. |
| `preferences.actualites.aide` | Quelques emails par an, jamais davantage. Vous pouvez vous désinscrire à tout moment. |
| `action` | Enregistrer mes informations |
| `succes` | Vos informations ont été enregistrées. |
| `erreurs.telephoneInvalide` | Ce numéro de téléphone n'est pas valide. |
| `erreurs.rejetServeur` | Vos informations n'ont pas pu être enregistrées. Réessayez dans quelques instants. |

> Prénom, nom, and the *Votre profil* select **reuse `inscription.champs` and
> `inscription.aideParChamp`** — the same four options *acheteur / consultant / étudiant /
> entreprise*, rendered with the same `FieldControl render={<select>}` pattern
> (`inscription/page.tsx:117-129`). `inscription.erreurs.requis` covers the required case.
> **No company field, no seat, no team** — a learner is a natural person (D-24).

**`src/locales/fr/donnees.json`** — `CPT-09`

| Key | Copy |
|---|---|
| `titre` | Vos données personnelles. Vous en gardez le contrôle. |
| `intro` | Le RGPD vous donne deux droits que vous exercez ici : récupérer une copie de ce que nous conservons à votre sujet, et demander la suppression de votre compte. |
| `export.titre` | Récupérer mes données |
| `export.message` | Vous obtenez un fichier contenant votre profil, vos préférences et vos documents. Le téléchargement démarre immédiatement. |
| `export.action` | Télécharger mes données |
| `export.erreur` | Vos données n'ont pas pu être préparées. Réessayez dans quelques instants. |
| `suppression.titre` | Supprimer mon compte |
| `suppression.message` | Votre demande est transmise au formateur. Votre compte et vos données sont supprimés, à l'exception des documents que la loi nous oblige à conserver, comme vos factures. |
| `suppression.action` | Demander la suppression de mon compte |
| `suppression.confirmation.titre` | Cette suppression est définitive. |
| `suppression.confirmation.message` | Vous perdez l'accès à votre espace, à vos documents et à votre historique. Rien ne pourra être restauré. |
| `suppression.confirmation.caseACocher` | J'ai compris que la suppression de mon compte est définitive. |
| `suppression.confirmation.confirmer` | Confirmer la suppression de mon compte |
| `suppression.accuseReception.titre` | Votre demande de suppression a bien été enregistrée. |
| `suppression.accuseReception.message` | Le formateur en a été informé. Vous recevrez un email dès que votre compte aura été supprimé. |
| `suppression.dejaDemandee.titre` | Une demande de suppression est déjà en cours. |
| `suppression.dejaDemandee.message` | Elle a été enregistrée le {date}. Écrivez au formateur si vous souhaitez l'annuler. |
| `suppression.erreur` | Votre demande n'a pas pu être enregistrée. Réessayez dans quelques instants. |

> *« RGPD »* is said; *« export JSON »*, *« bucket »*, *« lien signé »* and *« JWT »* never
> appear (D-11). The word chosen for the export artefact is **« fichier »**.

**Additions to `src/locales/fr/espace.json`** — `CPT-06`, `CPT-07`

| Key | Copy |
|---|---|
| `nav.apercu` | Mon espace |
| `nav.profil` | Mon profil |
| `nav.donnees` | Mes données |
| `deconnexion` | Se déconnecter |
| `documents.expiration` | Vos liens de téléchargement sont personnels et expirent rapidement. Relancez le téléchargement si le vôtre n'est plus valide. |
| `documents.erreur` | Ce document n'a pas pu être ouvert. Réessayez dans quelques instants. |

**The existing `espace.json` keys are not modified.** These are additive keys for surfaces Lot 3
introduces.

### Contract summary table

| Element | Copy |
|---------|------|
| Primary CTA — `/inscription` | S'inscrire (`common.actions.sInscrire`) |
| Primary CTA — `/connexion` | Se connecter (`common.actions.seConnecter`) |
| Primary CTA — `/mot-de-passe-oublie` | Envoyer le lien |
| Primary CTA — `/nouveau-mot-de-passe` | Enregistrer mon nouveau mot de passe |
| Primary CTA — `/espace/profil` | Enregistrer mes informations |
| Primary CTA — `/espace/donnees` | Télécharger mes données |
| Empty state heading | *Aucun document disponible.* (existing, unchanged) |
| Empty state body | *Les supports de formation apparaîtront ici après votre première session.* (existing, unchanged) |
| Error state | *Adresse email ou mot de passe incorrect.* / *Trop de tentatives. Réessayez dans quelques minutes.* / *La connexion n'a pas pu être établie. Réessayez dans quelques instants.* (all existing) |
| Destructive confirmation | **Supprimer mon compte:** *Cette suppression est définitive. Vous perdez l'accès à votre espace, à vos documents et à votre historique. Rien ne pourra être restauré.* — gated by a required checkbox, confirmed by *Confirmer la suppression de mon compte* |

**Destructive actions in this phase: exactly one** — the account-deletion request (`CPT-09`).
Confirmation approach in D-A4 below.

---

## Decisions taken autonomously

This was a headless run (`--auto`); no human was available. Each decision below is recorded for
the founder to review at Lot 3 recette.

| # | Decision | Rationale |
|---|---|---|
| **D-A1** | `/espace` gains a local nav band (`espace-nav.tsx`, rendered by `src/app/espace/layout.tsx`) carrying *Mon espace / Mon profil / Mes données* and a *Se déconnecter* `POST` form. | `src/components/layout/header.tsx` is write-forbidden (D-04) and has no session awareness. Without a local nav, the two new learner surfaces and the sign-out control are unreachable. |
| **D-A2** | Route names: `/mot-de-passe-oublie`, `/nouveau-mot-de-passe`, `/espace/profil`, `/espace/donnees`. | French, readable, consistent with the existing `/a-propos`, `/inscription`, `/connexion` naming (`GOL-01` will want readable URLs). |
| **D-A3** | Form bodies are **client islands**; page shells stay **static server components**. | Hard constraint 5 / D-16 — the 14 public routes must not become dynamic. `useActionState` in an island keeps `/inscription` and `/connexion` prerendered while still rendering server rejections. |
| **D-A4** | **No modal / dialog.** Account deletion is a **two-step in-page confirmation**: the *Demander la suppression* button (inside the `Card variant="muted"` deletion card) reveals a nested **`Card variant="default"`** confirmation panel — white on `--muted`, never `muted` on `muted` — carrying a required checkbox and a `Button variant="destructive"`. | Avoids authoring a ninth UI primitive for a single use, survives without JavaScript, and is more accessible than a hand-rolled modal. A dialog would also be the only focus-trap in the codebase. The tier change between outer card and revealed panel is what makes the new step visible. |
| **D-A5** | Primary submit and destructive buttons take `className="h-11"` at the call site. | `Button size="default"` is 32px — below the 44px touch target. The override idiom already exists at `header.tsx:100`; the component is not modified. |
| **D-A6** | Three new locale files (`mot-de-passe.json`, `profil.json`, `donnees.json`) rather than extending `connexion.json` / `espace.json`. | One file per surface family is the established convention (`agenda.json`, `paiement.json`, `reservation.json`). Only `espace.json` is extended, for keys that belong to `/espace` itself. |
| **D-A7** | `CPT-05` profile fields are: prénom, nom, email (**read-only**), téléphone, profil professionnel, two communication preferences. No company/société/fonction field. | `CPT-05`'s literal wording; D-24 forbids anything resembling a company account. Email is read-only because it is the auth identifier — changing it is an auth flow nobody signed. |
| **D-A8** | The export artefact is named **« fichier »** in the copy; the deletion acknowledgement never promises a delay. | D-11 forbids technical vocabulary; D-10 executes deletion in SQL until Lot 10, so a stated SLA would be a promise the product cannot keep. |
| **D-A9** | *Mes documents* is the only one of the six `/espace` cards that can render content in Lot 3. | D-05 — grants are seeded by SQL; the other five have no source of truth and stay honest empty states (D-03, hard constraint 3). |
| **D-A10** | The password-reset success message is shown identically whether or not the address exists. | Account enumeration is a real leak on a public form; the copy is phrased to stay true in both cases. |
| **D-A11** | **`CPT-04` gets no Lot 3 surface** — it is satisfied as a data-model invariant (the profile table as sole identity anchor, RLS-isolated), and enforced at the point of booking by Lot 4. | `03-CONTEXT.md` § Deferred sends every reservation surface to Lot 4, and D-03 forbids even a shell `reservation` table. Inventing a reservation entry point to "cover" `CPT-04` would be the scope breach, not the coverage. Recorded explicitly so the absence reads as deliberate. |
| **D-A12** | Focal-point hierarchy declared for `/espace` (the *aucune réservation* empty state, by position and by being the only actionable element — **not** by promoting its surface tier) and for `/espace/donnees` (export primary; deletion `muted` until revealed). | Two surfaces had multiple equal-weight anchors. On `/espace/donnees` in particular, giving the irreversible action the same prominence as the routine one is how accidental deletions happen. On `/espace`, the hierarchy is achieved without touching the existing composition (D-28). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | **none** — no `npx shadcn add` is run in this phase | not required |
| third-party | **none declared** | not applicable |

`components.json` and the `shadcn` devDependency are inherited from Lot 0/1 scaffolding. The eight
existing components are hand-authored on `@base-ui/react` and are **write-forbidden** (D-04). The
one new primitive (`checkbox.tsx`) is authored in-repo against the already-installed
`@base-ui/react`, not fetched from any registry. **No third-party code enters the tree in this
phase** (D-19: zero new dependencies).

---

## Acceptance criteria — applied outcomes

1. `git diff` for the whole phase touches **zero lines** in `src/app/globals.css`,
   `src/components/ui/{accordion,badge,button,card,empty-state,field,input,message}.tsx`, and
   `src/components/layout/*`.
2. `next build` output shows the **14 pre-existing routes still static**; the two new
   password-reset routes are static too; only `/espace*` is dynamic.
3. A codebase search for a literal French string inside `src/app/**` or `src/components/**`
   returns nothing new — every visible label resolves through `src/locales/fr/*.json`.
4. `/espace` renders the learner's **real** first name; `PRENOM_MAQUETTE` no longer exists.
   The six empty-state strings are byte-identical to today's `espace.json`.
5. No route matching `/admin` exists anywhere in `src/app/`, and **no reservation entry point is
   created** — `CPT-04` leaves no surface behind (D-A11).
6. Hitting `/espace`, `/espace/profil` or `/espace/donnees` without a session produces a
   redirect to `/connexion` — never a partially rendered page. Verifiable in the response
   status, not by eye.
7. On `/espace`, the *aucune réservation* `EmptyState` still renders **outside any `Card`**,
   ahead of the grid, carrying the page's only accent button; the six grid cards all resolve to
   `variant="default"` with no accent inside them. On `/espace/donnees`, the deletion card is
   `variant="muted"`, its revealed confirmation panel is `variant="default"`, and no
   `destructive` class is rendered until that panel is revealed.
8. The account-deletion confirm button is unreachable until the acknowledgement checkbox is
   checked.
9. Exactly one `cubic-bezier` value remains in the codebase (`--ease-brand`) — Lot 3 adds no
   second curve and no bare `ease`/`linear` transition.
10. Every form renders all five states (idle, pending, field error, server rejection, success);
    the server-rejection state uses `Field data-rejected="server"`, not a generic red border.
11. `npm run lint`, `npm run typecheck` and `next build` exit 0; `npm run content:check` still
    exits 1 on the single unresolved key (unchanged behaviour, D-20).

---

## Checker Sign-Off

*Verdicts are written by gsd-ui-checker, not by the researcher. They stay unchecked and
unlabelled until the checker rules.*

- [ ] Dimension 1 Copywriting: —
- [ ] Dimension 2 Visuals: —
- [ ] Dimension 3 Color: —
- [ ] Dimension 4 Typography: —
- [ ] Dimension 5 Spacing: —
- [ ] Dimension 6 Registry Safety: —

**Approval:** pending — and the twelve decisions in *Decisions taken autonomously* additionally
require founder review at Lot 3 recette, together with the new French copy (D-11).

---

*Phase: 03-comptes-connexion-et-espace-apprenant*
*Generated 2026-08-31 in autonomous mode; revised 2026-08-31 after checker review. The design
system in `src/` is the authority; where this file disagrees with it, this file is wrong.*
