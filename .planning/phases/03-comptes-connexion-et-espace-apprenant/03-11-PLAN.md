---
phase: 03-comptes-connexion-et-espace-apprenant
plan: 11
type: execute
wave: 5
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06, 03-07, 03-08, 03-09, 03-10]
files_modified:
  - .planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md
autonomous: false
requirements: [CPT-01, CPT-02, CPT-03, CPT-04, CPT-05, CPT-06, CPT-07, CPT-08, CPT-09]

must_haves:
  truths:
    - "The whole phase is re-verified from a clean database in one pass, not plan by plan"
    - "Every requirement carries an honest verdict: proven locally, proven on hosted, or blocked and why"
    - "The founder has seen and ruled on the new French copy and the autonomous UI decisions"
    - "The CIO has one written list of everything owed, with no hidden assumption"
  artifacts:
    - path: ".planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md"
      provides: "The phase evidence record, the hosted-dependency handoff and the honest limits"
      contains: "CPT-01"
  key_links:
    - from: "03-RECETTE.md"
      to: "the running local stack"
      via: "evidence captured in a single clean-state pass, not copied from plan summaries"
      pattern: "supabase db reset"
---

<objective>
Close the phase honestly. Re-run the whole of Lot 3 from a clean database in one
pass, record a per-requirement verdict that distinguishes *proven locally* from
*proven on hosted* from *blocked and owed by the CIO*, and put the two grouped
human gates in front of the founder — the visual and functional review, and the
validation of the new French copy and the autonomous UI decisions.

Purpose: `human_verify_mode` is `end-of-phase` (D-22), so this is the only plan
in the phase carrying human checkpoints. It also exists because this project has
already paid three findings for a "false green": a per-plan pass is not a phase
pass, and a green that only holds locally must say so.

Output: `03-RECETTE.md` and two founder rulings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-CONTEXT.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-UI-SPEC.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-01-SUMMARY.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-02-SUMMARY.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-03-SUMMARY.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-04-SUMMARY.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-05-SUMMARY.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-06-SUMMARY.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-07-SUMMARY.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-08-SUMMARY.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-09-SUMMARY.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-10-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the phase from a clean state and write the recette record</name>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 55-63 — the nine CPT requirements verbatim, the verdict table's left column)
    - .planning/phases/03-comptes-connexion-et-espace-apprenant/03-CONTEXT.md (the hosted_dependencies table — four CIO items and two carried debts)
    - .planning/phases/03-comptes-connexion-et-espace-apprenant/03-UI-SPEC.md (the eleven acceptance criteria and the twelve D-A decisions)
    - every 03-NN-SUMMARY.md in this directory (the recorded evidence and each plan's recorded local-only limit)
    - supabase/tests/lot3_rls_isolation.sql (the negative proof to re-run)
    - supabase/seed/lot3_acces_support_local.sql (the local-only seed to re-run)
  </read_first>
  <files>.planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md</files>
  <action>
Reset the local stack with `npx supabase db reset --local`, rebuild with
`npm run build`, and re-run the phase's evidence **in one pass from clean** —
not by copying each plan's recorded result. A plan that passed against a
database left dirty by the previous plan has proven nothing about a fresh
deployment.

Re-run in this order and capture the actual output of each:
1. `psql -v ON_ERROR_STOP=1 -f supabase/tests/lot3_rls_isolation.sql` — the four
   `RLS-ISOLATION: … DENIED` notices and exit code 0.
2. Sign up a learner through `/api/auth/inscription`; confirm sign-in is refused
   before confirmation; follow the confirmation link from the local mail
   collector; reach `/espace` showing the real first name.
3. Sign in, sign out, and hit `/espace` unauthenticated; observe the redirect and
   an empty body.
4. Request a password reset for a registered and for an unregistered address;
   `diff` the two responses; complete the reset; confirm the **old** password now
   fails.
5. Run the support seed for one learner; download as that learner; request the
   same document id as the other learner and observe the `404`; wait past the TTL
   and observe the expired link fail.
6. Update the profile; post a crafted `role` and a crafted `email`; read the
   stored values back with `psql`.
7. Download the export as each learner and `diff` the personal fields; record a
   deletion request with `RESEND_API_KEY` unset and confirm the row survives;
   repeat the request and observe the `409`.
8. `npm run typecheck`, `npm run lint`, `npm run build`, `npm run content:check`.

Then write `03-RECETTE.md` with four sections.

**Section 1 — Per-requirement verdict.** One row for each of `CPT-01` to
`CPT-09`, with four columns: the requirement text, the verdict, the evidence
(the exact command or observation from the run above, never a plan reference),
and the limit. The verdict vocabulary is exactly three values and nothing else:
`proven locally`, `proven on hosted`, `blocked`. A `blocked` verdict names the
owed item and its owner.

`CPT-01` and `CPT-02` cannot be `proven on hosted` from this seat (D-06, D-07).
`CPT-04` is a data-model invariant with no surface (D-A11): its evidence is the
`app.profil` primary key being `auth.users(id)`, the sign-up trigger firing on
both the email and the Google path, and the isolation proof — plus the
prohibition holding, evidenced by `find src/app -type d -name 'admin*'` being
empty and no reservation entry point existing anywhere in `src/`.

**Section 2 — What is not proven, in plain language.** No table. Sentences a
non-engineer can act on. At minimum: the `CPT-01` and `CPT-03` emails have only
ever been seen in a local collector, because custom SMTP and an authenticated
sending domain do not exist yet; the `CPT-02` Google flow has never run against
real credentials; the `CPT-07` bucket exists only locally; and no Lot 3
migration has been applied to either hosted project, so nothing behind `CPT-05`,
`CPT-07`, `CPT-08` or `CPT-09` exists on hosted at all. Then the sentence that
matters most: **`tsc` and `next build` pass with or without any migration
applied**, because the types come from `src/types/database.types.ts` and not from
a live database — so a green build is not evidence that the database is right.

**Section 3 — CIO handoff.** One checklist, one line per item, each naming what
it unblocks: the two Lot 3 migrations applied to both hosted projects
(`CPT-05`, `CPT-07`, `CPT-08`, `CPT-09`); the Google OAuth client and provider
configuration on both (`CPT-02`); custom SMTP with an authenticated sending
domain (`CPT-01`, `CPT-03`); the private `supports` bucket on both (`CPT-07`).
Then the two carried debts that touch Lot 3 without being part of it: the
`master` → `main` rename (disarmed CI gate, frozen Vercel production) and the
pausing of the Free-plan Supabase projects. State explicitly that **nothing was
pushed** — no `git push`, no `supabase db push`, no deployment — and that
`supabase/seed/lot3_acces_support_local.sql` and
`supabase/tests/lot3_rls_isolation.sql` are local-only and must never be run
against a hosted project.

**Section 4 — Open items for the founder.** The eleven UI-SPEC acceptance
criteria, each with a pass/fail mark and the command or observation behind it;
the twelve `D-A` decisions taken autonomously; the `SubmitButton` deviation
recorded in plan 03-03 (an explicit `pending` prop instead of `useFormStatus`,
because this repo has no server actions); the D-14 outcome recorded in plan
03-06; and the new French copy awaiting validation (D-11).

Do not mark anything green that was not observed in this run. An unobserved item
is `blocked` or `not run` — never `assumed`.
  </action>
  <verify>
    <automated>npx supabase db reset --local &amp;&amp; psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -f supabase/tests/lot3_rls_isolation.sql &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits `0` and the isolation proof prints its four `RLS-ISOLATION: … DENIED` notices from a freshly reset database.
    - `03-RECETTE.md` contains exactly nine requirement rows, one per `CPT-0N`, and `grep -cE '\| (proven locally|proven on hosted|blocked) \|' 03-RECETTE.md` returns `9` — no other verdict word appears.
    - `grep -ciE '\b(assumed|probably|should work|devrait|à priori)\b' 03-RECETTE.md` returns `0`.
    - `grep -c 'proven on hosted' 03-RECETTE.md` returns `0` for the `CPT-01` and `CPT-02` rows specifically — confirm by reading those two rows.
    - Section 3 lists all four `<hosted_dependencies>` items and both carried debts: `grep -ciE '(migration|google|smtp|bucket)' 03-RECETTE.md` in Section 3 finds all four, and `grep -ciE '(master|rename|free-plan|pause)' 03-RECETTE.md` finds both debts.
    - `grep -c 'nothing was pushed\|Rien n' 03-RECETTE.md` is `≥ 1`, and independently: `git log origin/HEAD..HEAD --oneline | wc -l` is greater than zero (commits exist locally and were not pushed), and `git reflog --date=iso | grep -ci 'push'` returns `0`.
    - Section 4 lists all eleven UI-SPEC acceptance criteria, each with a verdict and a command or observation.
    - `find src/app -type d -name 'admin*'` returns nothing; `grep -rn 'PRENOM_MAQUETTE' src/` returns nothing; `git diff --stat main -- src/app/globals.css src/components/ui/accordion.tsx src/components/ui/badge.tsx src/components/ui/button.tsx src/components/ui/card.tsx src/components/ui/empty-state.tsx src/components/ui/field.tsx src/components/ui/input.tsx src/components/ui/message.tsx src/components/layout/` shows **zero** changed lines across the whole phase (UI-SPEC acceptance criterion 1). Record the command output verbatim.
    - `npm run build` route table shows exactly fourteen `○` routes and `ƒ` on `/espace`, `/espace/profil`, `/espace/donnees` and the api routes. Paste it into the record.
    - `npm run content:check` exits `1` with totals `CADR-03: 72` and `CADR-01: 10`.
    - `git diff package.json package-lock.json` is empty across the whole phase (D-19, zero new dependencies).
  </acceptance_criteria>
  <done>One clean-state pass has re-established every claim the phase makes, and each of the nine requirements carries a verdict, its evidence and its limit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Founder review — the six Lot 3 surfaces, seen and used</name>
  <read_first>
    - .planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md (Task 1 — the evidence and the eleven UI-SPEC criteria)
    - .planning/phases/03-comptes-connexion-et-espace-apprenant/03-UI-SPEC.md (§ Focal point, § State matrix, acceptance criteria 7 and 8)
  </read_first>
  <what-built>
    Six surfaces, all previously either shells or non-existent: `/inscription`
    and `/connexion` rewritten as real forms; `/mot-de-passe-oublie` and
    `/nouveau-mot-de-passe` created; `/espace` now behind a session gate and
    greeting the learner by their real name; `/espace/profil` and
    `/espace/donnees` created, reachable from a new local nav band. All nine
    `CPT` requirements are implemented; the automated evidence is in
    `03-RECETTE.md`.
  </what-built>
  <how-to-verify>
    Start the app (`npm run build && npm start`) with the local Supabase stack
    running. Then, in a browser:

    1. `/inscription` — create an account. Check the page reads as one clear
       form and that the old block of five fake field states below the form is
       gone. Submit an empty form, then a seven-character password, then two
       different passwords: each should give a French sentence under the right
       field.
    2. Open the local mail collector (URL from `npx supabase status`). The
       confirmation email should be **in French**, in the voice of the other
       emails. Follow its link: you should land in your space, greeted by your
       real first name.
    3. `/espace` — confirm the thing your eye lands on first is *« Vous n'avez
       encore rien réservé. »* with its button, sitting above the six cards, and
       that the six cards look identical to each other with nothing highlighted.
       Five of them should still say they are empty; that is correct, not a bug.
    4. Sign out from the band at the top. Then try to open `/espace` directly:
       you should land on `/connexion`, never on a half-loaded page.
    5. `/connexion` — sign in with a wrong password, then with an address that
       does not exist. Both should say the same thing. Try eight or ten wrong
       attempts quickly: you should be told there have been too many.
    6. *Mot de passe oublié ?* — it should now be a link. Ask for a reset, read
       the French email, set a new password, sign in with it.
    7. `/espace/profil` — change your surname, your phone number and one of the
       two preference boxes. Save, reload, confirm the changes stayed.
    8. `/espace/donnees` — confirm the *Récupérer mes données* card is first and
       is the only thing on the page in the accent colour, and that the
       deletion card below is visibly quieter. Download the file. Then click
       *Demander la suppression* and confirm the button that actually deletes is
       **greyed out until you tick the box**.
    9. *Mes documents* — if the demo supports were seeded for your account, the
       card lists them; the download opens the PDF; a link copied and reopened a
       minute later no longer works.
  </how-to-verify>
  <acceptance_criteria>
    - Every one of the nine steps above is walked, and each is marked approved or rejected with a note. A step not walked is recorded as `not reviewed` — never inferred from the automated evidence.
    - Steps 3, 8 and 9 are the ones that cannot be proven by command and are the reason this gate exists; each must carry an explicit visual verdict.
    - Any rejection is recorded with the surface, the observed behaviour and the expected behaviour, and becomes a gap for `/gsd:plan-phase --gaps`.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the visual and functional review, or list the surfaces and behaviours to fix.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Founder ruling — the new French copy and the autonomous decisions</name>
  <read_first>
    - src/locales/fr/mot-de-passe.json, src/locales/fr/profil.json, src/locales/fr/donnees.json (the new copy, written by the lane under D-11)
    - src/locales/fr/espace.json (the six additive keys)
    - src/locales/fr/emails.json (the additive suppressionCompteNotification entry)
    - supabase/templates/confirmation.html, supabase/templates/recovery.html (the two francised Auth emails)
    - .planning/phases/03-comptes-connexion-et-espace-apprenant/03-UI-SPEC.md (§ Decisions taken autonomously — D-A1 to D-A12)
    - .planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md (Section 4)
  </read_first>
  <what-built>
    Three new copy files, six additive `espace.json` keys, one additive
    `emails.json` entry and two francised Supabase Auth email templates —
    written by the lane in the voice Lot 1 established, because these surfaces
    did not exist before Lot 3 and no signed copy covered them (D-11). Plus
    fourteen decisions that were taken without a human in the room: the twelve
    `D-A` decisions in the UI-SPEC, the `SubmitButton` deviation from plan
    03-03, and the D-14 `additional_redirect_urls` outcome from plan 03-06.
  </what-built>
  <how-to-verify>
    1. Read the three new files and the two email templates. They are the words
       a paying learner will read. Change anything that does not sound like you.
       Two things to check specifically: the word for the downloaded file is
       *« fichier »*, never a technical term; and *« RGPD »* is said once, on
       `/espace/donnees`.
    2. Confirm the two contradictions were resolved the right way round: the
       signed sentence *« Huit caractères minimum, avec au moins un chiffre. »*
       is unchanged, and the server configuration moved to match it — not the
       reverse.
    3. Read Section 4 of `03-RECETTE.md` and rule on each of the fourteen
       decisions. The three worth your attention: **D-A4**, account deletion
       confirmed in the page rather than in a pop-up; **D-A7**, the profile has
       no company or job-title field because a learner is a natural person; and
       the `SubmitButton` change, which delivers the same visible behaviour by a
       different mechanism because this codebase has no server actions.
    4. Read Sections 2 and 3. Section 2 is the honest list of what is **not**
       proven; Section 3 is what the CIO owes. Confirm both match your
       understanding before Lot 3 is called *recettable*.
  </how-to-verify>
  <acceptance_criteria>
    - Each of the three new copy files and both email templates is marked approved or returned with the exact rewording required.
    - Each of the fourteen decisions carries an explicit accepted / changed verdict; a decision left unruled is recorded as `not ruled`, never as accepted by default.
    - Section 2's list of unproven items is confirmed or corrected by the founder, in writing.
    - Any copy change is applied to `src/locales/fr/*.json` only — never hardcoded into a component (CLAUDE.md) — and `npm run content:check` still exits `1` with totals `CADR-03: 72` and `CADR-01: 10` afterwards.
  </acceptance_criteria>
  <resume-signal>Type "approved" to accept the copy and the fourteen decisions, or list the rewordings and the decisions to change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan summaries → phase verdict | a per-plan pass carried forward without re-running is an unverified claim |
| local evidence → hosted claim | a green that only holds locally, reported without its limit, is the phase's main failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-47 | Repudiation | false green at phase close | mitigate | Every claim is re-run from `supabase db reset` in one pass; the verdict vocabulary is limited to three values; `assumed`/`probably` are asserted absent from the record. This is the control for the failure mode that has already cost this project three findings (D-18) |
| T-03-48 | Repudiation | unreviewed human gate auto-advancing | mitigate | Both checkpoints are `gate="blocking"` and `autonomous: false`. Phase 02-04's unreviewed gate is the precedent this avoids: a step not walked is recorded `not reviewed`, never inferred from the automated evidence |
| T-03-49 | Information disclosure | local-only artefacts reaching hosted | mitigate | The record states that `supabase/seed/lot3_acces_support_local.sql` and `supabase/tests/lot3_rls_isolation.sql` live outside `supabase/migrations/` and must never run against a hosted project |
| T-03-50 | Tampering | unintended deployment | mitigate | Nothing is pushed in any plan of this phase; asserted here by `git reflog` carrying no `push` and by unpushed commits existing on the phase branch |
| T-03-51 | Tampering | design-system drift into Lot 2's parallel session | mitigate | A whole-phase `git diff --stat` against `main` over `globals.css`, the eight `ui/` components and `layout/` must show zero changed lines (D-04, UI-SPEC acceptance criterion 1) |
| T-03-SC | Tampering | package-manager installs | n/a | `git diff package.json package-lock.json` asserted empty across the whole phase — zero new dependencies (D-19), so the package legitimacy gate is not triggered anywhere in Lot 3 |
</threat_model>

<verification>
- The isolation proof passes from a freshly reset database.
- Nine requirement rows carry one of exactly three verdicts, with evidence from this run.
- The whole-phase diff over `globals.css`, the eight `ui/` components and `layout/` is zero lines.
- Fourteen static routes; `typecheck`, `lint`, `build` exit 0; `content:check` totals unchanged; `package.json` unchanged.
- Both human gates are ruled on explicitly, not auto-advanced.
</verification>

<hosted_limits>
This plan cannot make anything green on hosted. Its purpose is the opposite: to
write down precisely what is not, and hand the CIO one list. `CPT-01` and
`CPT-02` close the phase `blocked` on hosted by design (D-06, D-07), and the
record must say so in the founder's language, not in a footnote.
</hosted_limits>

<success_criteria>
- One clean-state pass re-establishes every claim the phase makes.
- Each of the nine requirements has a verdict, its evidence and its limit, with no assumption anywhere.
- The founder has ruled on the new copy and on all fourteen autonomous decisions.
- The CIO has one written handoff, and nothing was pushed.
</success_criteria>

<output>
Create `.planning/phases/03-comptes-connexion-et-espace-apprenant/03-11-SUMMARY.md` when done.
</output>
