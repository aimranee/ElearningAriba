---
phase: 03-comptes-connexion-et-espace-apprenant
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales/fr/mot-de-passe.json
  - src/locales/fr/profil.json
  - src/locales/fr/donnees.json
  - src/locales/fr/espace.json
  - src/locales/fr/emails.json
  - src/components/ui/checkbox.tsx
  - src/components/compte/auth-shell.tsx
  - src/components/compte/submit-button.tsx
  - src/lib/validation/auth.ts
  - src/lib/validation/profil.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every French string the five new Lot 3 surfaces need exists in a locale bundle, never in a component"
    - "A checkbox primitive exists that matches the eight existing components' idiom without modifying any of them"
    - "Form validation rejects invalid input and returns a locale key, never an English Zod message"
    - "The password schema enforces the same rule as the signed copy and as supabase/config.toml"
  artifacts:
    - path: "src/locales/fr/mot-de-passe.json"
      provides: "CPT-03 copy for the two reset surfaces"
      contains: "demande"
    - path: "src/locales/fr/profil.json"
      provides: "CPT-05 profile labels and preferences"
      contains: "emailNonModifiable"
    - path: "src/locales/fr/donnees.json"
      provides: "CPT-09 export and deletion copy"
      contains: "suppression"
    - path: "src/components/ui/checkbox.tsx"
      provides: "The ninth primitive, created alongside the eight (D-04)"
      contains: "checkboxVariants"
    - path: "src/components/compte/auth-shell.tsx"
      provides: "The centred auth card shell repeated on five surfaces"
    - path: "src/components/compte/submit-button.tsx"
      provides: "Primary submit with the pending state and the 44px touch target"
    - path: "src/lib/validation/auth.ts"
      provides: "Zod schemas and locale-key mappers for sign-up, sign-in and password reset"
      exports: ["inscriptionSchema", "connexionSchema", "demandeResetSchema", "nouveauMotDePasseSchema"]
    - path: "src/lib/validation/profil.ts"
      provides: "Zod schema and locale-key mapper for the profile form"
      exports: ["profilSchema"]
  key_links:
    - from: "src/lib/validation/auth.ts"
      to: "src/locales/fr/inscription.json erreurs"
      via: "locale-key union derived from the bundle's leaf keys"
      pattern: "InscriptionErreurKey"
    - from: "src/components/ui/checkbox.tsx"
      to: "@base-ui/react/checkbox"
      via: "already-installed primitive, zero new dependency"
      pattern: "@base-ui/react/checkbox"
---

<objective>
Write every French string the phase introduces, in the voice Lot 1 established,
and build the three shared pieces the later slices consume: the ninth UI
primitive, the auth shell, and the submit button. Add the two validation
modules so no later plan has to invent a boundary contract.

Purpose: front-loading the copy is what makes D-11 checkable — one plan, one
voice, one founder review at recette, and no later slice tempted to hardcode a
label. Front-loading the shared components is what keeps every later slice a
thin vertical slice rather than a component-authoring exercise.

Output: three new locale files, two additive locale edits, three components,
two validation modules. No route, no data access, no session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-CONTEXT.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-UI-SPEC.md
@.planning/phases/03-comptes-connexion-et-espace-apprenant/03-PATTERNS.md
@CLAUDE.md

<interfaces>
`03-UI-SPEC.md` § *New copy this phase authors* already contains the **exact**
French wording for every key in `mot-de-passe.json`, `profil.json`,
`donnees.json` and the `espace.json` additions. Transcribe it; do not rewrite
it.

`src/components/ui/input.tsx` (42 lines) is the whole-file skeleton for the new
primitive: `cva(...)` base string + `variants` + `defaultVariants`, exported
alongside the component (`export { Checkbox, checkboxVariants }`),
`data-slot="…"` on the root, `in-data-[density=compact]:` on every sizing class,
the shared state suffixes (`focus-visible:ring-3 focus-visible:ring-ring/50`,
`disabled:pointer-events-none disabled:opacity-50`,
`aria-invalid:border-destructive aria-invalid:ring-3
aria-invalid:ring-destructive/20`, `data-[loading=true]:pointer-events-none
data-[loading=true]:opacity-70`), class merging through `cn(...)`, and prop type
`<Primitive>.Props & VariantProps<typeof checkboxVariants>`. Formatting in
`src/components/ui/` differs from `src/app/`: **no semicolons, double quotes,
2-space indent** — match the folder.

`src/components/ui/accordion.tsx:1-3` is the `"use client"` + one-line `why:`
comment idiom for a stateful primitive.

`src/app/connexion/page.tsx:16-22` is the auth-shell chrome to extract:
`mx-auto flex max-w-md flex-col gap-12 px-4 py-16`, then a
`flex flex-col gap-2 text-center` title block with
`font-heading text-3xl font-semibold text-foreground` on the `h1` and
`text-muted-foreground` on the intro. `/inscription` uses `max-w-2xl`
(`inscription/page.tsx:25`).

`src/lib/validation/contact.ts` (73 lines) is the boundary-validation idiom, in
three parts: a Zod schema carrying **no** French text; a locale-key union whose
members are the leaf keys of the bundle's `erreurs`; and a reducer
`…IssuesToFieldErrors(issues: readonly { path: PropertyKey[]; code: string }[])`
returning a field-keyed map. Note it uses `PropertyKey` and a `readonly`
structural parameter rather than `ZodIssue[]` — no `any` anywhere.

The four professional-profile values are already an enum in
`contact.ts:16`: `acheteur`, `consultant`, `etudiant`, `entreprise`. Reuse the
same four verbatim.

`src/components/forms/contact-form.tsx:193-200` is the submit `Button` today —
`type="submit"`, `data-loading={isSubmitting ? "true" : undefined}`,
`disabled={isSubmitting}`. `src/components/layout/header.tsx:100` is the
`className="h-11"` call-site override idiom (D-A5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the new French copy</name>
  <read_first>
    - src/locales/fr/espace.json (tone reference, and the file being extended — D-11)
    - src/locales/fr/inscription.json (tone reference; the fields, errors and success block the profile page reuses — D-11)
    - src/locales/fr/connexion.json (the shape of a surface bundle: flat `titre`/`intro`, nested `champs` and `erreurs`)
    - src/locales/fr/emails.json (the seven-field email entry shape: objet, preheader, salutation, corps[], action{libelle,contexte}, signature, pied)
    - src/locales/fr/_mocks.emails.json (the registry that must NOT gain an entry — D-20)
    - .planning/phases/03-comptes-connexion-et-espace-apprenant/03-UI-SPEC.md (§ New copy this phase authors — the exact strings)
  </read_first>
  <files>src/locales/fr/mot-de-passe.json, src/locales/fr/profil.json, src/locales/fr/donnees.json, src/locales/fr/espace.json, src/locales/fr/emails.json</files>
  <action>
Create `src/locales/fr/mot-de-passe.json` with the fifteen keys listed in
`03-UI-SPEC.md` under that filename: `demande.titre`, `demande.intro`,
`demande.action`, `demande.succes.titre`, `demande.succes.message`,
`demande.retourConnexion`, `nouveau.titre`, `nouveau.intro`,
`nouveau.champs.motDePasse`, `nouveau.champs.confirmation`, `nouveau.action`,
`nouveau.succes.titre`, `nouveau.succes.message`, `erreurs.lienExpire`,
`erreurs.rejetServeur`. Transcribe the wording exactly as the spec gives it.
Do **not** add a "too many attempts" string — `connexion.erreurs.tropDeTentatives`
already exists and covers it (D-08, D-27).

Create `src/locales/fr/profil.json` with the eighteen keys listed in the spec:
`titre`, `intro`, `sections.{identite,contact,professionnel,preferences}`,
`champs.telephone`, `aideParChamp.telephone`, `emailNonModifiable`,
`preferences.rappels.{libelle,aide}`, `preferences.actualites.{libelle,aide}`,
`action`, `succes`, `erreurs.telephoneInvalide`, `erreurs.rejetServeur`.
Do **not** add prénom, nom or the professional-profile options — those reuse
`inscription.champs` and `inscription.aideParChamp` (D-A7). Do **not** add any
company, société, fonction, siège or team key (D-24).

Create `src/locales/fr/donnees.json` with the eighteen keys listed in the spec,
under `titre`, `intro`, `export.*` and `suppression.*` including
`suppression.confirmation.*`, `suppression.accuseReception.*` and
`suppression.dejaDemandee.*`. `suppression.dejaDemandee.message` carries a
`{date}` placeholder. The word for the export artefact is **« fichier »**;
« export JSON », « bucket », « lien signé » and « JWT » must appear nowhere
(D-11, D-A8). « RGPD » is said, once, in `intro`.

Extend `src/locales/fr/espace.json` additively with six keys only:
`nav.apercu`, `nav.profil`, `nav.donnees`, `deconnexion`,
`documents.expiration`, `documents.erreur`. Every existing key — the six surface
titles, their six empty states, `bienvenue`, `aucuneReservation.*` — must remain
byte-identical (D-28, UI-SPEC acceptance criterion 4).

Extend `src/locales/fr/emails.json` additively with one entry,
`suppressionCompteNotification`, in the same seven-field shape as
`contactNotification`: the trainer notification sent when a learner records a
deletion request (D-10, `CPT-09`). It must not name the learner's password, ID
or any technical artefact; `corps` carries the learner's name and email and the
date of the request, using single-brace `{prenom}`, `{nom}`, `{email}` and
`{date}` placeholders — the substitution idiom of `src/lib/email/render.ts:14-19`.
Do **not** register any of its keys in `src/locales/fr/_mocks.emails.json`
(D-20 — the mock totals must not move).

Every string: full sentence, *vouvoiement*, terminal punctuation, no technical
vocabulary exposed to the learner.
  </action>
  <verify>
    <automated>node -e "for (const f of ['mot-de-passe','profil','donnees','espace','emails']) JSON.parse(require('fs').readFileSync('src/locales/fr/'+f+'.json','utf8')); console.log('json ok')" &amp;&amp; npm run content:check; echo "content:check exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - All five files parse as JSON (the verify command prints `json ok`).
    - `node -e "const j=require('./src/locales/fr/mot-de-passe.json'); console.log([j.demande.titre,j.nouveau.action,j.erreurs.lienExpire].join('|'))"` prints the three spec strings verbatim.
    - `node -e "const j=require('./src/locales/fr/profil.json'); console.log(Object.keys(j).sort().join(','))"` prints `action,aideParChamp,champs,emailNonModifiable,erreurs,intro,preferences,sections,succes,titre` — no extra top-level key.
    - `grep -ciE '(societe|société|entreprise_nom|fonction|equipe|siege|seat)' src/locales/fr/profil.json` returns `0` (D-24).
    - `grep -ciE '(json|bucket|jwt|url signée|lien signé|token)' src/locales/fr/donnees.json` returns `0` (D-11).
    - `grep -c 'RGPD' src/locales/fr/donnees.json` returns `1`.
    - `git diff src/locales/fr/espace.json` shows **only added lines** — zero removed or modified lines. Confirm with `git diff --numstat src/locales/fr/espace.json`: the deletions column is `0`.
    - `git diff --numstat src/locales/fr/emails.json` deletions column is `0`; `git diff --numstat src/locales/fr/_mocks.emails.json` shows the file unchanged.
    - `npm run content:check` exits `1` and its final two lines still read `CADR-03: 72` and `CADR-01: 10` — the totals recorded before this plan. A different total means an entry was added or a mocked key was resolved by accident.
    - No string in any of the five files is in English: `grep -ciE '\b(the|your|please|click|password is)\b' src/locales/fr/mot-de-passe.json src/locales/fr/profil.json src/locales/fr/donnees.json` returns `0`.
  </acceptance_criteria>
  <done>Every label the phase introduces exists in a locale bundle in the Lot 1 voice, the existing `espace.json` keys are untouched, and the mock registry totals are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Build the checkbox primitive, the auth shell and the submit button</name>
  <read_first>
    - src/components/ui/input.tsx (the whole-file skeleton, the cva idiom, the `Omit` explanation, the export pair)
    - src/components/ui/accordion.tsx (the `"use client"` + Base UI import idiom for a stateful primitive)
    - src/components/ui/badge.tsx (the analog for a small, non-full-width control — `h-8 w-full` is wrong for a checkbox)
    - src/components/ui/button.tsx (the `data-loading` API and the variants the shell and submit button consume)
    - src/components/ui/field.tsx (the `data-slot=field-control` selector that makes slots load-bearing, and the `rejected="server"` prop contract)
    - src/app/connexion/page.tsx (lines 16-22 — the exact shell chrome to extract)
    - src/app/inscription/page.tsx (line 25 — the `max-w-2xl` variant of the same shell)
    - src/components/forms/contact-form.tsx (lines 193-200 — the submit button as it exists today)
    - src/lib/utils.ts (`cn` and `FOCUS_RING`)
  </read_first>
  <files>src/components/ui/checkbox.tsx, src/components/compte/auth-shell.tsx, src/components/compte/submit-button.tsx</files>
  <action>
**`src/components/ui/checkbox.tsx`** — a **new** file created alongside the
eight existing components. Do not open, edit or import-and-wrap any of the eight
in a way that requires changing them (D-04). Build on
`@base-ui/react/checkbox` — already installed, zero new dependency (D-19).
`Checkbox.Root` is stateful, so the file opens with `"use client"` and a
one-line `why:` comment, as `accordion.tsx` does. Author a `checkboxVariants`
`cva` whose base is a `size-4` square (not `h-8 w-full`), with rounded corners,
`border-input`, `bg-background`, the shared focus/disabled/invalid/loading
suffixes listed in the interfaces block, and an
`in-data-[density=compact]:size-3.5` sizing variant. Root gets
`data-slot="checkbox"`; the indicator gets `data-slot="checkbox-indicator"` and
renders the `Check` icon from `lucide-react` at `size-3`. Prop type is
`Checkbox.Root.Props & VariantProps<typeof checkboxVariants>`; `Omit` any native
attribute that collides with a variant name and explain why in a comment, as
`input.tsx:29-32` does for `size`. Export `{ Checkbox, checkboxVariants }`.
Formatting: no semicolons, double quotes, 2-space indent — match the `ui/`
folder, not `src/app`.

**`src/components/compte/auth-shell.tsx`** — a server component (no
`"use client"`), composition only, no new token and no new variant. Props:
`titre: string`, `intro: string`, `largeur?: "md" | "2xl"` defaulting to `"md"`,
and `children`. Renders the outer `div` with
`mx-auto flex flex-col gap-12 px-4 py-16` plus `max-w-md` or `max-w-2xl` chosen
by `largeur`, then the `flex flex-col gap-2 text-center` title block with the
`h1` at `font-heading text-3xl font-semibold text-foreground` and the intro at
`text-muted-foreground`, then `children`. It must not render a `Card` — the five
consuming surfaces compose their own card bodies. Add a `why:` comment naming
the five surfaces it serves and D-04 (extracted alongside, the read-only
`layout/` chrome is untouched).

**`src/components/compte/submit-button.tsx`** — a client island wrapping
`Button`. Props: `pending: boolean`, `children`, and an optional `className`.
Renders `Button` with `type="submit"`, `data-loading={pending ? "true" :
undefined}`, `disabled={pending}`, and `className` merged through `cn` with
`"h-11"` — the 44px touch target the call-site override idiom already
establishes at `header.tsx:100` (D-A5). Export `{ SubmitButton }`.

**Deliberate deviation from `03-UI-SPEC.md`, record it in the summary and flag
it for founder review at recette:** the spec describes this component as
wrapping `Button` with `useFormStatus`. `useFormStatus` only reports pending
state for a form driven by a server action, and this repo has **zero** server
actions — every mutation is a `fetch` to a route handler (`contact-form.tsx` →
`api/contact/route.ts`), which `03-PATTERNS.md` recommends staying on. A
`useFormStatus` wrapper would report `pending: false` forever against a
`fetch`-driven form, which is a silent broken state, not a working one. The
explicit `pending` prop delivers the same rendered contract — `data-loading` and
`disabled` on the primary button — through the mechanism this codebase actually
uses. Nothing about the visual contract changes.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only src/components/ui/` lists **only** `src/components/ui/checkbox.tsx`. `accordion.tsx`, `badge.tsx`, `button.tsx`, `card.tsx`, `empty-state.tsx`, `field.tsx`, `input.tsx` and `message.tsx` are unmodified.
    - `git diff --name-only src/components/layout/ src/app/globals.css` is empty.
    - `grep -c '@base-ui/react/checkbox' src/components/ui/checkbox.tsx` returns `1`; `git diff package.json` is empty (zero new dependencies — D-19).
    - `grep -c 'data-slot="checkbox"' src/components/ui/checkbox.tsx` returns `1` and `grep -c 'data-slot="checkbox-indicator"' src/components/ui/checkbox.tsx` returns `1`.
    - `grep -c 'in-data-\[density=compact\]' src/components/ui/checkbox.tsx` is `≥ 1`.
    - `grep -c 'export { Checkbox, checkboxVariants }' src/components/ui/checkbox.tsx` returns `1`.
    - `grep -c ';$' src/components/ui/checkbox.tsx` returns `0` (folder formatting: no semicolons).
    - `grep -c 'h-11' src/components/compte/submit-button.tsx` returns `1`.
    - `grep -c '"use client"' src/components/compte/auth-shell.tsx` returns `0` (server component — the page shells must stay static).
    - `grep -cE '>[A-ZÀ-Ý][a-zà-ÿ]+ [a-zà-ÿ]' src/components/compte/auth-shell.tsx src/components/compte/submit-button.tsx src/components/ui/checkbox.tsx` returns `0` — no literal French sentence in a component (CLAUDE.md).
    - `npm run typecheck` and `npm run lint` exit `0`.
    - `grep -rn ': any' src/components/ui/checkbox.tsx src/components/compte/` returns nothing.
  </acceptance_criteria>
  <done>A ninth primitive and two shared compte components exist, built alongside the read-only design system, with zero new dependencies and no literal string.</done>
</task>

<task type="auto">
  <name>Task 3: Write the two validation modules and close the green gates</name>
  <read_first>
    - src/lib/validation/contact.ts (the whole 73-line boundary-validation idiom: schema without messages, locale-key union, issue mapper, field-error reducer, `PropertyKey` typing)
    - src/locales/fr/inscription.json (`erreurs` leaf keys: requis, emailInvalide, motDePasseFaible, confirmationDifferente, rejetServeur)
    - src/locales/fr/connexion.json (`erreurs` leaf keys: identifiantsInvalides, rejetServeur, tropDeTentatives)
    - src/locales/fr/mot-de-passe.json (written in Task 1 — `erreurs` leaf keys)
    - src/locales/fr/profil.json (written in Task 1 — `erreurs` leaf keys)
    - supabase/config.toml (the `minimum_password_length` and `password_requirements` values plan 03-02 sets — the client-side schema must not be looser or stricter than the server)
  </read_first>
  <files>src/lib/validation/auth.ts, src/lib/validation/profil.ts</files>
  <action>
**`src/lib/validation/auth.ts`** — four Zod schemas, no French text anywhere in
the file (Zod's defaults are English; `CLAUDE.md` forbids hardcoded strings, so
messages are mapped to locale keys afterwards).

Author a shared `motDePasseSchema`: `z.string().min(8).regex(/\d/)`. Add a
`why:` comment stating it mirrors `supabase/config.toml`
`minimum_password_length = 8` and `password_requirements = "letters_digits"`,
which in turn were moved to match the signed copy
`inscription.aideParChamp.motDePasse` (D-12) — three places, one rule, and if
one moves all three must move.

`inscriptionSchema`: `prenom` and `nom` as trimmed non-empty strings, `email`
via `z.email()`, `motDePasse` via the shared schema, `confirmation` as a string,
`profil` as the four-value enum reused verbatim from `contact.ts:16`, and a
`.refine` asserting `motDePasse === confirmation` reported on the `confirmation`
path.

`connexionSchema`: `email` via `z.email()`, `motDePasse` as a non-empty string —
**not** the strength schema. A sign-in form must never tell an attacker that the
stored password is weak or strong; length validation on sign-in is an oracle.
Add that as the `why:` comment.

`demandeResetSchema`: `email` via `z.email()` only.

`nouveauMotDePasseSchema`: `motDePasse` via the shared schema, `confirmation`,
and the same equality `.refine`.

For each schema, export a locale-key union whose members are the leaf keys of
the matching bundle's `erreurs` — `InscriptionErreurKey` from
`inscription.json`, `ConnexionErreurKey` from `connexion.json`,
`MotDePasseErreurKey` from `mot-de-passe.json` — and a
`…IssuesToFieldErrors(issues: readonly { path: PropertyKey[]; code: string }[])`
reducer returning `Partial<Record<keyof …Input, …ErreurKey>>`, exactly as
`contactIssuesToFieldErrors` does. Map `too_small` and `invalid_format` on
`motDePasse` to `motDePasseFaible`, the `.refine` code on `confirmation` to
`confirmationDifferente`, `invalid_format` on `email` to `emailInvalide`, and
everything else to `requis`.

**`src/lib/validation/profil.ts`** — `profilSchema`: `prenom` and `nom` as
trimmed non-empty strings, `telephone` as an optional trimmed string validated
against a French-tolerant pattern (`+`, digits, spaces, dots and hyphens, 10 to
20 characters after normalisation) — never a strict E.164 that would reject a
valid `01 23 45 67 89`, `profil_professionnel` as the same four-value enum, and
`preference_rappels` / `preference_actualites` as booleans with
`z.coerce.boolean()` at the form boundary. **No `email` field** — it is the auth
identifier and read-only (D-A7), and no company/team field (D-24). Export
`ProfilErreurKey` from `profil.json` plus `inscription.erreurs.requis`, and the
matching reducer. Map a telephone pattern failure to `telephoneInvalide`.

Neither file imports `server-only` — both run on the client island and on the
route handler.

Then run the green gates.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node --experimental-strip-types -e "import {inscriptionSchema} from './src/lib/validation/auth.ts'; const r=inscriptionSchema.safeParse({prenom:'A',nom:'B',email:'a@b.fr',motDePasse:'abcdefg',confirmation:'abcdefg',profil:'acheteur'}); if(r.success) throw new Error('7-char password accepted'); console.log('short password REJECTED')"` prints `short password REJECTED`.
    - The same call with `motDePasse` and `confirmation` set to `abcdefgh` (eight letters, no digit) is also rejected — the `letters_digits` half is enforced.
    - The same call with `abcdefg1` succeeds.
    - Mismatched `motDePasse`/`confirmation` produces an issue whose `path[0]` is `confirmation`, and the reducer maps it to `confirmationDifferente`.
    - `connexionSchema.safeParse({email:'a@b.fr',motDePasse:'x'})` **succeeds** — sign-in does not validate password strength (no oracle).
    - `profilSchema.safeParse({prenom:'A',nom:'B',telephone:'01 23 45 67 89',profil_professionnel:'consultant',preference_rappels:true,preference_actualites:false})` succeeds; `telephone:'abc'` is rejected and maps to `telephoneInvalide`.
    - `grep -rn ': any\|as any' src/lib/validation/auth.ts src/lib/validation/profil.ts` returns nothing.
    - `grep -ciE '[éèàùç]|"[A-Z][a-z]+ [a-z]+ [a-z]+' src/lib/validation/auth.ts src/lib/validation/profil.ts` returns `0` — no French message literal in a validation module.
    - `grep -c 'email' src/lib/validation/profil.ts` returns `0` (D-A7 — email is not an editable profile field).
    - `npm run typecheck`, `npm run lint`, `npm run build` each exit `0`.
    - `npm run content:check` exits `1` with totals still `CADR-03: 72` and `CADR-01: 10`.
    - `npm run build` output shows the same thirteen `○` routes and the single `ƒ /api/contact` as before this plan.
  </acceptance_criteria>
  <done>Both boundaries validate in the shape the repo already uses, the password rule is identical in the schema, the configuration and the signed copy, and no English message or `any` reaches the codebase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → route handler | untrusted `FormData` crosses into the validation modules |
| locale bundle → rendered markup | copy is data, not code; no interpolation of user input into a locale string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Tampering | `src/lib/validation/auth.ts`, `profil.ts` | mitigate | Zod at the boundary per `CLAUDE.md`; `unknown` and `PropertyKey` typing, never `any`; the enum for `profil_professionnel` is a closed four-value set so an arbitrary string cannot reach the check constraint |
| T-03-21 | Information disclosure | `connexionSchema` | mitigate | Sign-in validates only that a password is present, never its strength — a strength failure returned on sign-in is an oracle telling an attacker the stored value is short |
| T-03-22 | Tampering | password rule drift | mitigate | The rule lives in three places (signed copy, `supabase/config.toml`, `motDePasseSchema`); the acceptance criteria assert the schema rejects seven characters and rejects eight letters without a digit, so a drift in either direction fails the plan |
| T-03-23 | Elevation of privilege | `profilSchema` | mitigate | The schema has no `role` and no `email` field, so a crafted form body cannot reach either column even before the column-scoped grant from plan 03-01 refuses it |
| T-03-24 | Information disclosure | new locale copy | mitigate | `donnees.json` acceptance criteria assert the absence of `json`, `bucket`, `jwt`, `token` and `lien signé` — technical vocabulary in learner-facing copy is an information leak as well as a D-11 violation |
| T-03-SC | Tampering | package-manager installs | n/a | No `npm install`; `git diff package.json` is asserted empty (D-19) |
</threat_model>

<verification>
- All five locale files parse; `espace.json` and `emails.json` diffs are additions only; `_mocks.emails.json` is untouched and `content:check` still reports 72/10 and exits 1.
- Only `checkbox.tsx` is added under `src/components/ui/`; the eight existing components, `src/components/layout/` and `globals.css` are byte-identical.
- The password schema rejects seven characters and rejects eight letters with no digit; sign-in accepts a one-character password.
- `npm run typecheck`, `npm run lint`, `npm run build` exit 0; the route rendering column is unchanged.
</verification>

<hosted_limits>
Nothing in this plan depends on a hosted resource. The new French copy is
subject to founder validation at Lot 3 recette (D-11) — it is written, not
approved, and the summary must say so rather than record it as signed.
</hosted_limits>

<success_criteria>
- Every string the phase introduces exists in `src/locales/fr/*.json`, in the Lot 1 voice, with the existing keys untouched.
- A ninth UI primitive exists without any of the eight being modified, and with no new dependency.
- Both validation boundaries return locale keys, never English, and never widen to `any`.
- All green gates hold; `content:check` totals unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-comptes-connexion-et-espace-apprenant/03-03-SUMMARY.md` when done.
</output>
