---
quick_id: 260907-k8j
mode: quick-full
status: complete
---

# Quick Task 260907-k8j: Replace the triplicated hydration gate with a shared hook

Fully specified by the requester (three atomic commits, exact order, hard rules on
staging/branching/pushing). No planning ambiguity — executed directly.

## Task 1 — Shared hydration hook

- Create `src/lib/hooks/use-hydrated.ts` (useSyncExternalStore-based `useHydrated`).
- Replace the `useState`/`useEffect` hydration gate at:
  - `src/components/compte/submit-button.tsx`
  - `src/components/compte/connexion-form.tsx`
  - `src/components/forms/contact-form.tsx`
- Preserve FUITE-02 / UI-SPEC-deviation comments; reword only the "effect below" clause.
- Rendered contract (disabled state, preparationFormulaire paragraph) unchanged.
- files: src/lib/hooks/use-hydrated.ts, src/components/compte/submit-button.tsx, src/components/compte/connexion-form.tsx, src/components/forms/contact-form.tsx
- verify: `npx eslint src` 0 problems, `npx tsc --noEmit` clean
- done: hook created, three sites migrated, imports pruned only where genuinely unused

## Task 2 — Ignore orphaned agent worktrees

- Add `.claude/worktrees/` to `.gitignore` with a why-comment. Target the subdirectory only.
- files: .gitignore
- verify: `git status --porcelain` no longer lists `.claude/`
- done: line added, `.claude/` untracked directory no longer shown

## Task 3 — Track five untracked quick plans

- Stage and commit the five listed PLAN.md paths unchanged.
- files: five PLAN.md/dir paths under .planning/quick/
- verify: `git status --porcelain` clean after commit
- done: all five committed unmodified

## must_haves

- truths:
  - Server HTML still renders the submit control disabled with the preparationFormulaire paragraph; post-hydration it is governed by `pending`/`isSubmitting` alone.
  - `.claude/worktrees/` ignored, not `.claude/` wholesale.
  - Exactly three commits, no push, no branch change, no `git add -A`.
- artifacts:
  - src/lib/hooks/use-hydrated.ts
- key_links:
  - src/components/compte/submit-button.tsx
  - src/components/compte/connexion-form.tsx
  - src/components/forms/contact-form.tsx
  - .gitignore
