---
phase: quick
plan: 260904-h5r
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/motion/typewriter.tsx
  - src/components/sections/hero.tsx
  - src/locales/fr/landing.json
  - src/locales/fr/_mocks.public.json
  - src/app/globals.css
  - .planning/phases/02-site-public/02-CONTEXT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Typewriter cycles whole words via a crossfade of two stacked spans, never a fragment or a blank word"
    - "Width lock (D-09) and font/resize relock, gradient, ref-only mutation, reduced-motion static word[0], and visibilitychange whole-word resync are preserved"
    - "accrocheLead carries no flat --violet color — the cycled word's gradient is the H1's sole accent"
    - "A three-entry proof line renders under the CTA row: module count + total hours derived from getModules()/formatHours, plus two registered placeholders"
    - "hero.preuve.formateur and hero.preuve.groupe are registered in _mocks.public.json so content:check names them"
    - "Hero section padding is py-[clamp(5rem,9vw,7rem)]"
---

<objective>
Run 5 — le héros. Source: `ariba-cto/notes/2026-09-04-brief-run-5-le-heros.md` (founder brief,
2026-09-04). Four measured defects fixed: (A) typewriter rewritten as a whole-word crossfade
(180ms overlap, 2200ms hold, ease-brand) replacing per-character typing that left a fragment or
blank word 41.8% of the time; blink keyframe removed as orphaned; (B) accrocheLead's flat violet
removed so the cycled word's gradient is the H1's only accent; (C) a three-entry proof line added
below the CTA row — modules/hours derived from the database, formateur/groupe registered as
CADR-03 mock placeholders; (D) hero vertical padding tightened.
</objective>

<tasks>
1. Rewrite `typewriter.tsx`: two stacked grid-area:1/1 spans, ref-mutated opacity/translateY
   crossfade, width lock (D-09/D-104) preserved, fonts.ready + debounced resize relock preserved,
   reduced-motion static words[0] preserved, visibilitychange whole-word resync (D-10) preserved.
   Remove the blink caret span; remove `@keyframes blink` from `globals.css` (orphaned — sole
   consumer was this file).
2. `hero.tsx`: drop `text-[var(--violet)]` from accrocheLead (→ `text-[var(--ink)]`); compute
   module count + total hours from `getModules()`/`formatHours`/`formatNumber`; add
   `hero.preuve.modules` template + `hero.preuve.formateur`/`groupe` to `landing.json`; render the
   three-entry proof line (Reveal dataD=4, chips bumped to dataD=5); tighten padding to
   `py-[clamp(5rem,9vw,7rem)]`.
3. Register `hero.preuve.formateur` and `hero.preuve.groupe` in `_mocks.public.json` (CADR-03,
   awaiting `Cadrage_Formation_SAP_Ariba_Questions_Client.docx`).
4. Record D-101 to D-104 in `02-CONTEXT.md` after D-100.
</tasks>
