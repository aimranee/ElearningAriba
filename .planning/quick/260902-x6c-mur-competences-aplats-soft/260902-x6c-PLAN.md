---
quick_id: 260902-x6c
status: complete
---

# Quick Task 260902-x6c: Mur competences — aplats soft (run 2/5)

Source d'autorite : `ariba-cto/notes/2026-09-02-brief-run-2-le-mur-en-aplats-soft.md`.
Fichier unique : `src/components/sections/competences.tsx`.

## Task 1 — Reecrire competences.tsx (teintes + tuile + typo)

**Files:** `src/components/sections/competences.tsx`

**Action:**
1. Renommer `CARD_ACCENTS` en `CARD_TEINTES`, type `Record<string, { soft: string; ink: string }>`, valeurs exactes du brief section A (azur pour procure-to-pay, magenta pour gestion-catalogues, coral pour certification, violet/mint/amber inchanges).
2. Repli sur `{ soft: "var(--violet-soft)", ink: "var(--violet-ink)" }` si cle absente.
3. Remplacer le rendu de la tuile par la forme cible litterale du brief section B : suppression bordure color-mix, calque degrade + group-hover, filigrane 150px, regle 3px, bg-white, classe group, wrapper relative z-[1]; conservation rounded-[20px] overflow-hidden p-[1.6rem] min-h-[13.5rem] h-full + hover translate D-17; nouveau fond `bg-[var(--tuile-soft)]` via style vars `--tuile-soft`/`--tuile-ink`; conteneur `flex flex-col gap-[0.9rem]`; pictogramme en flux normal `size-7 shrink-0 text-[var(--tuile-ink)]`.
4. Typo : titre `text-[length:var(--text-card)] leading-[var(--text-card--line-height)]` sur `text-[var(--ink)]`; description `text-[length:var(--text-small)] leading-[var(--text-small--line-height)]` sur `text-[var(--ink-soft)]`.
5. Ne rien changer d'autre : Section tone="default", resolvePictogram/Award, Reveal+dataD, grille et gouttieres, contenu.

**Verify:** `npm run lint`, `npm run typecheck`, `npm run content:check` — reporter les sorties reelles (content:check attendu en exit 1, faq.items CADR-03).

**Done:** Fichier compile, aucune reference residuelle a CARD_ACCENTS/border color-mix/filigrane/regle 3px/bg-white/group/relative z-[1] dans ce fichier.

## Task 2 — Consigner D-80 a D-84

**Files:** `.planning/phases/02-site-public/02-CONTEXT.md`

**Action:** Ajouter les puces D-80 a D-84 dans la forme exacte des puces existantes (texte impose par le brief/l'argument utilisateur), a la suite de D-79.

**Verify:** Lecture du fichier — 5 puces presentes, texte conforme.

**Done:** D-80..D-84 presentes.
