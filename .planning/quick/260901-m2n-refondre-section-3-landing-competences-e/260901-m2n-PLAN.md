---
phase: quick
plan: 260901-m2n
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales/fr/landing.json
  - scripts/seed-content.mjs
  - src/components/sections/hero.tsx
  - src/components/sections/competences.tsx
  - src/app/globals.css
  - .planning/phases/02-site-public/02-CONTEXT.md
  - .planning/phases/02-site-public/02-UI-SPEC.md
autonomous: true
requirements: [PUB-03]
must_haves:
  truths:
    - "Section 3 shows six verb-first result headlines, each with a permanently visible sentence"
    - "\"Préparer la certification SAP Ariba\" is present as the sixth item (signed Lot 2 §3 item restored)"
    - "\"catalogues\" and \"contrats et workflows\" are merged into one competency, not two"
    - "Each tile is a full-color chromatic wall tile — colored wash, giant clipped watermark pictogram, full-saturation accent rule — no numbered pastille, no shadow"
    - "The accent rule draws left-to-right on scroll entry, staggered per tile, and is static under prefers-reduced-motion"
    - "The hero H1 still splits correctly on \"SAP Ariba\" (first competence item's cle is unchanged)"
    - "Local Supabase content_item table holds exactly six competences rows after seed, contrats-workflows is gone"
  artifacts:
    - path: "src/locales/fr/landing.json"
      provides: "competences.items as 6 {titre, description, picto} objects, exact brief copy"
    - path: "scripts/seed-content.mjs"
      provides: "competenceItems reshaped on the profilItems pattern + RETIRED_ITEMS explicit retirement"
    - path: "src/components/sections/hero.tsx"
      provides: "TYPEWRITER_WORDS with certification swapped in for contrats-workflows"
    - path: "src/components/sections/competences.tsx"
      provides: "chromatic-wall tile rewrite, server component, no use client"
    - path: "src/app/globals.css"
      provides: "[data-slot=\"competence-rule\"] reveal + reduced-motion rules"
  key_links:
    - from: "src/components/sections/competences.tsx"
      to: "src/app/globals.css"
      via: "data-slot=\"competence-rule\" attribute selector"
      pattern: "data-slot=\"competence-rule\""
    - from: "scripts/seed-content.mjs"
      to: "src/locales/fr/landing.json"
      via: "landing.competences.items.map(...) with competence.picto as cle"
      pattern: "competence\\.picto"
---

<objective>
Rebuild landing section 3 ("Ce que vous allez apprendre") from six subject
labels into six verb-first result headlines on a "chromatic wall" — six
full-color tiles, one hue per competency — and restore the signed
"Préparer la certification SAP Ariba" item (Lot 2 §3) by merging
"catalogues" and "contrats et workflows" into one competency.

Purpose: PUB-03 currently redisplays the Programme accordion's module names
one-for-one and omits a signed deliverable. This run executes the founder's
2026-09-01 brief word-for-word — content, seed retirement, hidden hero
coupling, component rewrite, CSS reveal, and phase contract — no
reinterpretation.

Output: `landing.json` competences content as objects, `seed-content.mjs`
reshaped with explicit retirement of the dropped row, `hero.tsx` typewriter
table updated, `competences.tsx` rewritten as the chromatic wall, reveal CSS
added, D-51/D-52/D-53 recorded in `02-CONTEXT.md` (and `02-UI-SPEC.md`),
local content re-seeded, full verification against the rendered HTML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-09-01-brief-section-03-competences-resultats.md
@src/components/sections/pour-qui.tsx
</context>

<interfaces>
Current `src/components/sections/competences.tsx` (to be replaced in Task 2)
fetches via `getSection("competences")` / `getSectionItems("competences")`,
renders `<Section tone="default">` in both the error branch and the main
branch, uses `SectionHeader` with `eyebrow`/`title`/`titleAccent` from the
section row, and currently maps items into a numbered-pastille row inside
`<ul className="mt-10 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">`.
Preserve the data-fetching and error-branch shape; only the item-rendering
markup and grid classes change.

From `src/lib/content/queries.ts` (read-only, unchanged) — `getSectionItems`
already selects `*`, so each row exposes `id`, `cle`, `titre`, `description`,
`picto`, `position`; no query change needed.

From `src/components/icons/pictograms.tsx` (read-only, do not edit):
```ts
export const pictograms: Record<string, ComponentType<ComponentProps<"svg">>>;
export type PictogramName = keyof typeof pictograms;
// keys present: ecosysteme-ariba, procure-to-pay, source-to-pay, rfq-rfp,
// gestion-catalogues, contrats-workflows (kept, unused by this section now)
```
"certification" is not in this registry — resolve it locally with `Award`
from `lucide-react`, per brief §4. Do not touch this file.

From `pour-qui.tsx` — the established pattern to mirror: a module-scope
`const ACCENTS: Record<string, string>` (or similar) mapped by `cle`, a
fallback for unknown `cle`, and inline
`style={{ "--card-accent": accent } as React.CSSProperties}` on the element
consuming it via Tailwind arbitrary-value classes — never a raw hex in JSX.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Content, seed, and the hidden hero coupling</name>
  <files>src/locales/fr/landing.json, scripts/seed-content.mjs, src/components/sections/hero.tsx</files>
  <action>
  In `src/locales/fr/landing.json`, replace `competences.items` (currently a
  string array) with the exact array of 6 objects from brief §1 table
  ("1. Le contenu : six résultats"), each `{ "titre": "…", "description": "…",
  "picto": "…" }` verbatim, in this order: ecosysteme-ariba, procure-to-pay,
  source-to-pay, rfq-rfp, gestion-catalogues, certification. Leave
  `competences.eyebrow` and `competences.titre` untouched.

  In `scripts/seed-content.mjs`: delete the `COMPETENCE_PICTOS` constant and
  its preceding `why` comment (lines ~63-74) entirely. Replace the
  `competenceItems` mapping (currently keyed off `COMPETENCE_PICTOS[index]`)
  with the profilItems-shaped version from brief §2: map
  `landing.competences.items` to `{ section_cle: "competences", cle:
  competence.picto, titre: competence.titre, description:
  competence.description, picto: competence.picto, position: index + 1 }`.
  After the existing `await upsertItems([...])` call in `main()`, add a
  narrow, explicit retirement step: a `RETIRED_ITEMS` constant
  `[{ section_cle: "competences", cle: "contrats-workflows" }]` with the
  `why` comment from brief §2 verbatim (dated 2026-09-01), then a loop over
  `RETIRED_ITEMS` that deletes each row by matching BOTH `section_cle` and
  `cle` columns (`.eq("section_cle", entry.section_cle).eq("cle", entry.cle)`)
  — never a bare `.eq("section_cle", …)` delete. On error, `console.error` in
  the same style as the existing `upsertItems`/`upsertSections` error
  handlers and `process.exit(1)`.

  In `src/components/sections/hero.tsx`, in `TYPEWRITER_WORDS`
  (lines ~32-39): remove the `"contrats-workflows": "les workflows"` entry,
  add `"certification": "la certification"`. The other five entries and their
  order are unchanged. Rewrite the `why` comment above the table (lines
  ~25-31) to describe the post-run state per brief §3: section 3 now carries
  verb-first result headlines plus a permanently-visible sentence
  (`content_item.description`), the typewriter cycles the short form of the
  same term, and the first item's `cle` is load-bearing for the H1 split.
  Touch nothing else in `hero.tsx`.
  </action>
  <verify>
    <automated>node -e "const j=require('./src/locales/fr/landing.json'); const items=j.competences.items; if(items.length!==6) process.exit(1); if(!items.every(i=>i.titre&&i.description&&i.picto)) process.exit(1); if(items[0].picto!=='ecosysteme-ariba') process.exit(1); console.log('landing.json ok')"</automated>
  </verify>
  <done>landing.json competences.items is 6 objects in the specified order; seed-content.mjs has no COMPETENCE_PICTOS, competenceItems matches the profilItems shape, RETIRED_ITEMS retires contrats-workflows on both columns after upsertItems, errors exit 1; hero.tsx TYPEWRITER_WORDS has exactly the 6 expected keys with certification replacing contrats-workflows, why-comment rewritten, nothing else in hero.tsx changed</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite competences.tsx as the chromatic wall + reveal CSS</name>
  <files>src/components/sections/competences.tsx, src/app/globals.css</files>
  <action>
  Rewrite `src/components/sections/competences.tsx` in full as a server
  component (no `"use client"`) implementing the anatomy in brief §6-8
  exactly:

  - Keep both `<Section tone="default">` occurrences (error state and main
    render) — do not change the tone prop.
  - Keep `getSection`/`getSectionItems` data fetching, `SectionHeader` with
    eyebrow/title/titleAccent from the section row, unchanged.
  - Add a module-scope `const CARD_ACCENTS: Record<string, string>` mapping
    `cle` to a CSS var string, in this exact order (brief §5, non-adjacent
    hue order — do not reorder): ecosysteme-ariba → `var(--violet)`,
    procure-to-pay → `var(--blue)`, source-to-pay → `var(--mint)`, rfq-rfp →
    `var(--amber)`, gestion-catalogues → `var(--indigo)`, certification →
    `var(--sky)`. Unknown key falls back to `var(--violet)`.
  - Add a local icon resolver for the filigrane pictogram: import `Award`
    from `lucide-react` plus the existing `pictograms`/`PictogramName` from
    the registry; for `cle === "certification"` use `Award`, for the five
    known domain `cle`s look up `pictograms[cle]`, for any other key resolve
    to `null`. Include the `why (CADR-04)` comment from brief §4 verbatim
    directly above this resolver. Unknown `cle` renders with no watermark —
    silent degrade, never an exception.
  - Grid: `<ul className="mt-10 grid grid-cols-1 gap-[1.25rem] sm:grid-cols-2 lg:grid-cols-3 grid-auto-rows-[1fr]">`.
    Each item wrapped in
    `<Reveal as="li" dataD={((index % 5) + 1) as 1|2|3|4|5} className="h-full">`
    (Reveal/data-d cascade unchanged, 6 tiles over a 5-cycle).
  - Inside each `Reveal`, the tile element:
    `rounded-[20px] overflow-hidden p-[1.6rem] min-h-[13.5rem] h-full
    relative group bg-white
    border border-[color-mix(in_srgb,var(--card-accent)_22%,transparent)]
    transition-transform duration-[var(--duration-base)] ease-[var(--ease-brand)]
    hover:-translate-y-[2px]`, with inline
    `style={{ "--card-accent": accent } as React.CSSProperties}`. No shadow
    class anywhere, no `cursor-pointer`, no link/anchor, no arrow icon.
  - Layer 1 (wash): `absolute inset-0` `aria-hidden="true"`
    `pointer-events-none`, inline `style` with
    `background: "linear-gradient(150deg, color-mix(in srgb, var(--card-accent) 30%, white) 0%, white 72%)"`,
    class `opacity-[0.73] transition-opacity duration-[var(--duration-base)]
    ease-[var(--ease-brand)] group-hover:opacity-100`.
  - Layer 2 (filigrane), only rendered when an icon resolved: `absolute
    -bottom-7 -right-7 size-[150px] pointer-events-none` `aria-hidden="true"`,
    class `text-[var(--card-accent)] opacity-[0.18]
    transition-[opacity,transform] duration-[var(--duration-base)]
    ease-[var(--ease-brand)] group-hover:opacity-[0.26]
    group-hover:translate-x-[6px] group-hover:translate-y-[6px]`.
  - Layer 3 (rule): `data-slot="competence-rule"` `aria-hidden="true"`,
    class `absolute inset-x-0 top-0 h-[3px] bg-[var(--card-accent)]`.
  - Layer 4 (text), `relative z-[1]`: title
    `font-heading text-[1.08rem] leading-[1.3] font-bold tracking-[-0.02em]
    text-[var(--ink)]` rendering `item.titre`; sentence
    `text-[0.92rem] leading-[1.55] text-[var(--ink-soft)] mt-[0.5rem]`
    rendering `item.description`. No `--muted-ink` anywhere in the file.

  In `src/app/globals.css`, inside the same `@layer` block as `.reveal`
  (near lines 285-316, immediately after the existing `.reveal[data-d="5"]`
  rule), add the `why (D-53, 2026-09-01)` comment from brief §7 verbatim,
  then:
  `.reveal [data-slot="competence-rule"] { transform: scaleX(0);
  transform-origin: left center; transition: transform
  var(--duration-reveal) var(--ease-brand); }`,
  `.reveal.in [data-slot="competence-rule"] { transform: scaleX(1); }`, and
  five `.reveal[data-d="N"] [data-slot="competence-rule"] { transition-delay:
  Xs; }` rules for `data-d` 1-5 at delays 0.34s / 0.43s / 0.52s / 0.61s /
  0.70s. Inside the existing `@media (prefers-reduced-motion: reduce)` block
  (~339-352), add exactly one rule:
  `.reveal [data-slot="competence-rule"] { transform: none; }` — no other new
  rule in that block.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/sections/competences.tsx','utf8'); const g=fs.readFileSync('src/app/globals.css','utf8'); const checks=[!s.includes('use client'), !s.includes('--muted-ink'), !/shadow-/.test(s), s.includes('data-slot=\"competence-rule\"'), s.includes('CARD_ACCENTS'), s.includes('Award'), s.includes('grid-auto-rows-[1fr]'), g.includes('data-slot=\"competence-rule\"')]; if(checks.every(Boolean)) console.log('ok'); else { console.error(checks); process.exit(1); }"</automated>
  </verify>
  <done>competences.tsx is a server component (no "use client") rendering six chromatic tiles per brief §6, CARD_ACCENTS maps all 6 cle in the specified non-adjacent order with a violet fallback, certification resolves via Award and unknown keys render no watermark, no --muted-ink/shadow-/cursor-pointer/link anywhere, grid classes match brief §8 exactly; globals.css has the competence-rule scaleX reveal rules with the five staggered delays plus exactly one added line in the reduced-motion block</done>
</task>

<task type="auto">
  <name>Task 3: Phase contract, re-seed, and final verification</name>
  <files>.planning/phases/02-site-public/02-CONTEXT.md, .planning/phases/02-site-public/02-UI-SPEC.md</files>
  <action>
  In `.planning/phases/02-site-public/02-CONTEXT.md`, following the exact
  form of the existing `D-` entries (the last is `D-50`, in the "Process
  constraints" section), append three new entries dated 2026-09-01, using
  the text from brief §10 verbatim: `D-51` (results over subjects, six
  verb-first headlines each with a `content_item.description` sentence, no
  numbered pastille, signed list restored with certification as sixth item,
  first item's `cle` load-bearing for the hero H1), `D-52` (seed retires
  content rows by name, `upsertItems` never deletes, explicit two-column
  retirement required), `D-53` (the chromatic wall — six full tiles, one hue
  each, 30%-capped wash, clipped watermark, full-saturation accent rule that
  draws on scroll, `--muted-ink` forbidden on these washes, calculated
  non-adjacent hue order).

  Check whether section 3 ("Ce que vous allez apprendre") is described in
  `.planning/phases/02-site-public/02-UI-SPEC.md` (grep for "Ce que vous
  allez apprendre" or "compétences"). If it is, update that description to
  match D-51/D-52/D-53 (chromatic wall, six results, certification restored)
  in the same style as the existing "Pour qui" D-49/D-50 cross-reference
  nearby. If the section is only listed by name with no further description,
  leave it as-is — no fabricated detail beyond what the brief specifies.

  Then re-seed local content only: run `npm run content:seed`. Do not run
  any `supabase db reset`/`stop`/`start` command.

  Finally run the full verification suite: `npm run lint`, `npm run
  typecheck`, `npm run build`. All three must exit 0, and the build output
  must show 14 routes with the same static/dynamic split as before (13
  static + `/api/contact` dynamic). Then assert against
  `.next/server/app/index.html` (never against source files) that: all six
  new titles are present verbatim (`Vous orienter dans l'écosystème Ariba`,
  `Dérouler un Procure-to-Pay complet`, `Piloter un Source-to-Pay de bout en
  bout`, `Mener un appel d'offres jusqu'à la décision`, `Tenir catalogues,
  contrats et workflows`, `Préparer la certification SAP Ariba`); all six
  sentences are present verbatim; the five retired strings
  (`L'écosystème Ariba dans son ensemble`, `Le processus Procure-to-Pay`, `Le
  processus Source-to-Pay`, `La gestion des catalogues`, `Les contrats et les
  workflows`) each occur zero times; `Maîtrisez` and `SAP Ariba` are both
  still present (hero H1 split intact). Also assert, on the touched-files
  discipline list: zero `--muted-ink` in `competences.tsx`; no new
  `cubic-bezier` outside `--ease-brand`/`ease-in`/`ease-out`/`linear`; no new
  raw hex in JSX; no new `shadow-` class; no `"use client"` added; no
  `min-[` arbitrary breakpoint added.
  </action>
  <verify>
    <automated>cd "C:\Users\Essakhi\Desktop\ElearningSAP\ElearningAriba" && npm run lint && npm run typecheck && npm run build</automated>
  </verify>
  <done>D-51/D-52/D-53 recorded in 02-CONTEXT.md following existing D- form, 02-UI-SPEC.md updated only if it already described this section; npm run content:seed completed against local stack only; lint/typecheck/build all exit 0, 14 routes with the prior static/dynamic split; .next/server/app/index.html contains all six new titles and sentences plus the certification phrase, zero occurrences of all five retired strings, and both hero H1 fragments; discipline checks (--muted-ink, cubic-bezier, hex, shadow-, use client, min-[) all clean on touched files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Content JSON → Supabase seed | `landing.json` values flow into `content_item` rows via a service-role script run only against the local stack |
| Supabase row → rendered HTML | `content_item.titre`/`description`/`cle` values render directly into the tile without escaping beyond React's default JSX escaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | scripts/seed-content.mjs RETIRED_ITEMS delete | mitigate | delete matches both section_cle and cle columns per row, never a bare section_cle delete that could empty the whole section |
| T-quick-02 | Information Disclosure | seed script still requires SUPABASE_SERVICE_ROLE_KEY env var | accept | unchanged existing pattern, script already exits 1 if unset, no new exposure introduced |
| T-quick-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan |
</threat_model>

<verification>
All three tasks' `<verify><automated>` commands must exit 0 in sequence.
Task 3's build + HTML assertions are the authoritative gate for the whole
plan — Tasks 1 and 2 only self-check their own file shape.
</verification>

<success_criteria>
- `npm run lint` / `npm run typecheck` / `npm run build` all exit 0, 14 routes with the unchanged static/dynamic split
- `.next/server/app/index.html` contains the six new titles, six new sentences, and "Préparer la certification SAP Ariba"; zero occurrences of the five retired strings; H1 still splits on "Maîtrisez"/"SAP Ariba"
- Local `content_item` table holds exactly six `competences` rows after `npm run content:seed`, `contrats-workflows` gone
- Three atomic commits minimum (content+seed+hero coupling; competences.tsx+globals.css; contract docs), nothing pushed, branch unchanged (`gsd/phase-02-site-public`)
</success_criteria>

<output>
Create `.planning/quick/260901-m2n-refondre-section-3-landing-competences-e/260901-m2n-SUMMARY.md` when done
</output>
