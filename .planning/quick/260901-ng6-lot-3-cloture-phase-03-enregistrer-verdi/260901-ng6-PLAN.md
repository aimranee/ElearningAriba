---
quick_id: 260901-ng6
status: complete
---

# Quick Task 260901-ng6: Lot 3 — clôture de la phase 03, enregistrer les verdicts du fondateur

Tâche **documentaire uniquement**. Les deux portes humaines du plan `03-11`
(Tâches 2 et 3) ont été **explicitement tranchées par le fondateur le
2026-09-01** — voir
`C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-09-01-brief-lot3-cloture-de-phase.md`
(brief complet, source de vérité) et
`ariba-cto/notes/2026-09-01-revue-fondateur-lot-3-tache-2.md` (preuves Tâche
2). Ce run **enregistre** ces verdicts, il ne les déduit pas et ne les
rejoue pas.

**Contraintes non négociables pour les deux tâches ci-dessous :**
- Ne modifie aucun fichier sous `src/` ni `supabase/` — uniquement des
  fichiers sous `.planning/`.
- Jamais `git push`, jamais `supabase db push`, jamais `supabase db reset`.
- Reste sur la branche `gsd/phase-03-comptes-connexion-et-espace-apprenant`
  (vérifier au départ et à l'arrivée).
- Vocabulaire de verdict CPT dans `03-RECETTE.md` : trois valeurs
  uniquement — `proven locally`, `proven on hosted`, `blocked`. Jamais
  `assumed`, `probably`, `devrait`, `à priori`.

## Task 1 — Mettre à jour 03-RECETTE.md avec les verdicts du fondateur

Files: `.planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md`

**A. Section 4 — critère 1 (table des onze critères UI-SPEC).** La preuve du
critère 1 reste `pass` (ne pas changer le verdict). Ajouter à la cellule
« Preuve / observation » la précision suivante, sans rien retirer
d'existant : la commande citée (`git diff --stat c124a827 --
src/components/ui/{accordion,badge,button,card,empty-state,field,input,message}.tsx`)
ne nomme que les **huit** composants pré-existants de `ui/`. Un lecteur qui
diffe le répertoire `ui/` entier voit en plus `src/components/ui/checkbox.tsx`
avec 43 insertions — c'est un **fichier neuf**, ajouté par `c862cad` pour la
case d'accusé de suppression de compte, jamais une modification d'un des
huit fichiers nommés par le critère. Le critère reste vrai ; le dire
explicitement pour qu'une prochaine passe ne le lise pas comme un échec.

**B. Section 4 — critères 8 et 10.** Les deux passent de « garantie source
seule » à observation réelle, confirmée par la Tâche 2 du fondateur
(2026-09-01, contre `next build && next start`, preuves complètes dans
`ariba-cto/notes/2026-09-01-revue-fondateur-lot-3-tache-2.md`) :
- Critère 8 : changer le verdict de `pass (garantie source)` à `pass`, et
  ajouter à la preuve : observation au clic réel — case décochée → bouton
  de confirmation désactivé ; case cochée → bouton actif.
- Critère 10 : changer le verdict de `pass pour idle/erreur de
  champ/rejet serveur/succès ; non observé pour pending` à `pass`, et
  ajouter à la preuve : état pending observé au clic réel — `data-loading="true"`
  posé avec `disabled` et `data-disabled` sur soumission.

**C. Section 4 — table des douze décisions D-A.** Changer le statut des
douze lignes (D-A1 à D-A12) de `en attente de ratification` à `acceptée`
(Tâche 3, fondateur, 2026-09-01).

**D. Section 4 — déviation SubmitButton.** Remplacer la dernière phrase
(« **En attente de ratification.** ») par « **Acceptée** (Tâche 3,
fondateur, 2026-09-01). »

**E. Section 4 — Issue D-14.** Réécrire entièrement ce paragraphe (il ne
s'agit pas d'une ratification simple mais d'une correction de fond) :
l'ancienne clôture était fausse — elle observait qu'un `redirect_to`
**forgé** est accepté par le poste local GoTrue, alors que c'est le
`redirect_to` **légitime** (`http://localhost:3100/api/auth/callback`) qui
était refusé, du fait de l'écart de schéma `http`/`https` dans
`additional_redirect_urls`. Corrigé par `65dea14` (allow-list le vrai
callback dans `supabase/config.toml`). Statut final : `changée, non
acceptée telle quelle` (Tâche 3, fondateur, 2026-09-01) — remplace `en
attente de ratification`.

**F. Section 4 — copie française (D-11).** Remplacer « **Aucune de ces
chaînes n'a été jugée par un humain à cette clôture.** » par : approuvée
(Tâche 3, fondateur, 2026-09-01), avec les changements déjà livrés —
nouvelle entrée `verificationAdresse`, sujet et corps de l'email de
vérification, promesse d'email retirée de `donnees.json →
suppression.accuseReception.message` ; `confirmationInscription` reste
intact, réservé à un email de bienvenue ultérieur ; le libellé « Confirmer
mon adresse email » est une décision CTO, ratifiée par le fondateur.

**G. Section 2 — ajouter un nouveau tiret** documentant le défaut résiduel
non corrigé : `NEXT_PUBLIC_SITE_URL` fixe une seule origine et le cookie
`code_verifier` PKCE est posé sur l'hôte de **navigation** — naviguer sur un
hôte différent de celui du lien casse l'échange (`/connexion?erreur=session`).
Mesuré : inscription sur `127.0.0.1:3100` + lien vers `localhost:3100` →
échec ; parcours entièrement sur `localhost:3100` → succès. Conséquence
recette : tester sur `localhost:3100`, jamais `127.0.0.1:3100`. Conséquence
mise en ligne (Lot 5) : si le site répond à la fois sur l'apex et sur
`www`, un apprenant inscrit sur l'hôte non canonique perdra sa session à la
confirmation — à traiter par une redirection canonique.

**H. Section 3 — ajouter une nouvelle entrée** à la liste des postes dus par
le CIO : la liste d'autorisation de callback Google **hébergée** devra
porter `https://<domaine>/api/auth/callback**` — `google/route.ts` vise la
même URL de callback que les deux parcours corrigés par `65dea14`/`3eee9bc`/
`2a1b518` ; sans cette entrée, le même défaut se reproduira en production et
Google échouera dès sa livraison.

**I. Statut des portes humaines (fin du fichier).** Remplacer les deux
lignes `not reviewed` par les verdicts réels :
- Tâche 2 : `approuvée, 9/9 étapes` — menée par le fondateur le 2026-09-01
  contre `next build && next start` (preuves :
  `ariba-cto/notes/2026-09-01-revue-fondateur-lot-3-tache-2.md`). Sept
  étapes approuvées du premier passage ; les étapes 2 (email de
  confirmation) et 6 (réinitialisation) ont d'abord été **rejetées**, puis
  corrigées par `65dea14`, `3eee9bc`, `2a1b518`, et **revérifiées** —
  approuvées au second passage. Après correctif : l'email porte
  `redirect_to=http://localhost:3100/api/auth/callback`, le lien suivi tel
  que délivré aboutit sur `/espace` avec le prénom réel rendu, le lien de
  récupération aboutit sur `/nouveau-mot-de-passe`, aucun code forgé ni
  échangé à la main. Enregistrer les deux moments (rejet puis correction),
  pas seulement le second.
- Tâche 3 : `ratifiée` — 2026-09-01. Treize des quatorze décisions
  acceptées (D-A1 à D-A13, où D-A13 est la déviation SubmitButton). D-14 :
  changée, non acceptée telle quelle — voir l'entrée réécrite en Section 4.
  Copie française approuvée avec les changements déjà livrés (détail en
  Section 4.F).

Also update the paragraph immediately below (currently stating the run is
headless and infers nothing) to reflect that both gates are now decided,
by a human, outside this headless run — do not claim this run observed
anything itself.

Verify:
- `grep -cE '\| (proven locally|proven on hosted|blocked) \|' .planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md` → `9`
- `grep -ciE '\b(assumed|probably|should work|devrait|à priori)\b' .planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md` → `0`
- `grep -c 'not reviewed' .planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md` → `0`
- `grep -c 'en attente de ratification' .planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md` → `0`

Done: 03-RECETTE.md Section 2, 3 and 4 fully reflect the founder's 2026-09-01
verdicts; verdict vocabulary for CPT-01..CPT-09 (Section 1) untouched and
still counts 9; no forbidden hedging words anywhere in the file.

## Task 2 — Propagate the verdicts to REQUIREMENTS.md, STATE.md and 03-11-SUMMARY.md

Files: `.planning/REQUIREMENTS.md`, `.planning/STATE.md`,
`.planning/phases/03-comptes-connexion-et-espace-apprenant/03-11-SUMMARY.md`

**A. REQUIREMENTS.md — Lot 3 checklist (`### Comptes — Lot 3` section,
currently around lines 55–63).** Change the checkbox to `[x]` for
**CPT-01, CPT-03, CPT-04, CPT-05, CPT-06, CPT-07, CPT-09** (CPT-08 is
already `[x]`). **CPT-02 stays `[ ]`.** Append to the CPT-02 requirement
line (after its existing text, before the line break): ` — **bloquée** :
aucun identifiant Google (client OAuth) n'existe encore ;
`src/app/api/auth/google/route.ts` vise la même URL de callback que les
parcours corrigés (`65dea14`/`3eee9bc`/`2a1b518`) ; la liste d'autorisation
**hébergée** devra porter `https://<domaine>/api/auth/callback**`, faute de
quoi Google échouera dès sa livraison.`

**B. REQUIREMENTS.md — Traceability table.** For rows `CPT-01`, `CPT-03`
through `CPT-09` (all except `CPT-02`, which already reads `Pending` and
should now read `Blocked — Google OAuth client missing`), change `Pending`
to `Complete (03-11)`.

**C. STATE.md — frontmatter.** Update `stopped_at` to reflect phase 03
closure: founder ratified 03-11 Tasks 2 and 3 on 2026-09-01, phase 03 is
complete; only hosted CIO items remain open (Google OAuth client for
CPT-02, custom SMTP, private bucket, Lot 3 migrations push). Bump
`completed_phases` from `2` to `3` and `completed_plans` from `38` to `39`.
Update `last_updated` to the current ISO timestamp.

**D. STATE.md — Current Position.** Change `Phase: 03 (...) — EXECUTING` to
`Phase: 03 (...) — COMPLETE`. Update the `Plan:` line to state all 11 plans
(03-01..03-11) are complete, founder ratified 03-11 Tasks 2/3 on
2026-09-01. Update `Current focus` under Project Reference to point at
Phase 04 (agenda-et-prise-de-rendez-vous) as next. Update `Last activity`
to 2026-09-01.

**E. STATE.md — Blockers/Concerns.** Replace the line "Plan 03-11
(checkpoint autonomous:false) intentionally not run headlessly per run
instructions -- clean-database re-run and human sign-off on Lot 3 owed to
the founder before phase 03 can close" with a resolved note: founder ruled
on both 03-11 checkpoints 2026-09-01 (Task 2 approved 9/9 after two fixed
regressions, Task 3 ratified 13/14 decisions with D-14 rewritten); phase 03
is closed. Add/keep a clearly separate line for what is **still** open and
hosted-only: CPT-02 (no Google OAuth client anywhere), custom SMTP for
CPT-01/CPT-03 confirmation and reset emails, the private `supports` bucket
for CPT-07, and the two Lot 3 migrations not yet pushed to either hosted
Supabase project (all four are CIO-owed, per `03-RECETTE.md` Section 3).

**F. 03-11-SUMMARY.md.** Grep for every occurrence of "not executed" /
"were not executed" / "cannot be marked complete" (title paragraph line 41,
"What Was Not Done" section around line 83, "Next Phase Readiness" line
93) and replace each with the real verdict and date: Tasks 2 and 3 were
executed by the founder directly (not by this headless run) on 2026-09-01
— Task 2 approved 9/9 steps (steps 2 and 6 initially rejected, fixed by
`65dea14`/`3eee9bc`/`2a1b518`, then reverified), Task 3 ratified 13 of 14
decisions (D-A1..D-A13) with D-14 rewritten as changed/not-accepted-as-was.
Phase 03 is now complete. Do not alter frontmatter fields unrelated to this
correction (`key-decisions`, `metrics`, etc. stay as originally recorded —
they describe what Task 1 did, which is still accurate).

Verify:
- `grep -c 'not executed' .planning/phases/03-comptes-connexion-et-espace-apprenant/03-11-SUMMARY.md` → `0`
- `grep -c '\[x\] \*\*CPT-0[1345679]\*\*' .planning/REQUIREMENTS.md` → `7`
- `grep -c '\[ \] \*\*CPT-02\*\*' .planning/REQUIREMENTS.md` → `1`
- `grep -c 'completed_phases: 3' .planning/STATE.md` → `1`
- `grep -c 'completed_plans: 39' .planning/STATE.md` → `1`
- `git rev-parse --abbrev-ref HEAD` → `gsd/phase-03-comptes-connexion-et-espace-apprenant`
- `git diff --stat -- src/ supabase/` → empty

Done: REQUIREMENTS.md, STATE.md and 03-11-SUMMARY.md all agree that phase
03 is complete except for the four hosted CIO dependencies and CPT-02's
Google-client blocker; nothing under `src/` or `supabase/` touched; branch
unchanged; nothing pushed.

## Final verification

- `grep -cE '\| (proven locally|proven on hosted|blocked) \|' .planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md` → `9`
- `grep -ciE '\b(assumed|probably|devrait|à priori)\b' .planning/phases/03-comptes-connexion-et-espace-apprenant/03-RECETTE.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/phases/03-comptes-connexion-et-espace-apprenant/03-11-SUMMARY.md` → `0`
- `git diff --stat -- src/ supabase/` → empty (no source files touched)
- `git rev-parse --abbrev-ref HEAD` → `gsd/phase-03-comptes-connexion-et-espace-apprenant`
- Nothing pushed: no `git push` executed during this run.
