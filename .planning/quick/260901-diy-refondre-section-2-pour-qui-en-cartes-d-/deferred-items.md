# Deferred items — 260901-diy

- `src/app/layout.tsx:23` — `Cannot find name 'LayoutProps'` (TS2304). Pre-existing,
  confirmed via `git stash` before this plan's changes. Out of scope (not touched by
  this plan's files). Not fixed here.
