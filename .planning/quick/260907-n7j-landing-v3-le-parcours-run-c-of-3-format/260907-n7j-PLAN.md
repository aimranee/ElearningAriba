---
task: 260907-n7j
type: quick
branch: cto/landing-v3
autonomous: true
files_modified:
  - src/components/sections/format-modalites.tsx
  - src/components/motion/format-deroule.tsx
  - src/components/sections/confiance.tsx
  - src/components/motion/confiance-faits.tsx
  - src/components/sections/cta-final.tsx
  - src/components/illustrations/mini-calendrier.tsx
  - src/components/sections/faq.tsx
---

<objective>
Run C of 3 for "Landing v3 - Le parcours". Runs A and B (already landed on `cto/landing-v3`)
shipped v3 tokens, `Section tone="night"`, the new hero/rail/sticky bar, and restyled
Pour-qui/Competences/Programme. This run ports the bottom third of the page to the v3
maquette: Format et modalites (session deroule + night preview window), Confiance
(trainer card + three verifiable facts as a single-column list), CTA final (violet
panel + a new mini-calendrier illustration replacing the "ce qui se passe ensuite"
column moved to `et-ensuite.tsx` in run A), and FAQ (two-column layout, sticky left
rail). No branch change, no push, no dev server.

Purpose: close out the "bas de page" scope row (brief §G, Run C) with four atomic
commits, each independently buildable.
Output: four commits on `cto/landing-v3`; one new illustration file
(`src/components/illustrations/mini-calendrier.tsx`); a final verification pass (lint,
typecheck, build, content:check, git status) run once after all four commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Source brief (read-only, external to repo - do not modify):
@C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-09-07-brief-refonte-landing-v3.md
@C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-09-07-maquette-landing-v3.html

Code to change:
@src/components/sections/format-modalites.tsx
@src/components/motion/format-deroule.tsx
@src/components/sections/confiance.tsx
@src/components/motion/confiance-faits.tsx
@src/components/sections/cta-final.tsx
@src/components/sections/faq.tsx

Read-only reference:
@src/components/sections/section.tsx
@src/components/sections/et-ensuite.tsx
@src/components/ui/accordion.tsx
@src/components/ui/card.tsx
@src/components/motion/reveal.tsx
@src/components/illustrations/flux-achat.tsx
@src/app/globals.css
@src/locales/fr/common.json
@src/locales/fr/landing.json
@src/locales/fr/agenda.json
</context>

<interfaces>
<!-- Facts already verified in the repo at the time this plan was written.
     Executor must still re-verify anything time-sensitive per the
     STOP-and-report rule below - these are a starting point, not a
     substitute for reading the live files. -->

**`Section` (`src/components/sections/section.tsx`)**: `SectionTone = "default" | "wash" |
"night"`. `wash` renders `bg-[linear-gradient(180deg,transparent_0%,var(--wash-ground)_50%,transparent_100%)]`;
`default` is fully transparent. No `band` tone exists anymore (removed run 4 of the
scale/palette rework, D-96). `SectionHeader` reads `tone` via the ancestor's `data-tone`
attribute automatically (`in-data-[tone=night]:` variants) - never pass tone to
`SectionHeader` itself.

**Maquette section classes for this run's four sections** (ground truth for the `tone`
prop, read directly off the `<section>` tags in the maquette, lines ~719, ~773, ~821,
~851): Format = `class="section section--wash"` -> `tone="wash"`. Confiance = `class="section"`
(no modifier) -> `tone="default"`. CTA final = `class="section"` (no modifier; the violet
panel is an inner `.cta-panel` div, not the Section itself) -> `tone="default"` (already
correct, no change). FAQ = `class="section"` -> `tone="default"` (already correct, no
change). **Format is currently `tone="default"` and Confiance is currently `tone="wash"`
- Task 1 and Task 2 below swap these to match the maquette exactly.**

**D-68 (single niveau-3 elevation on the page):** the hero's `flux-achat.tsx` board is the
page's sole `--shadow-4` consumer. `format-deroule.tsx`'s preview window currently uses
`shadow-[var(--shadow-4),var(--inset-hi)]`, which is a **pre-existing violation** the
maquette's own `.window{box-shadow:var(--shadow-2)}` rule corrects. Task 1 fixes this as
part of the restyle (shadow-4 -> shadow-2), do not treat it as an unrelated change to skip.

**`pulse-live` keyframe (`globals.css` `@layer components`, ~line 352) is hardcoded to a
red box-shadow** (`rgba(255,95,87,...)`, matching `--live`/`#ff5f57`) - it is not
parameterized by a CSS custom property, so it cannot be recoloured mint via a class or
inline style alone. The maquette's format-window "En direct" pill (`.window .bar .live`)
asks for a mint dot; rather than forking a second colour-variant keyframe (D-17 protects
one easing curve, and forking keyframes purely for colour is the same kind of
proliferation), Task 1 reuses the exact same `--live` red dot + `pulse-live` animation the
hero already uses for its own "En direct" floating label (`flux-achat.tsx`) — visually
consistent site-wide "live" treatment, zero new CSS. Do not add a new keyframe for this.

**`agenda.json` `typesRendezVous[0]`** = `{ id: "decouverte", libelle: "Appel découverte",
dureeMinutes: 30, prix: 0 }`. The literal composition `` `${item.libelle} · ${item.dureeMinutes} min` ``
is an **already-established pattern** in this codebase (`src/components/motion/barre-reservation.tsx`
line 97: `{decouverte.libelle} · {decouverte.dureeMinutes} min`) — Task 3's mini-calendrier
mirrors this exact JSX shape, not a new string.

**`landing.json` `formatModalites.apercu.panneaux[0].titre`** = `"Votre créneau réservé"` —
this exact string is reused verbatim by both `format-deroule.tsx` (already) and the new
`mini-calendrier.tsx` (Task 3), per D-24 (no phrase authored twice, no new fact). Do not
hardcode this text; import `landing.json` and read the same path.

**`landing.json` `ctaFinal.etapes.items[1].description`** = `"Une date de session live fixée
ensemble, selon vos disponibilités."` — reused verbatim as the mini-calendrier's closing
line (maquette `.conf-line` text), per the same D-24 rule. `cta-final.tsx` already imports
`landing.ctaFinal.etapes`; Task 3 reads `etapes.items[1].description` from that same object,
no new query.

**`Accordion` contract (`src/components/ui/accordion.tsx`, confirmed current):** Base UI
puts the open state as `data-open` on `AccordionItem` and the trigger-side open indicator
as `data-panel-open` on `AccordionTrigger`. The built-in chevron SVG carries
`data-slot="accordion-chevron"` and rotates via `data-[panel-open]:[&_svg[data-slot=accordion-chevron]]:rotate-180`
baked into `accordionVariants` — this file is **not** in this run's `files_modified` and
must not be edited (`programme-accordion.tsx` from Run B depends on its current chevron
behaviour). Task 4's "+/−" toggle is built entirely from `faq.tsx`'s own JSX: hide the
built-in chevron on this call site only (`className` targeting
`[&_svg[data-slot=accordion-chevron]]:hidden` on `AccordionTrigger`), add `group` to the
trigger's `className`, and render a custom two-span plus/minus icon as a JSX child that
reacts to the ancestor via `group-data-[panel-open]:` variants (Tailwind reads the
`data-panel-open` attribute off the nearest ancestor carrying `.group`, which is the
trigger itself here).

**Illustration component precedent (`src/components/illustrations/flux-achat.tsx`):**
server component (no `"use client"`), pure inline SVG/CSS/HTML, `aria-hidden="true"` on
the outermost wrapper, reads strings from `landing`/`agenda` JSON only, `export { Name };`
named export (not default). `mini-calendrier.tsx` (Task 3) follows the same shape.

**Confiance's `--ta`/`--tb` tile-colour technique (maquette, primitives `.tile{background:
linear-gradient(140deg,var(--ta) 0%,var(--tb) 100%)}`):** the maquette sets `--ta`/`--tb` as
inline custom properties on the outer element (e.g. `style="--ta:var(--violet);--tb:var(--indigo)"`
on each `.fait` `<li>`) and the shared `.tile` rule reads them generically. Task 2 ports this
by giving each fact item a `gradient: { ta: string; tb: string }` pair (computed in
`confiance.tsx` from the item's `cle`, mirroring the existing `CONFIANCE_ICONS` map pattern)
and applying `style={{ background: "linear-gradient(140deg, var(--ta), var(--tb))" }}` with
`"--ta"`/`"--tb"` set as CSS custom properties via the same style object (cast through
`React.CSSProperties` with an index signature, or `as React.CSSProperties` — TypeScript does
not type custom properties natively; do not use `any`, use the established
`{ ["--ta" as string]: value } as React.CSSProperties` cast pattern or an equivalent typed
helper if one already exists in the codebase — check `assembly-connectors.tsx`/`format-deroule.tsx`
for prior art of custom-property inline styles before inventing a new pattern).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 (COMMIT 1): Format restyled, mechanism kept</name>
  <files>src/components/sections/format-modalites.tsx, src/components/motion/format-deroule.tsx</files>
  <action>
Per brief C-05 and maquette `.format`/`.etapes`/`.etape`/`.window`/`.fourni` (lines
~718-770). Recompose visual treatment only — every query, error guard, the five
selectable steps (`aria-pressed`, keyboard activation via native `<button>`), the
`ETAPE_TEINTES` flat-fill array (D-57), the resume line's computed text, and the
non-contractual caption all keep their exact current logic. No string changes anywhere
in either file.

**`format-modalites.tsx`:** change `<Section tone="default" data-section="format">` to
`<Section tone="wash" data-section="format">` (maquette `class="section section--wash"`
— see `<interfaces>`). Nothing else in this file changes.

**`format-deroule.tsx`:**

1. Left column (`.etapes`): add `role="tablist" aria-label="Déroulé d'une session"` to the
   `<ol>` (maquette adds this landmark; the five `<li>`/`<button>` pairs already behave
   like a tab list). Simplify each button's visual treatment to match maquette `.etape`:
   remove the absolute-positioned left accent-rail `<span>` (not present in the maquette —
   it is bespoke to the current build, drop it), keep the flat-colour numbered pastille
   (`ETAPE_TEINTES[index]`, unchanged), and set the button's at-rest/hover/pressed classes
   to mirror `.etape:hover{background:#fff;border-color:var(--hairline)}` and
   `.etape[aria-pressed="true"]{background:#fff;border-color:var(--hairline-2);box-shadow:var(--shadow-1)}`
   — i.e. drop the current `bg-[var(--tint-violet)]` pressed fill in favour of white +
   `--shadow-1` on press, and `bg-transparent hover:bg-transparent hover:border-[var(--hairline)]`
   at rest/hover. Remove the five-dot connector progress indicator block below the `<ol>`
   entirely (the `aria-hidden` div mapping `[1,2,3,4,5]` with the small circle/line
   indicators) — it has no equivalent in the maquette and is superseded by the resume line
   below.
2. Move the `resume` paragraph: delete the current `<GraduationCap>` + `resume` block from
   inside the "Ce qui est fourni" footer (bottom of the function). Add a
   `<p className="mt-4 text-[length:var(--text-small)] leading-[var(--text-small--line-height)] text-[var(--muted-ink)]">{resume}</p>`
   directly under the `</ol>` inside the left column (maquette places `.resume` right under
   `.etapes`'s buttons, no icon). The `resume` prop itself and its computation in
   `format-modalites.tsx` (`common.formatModalitesAside.resume.replace(...)`) do not
   change — only where the string renders.
3. Right column (`.window`): change the outer chrome from a white `bg-white`
   `shadow-[var(--shadow-4),var(--inset-hi)]` panel to
   `rounded-[22px] bg-[var(--night)] text-white shadow-[var(--shadow-2)] overflow-hidden`
   (D-68 fix, see `<interfaces>`). The bar row (`border-b border-[var(--border2)] bg-white`)
   becomes `border-b border-white/10` with no background fill (transparent over the night
   chrome); the three coloured traffic-light dots keep their existing hardcoded hex values
   unchanged (decorative macOS-chrome convention, not a brand colour). The `ttl` label
   (`{apercu.frameLabel}`) changes from `text-[var(--muted-ink)]` to `text-white/60`. Add a
   live pill to the right end of the bar (`ml-auto`, maquette `.bar .live`): a `<span>` with
   `inline-flex items-center gap-[0.4rem] rounded-full bg-[rgba(31,199,155,.16)] px-[0.6rem] py-[0.25rem] text-[length:var(--text-micro)] font-extrabold tracking-[0.06em] text-[var(--mint-soft)] uppercase`
   containing the red `--live` pulse dot (per `<interfaces>`: reuse `flux-achat.tsx`'s exact
   dot markup/animation, `bg-[var(--live)]` + `animation: pulse-live 1.8s var(--ease-brand) infinite`)
   and the text `{apercu.enDirect}` (already-existing string, reused, not new). The scene
   background changes from implicit white to `bg-[linear-gradient(180deg,var(--night-2),var(--night))]`.
   The inner white `.card` (containing the five conditional preview panels for
   `selected === 0..4`) keeps its current white background and all five panels' internal
   content/logic completely unchanged — only the chrome around it changes.
4. "Ce qui est fourni" footer: keep the existing grid/checkmark/dashed-item structure as-is
   (it already matches maquette `.fourni` closely); only remove the resume-line block per
   step 2 above. Do not otherwise restructure this block.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "format-modalites|format-deroule" || echo "no format type errors"</automated>
  </verify>
  <done>
`format-modalites.tsx` renders `Section tone="wash"`. `format-deroule.tsx`'s left column
is a `role="tablist"` list of five buttons with flat-colour pastilles and a resume line
directly beneath (no five-dot connector indicator), its right column is a night-chrome
window (`--night` background, `--shadow-2`, mint "En direct" pill reusing the red
`--live`/`pulse-live` treatment) wrapping the five unchanged preview panels, and the
"Ce qui est fourni" band no longer duplicates the resume line. All five steps remain
selectable via `aria-pressed`/click/keyboard, `ETAPE_TEINTES` unchanged, zero new
translation keys. `tsc --noEmit` reports no errors in either file. Commit with message
`#feat: restyle format et modalites as a night preview window`.
  </done>
</task>

<task type="auto">
  <name>Task 2 (COMMIT 2): Confiance recomposed, mechanism kept</name>
  <files>src/components/sections/confiance.tsx, src/components/motion/confiance-faits.tsx</files>
  <action>
Per brief C-06 and maquette `.conf`/`.trainer`/`.faits`/`.fait` (lines ~772-818). Keep
both queries (`getSection`, `getConfianceFaits`), the error guard, the `ConfianceFaits`
toggle island's open/close mechanism (D-62/63/64: two independently toggled
`grid-template-rows` 0fr/1fr panels swapping description vs. proof, `aria-expanded`/
`aria-controls`, `voir`/`masquer` labels), the trainer card's content, and the D-63
monogram slot comment. No string changes; the only content-level change is the href
override described below.

**`confiance.tsx`:**

1. Change `<Section tone="wash" data-section="confiance">` to
   `<Section tone="default" data-section="confiance">` (maquette has no modifier class —
   see `<interfaces>`).
2. Trainer card (`Card variant="default"`): add `rounded-[22px]` to its `className`
   (maquette `.trainer{border-radius:22px}`; `Card`'s base `rounded-xl` is 12px). Change
   the avatar span's background from flat `bg-[var(--violet)]` to
   `bg-[linear-gradient(135deg,var(--violet),var(--indigo))]` (maquette `.trainer .mono`) —
   keep its `size-16 rounded-[18px]` and the D-63 "future portrait" comment unchanged.
   Everything else in this card (eyebrow, name/intitule, points list with mint check
   icons, "Voir le parcours complet" link) stays as-is.
3. Add a `CONFIANCE_TEINTES` map next to the existing `CONFIANCE_ICONS` map, keyed by the
   same `cle` values, giving each fact its maquette gradient pair: `appel-decouverte` ->
   `{ ta: "var(--violet)", tb: "var(--indigo)" }`, `formateur-identifie` ->
   `{ ta: "var(--sky-ink)", tb: "var(--azur-ink)" }`, `groupe-limite` ->
   `{ ta: "var(--mint-ink)", tb: "var(--azur-ink)" }`. Pass `gradient: CONFIANCE_TEINTES[fait.cle]`
   (with a safe fallback to the `appel-decouverte` pair if a `cle` is ever unmapped — do not
   throw) through to each item in the `ConfianceFaits` `items` prop, alongside the existing
   `icon`.
4. Add the href override: when mapping `faitsResult.data`, for the item where
   `fait.cle === "appel-decouverte"`, set `preuveLienHref: "/reservation"` instead of the
   value read from `fait.preuveLienHref` (currently `/contact` from the database). Add a
   one-line comment directly above this override: `// D-28: the discovery-call proof link
   points to the booking flow (/reservation), not /contact — a call-site override, the
   seed/JSON value is left untouched.` The other two items' `preuveLienHref`/`preuveLienLabel`
   pass through unchanged (including `formateur-identifie`'s `/a-propos` link).

**`confiance-faits.tsx`:**

1. Outer list: change `<ul className="mt-[3.25rem] grid grid-cols-1 gap-[1.35rem] sm:grid-cols-2 lg:grid-cols-3">`
   to a single-column stack: `<ul className="mt-[3.25rem] flex flex-col gap-4">` (maquette
   `.faits{display:flex;flex-direction:column;gap:1rem}`).
2. Update the `ConfianceFaitItem` interface: replace the single hardcoded tile-gradient
   assumption with a `gradient: { ta: string; tb: string }` field.
3. Restructure each `<Card>` from the current vertical (icon-on-top, then title, then
   description) layout to a maquette `.fait` two-column grid:
   `className="group grid grid-cols-[52px_1fr] gap-4 rounded-[22px] p-[1.4rem]"` (keep
   `Card variant="default"` for the border/shadow/hover-lift base, override radius to 22px
   to match maquette). The icon tile becomes
   `style={{ background: "linear-gradient(140deg, var(--ta), var(--tb))", boxShadow: "0 12px 24px -12px var(--tb)", ["--ta" as string]: item.gradient.ta, ["--tb" as string]: item.gradient.tb } as React.CSSProperties}`
   — read `<interfaces>` for the custom-property cast pattern; do not use `any`. Keep the
   tile's existing hover scale/rotate transform and `size-[52px] rounded-[16px]` sizing.
4. Content column: `<h3>` for `item.titre` (`text-[length:var(--text-card)] font-bold
   tracking-[-0.02em]`, matching the current title styling but as a semantic heading
   instead of a `<span>`), then a plain always-visible `<p className="mt-[0.3rem]
   text-[length:var(--text-small)] leading-[var(--text-small--line-height)]
   text-[var(--muted-ink)]">{item.description}</p>` directly below the title (maquette
   shows title+description together, always visible — this is new: currently the
   description sits inside the first toggled 0fr/1fr panel and disappears when the proof
   is shown; per brief "keep mechanism D-62/63/64", the *toggle logic itself* (two
   `grid-template-rows` panels swapping visibility) is what must survive unchanged — but
   move `item.titre`+`item.description` OUT of the first toggled panel so they render as
   permanently-visible header text per the maquette, and keep ONLY the proof text +
   optional link inside the surviving toggle mechanism (a single 0fr/1fr panel now,
   revealing/hiding just the proof). This means: delete the first toggled panel entirely
   (the one currently wrapping `item.description`), keep the second toggled panel
   (currently wrapping `item.preuveTexte` + link) exactly as-is mechanically (same
   `gridTemplateRows`/`visibility`/`transition` inline style object, same `aria-hidden`
   logic), but restyle its inner content into a `.box` treatment: wrap the proof paragraph
   and link in a `<div className="mt-2 rounded-[14px] border border-[var(--hairline-2)]
   bg-[var(--tint-violet)] px-[1.1rem] py-[1rem]">`.
5. Toggle button restyle: change from the current underline-text style to maquette `.tog`
   — `className="mt-[0.35rem] inline-flex min-h-11 items-center gap-[0.4rem]
   text-[length:var(--text-small)] leading-[var(--text-small--line-height)] font-bold
   text-[var(--deep)]"`, remove the `underline`/`underline-offset-2` classes, and add a
   `ChevronDown` icon (import from `lucide-react`) after the label text:
   `<ChevronDown aria-hidden="true" className={cn("size-[15px] transition-transform duration-[var(--duration-base)] ease-[var(--ease-brand)]", isOpen && "rotate-180")} />`
   — import `cn` from `@/lib/utils` if not already imported in this file (check the
   existing import block first).
6. Everything else (the `Reveal` wrapper, `dataD` stagger, `aria-expanded`/`aria-controls`
   wiring on the toggle button, the `preuveId` construction) stays unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "confiance" || echo "no confiance type errors"</automated>
  </verify>
  <done>
`confiance.tsx` renders `Section tone="default"`, a gradient-avatar trainer card at
22px radius, and passes a per-item `gradient` pair plus a call-site `/reservation` href
override (with its D-28 comment) for the `appel-decouverte` fact only. `confiance-faits.tsx`
renders a single-column stack of `.fait`-style two-column cards (52px gradient tile +
content), title/description always visible, a single surviving toggle panel revealing a
tinted `.box` proof paragraph, and a bold chevron-toggle button at 44px min height. Both
queries, the error guard, and the toggle island's underlying grid-rows mechanism are
unchanged. `tsc --noEmit` reports no errors in either file. Commit with message
`#feat: recompose confiance section as a single-column fact list`.
  </done>
</task>

<task type="auto">
  <name>Task 3 (COMMIT 3): Final CTA with mini-calendar</name>
  <files>src/components/sections/cta-final.tsx, src/components/illustrations/mini-calendrier.tsx</files>
  <action>
Per brief C-07 and maquette `.cta-panel`/`.cal` (lines ~820-848). Keep the query
(`getSection`), the error guard, the gradient panel (`linear-gradient(125deg,var(--indigo),var(--deep)_45%,var(--violet))`),
the grid-overlay decorative `<span>`, `shadow-[var(--shadow-brand)]`, the eyebrow/H2/lead
block, and both buttons (primary white "Réserver" -> `/reservation`, secondary glass
"Voir le programme" -> `/programme`) exactly as they render today.

**`cta-final.tsx`:** delete the entire right-column `<div>` currently rendering "Ce qui se
passe ensuite" (the `<h3>{etapes.titre}</h3>` + mapped step rows — this content already
moved to `src/components/sections/et-ensuite.tsx` in run A and must not be duplicated).
Replace it with `<MiniCalendrier />` (new import from
`@/components/illustrations/mini-calendrier`). Remove the now-unused `etapes` local
variable and its destructuring from `landing.ctaFinal` if nothing else in this file reads
it after the deletion — re-check the full file before removing the import to confirm no
other reference remains (the `landing` import itself is likely still needed for
`ctaFinal.eyebrow`/`titre`/`lead` via `section` — actually those come from `getSection`'s
Supabase row, not `landing.json`; verify by reading the file's current full body before
deciding whether the `landing` import can be dropped entirely, and only drop imports that
become genuinely unused — do not leave an unused import, `eslint` will fail the run).

**New file `src/components/illustrations/mini-calendrier.tsx`** (server component, no
`"use client"`, named export `export { MiniCalendrier };`, mirroring `flux-achat.tsx`'s
shape): renders the maquette `.cal` card, `aria-hidden="true"` on the outer wrapper, no
image asset, no date/seat-count content anywhere (D-45/D-61/D-66/D-45):

1. Outer: `<div aria-hidden="true" data-slot="mini-calendrier" className="relative mx-auto max-w-[420px] rotate-[-1.5deg] rounded-[24px] bg-white p-5 text-[var(--ink)] shadow-[0_40px_80px_-30px_rgba(0,0,0,.5),var(--inset-hi)]">`.
2. Top row: `<div className="flex items-center justify-between gap-3">` containing
   `<b className="font-heading text-[length:var(--text-body)] font-bold">{landing.formatModalites.apercu.panneaux[0].titre}</b>`
   (renders "Votre créneau réservé" — see `<interfaces>`, this is the exact existing
   string, imported from `landing.json`, not typed literally) and a type pill
   `<span className="inline-flex items-center gap-[0.4rem] rounded-full bg-[var(--lav)] px-[0.65rem] py-[0.3rem] text-[length:var(--text-micro)] font-extrabold text-[var(--deep)]">{decouverte.libelle} · {decouverte.dureeMinutes} min</span>`
   where `const decouverte = agenda.typesRendezVous[0];` (import `agenda` from
   `@/locales/fr/agenda.json`) — mirror `barre-reservation.tsx`'s exact composition; if
   `decouverte` is ever `undefined` (empty array), render the pill only when it exists,
   same defensive pattern `barre-reservation.tsx` already uses.
3. Weekday initials row: `<div className="mt-4 grid grid-cols-7 gap-[0.4rem] text-center text-[length:var(--text-micro)] font-bold text-[var(--muted2)]">` with seven `<span>L</span><span>M</span><span>M</span><span>J</span><span>V</span><span>S</span><span>D</span>`
   — these seven single-letter day initials are chrome, not translated content, and are
   pre-authorized (see run constraints; do not add a locale key for them).
4. Abstract slot grid: `<div className="mt-2 grid grid-cols-7 gap-[0.4rem]">` with 21
   `<span>` cells (3 rows of 7), each `h-[26px] rounded-[8px]`. Base cell:
   `bg-[var(--tint)] border border-[var(--hairline)]`. "Available" cell (`on`):
   `bg-[var(--lav)] border-[var(--violet-soft)]`. The single "picked" cell (`pick`):
   `relative bg-[var(--violet)] border-[var(--violet)] shadow-[0_8px_16px_-8px_rgba(99,91,255,.9)]`
   with a small centred checkmark — render the checkmark as an inline `<svg>` (viewBox
   `0 0 24 24`, `stroke="white"`, `strokeWidth={3}`, path `M5 12l5 5L20 7`) absolutely
   centred inside the cell rather than the maquette's CSS `background:url(data:image/svg+xml...)`
   trick (no data-URI SVGs in a React component - render real markup). Pattern the 21
   cells' on/pick/empty states exactly per the maquette's three rows (`.slots` lines
   ~838-841): row 1 = on, empty, on, on, empty, empty, empty; row 2 = empty, on, empty,
   **pick**, on, empty, empty; row 3 = on, empty, empty, on, on, empty, empty. Build this
   as a literal array of 21 state strings (`"on" | "pick" | ""`) rather than deriving it
   from any date logic — it is decoration, never a real calendar (D-45/D-66).
5. Confirmation line: `<div className="mt-4 flex items-center gap-[0.6rem] border-t border-[var(--hairline)] pt-[0.9rem] text-[length:var(--text-small)] font-semibold">` containing
   the existing green "Confirmé" pill pattern (reuse the exact markup already used
   elsewhere in this codebase for the mint checkmark pill, e.g. `format-deroule.tsx`'s
   `apercu.confirme` pill: `bg-[var(--success-muted)] text-[var(--mint-ink)]` with a small
   check svg) reading `{apercu.confirme}` from `landing.formatModalites.apercu`, followed
   by the text `{landing.ctaFinal.etapes.items[1]?.description}` (see `<interfaces>` — the
   exact existing "Une date de session live fixée ensemble..." string; guard with `?.` and
   render nothing if the item is missing rather than throwing).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "cta-final|mini-calendrier" || echo "no cta-final type errors"</automated>
  </verify>
  <done>
`cta-final.tsx` no longer renders its own "Ce qui se passe ensuite" column; its right
column is `<MiniCalendrier />`. `src/components/illustrations/mini-calendrier.tsx` exists,
exports a server component, renders a rotated white card with the exact reused
"Votre créneau réservé" title, an "Appel découverte · 30 min" pill sourced from
`agenda.typesRendezVous[0]`, seven day-initial letters (chrome, no locale key), a 21-cell
abstract slot grid with exactly one picked cell, and a "Confirmé" line reusing the exact
`ctaFinal.etapes.items[1].description` sentence — no date, no seat count, no new fact,
`aria-hidden="true"` throughout. No unused imports left in `cta-final.tsx`. `tsc --noEmit`
reports no errors in either file. Commit with message
`#feat: replace cta-final ce-qui-se-passe-ensuite column with mini-calendar illustration`.
  </done>
</task>

<task type="auto">
  <name>Task 4 (COMMIT 4): FAQ in two columns</name>
  <files>src/components/sections/faq.tsx</files>
  <action>
Per brief C-08 and maquette `.faq`/`.q` (lines ~850-869). Keep both queries (`getSection`,
`getSectionItems`), the `faqDonneesSchema` zod boundary, the error guard, the full
`Accordion`/`AccordionItem`/`AccordionHeader`/`AccordionTrigger`/`AccordionPanel` family
(D-42, no second accordion implementation), `defaultValue={entries[0] ? [entries[0].id] : []}`
(first entry open), and the closing sentence + `/contact` link content exactly as they
render today — only their position and the accordion's outer layout change.

1. Replace the current single-column structure (`SectionHeader` then `Accordion` then a
   centred closing paragraph) with a two-column grid at `lg`:
   `<div className="grid gap-8 lg:grid-cols-[0.8fr_1.2fr] lg:items-start lg:gap-16">`.
2. Left column (`<div className="lg:sticky lg:top-[100px]">`): move the section header
   here as a left-aligned block instead of the centred `SectionHeader` — reuse
   `SectionHeader` with `className="mx-0 max-w-none text-left"` (same pattern
   `confiance.tsx` already uses for its left-aligned header), passing the same
   `eyebrow`/`title`/`titleAccent` props (drop `className="max-w-[880px]"` since it no
   longer applies here). Immediately below it, move the closing paragraph here too:
   `<p className="mt-6 text-[length:var(--text-body)] leading-[var(--text-body--line-height)] text-[var(--muted-ink)]">{landing.faq.cloture.question}{" "}<Link href="/contact" className="font-semibold text-[var(--deep)] underline underline-offset-4 hover:text-[var(--violet-ink)]">{landing.faq.cloture.lien}</Link></p>`
   — same strings, same href (`/contact` stays; this is not part of the `/reservation`
   CTO decision, see brief §D "le contact reste joignable par la FAQ et le pied"), just
   relocated from below the accordion to the sticky left column.
3. Right column: the existing `<Accordion className="mt-10 max-w-[880px] gap-[0.9rem]" ...>`
   moves here unchanged in content, drop the now-inapplicable `mt-10 max-w-[880px]`
   centring classes (`className="gap-[0.9rem]"` is enough — the column's own grid track
   width now constrains it). Each `AccordionItem`'s existing className (tint at rest,
   white + `--hairline-2` on hover/`data-[open]`) stays unchanged — it already matches
   maquette `.q`.
4. On each `AccordionTrigger`, per `<interfaces>` (do not edit `accordion.tsx`): add
   `group` to its existing `className` string, and append
   `[&_svg[data-slot=accordion-chevron]]:hidden` to hide the built-in chevron for this
   call site only. Inside the trigger's children, after the existing question `<span>`,
   add a plus/minus toggle: `<span aria-hidden="true" className="relative flex size-[26px] shrink-0 items-center justify-center rounded-full bg-[var(--lav)] text-[var(--deep)]"><span className="absolute h-0.5 w-3 rounded-full bg-current" /><span className="absolute h-0.5 w-3 rounded-full bg-current transition-transform duration-[var(--duration-base)] ease-[var(--ease-brand)] group-data-[panel-open]:rotate-0 rotate-90" /></span>`
   — the first inner span is the permanent horizontal bar, the second is the vertical bar
   that rotates from 90deg (vertical, forming a "+") to 0deg (aligned with the horizontal
   bar, forming a "−") when the ancestor trigger carries `data-panel-open` (Base UI sets
   this on open — verified per `<interfaces>`; re-read `accordion.tsx` before writing this
   if any doubt remains about which element receives `data-panel-open` vs `data-open`).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "faq" || echo "no faq type errors"</automated>
  </verify>
  <done>
`faq.tsx` renders a two-column layout at `lg:` breakpoint and above (sticky left header +
closing link, `Accordion` on the right) and a single stacked column below `lg:`; all seven
entries retain their existing content, the first opens by default, the zod boundary and
error guard are unchanged, and each trigger shows a custom plus/minus toggle (built-in
chevron hidden for this call site only, `accordion.tsx` untouched) that flips via
`group-data-[panel-open]:`. `tsc --noEmit` reports no errors in this file. Commit with
message `#feat: restyle faq as a two-column sticky layout`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| Server component render → static HTML | All four sections/illustration read Supabase content and locale JSON server-side; no new client-side data entry point introduced this run |
| Confiance href override | `confiance.tsx` overrides one item's link target at render time based on a static `cle` match, not user input — no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260907n7j-01 | Information Disclosure | `mini-calendrier.tsx` | accept | Pure decorative SVG/HTML, no data, `aria-hidden`, no fetch, no date/seat facts (D-45/D-61/D-66) |
| T-260907n7j-02 | Tampering | Confiance href override | accept | Static string comparison on a known enum-like `cle` value from a trusted server-side query result, not user-controlled input |
| T-260907n7j-03 | Denial of Service | `pulse-live`/chevron-rotation CSS transitions | accept | CSS-only, capped, honours `prefers-reduced-motion` via existing globals.css rules, no unbounded loop |

No package installs this run — package legitimacy gate not applicable.
</threat_model>

<verification>
Run once, after all four commits land, in this exact order. Do not run any of these in
the background; do not start a dev server. Assert only on exit codes and reported
build/log output — never by grepping source to "confirm" a UI change happened (D-74/D-79).

1. `npm run lint` — must report zero problems.
2. `npm run typecheck` — must pass.
3. `npm run build` — must pass. Report the prerendered size of `/` from the build output.
   A `/` around 44 KB means an empty database (content queries returned nothing) — report
   this explicitly as a **FAILURE**, not a success, even though the build itself exited 0.
   Expected order of magnitude given the brief: roughly 250 KB.
4. `npm run content:check` — expected to exit 1, naming the same pre-existing placeholders
   as before this run (confirm the actual current baseline by noting `content:check`'s
   output before this run's first commit if unsure) and **zero new** placeholders. If a new
   placeholder name appears, that is a regression — report it, do not silently accept it.
5. `git status --short` must be clean (all four commits landed, nothing left uncommitted).
6. `git rev-parse --abbrev-ref HEAD` must read `cto/landing-v3` (no branch was created or
   switched).

Never run `git push`. If any fact in the brief conflicts with the code on the ground while
executing any task above, STOP and report precisely rather than inventing a substitute.
</verification>

<success_criteria>
- Four commits exist on `cto/landing-v3`, one per task above, each with a `#<type>: ...`
  message per project CLAUDE.md format, committed before verification runs.
- Format et modalites renders on `Section tone="wash"` with a tablist of five steps (a
  resume line beneath, no five-dot connector), and a night-chrome preview window
  (`--shadow-2`, not `--shadow-4` — D-68 fix) with a mint-labelled red-dot "En direct" pill.
- Confiance renders on `Section tone="default"`, a gradient-avatar trainer card, and a
  single-column stack of two-column fact cards with per-item gradient tiles, a
  bold chevron toggle, and the `appel-decouverte` proof link overridden to `/reservation`
  (D-28) via a call-site override with its own comment — no seed/JSON edit.
- CTA final's right column is the new `mini-calendrier.tsx` illustration (no date, no seat
  count, reused existing strings only); the panel/eyebrow/H2/lead/buttons are otherwise
  unchanged from today.
- FAQ renders two columns at `lg:` and above (sticky header+link left, accordion right)
  and stacks below `lg:`; all seven entries, the zod boundary, and the first-open default
  are unchanged; each trigger shows a custom plus/minus toggle without editing
  `accordion.tsx`.
- No new translation keys anywhere except the seven day-initial letters in
  `mini-calendrier.tsx`, which carry no locale key (chrome, not content).
- No `any` type anywhere in the touched/new files.
- Final verification checklist (above) run once, after all four commits, with results
  reported honestly (including the `/` prerender-size sanity check and the
  `content:check` baseline comparison).
</success_criteria>

<output>
No SUMMARY.md required for this quick task — report results directly at the end of
execution: the four commit hashes/messages, and the outcome of each of the six final
verification steps (pass/fail, with the reported `/` prerender size and the
`content:check` placeholder list).
</output>
