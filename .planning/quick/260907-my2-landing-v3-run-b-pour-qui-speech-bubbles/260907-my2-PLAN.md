---
task: 260907-my2
type: quick
branch: cto/landing-v3
autonomous: true
files_modified:
  - src/components/sections/pour-qui.tsx
  - src/components/sections/competences.tsx
  - src/components/sections/programme-accordion.tsx
  - src/components/illustrations/constellation.tsx
  - src/app/globals.css
---

<objective>
Run B of 3 for "Landing v3 - Le parcours". Run A (already landed on `cto/landing-v3`)
shipped v3 tokens, `Section tone="night"`, the new hero, rail and sticky bar. This run
ports the three middle-of-page sections to the v3 maquette: Pour qui becomes speech
bubbles, Competences becomes the night wall with a constellation backdrop, Programme
becomes a drawn timeline. No branch change. No push. No dev server.

Purpose: close out the "milieu de page" scope row (brief §G, Run B) with three atomic
commits, each independently buildable.
Output: three commits on `cto/landing-v3`; a final verification pass (lint, typecheck,
build, content:check, git status) run once after all three.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Source brief (read-only, external to repo — do not modify):
@C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-09-07-brief-refonte-landing-v3.md
@C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-09-07-maquette-landing-v3.html

Code to change:
@src/components/sections/pour-qui.tsx
@src/components/sections/competences.tsx
@src/components/sections/programme-accordion.tsx

Read-only reference:
@src/components/sections/section.tsx
@src/components/ui/accordion.tsx
@src/components/motion/card-spotlight.tsx
@src/components/motion/reveal.tsx
@src/components/illustrations/flux-achat.tsx
@src/app/globals.css
@src/locales/fr/common.json
@src/locales/fr/landing.json
</context>

<interfaces>
<!-- Facts already verified in the repo at the time this plan was written.
     Executor must still re-verify anything time-sensitive per the
     STOP-and-report rule below — these are a starting point, not a
     substitute for reading the live files. -->

**`Section` (`src/components/sections/section.tsx`)** already has `tone="night"`
(shipped in Run A): `--night` fill, white text, masked dot grid on `::before`, two
radial glows on `::after`. The inner content wrapper is
`<div className="relative mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 z-[1]">` when
`tone="night"` (z-[1] only applies for night). `SectionHeader` already reads
`data-tone` via `in-data-[tone=night]:` Tailwind variants for eyebrow/H2 accent/lead —
no changes needed there.

**Tokens already in `globals.css`** (Run A, `:root`, ~line 209-219): `--night`,
`--night-2`, `--glass`, `--glass-2`, `--glass-line`, `--glass-line-2`, `--grid-dot`,
`--grid-dot-night`, `--live`. Keyframes already in `@layer components` (~line 325-354):
`@keyframes comet`, `@keyframes pulse-live`, `@keyframes dash`. Do not add any of these
again — add nothing to `:root`, this run only adds `@keyframes twinkle` plus a handful
of `@layer components` rules (timeline line, node reveal wiring) as detailed per task.

**Accordion contract (`src/components/ui/accordion.tsx`, confirmed current, fixed
2026-09-05 in commit `3d9f796`):** Base UI puts the **open** state as a `data-open`
attribute on `AccordionItem` (also readable on `AccordionPanel`/`AccordionHeader`),
and the **trigger-side** open indicator as `data-panel-open` on `AccordionTrigger`
itself. `programme-accordion.tsx` already styles the item with `data-[open]:bg-white
data-[open]:border-[var(--hairline-2)]`, and `accordion.tsx`'s `accordionVariants`
already rotates the chevron on `data-[panel-open]:`. This is correct — re-verify by
reading `accordion.tsx` before touching Task 3, do not assume this summary is still
accurate if the file has changed since.

**Translation keys confirmed to exist** (no new strings needed): `common.json` →
`actions.prendreRdv` ("Prendre RDV"), `actions.voirProgramme` ("Voir le programme"),
`actions.reserver` ("Réserver"), `actions.enSavoirPlus` ("En savoir plus").
`landing.json` → `programme.auProgramme` ("Au programme"). If any task below needs a
string not in this list, STOP and report — do not invent a key or hardcode text.

**`CardSpotlight` (`src/components/motion/card-spotlight.tsx`):** the hover halo
opacity is a hardcoded literal `"0.1"` inside the component's own `onMouseMove`
handler (line ~37), not a prop. It does not accept a configurable opacity. Per the
task brief, Task 2 leaves it at 0.1 (do not edit `card-spotlight.tsx` to add a prop —
that would be scope creep beyond this run).

**`[data-slot="competence-rule"]` is currently DEAD CSS**: the rule exists in
`globals.css` (~line 293-321, `.reveal [data-slot="competence-rule"]` /
`.reveal.in [data-slot="competence-rule"]` / per-`data-d` transition-delay variants,
gradient trace using `--tuile-a`/`--tuile-b`) but **no element in
`competences.tsx` currently carries that `data-slot`**. Task 2 must ADD the element,
not merely "keep" it — correct this against the brief's wording, which assumes it
already exists in JSX.

**Illustration component precedent (`src/components/illustrations/flux-achat.tsx`):**
server component (no `"use client"`), pure inline SVG/CSS, `aria-hidden="true"`,
reads strings from `landing`/`common` JSON only when a label is genuinely new schema
copy — otherwise carries zero strings. `constellation.tsx` (Task 2) follows the same
shape but needs **no strings at all** (it is pure decoration, all circles/paths are
`aria-hidden`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 (COMMIT 1): Pour qui as speech bubbles</name>
  <files>src/components/sections/pour-qui.tsx</files>
  <action>
Rework the card markup inside the existing `.map` in `PourQui` (`src/components/sections/pour-qui.tsx`) to match maquette `.bubble` (brief C-02, maquette lines ~612-641), while preserving 100% of the existing data/logic layer:

KEEP UNCHANGED: both `getSection`/`getSectionItems` calls, the `!sectionResult.ok || !itemsResult.ok` error guard rendering `EmptyState`, `PROFIL_ACCENTS` + `FALLBACK`, the `profilDonneesSchema.safeParse` / `hasAccroche` / `accroche` derivation (accroche falls back to `titre` exactly as today — do not change this fallback), the outer `<ul className="mt-10 grid ... lg:grid-cols-6">` with the 2+3 span logic (`index < 3 ? "lg:col-span-2" : "lg:col-span-3"`), the `Reveal` wrapper with its `dataD` stagger, the outer `<Link href="/programme">` with its `aria-label`, and the D-49 hover/focus expansion mechanism (`grid-rows-[0fr]` → `group-hover:grid-rows-[1fr]` / `group-focus-within:grid-rows-[1fr]`, always `grid-rows-[1fr]` under `[@media(hover:none)]`).

REMOVE: the `<CardSpotlight />` element and its import — the bubble uses its own tinted-quote hover treatment instead (D-100's spotlight stays only on Competences' cards, Task 2).

RESTRUCTURE the card's inner content (still inside the same `data-slot="card"` div, same CSS custom properties `--tuile-a`/`--tuile-b`/`--card-ink` already set from `accentInk`) into three stacked blocks matching maquette `.quote` / `.who` / `.more` / `.cta`:
1. A quote panel: a `<div>` styled per maquette `.quote` (`background: var(--tint)`, `border-radius: 18px`, padding ~`1rem 1.1rem 1.05rem`) containing `{accroche}` in the display face (reuse the existing `CardTitle` font treatment: `font-heading text-[length:var(--text-card)] leading-[var(--text-card--line-height)] font-bold tracking-[-0.02em]`), plus two decorative pseudo-elements achieved via two absolutely-positioned `aria-hidden` `<span>`s (not `::before`/`::after`, since Tailwind arbitrary pseudo-classes for `content` are awkward here — a real DOM span with `aria-hidden="true"` is acceptable and matches the flux-achat.tsx precedent of inline decorative markup): (a) an opening-quote glyph `"` positioned top-right, sized ~`text-[3rem]` line-height 1, colour `color-mix(in srgb, var(--tuile-a) 35%, transparent)`; (b) a tail: a small `18px` square rotated/clipped with `clip-path: polygon(0 0, 100% 0, 0 100%)`, `background: inherit` (inherits the quote panel's own background so it reads as the panel's corner), positioned `left-[26px] -bottom-[9px]`. On card hover, tint the quote panel to `color-mix(in srgb, var(--tuile-a) 9%, white)` (matches maquette `.bubble:hover .quote`) — implement via `group-hover:bg-[color-mix(...)]` on the quote div; keep the CSS var-driven approach already used elsewhere in this file (no raw hex).
2. An author row below the quote panel: `<div className="mt-[1.15rem] flex items-center gap-[0.85rem]">` containing the existing 52px gradient pictogram tile (`Picto` element, unchanged: `size-[52px]`, gradient background from `--tuile-a`/`--tuile-b`, same hover/focus-within scale+rotate transform already present) and, next to it, the profile `titre` in a `<b>`/`<span>`-equivalent styled `font-semibold text-[length:var(--text-small)] text-[var(--card-ink)]` — no "Profil" caption, no new string, just `{titre}`.
3. The expanding description block: keep the existing `grid-rows-[0fr]`→hover/focus `[1fr]` wrapper exactly as today, containing `CardDescription` with `{profil.description}` exactly as today.
4. The bottom row: keep the existing `mt-auto flex items-center justify-between [@media(hover:none)]:hidden` row with `{hasAccroche ? titre : common.actions.enSavoirPlus}` (unchanged logic — do not touch this ternary) and the `ChevronDown` icon **replaced** by an arrow icon that slides right on hover (use lucide `ArrowRight`, `className="size-4 ... transition-transform duration-[var(--duration-reveal)] ease-[var(--ease-brand)] group-hover:translate-x-1 group-focus-within:translate-x-1"` — matches maquette `.bubble:hover .cta svg{transform:translateX(4px)}`; this replaces the existing rotating chevron, do not keep both icons).

Card-level hover treatment: change the outer card's hover transform from the current `hover:-translate-y-[7px]` (keep this) and add `hover:border-[color-mix(in_srgb,var(--tuile-b)_40%,transparent)]` (border tints toward the tile colour on hover, per maquette `.bubble:hover{border-color:...}`) — the existing `border border-[var(--hairline)]` base stays.

Do not add, remove, or rename any translation key. Every string in the rewritten JSX must be one of: `accroche`, `titre`, `profil.description`, `common.actions.enSavoirPlus` — identical sources to today, no literal French text typed into the file.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "pour-qui" || echo "no pour-qui type errors"</automated>
  </verify>
  <done>
`pour-qui.tsx` renders five bubble cards (quote panel with tail + quote mark, author row with picto + titre, expanding description, bottom CTA row with sliding arrow), `CardSpotlight` import and usage removed from this file, zero new translation keys, `tsc --noEmit` reports no errors in this file. Commit with message `#feat: restyle pour-qui section as speech bubbles` (per project CLAUDE.md commit format `#<type>: <one sentence>`).
  </done>
</task>

<task type="auto">
  <name>Task 2 (COMMIT 2): Competences as the night wall</name>
  <files>src/components/sections/competences.tsx, src/components/illustrations/constellation.tsx, src/app/globals.css</files>
  <action>
**Step 1 — `src/components/illustrations/constellation.tsx` (new server component, no `"use client"`):**
Render the exact SVG from the maquette (lines ~648-654 of `2026-09-07-maquette-landing-v3.html`, class `.constel`): `viewBox="0 0 1440 900"`, `preserveAspectRatio="xMidYMid slice"`, root wrapper `<svg aria-hidden="true" className="absolute inset-0 h-full w-full pointer-events-none opacity-60">` (opacity .6 per brief). Port verbatim: the `<defs><linearGradient id="cg" ...>` (rename the `id` to something collision-safe, e.g. `constel-link-gradient`, and update its two `<path>` consumers' `stroke="url(#...)"` references accordingly — do not reuse the maquette's bare `id="cg"` in case another SVG on the page ever defines the same id), the twelve faint `<line>` elements (group, stroke `currentColor`-equivalent — use a token-based stroke, e.g. `stroke="rgba(255,255,255,.14)"` matching maquette `.constel g{stroke:...}` — read the maquette's `.constel` CSS block just above line 648 for the exact line/circle/spark stroke and fill values and port them as inline SVG attributes since this is a server component with no CSS file of its own for that scoping, except the new `@keyframes twinkle` which goes in globals.css per Step 3), the two gradient dashed `<path class="link">` elements (animate via the *existing* `dash` keyframe: `style={{ animation: "dash 18s linear infinite" }}` — check the maquette's `.constel .link` rule for its exact duration/timing-function and reuse `var(--ease-brand)` in place of any non-brand easing per D-17, never introduce a second curve), the fifteen `<circle>` node dots (five `class="big"` get the azur-soft stroke, ten plain get violet-l stroke at 75% per the brief), and the five `<circle class="spark">` dots with their exact `cx`/`cy`/`r`/`style="animation-delay:...">` values, now animated by the new `twinkle` keyframe (Step 3) instead of a CSS class selector — apply `style={{ animation: "twinkle 5s var(--ease-brand) infinite alternate", animationDelay: "-2s" }}` etc. per spark, since this is a standalone component without access to a `.spark` class rule unless you also add a `.spark` rule under `@layer components` in Step 3 (either approach is acceptable — prefer adding a `[data-slot="constellation-spark"]` class-equivalent in globals.css so the reduced-motion rule in Step 3 can target it declaratively, matching the file's existing `[data-slot="..."]` idiom rather than inline `style` for the animation-name itself; inline `animationDelay` per-spark for the stagger is fine since each spark's delay is a distinct literal).
Export the component: `export { Constellation };` (or default export if that breaks the illustrations-folder pattern — check `flux-achat.tsx`'s export style and mirror it exactly).

**Step 2 — `src/app/globals.css`, `@layer components`:**
Add `@keyframes twinkle { from { opacity: .25; } to { opacity: 1; } }` (no easing inside the keyframe itself, per the `pulse-live`/`dash` precedent already in this file — the easing lives in the consumer's `animation` shorthand as `var(--ease-brand)`). Add a rule for whatever slot/class you chose in Step 1 for the sparks, e.g.:
```
[data-slot="constellation-spark"] {
  animation: twinkle 5s var(--ease-brand) infinite alternate;
}
```
(adjust to match whatever selector Step 1 actually emits — keep them consistent). In the existing `@media (prefers-reduced-motion: reduce)` block (~line 356-388), add: sparks `display: none`, and the two `.link` gradient dash paths stop animating (either add `[data-slot="constellation-link"] { animation: none; }` if you gave the link paths their own data-slot, or fold them into the existing catch-all if simpler — pick whichever is more consistent with the file's existing pattern of listing `data-slot` selectors comma-separated in the reduced-motion block, e.g. the existing `[data-slot="hero-spotlight"], [data-slot="card-spotlight"], [data-slot="comet"] { display: none; }` group).
Do NOT add any new design token to `:root` — this section only touches `@layer components` and the reduced-motion block.

**Step 3 — `src/components/sections/competences.tsx`:**
Wrap the section's return in `<Section tone="night" data-section="competences">` (currently `tone="default"` — change this one prop plus keep `data-section="competences"`). Render `<Constellation />` as the **first child inside `Section`**, before `SectionHeader`. Because `Section`'s inner content wrapper (the div children actually land in) is itself `position: relative` and — for `tone="night"` — carries `z-[1]`, an absolutely-positioned `z-index: auto` first child paints ABOVE later in-flow siblings per CSS stacking rules (auto/0 z-index positioned descendants paint after in-flow non-positioned content, regardless of DOM order) — the brief's "first child so `.wrap` content stacks above it" will NOT work with a bare `absolute inset-0` and no explicit z-index. To get the constellation behind the real content: give the `<Constellation />` wrapper an explicit `className="... z-0"` and wrap the section's actual content (the `SectionHeader`, the `<ul>` grid, and the new CTA row from Step 4) in a single `<div className="relative z-10">` sibling immediately after `<Constellation />`. Verify this visually is out of scope for this run (no dev server) — just implement the z-index pairing described here exactly, since it is the only construction that reliably stacks correctly given `section.tsx`'s current structure (re-read `section.tsx` before writing this to confirm the wrapper is still `position: relative` — if it has changed since this plan was written, STOP and report the discrepancy rather than guessing).

KEEP UNCHANGED inside the wall: both `getSection`/`getSectionItems` calls, the error guard, `CARD_TEINTES`, `resolvePictogram`, the `Reveal` stagger wrapper, `<CardSpotlight />` (leave its hardcoded 0.1 opacity as-is — do not edit `card-spotlight.tsx`).

Per-tile (maquette `.skill`) changes to the `data-slot="card"` div: replace `bg-white` with `bg-[var(--glass)]`, replace `border-[var(--hairline)]` with `border-[var(--glass-line)]`, add `backdrop-blur-[6px]`, keep `rounded-[22px]` and `min-h-[13.5rem]` (already present), replace the hover transform/shadow with `hover:bg-[var(--glass-2)] hover:border-[var(--glass-line-2)] hover:-translate-y-[7px]` (drop the coloured box-shadow hover, the glass tile brief doesn't call for it — keep whatever base shadow already exists or drop it to match the flatter glass look, your call, but do not invent a new box-shadow token). Gradient icon tile: add `shadow-[0_14px_34px_-10px_color-mix(in_srgb,var(--tuile-b)_80%,transparent)]` (coloured drop shadow). Title (`item.titre` paragraph): change `text-[var(--ink)]` to `text-white`. Description (`item.description` paragraph): change `text-[var(--ink-soft)]` to `text-white/72`.

ADD the trace rule element (currently missing from this file — see `<interfaces>` note above): a `<span aria-hidden="true" data-slot="competence-rule" className="mt-auto h-0.5 w-full rounded-full bg-[linear-gradient(90deg,var(--tuile-a),var(--tuile-b))]" />` as the last child inside the card, after the title/description block — this is what the already-existing (currently dead) globals.css rule animates via `.reveal.in [data-slot="competence-rule"]`.

**Step 4 — CTA row:** under the `</ul>`, add a centred row: `<div className="mt-10 flex flex-wrap items-center justify-center gap-3">` containing a primary white button (`Button` component or a styled `<Link>` — check `Button`'s existing `variant` options in `src/components/ui/button.tsx` first; if no white-on-dark variant exists, apply overriding `className` rather than inventing a new `variant` entry in `buttonVariants` — that would be scope creep) with text `{common.actions.prendreRdv}`, `href="/reservation"`, `data-magnetic="true"` (matches the existing magnetic-CTA pattern used elsewhere, e.g. `programme-accordion.tsx`'s reservation button), and a glass-outline button with text `{common.actions.voirProgramme}`, `href="/programme"`, styled with `border-[var(--glass-line)] bg-[var(--glass)] text-white hover:bg-[var(--glass-2)]` (or nearest equivalent using the `outline`/`ghost` variant as a base plus className overrides — again prefer className overrides over new variants). Both labels must come from the confirmed-existing `common.actions.prendreRdv` / `common.actions.voirProgramme` keys — do not add new ones.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "competences|constellation" || echo "no type errors"</automated>
  </verify>
  <done>
`competences.tsx` renders inside `Section tone="night"`, six glass tiles with white text, coloured icon-tile shadow, a `data-slot="competence-rule"` trace element per tile, and a two-button CTA row beneath the grid using only existing translation keys. `src/components/illustrations/constellation.tsx` exists, exports a server component with no `"use client"`, renders the full maquette SVG (twelve lines, two animated gradient link paths, fifteen node circles, five twinkling sparks), and is mounted as the section's backdrop with an explicit z-index pairing (`z-0` on the illustration, `relative z-10` on the content wrapper). `globals.css` gained `@keyframes twinkle` plus its consumer rule under `@layer components`, and sparks/link paths are silenced under `prefers-reduced-motion`, with no new `:root` tokens. `tsc --noEmit` reports no errors in these files. Commit with message `#feat: restyle competences section as night wall with constellation backdrop`.
  </done>
</task>

<task type="auto">
  <name>Task 3 (COMMIT 3): Programme as a drawn timeline</name>
  <files>src/components/sections/programme-accordion.tsx, src/app/globals.css</files>
  <action>
**Step 0 — re-verify the accordion open-state contract before writing any styling.** Read `src/components/ui/accordion.tsx` in full (again, live, not from memory of this plan) and confirm: which element receives `data-open` (should be `AccordionItem`/its descendants per Base UI's item-open state) and which receives `data-panel-open` (should be `AccordionTrigger`, driving the chevron rotation already wired in `accordionVariants`). If the contract differs from what's documented in `<interfaces>` above (e.g. because the file changed after this plan was written), STOP and report the actual contract precisely instead of guessing or copying this plan's assumption.

**Step 1 — `src/app/globals.css`, `@layer components`:** add a rule keyed on `.reveal.in [data-slot="timeline-line"]` that draws the vertical line: base state (outside `.in`) is `transform: scaleY(0); transform-origin: top; transition: transform 1.6s var(--ease-brand);` and the `.in` variant sets `transform: scaleY(1);`. The `[data-slot="timeline-line"]` element itself lives in the component markup (Step 2) — this CSS only supplies the transform/transition pair, following the exact pattern already used for `[data-slot="competence-rule"]` a few lines above it in the same file. Note `[data-slot="timeline-line"]` already has a `transform: none;` override inside the existing `prefers-reduced-motion` block (line ~385) — do not add a duplicate rule there, it is already covered.

**Step 2 — `src/components/sections/programme-accordion.tsx`:** restructure the module list into the maquette `.tl`/`.mod` composition while keeping unchanged: all four queries (`getSection`, `getModules`, `getSectionItems` ×2), the error guard, the `Accordion`/`AccordionItem`/`AccordionHeader`/`AccordionTrigger`/`AccordionPanel` family (no second accordion, D-42), `defaultValue={modules[0] ? [modules[0].id] : []}` (first module open), the hours badge (`formatHours`), the PDF ghost button, and the primary `common.actions.reserver` button with `href="/reservation"` and `data-magnetic="true"` (already correct today, keep as-is).

Wrap the whole module list in a `<Reveal as="div" className="relative mx-auto mt-11 max-w-[54rem]">` (the `Reveal` wrapper is what lets `RevealScope`'s `IntersectionObserver` add `.in`, which the Step 1 CSS keys off — do not implement a separate observer). Inside it, add the line element: `<span aria-hidden="true" data-slot="timeline-line" className="absolute left-[27px] top-5 bottom-5 w-0.5 bg-[linear-gradient(180deg,var(--violet),var(--sky-ink)_60%,var(--mint-ink))]" />`. Change the `Accordion` wrapper's className from a flat `gap-[0.9rem]` list to a `grid` (needed so each module row can use `grid-cols-[56px_1fr]` internally while the outer list still stacks vertically — keep `Accordion`'s own default `flex flex-col gap-2` from `accordion.tsx` unless it conflicts, in which case override with `className="relative gap-4"` and let each `AccordionItem`'s own row markup carry the two-column grid).

Each module's `Reveal`-wrapped row becomes a `grid-cols-[56px_1fr]` grid: first column is the 38px gradient pin (`w-[38px] h-[38px] rounded-[12px] bg-[linear-gradient(135deg,var(--violet),var(--deep))] text-white font-heading font-extrabold text-[length:var(--text-micro)] tabular-nums flex items-center justify-center`, with the maquette's white ring via `shadow-[0_0_0_6px_white,0_10px_20px_-10px_rgba(99,91,255,.7)]`), showing `{String(index + 1).padStart(2, "0")}` (reuse the exact zero-pad logic already in the file, currently on the small badge inside `AccordionTrigger` — move it here, remove the old smaller `size-[34px]` badge from inside the trigger since the pin now carries that role). Second column is the existing `AccordionItem`, restyled: base state `bg-[var(--tint)] border-[var(--hairline)]`, hover **and** open state `bg-white border-[var(--hairline-2)]` using the verified contract from Step 0 (`hover:` plus `data-[open]:` on whichever element actually carries `data-open` per Step 0's findings — do not assume it's still `AccordionItem` without having just re-read the file), and when open additionally apply `data-[open]:shadow-[var(--shadow-1)]` (module item gets `--shadow-1` when open, per maquette `.mod .item[data-open]{box-shadow:var(--shadow-1)}` — confirm `--shadow-1` exists in `globals.css` `@theme inline`, it does per this file's line ~81).

Keep `AccordionPanel` content unchanged: `module.description`, the `landing.programme.auProgramme` label, and the `module.contenu` list with its existing bullet styling.

Do not change the PDF button or the reservation button beyond whatever wrapper/spacing adjustments are needed to sit under the restructured list (`mt-8 flex flex-wrap justify-center gap-3` row, unchanged from today).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "programme-accordion" || echo "no type errors"</automated>
  </verify>
  <done>
`programme-accordion.tsx` renders a `max-w-[54rem]` centred timeline: a `Reveal`-wrapped container with a `data-slot="timeline-line"` vertical gradient line that scales in via the new `.reveal.in [data-slot="timeline-line"]` CSS rule, five `grid-cols-[56px_1fr]` module rows each with a `01`..`05` gradient pin (white ring) and a restyled `AccordionItem` (tint at rest, white + `--hairline-2` + `--shadow-1` when open, verified against the actual live `data-open`/`data-panel-open` contract in `accordion.tsx` — not assumed), same Accordion family, same first-module-open default, same PDF/reservation buttons. `globals.css` gained one new `@layer components` rule for `.reveal.in [data-slot="timeline-line"]` (no duplicate reduced-motion entry, since one already exists). `tsc --noEmit` reports no errors in this file. Commit with message `#feat: restyle programme section as a drawn timeline`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| Server component render → static HTML | All three sections read Supabase content server-side; no new client-side data entry point introduced this run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260907my2-01 | Information Disclosure | `constellation.tsx` | accept | Pure decorative SVG, no data, `aria-hidden`, no external fetch |
| T-260907my2-02 | Denial of Service | `@keyframes twinkle` / dash animations | accept | CSS-only animation, capped opacity, honours `prefers-reduced-motion`, no unbounded loop or layout thrash |

No package installs this run — package legitimacy gate not applicable.
</threat_model>

<verification>
Run once, after all three commits land, in this exact order. Do not run any of these in
the background; do not start a dev server. Assert only on exit codes and reported
build/log output — never by grepping source to "confirm" a UI change happened (D-74/D-79).

1. `npm run lint` — must report zero problems.
2. `npm run typecheck` — must pass.
3. `npm run build` — must pass. Report the prerendered size of `/` from the build output.
   A `/` around 44 KB means an empty database (content queries returned nothing) — report
   this explicitly as a **FAILURE**, not a success, even though the build itself exited 0.
   Expected order of magnitude given the brief: roughly 250 KB.
4. `npm run content:check` — expected to exit 1, naming the same pre-existing placeholders
   as before this run (`hero.preuve.formateur`, `hero.preuve.groupe`, formateur, plafond —
   confirm the actual current baseline by running it once before this run's commits if
   unsure) and **zero new** placeholders. If a new placeholder name appears, that is a
   regression — report it, do not silently accept it.
5. `git status --short` must be clean (all three commits landed, nothing left uncommitted).
6. `git rev-parse --abbrev-ref HEAD` must read `cto/landing-v3` (no branch was created or
   switched).

Never run `git push`.
</verification>

<success_criteria>
- Three commits exist on `cto/landing-v3`, one per task above, each with a `#<type>: ...`
  message per project CLAUDE.md format.
- Pour qui renders as speech bubbles per maquette `.bubble` (quote panel + tail + quote
  mark, author row with existing picto tile, expanding description, sliding-arrow CTA
  row), `CardSpotlight` removed from this section, D-49 hover/focus expansion preserved,
  zero new translation keys.
- Competences renders on `Section tone="night"` as glass tiles per maquette `.skill`,
  with a `constellation.tsx` SVG backdrop (twelve lines, two animated link paths, fifteen
  nodes, five twinkling sparks honouring reduced-motion), a `data-slot="competence-rule"`
  trace element per tile, and a two-button CTA row using only existing translation keys.
- Programme renders as a drawn timeline per maquette `.tl`/`.mod` (vertical gradient line
  that scales in via `.reveal.in`, five gradient pins, restyled accordion items verified
  against the live `data-open`/`data-panel-open` contract), same Accordion family, same
  content, same buttons.
- No new design tokens in `:root`; only `@keyframes twinkle` plus consumer rules added to
  `@layer components` in `globals.css`.
- No `any` type anywhere in the touched files.
- Final verification checklist (above) run once, after all three commits, with results
  reported honestly (including the `/` prerender-size sanity check and the
  `content:check` baseline comparison).
</success_criteria>

<output>
No SUMMARY.md required for this quick task — report results directly at the end of
execution: the three commit hashes/messages, and the outcome of each of the six final
verification steps (pass/fail, with the reported `/` prerender size and the
`content:check` placeholder list).
</output>
