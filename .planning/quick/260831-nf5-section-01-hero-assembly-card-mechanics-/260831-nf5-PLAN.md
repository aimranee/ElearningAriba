---
quick_id: 260831-nf5-section-01-hero-assembly-card-mechanics-
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales/fr/common.json
  - src/components/motion/assembly-card.tsx
  - src/components/sections/hero.tsx
  - src/components/motion/assembly-connectors.tsx
autonomous: true
requirements: []
---

<objective>
Give the hero assembly card its mechanics back, per
`ariba-cto/notes/2026-08-31-brief-section-01-mecanique-carte-assemblage.md`
(read in full — authoritative, treat as locked): a five-module carousel
inside the violet result card, an honest progress bar tied to
`module.position`, a three-row checklist (the existing pills' labels) that
validates once on viewport entry, and hand-authored connector cables that
terminate on a small node just before the card's edge instead of crossing
onto it. All new client behavior is one island, patterned on
`typewriter.tsx` (refs, one `useEffect`, direct DOM writes, `matchMedia`,
height-lock) and `assembly-connectors.tsx` (IntersectionObserver-driven
entrance, `--ease-brand` only). `hero.tsx` stays a server component and
keeps reading `getModules()`; it now also passes the modules to the new
island as props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-08-31-brief-section-01-mecanique-carte-assemblage.md
@src/components/sections/hero.tsx
@src/components/motion/typewriter.tsx
@src/components/motion/assembly-connectors.tsx
@src/locales/fr/common.json
@src/lib/i18n/fr.ts
@src/lib/content/queries.ts

<interfaces>
From `src/lib/content/queries.ts` — the shape `getModules()` already returns,
ordered by `position`:
```typescript
export type ModuleContent = {
  id: string;
  cle: string;
  titre: string;
  description: string | null;
  dureeHeures: number;
  position: number;
  objectifs: string[];
  contenu: string[];
};
```

From `src/lib/i18n/fr.ts` — existing formatters, already used in
`hero.tsx` for the (now-removed) résumé line:
```typescript
export function formatNumber(value: number): string;
export function formatHours(value: number): string; // "17 h", unit-styled
```

From `src/locales/fr/common.json`, current `assemblage` block (to be edited
in Task 1):
```json
"assemblage": {
  "frameLabel": "Votre parcours SAP Ariba — assemblage",
  "statut": "Sessions ouvertes",
  "badge": "Parcours SAP Ariba",
  "resume": "{modules} modules · {heures} de formation live",
  "certification": "Préparation à la certification",
  "pills": [
    { "cle": "appel-decouverte", "label": "Appel découverte" },
    { "cle": "session-live", "label": "Session live" },
    { "cle": "support-pdf", "label": "Support PDF" }
  ]
}
```
`resume` and `certification` are each used exactly once, both in
`hero.tsx`, both inside the violet card content this plan replaces —
confirmed via full-repo search, safe to remove.

From `src/components/sections/hero.tsx`, the exact block Task 2 edits (the
violet result card, currently lines ~207-225): a `div` with the brand
gradient background containing a badge chip (`assemblage.badge`, kept), the
`resume` line (removed), and a single mint-check "certification" row
(removed) — both replaced by `<AssemblyCard />`.
</interfaces>

Geometry measured 2026-08-31 at 1440px (from the brief, authoritative for
Task 3): pastilles' right edge `x=44.6`, violet card's left edge `x=48.3`.
Cables now run `x=44.6` → `x≈47.8` at `y=18/50/82` (unchanged from the prior
fix), each ending on its own small circular node — they no longer reach the
card, so the white-at-40% survival trick this morning's fix added is now
dead code to remove, not preserve.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Locale keys + new client island (carousel, progress, checklist)</name>
  <files>src/locales/fr/common.json, src/components/motion/assembly-card.tsx</files>
  <action>
In `src/locales/fr/common.json`, inside the `assemblage` object: remove the
`resume` and `certification` keys (both go unused after Task 2) and add two
templates in their place — `moduleLigne` with value `"Module {n} · {heures}"`
(the honest replacement for the reference's "RNCP 35633 · Niveau 5" line,
built from `module.position` and `module.dureeHeures`) and `progression`
with value `"Module {n} sur {total}"` (the progress bar's label, per D
"Module N sur 5", total always from the data). Leave `frameLabel`,
`statut`, `badge`, and `pills` untouched.

Create `src/components/motion/assembly-card.tsx`, a `"use client"` island
exporting `AssemblyCard`, modeled on `typewriter.tsx` and
`assembly-connectors.tsx`: refs only, one `useEffect`, direct DOM writes,
`matchMedia("(prefers-reduced-motion: reduce)")` checked once at mount, no
per-frame `useState`. Props: `modules: ModuleContent[]` (from
`@/lib/content/queries`, already ordered by position),
`moduleLigneTemplate: string`, `progressionTemplate: string`, `pills:
readonly { cle: string; label: string }[]`. Import `formatNumber` and
`formatHours` from `@/lib/i18n/fr` for template interpolation (replace
`"{n}"`, `"{heures}"`, `"{total}"` literally — never string-concatenate the
label).

Render, in initial JSX (not left empty for `useEffect` to fill, unlike
`typewriter.tsx`'s word span — this content must be present in prerendered
HTML per the brief's own verification list):
- A height-locked title box wrapping a title element showing `modules[0].titre`,
  plus a hidden sizer element (same block width, `invisible absolute
  inset-x-0`, normal wrapping — not `nowrap`, since titles can wrap) that on
  mount cycles every module's `titre` through it, measures `offsetHeight`,
  and sets the title box's `minHeight` to the tallest value. Relock on
  `document.fonts.ready` and on a ~180ms-debounced `resize` listener, exactly
  as `typewriter.tsx`'s `lockWidth`/`RESIZE_DEBOUNCE_MS` pattern but for
  height instead of width.
- An id line below the title rendering `moduleLigneTemplate` interpolated
  with `modules[0].position` (via `formatNumber`) and
  `modules[0].dureeHeures` (via `formatHours`).
- A progress track (rounded bar, translucent white background) with a fill
  element whose initial inline width is
  `${(modules[0].position / modules.length) * 100}%`, and a label span
  showing `progressionTemplate` interpolated with `modules[0].position` and
  `modules.length`.
- A three-row checklist `<ul>`, one `<li>` per `pills` entry, each rendering
  a small check-circle (lucide `Check` icon inside a circle) plus the pill's
  `label` text. Initial (idle) classes: label text at reduced opacity no
  lower than 70% white (e.g. `text-white/75`) — never near-transparent, this
  exact card has broken contrast on the violet-to-indigo gradient three
  times already (white pastilles on white, a white console face on violet,
  violet cables on violet) by letting a dimmed element approach zero
  contrast; verify `text-white/75` reads clearly against both `--violet` and
  `--indigo` before committing to it. Check-circle idle: outlined only
  (`border border-white/40`, transparent fill, icon at a similar
  medium-white opacity), not a solid `--mint` fill.

In the single `useEffect` (guard on `modules.length === 0` and reduced
motion first):
- If `prefers-reduced-motion: reduce`: skip the interval entirely (module 1
  stays shown, already correct from initial render), and immediately apply
  the validated classes to all three checklist rows with no stagger and no
  transition (full white label, solid `--mint` check-circle with a white
  check icon) — do not attach the IntersectionObserver in this branch.
- Else: run the height-lock measurement described above, then
  `setInterval` at 3200ms advancing an index (`(index + 1) % modules.length`,
  wrapping to the first module after the fifth) that updates the title text,
  id line text, progress fill width, and progress label text via direct
  `textContent`/`style.width` writes, transitioning only via `opacity`
  driven by `transition: opacity Xms var(--ease-brand)` set once on mount
  on the title/id-line/fill/label elements (fade to 0, swap content on a
  short timeout, fade back to 1) — no second easing curve, no
  `ease-in`/`ease-out`/`ease-in-out`/`linear` anywhere in this file. Also
  create an `IntersectionObserver` (threshold ~0.3) on the card's outer
  ref, firing once: on intersect, stagger the three rows to validated state
  roughly 260ms apart (per-row `setTimeout`), then `observer.disconnect()`
  immediately so it never re-fires and the carousel's own looping never
  re-triggers it (explicit CTO arbitration in the brief — checklist
  animates once, carousel loops independently, they are not coupled).
- Cleanup (`useEffect` return): clear the interval, clear every pending
  `setTimeout` this effect created (fade swaps, checklist stagger, resize
  debounce) by tracking their handles, and disconnect the observer if it
  hasn't already self-disconnected.

No raw hex in this file — colors via `--mint`, `--card`, or Tailwind's
`white/NN` opacity utilities only. No hardcoded French strings — every
label is a prop or `pill.label`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
`src/locales/fr/common.json`'s `assemblage` block has `moduleLigne` and
`progression` in place of `resume`/`certification`, all other keys
unchanged. `src/components/motion/assembly-card.tsx` exists, exports
`AssemblyCard`, compiles clean under `tsc --noEmit`, and contains exactly
one `useEffect`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire AssemblyCard into hero.tsx</name>
  <files>src/components/sections/hero.tsx</files>
  <action>
In `hero.tsx`, remove the `resume` computation (the `assemblage.resume`
`.replace()` chain building the old "5 modules · 17h" line — now dead, the
carousel replaces it) and the `moduleCount`/`totalHours` locals it depended
on if nothing else in the file still reads them. Inside the violet result
card `div` (the one with the brand gradient background), keep the existing
badge chip (`assemblage.badge`) exactly as is, then replace the old résumé
line and the single mint-check "certification" row with
`<AssemblyCard modules={modulesResult.data} moduleLigneTemplate={assemblage.moduleLigne} progressionTemplate={assemblage.progression} pills={assemblage.pills} />`.
Import `AssemblyCard` from `@/components/motion/assembly-card`. Leave the
pills column (the left-hand list of three icon pastilles) completely
untouched — those stay the static "entries", per the brief, and are not the
checklist. Leave the outer card frame (traffic-light dots, `frameLabel`,
`statut` pill), the `AssemblyConnectors` mount point, and the H1/CTA/chips
column above untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
`hero.tsx` imports and renders `AssemblyCard` with the four props above;
no reference to `assemblage.resume`/`assemblage.certification` remains;
`tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cables terminate on a node, drop the white-crossing gradient</name>
  <files>src/components/motion/assembly-connectors.tsx</files>
  <action>
Replace the three `PATHS` entries: each cable now runs a short straight
segment from `x=44.6` to `x≈47.8` at its own unchanged y (`18`, `50`, `82`
for the violet/amber/mint cables respectively) instead of curving in to
converge at the card. They no longer touch the card (whose left edge is
`x=48.3`), so remove the `<defs>`/`<linearGradient>` block entirely and set
each `<path>`'s `stroke` directly to its own accent color (`var(--violet)`,
`var(--amber)`, `var(--mint)`) — solid for the full run, no more
white-at-40% stop (that stop existed only to survive the crossing onto the
card, which no longer happens). Keep `strokeWidth="2"` and
`vectorEffect="non-scaling-stroke"` unchanged.

Add one small filled `<circle>` per cable at its terminal point
(`cx≈47.8`, `cy` matching the cable's y), radius small enough to read as a
node rather than a dot (roughly matching the existing comet's `r="1.4"`),
`fill` set to that cable's own accent color — this is the "petit nœud
circulaire" the cable ends on. These nodes are static (no animation), always
visible once the trace has drawn in (or immediately under reduced motion,
alongside the already-static full-drawn paths).

Recolor the comet: it currently fills `var(--card)` (white, matched to the
old crossing); change each comet's `fill` to its own cable's accent color
(same value used for that cable's `stroke`), since it no longer travels onto
the white/violet card surface.

Drop the `z-[2]` class from the `<svg>`'s `className` (currently
`"pointer-events-none absolute inset-0 z-[2] hidden size-full sm:block"` →
remove `z-[2]`, keep the rest) — the cables no longer need to render above
the card's content, only above the card's own white background, which they
already do by DOM position without an explicit z-index. Do not touch the
`hidden sm:block` mobile guard, the trace-in `IntersectionObserver`, the
`TRACE_STAGGER_MS`/`COMET_STAGGER_S` timing, or the reduced-motion branch's
existing behavior (dasharray removed, comets hidden) beyond the color change
above.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
`assembly-connectors.tsx` has no `<linearGradient>`/`<defs>` block, no
`var(--card)` color value anywhere in the file, each path/comet pair uses a
single matching accent color, a terminal `<circle>` node exists per cable,
`z-[2]` is gone from the SVG's className, and `tsc --noEmit` is clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Full verification chain</name>
  <files>(none — verification only, reads build output)</files>
  <action>
Run `npm run lint`, `npm run typecheck`, and `npm run build` in sequence;
all three must exit 0, and the build output must still list 14 routes.
Then, without grepping component source to "prove" behavior (forbidden by
the brief — it pressures rewriting correct code to match an imagined grep
shape), check the actually-rendered artifacts:
- Read `.next/server/app/index.html` (or the hero route's prerendered HTML
  if hero is not on `/`) and confirm it contains the first module's title
  text and the three pill labels (`Appel découverte`, `Session live`,
  `Support PDF`) as rendered content — this is a content assertion on
  build output, not a source grep.
- Across the four files this plan touched, confirm no occurrence of
  `ease-in`, `ease-out`, `ease-in-out`, or `linear` (the brief explicitly
  asks for this exact negative check — it is a scan for a banned token
  across touched files, not a grep standing in for proof of behavior).
- Confirm no compiled CSS or the touched component source carries a raw
  hex color that wasn't already present before this plan (spot-check only
  the diff, not the whole file).
Note in the summary that `setInterval` cleanup and the interval/observer
correctness are established by `tsc --noEmit`/`next build` passing plus
code review of Task 1's `useEffect` return — not by a source grep, per the
brief's own instruction. Also note that 1440px/375px pixel-level review
(node position, dimmed-row contrast on `--violet`/`--indigo`, absence of
CLS on module change) is explicitly left to the CTO after this run — no
browser is available in this environment.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build</automated>
  </verify>
  <done>
Lint/typecheck/build all exit 0, 14 routes reported; prerendered HTML
contains the first module's title and all three checklist labels; zero
occurrences of the four banned easing tokens across the four touched files;
zero `var(--card)` color stops remain in `assembly-connectors.tsx`.
  </done>
</task>

</tasks>

<threat_model>
Pure presentational/motion change to existing public marketing content —
no new trust boundary, no user input, no new dependency, no data write
path touched. No STRIDE register required.
</threat_model>

<verification>
1. `npx tsc --noEmit` clean after each of Tasks 1-3.
2. `npm run lint && npm run typecheck && npm run build` all exit 0 (Task 4).
3. Build output: 14 routes, same static/dynamic split as before this plan.
4. Prerendered hero HTML contains the first module's title and the three
   checklist labels.
5. No `ease-in`/`ease-out`/`ease-in-out`/`linear` in any of the four
   touched files.
6. `assembly-connectors.tsx` carries no `var(--card)` gradient stop and no
   `<linearGradient>` — cables are solid accent color end to end.
7. Not a substitute for pixel measurement: 1440px/375px visual review
   (node placement, dimmed-row contrast, module-change CLS) is left to the
   CTO after this run.
</verification>

<success_criteria>
- Violet card cycles through the five modules (title + "Module N · Xh")
  every ~3.2s, looping, fading via `--ease-brand` only, with a height-locked
  title box so module changes never shift layout.
- Progress bar fills to `module.position / 5` and labels "Module N sur 5"
  from a locale template, animating with the module change.
- Three-row checklist reuses the pills' labels, starts dim-but-legible on
  both `--violet` and `--indigo`, and animates to validated (mint check,
  full white label) once on viewport entry, staggered ~260ms, never
  replaying on carousel loops.
- Cables terminate on a small node just before the card's left edge, full
  accent color the whole run, no white stop, comet recolored to match.
- `prefers-reduced-motion: reduce` shows module 1 only, checklist already
  validated and static, progress bar filled with no transition, cables
  fully drawn and static with no comet.
- Mobile (<640px) carousel/checklist still function in the single-column
  layout; only cables stay hidden below 640px.
- Zero hardcoded visible strings, zero new raw hex, exactly one easing
  curve (`--ease-brand`), no new level-3 surface, local commits only,
  branch stays `gsd/phase-02-site-public`, nothing pushed.
</success_criteria>

<output>
Create `.planning/quick/260831-nf5-section-01-hero-assembly-card-mechanics-/SUMMARY.md` when done.
</output>
