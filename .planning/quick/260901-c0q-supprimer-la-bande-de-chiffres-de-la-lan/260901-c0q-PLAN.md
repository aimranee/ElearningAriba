---
quick_id: 260901-c0q-supprimer-la-bande-de-chiffres-de-la-lan
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/sections/stats-band.tsx
  - src/components/motion/count-up.tsx
  - src/locales/fr/common.json
  - .planning/phases/02-site-public/02-CONTEXT.md
  - .planning/phases/02-site-public/02-UI-SPEC.md
autonomous: true
requirements: []
---

<objective>
Remove the animated figures band (section 02, directly under the hero) from
the landing page, per
`C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-09-01-brief-section-02-suppression-bande-chiffres.md`
(read in full — authoritative, treat as locked). `[DÉCISION 2026-09-01,
fondateur]`: the band is deleted, nothing replaces it — `<Hero />` is
directly followed by `<PourQui />`. Its four figures are all restated
elsewhere on the page already (module/hours line in Format et modalités,
six compétences in the Compétences section, "Aucun prérequis SAP" twice).
`CountUp` and the `statsBand` locale keys go with it if — and only if —
nothing else consumes them. No `tone=` prop changes anywhere; the hero and
"Pour qui" both being transparent after this run is the intended outcome,
not a bug to fix. Rewrite `D-03` in `02-CONTEXT.md` and the 26academy-style
bands paragraph in `02-UI-SPEC.md` to record the removal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Essakhi/Desktop/ElearningSAP/ariba-cto/notes/2026-09-01-brief-section-02-suppression-bande-chiffres.md
@src/app/page.tsx
@src/components/sections/stats-band.tsx
@src/components/motion/count-up.tsx
@src/locales/fr/common.json
@.planning/phases/02-site-public/02-CONTEXT.md
@.planning/phases/02-site-public/02-UI-SPEC.md

<interfaces>
Current `src/app/page.tsx` render order (Task 1 removes the import line and
the element only — everything else in this file is untouched):

```tsx
import { Hero } from "@/components/sections/hero";
import { StatsBand } from "@/components/sections/stats-band"; // remove this import
...
<Hero />
<StatsBand /> // remove this element — Hero must be directly followed by PourQui
<PourQui />
```

Current `src/locales/fr/common.json` block to remove in Task 2 (verify sole
consumer first):

```json
"statsBand": {
  "modules": "modules progressifs",
  "heures": "de formation live",
  "competences": "compétences pratiquées",
  "prerequis": "prérequis SAP exigé"
}
```

`common.statsBand.modules/heures/competences/prerequis` are read only in
`stats-band.tsx` (Task 1 deletes it) — confirmed by the brief.
`src/lib/i18n/fr.ts` (`formatNumber`, `formatHours`) is NOT touched by this
plan — it has six other consumers (`assembly-card.tsx`,
`format-modalites.tsx`, `programme-accordion.tsx`, `cta-final.tsx`,
`programme/page.tsx`, `lib/pdf/`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove StatsBand from the page and delete its files</name>
  <files>src/app/page.tsx, src/components/sections/stats-band.tsx, src/components/motion/count-up.tsx</files>
  <action>
In src/app/page.tsx, remove the import line for StatsBand
(`import { StatsBand } from "@/components/sections/stats-band";`) and the
`<StatsBand />` element that currently sits between `<Hero />` and
`<PourQui />`. Do not add anything in its place — `<Hero />` must be
immediately followed by `<PourQui />` with no other element between them.
Leave every other import and element in this file untouched (Competences,
ProgrammeAccordion, FormatModalites, Confiance, CtaFinal, Faq, and the
revalidate export).

Delete src/components/sections/stats-band.tsx.

Before deleting src/components/motion/count-up.tsx, run
`grep -rn "count-up|CountUp" src/` and inspect every match. If the only
matches were in stats-band.tsx (now deleted) and count-up.tsx itself,
delete count-up.tsx. If any other file still imports or references
CountUp or count-up, do NOT delete the file — keep it and record the exact
file and line in the plan summary instead of inventing a fix.
  </action>
  <verify>
    <automated>grep -rln "count-up|CountUp|StatsBand|stats-band" src</automated>
  </verify>
  <done>
src/app/page.tsx renders Hero immediately followed by PourQui with no
StatsBand import or element remaining anywhere in src/. stats-band.tsx is
deleted. count-up.tsx is deleted unless a live consumer besides the two
removed files was found, in which case it is kept and the finding is
flagged in the summary.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the statsBand locale block</name>
  <files>src/locales/fr/common.json</files>
  <action>
Run `grep -rn "statsBand" src` first and confirm every match is inside
common.json itself (the block being removed) — no .tsx or .ts file should
reference common.statsBand after Task 1's deletions. If confirmed, remove
the entire "statsBand" object (the modules, heures, competences, prerequis
keys) from src/locales/fr/common.json, keeping the file valid JSON. Do not
touch any other key in the file, and do not touch src/lib/i18n/fr.ts
(formatNumber and formatHours stay — six other consumers listed above).
  </action>
  <verify>
    <automated>grep -c statsBand src/locales/fr/common.json; grep -rln statsBand src --include=*.tsx --include=*.ts</automated>
  </verify>
  <done>
src/locales/fr/common.json no longer contains a "statsBand" key, remains
valid JSON, and no .tsx/.ts source file references common.statsBand.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite D-03 and the UI-SPEC bands paragraph</name>
  <files>.planning/phases/02-site-public/02-CONTEXT.md, .planning/phases/02-site-public/02-UI-SPEC.md</files>
  <action>
In .planning/phases/02-site-public/02-CONTEXT.md, replace the current D-03
bullet ("The seven signed sections ship, plus the 26academy-style bands —
animated figures band and honest trust placeholders. Not routed for
pricing.") with a decision-dated entry on the model of the revised D-04:
state that the animated stats band is removed 2026-09-01 by founder
decision — its four figures are all restated elsewhere on the page (Format
et modalités repeats the module/hours line verbatim in
formatModalitesAside.resume, the Compétences section renders the six
skills, "Aucun prérequis SAP" already appears twice — hero chip and Format
et modalités). State that the honest trust placeholders remain, the seven
signed sections remain, and no signed requirement (PUB-01..PUB-07) is
affected since none of them names a stats band.

In .planning/phases/02-site-public/02-UI-SPEC.md (around lines 137-139),
replace the paragraph "Plus the 26academy-style bands the founder approved:
the animated figures band (the five module durations 3 h / 4 h / 4 h / 3 h
/ 3 h — 17 h total) and honest trust placeholders." with the same
treatment: the stats band is removed 2026-09-01, the trust placeholders
remain. Make explicit that the 17 h total itself is NOT removed from the
site — it is still computed from Supabase rows and shown by Format et
modalités and the /programme.pdf route.

Do not change any other content in either file.
  </action>
  <verify>
    <automated>grep -c "2026-09-01" .planning/phases/02-site-public/02-CONTEXT.md .planning/phases/02-site-public/02-UI-SPEC.md</automated>
  </verify>
  <done>
D-03 in 02-CONTEXT.md and the bands paragraph in 02-UI-SPEC.md both
document the 2026-09-01 removal with rationale; neither file mentions the
stats band as a still-shipping surface.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Full verification chain</name>
  <files>(none — verification only, reads build output)</files>
  <action>
Run `npm run lint`, `npm run typecheck`, and `npm run build` in sequence;
all three must exit 0, and the build output must still list 14 static
routes. Then read .next/server/app/index.html and confirm:
- Zero occurrences of "modules progressifs", "compétences pratiquées", and
  "prérequis SAP exigé" (with accents).
- "de formation live" still appears at least once (it survives legitimately
  in formatModalitesAside.resume — its presence is correct, not a leftover;
  do not treat it as a failure).
- "Pour qui" still appears, and the Format et modalités summary text
  ("{modules} modules · {heures} de formation live" interpolated) still
  appears.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build</automated>
  </verify>
  <done>
Lint/typecheck/build all exit 0, 14 routes reported static; prerendered
index.html has zero occurrences of the three removed strings, still
contains "de formation live", "Pour qui", and the Format et modalités
summary text.
  </done>
</task>

</tasks>

<threat_model>
Pure content/structure removal on existing public marketing content — no
new trust boundary, no user input, no new dependency, no data write path
touched. No STRIDE register required.
</threat_model>

<verification>
1. `src/app/page.tsx` renders Hero directly followed by PourQui, no
   StatsBand reference anywhere in `src/`.
2. `stats-band.tsx` deleted; `count-up.tsx` deleted unless a surviving
   consumer was found (flagged in summary if so).
3. `common.json`'s `statsBand` block removed, file stays valid JSON.
4. `src/lib/i18n/fr.ts` unchanged.
5. No `tone=` prop changed on any section.
6. `D-03` in `02-CONTEXT.md` and the bands paragraph in `02-UI-SPEC.md`
   both rewritten with the 2026-09-01 removal rationale.
7. `npm run lint && npm run typecheck && npm run build` all exit 0, 14
   routes static.
8. Prerendered `index.html`: zero "modules progressifs" / "compétences
   pratiquées" / "prérequis SAP exigé"; "de formation live", "Pour qui",
   and the Format et modalités summary all still present.
</verification>

<success_criteria>
- The stats band section is gone from the rendered landing page; nothing
  replaces it.
- `CountUp` and the `statsBand` locale keys are removed if orphaned, kept
  and flagged if not.
- No other section, no `tone=` prop, and no other locale key is touched.
- `D-03` and the UI-SPEC bands paragraph read as decision-dated removals,
  matching the revised-D-04 style already used in these two files.
- Build stays green, 14 routes static, atomic local commits only, branch
  stays `gsd/phase-02-site-public`, nothing pushed.
</success_criteria>

<output>
Create `.planning/quick/260901-c0q-supprimer-la-bande-de-chiffres-de-la-lan/SUMMARY.md` when done.
</output>
