---
quick_id: 260831-gb0-run-1-echelle-de-surface-lot2-landing-re
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/ui/card.tsx
  - src/components/sections/pour-qui.tsx
  - src/components/sections/confiance.tsx
  - src/components/sections/format-modalites.tsx
  - src/components/sections/competences.tsx
  - src/components/sections/programme-accordion.tsx
  - src/components/sections/faq.tsx
  - .planning/phases/02-site-public/02-CONTEXT.md
  - .planning/phases/02-site-public/02-UI-SPEC.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The landing page (/) renders three distinct, named card surface tiers: tint (zero box-shadow), default (contact shadow), raised (shadow-3/shadow-4)"
    - "npm run build exits 0 and all 14 routes stay statically prerendered"
    - "In .next/server/app/index.html, the substrings --shadow-3 and --shadow-4 combined appear on at most two rendered elements (hero console frame, CTA final card)"
    - "No component file introduces a raw hex color for the new surface tokens — every consumer reads var(--hairline), var(--hairline-2), var(--tint), var(--contact) or var(--inset-hi)"
    - "AC-1 and D-19 in 02-CONTEXT.md and their prose equivalents in 02-UI-SPEC.md describe the tri-level surface contract, not the old raised-everywhere rule"
  artifacts:
    - path: "src/app/globals.css"
      provides: "--hairline, --hairline-2, --tint, --tint-violet, --contact, --inset-hi tokens in :root"
    - path: "src/components/ui/card.tsx"
      provides: "cardVariants with tint (niveau 1), default (niveau 2), raised (niveau 3, unchanged tier but now paired with --inset-hi)"
    - path: ".planning/phases/02-site-public/02-CONTEXT.md"
      provides: "Rewritten D-19 and acceptance-criteria item 1 (AC-1)"
    - path: ".planning/phases/02-site-public/02-UI-SPEC.md"
      provides: "Rewritten composition item 1 and acceptance-criteria item 1, mirroring CONTEXT.md"
  key_links:
    - from: "src/components/sections/pour-qui.tsx, src/components/sections/confiance.tsx"
      to: "src/components/ui/card.tsx default variant"
      via: "variant=\"default\" prop (was \"raised\")"
      pattern: "variant=\"default\""
    - from: "src/components/sections/format-modalites.tsx"
      to: "src/components/ui/card.tsx tint variant"
      via: "variant=\"tint\" prop (was \"raised\")"
      pattern: "variant=\"tint\""
    - from: "src/components/sections/competences.tsx, src/components/sections/programme-accordion.tsx, src/components/sections/faq.tsx"
      to: "src/app/globals.css tokens"
      via: "inline className reading var(--tint)/var(--hairline)/var(--hairline-2), replacing shadow-2/shadow-3 classes"
      pattern: "var\\(--tint\\)|var\\(--hairline"
---

<objective>
Run 1 of 3 for the Lot 2 landing visual-surface refonte: establish the CSS token
foundation and the three-tier Card surface system, retrograde the landing's
current card consumers onto the correct tier, and rewrite AC-1/D-19 in the
phase docs to describe the new contract. Run 2 (header/hero) and Run 3
(numbered parcours + CTA assembly card + photo slots) are out of scope here.

Purpose: today's landing renders too many `raised` (shadow-3/4) cards — 3
section families use it unconditionally, which is exactly the failure the
CTO's brief is correcting. This run caps `raised` at the reserved tier (hero +
CTA final only, both already implemented outside the Card component) and
introduces two lighter tiers so the rest of the page reads as calmer surfaces.

Output: updated globals.css tokens, a three-variant Card component, six
section files retrograded to the correct tier, and AC-1/D-19 rewritten in
02-CONTEXT.md and 02-UI-SPEC.md to match.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<discovery_notes>
Read directly from the repo before this plan was written (do not re-derive):

- `--shadow-1` through `--shadow-4` and `--shadow-brand` already exist in
  `src/app/globals.css`'s `@theme inline` block (lines 63-69) — do not
  duplicate them. `--shadow-1` = `0 1px 2px rgba(10,37,64,.04), 0 4px 14px
  -8px rgba(10,37,64,.18)`.
- `src/components/ui/card.tsx` currently has 5 variants: `default` (border +
  `--shadow-1`), `raised` (border-transparent, `--shadow-3` rest /
  `--shadow-4` + `translateY(-7px)` hover, `rounded-[22px]`), `outline`,
  `success`, `muted`. `raised`'s comment block (lines 14-18) references D-19's
  old "every floating card lifts unconditionally" rule — update it.
- Only `pour-qui.tsx` (line 55), `confiance.tsx` (line 59) and
  `format-modalites.tsx` (line 64) actually consume `<Card variant="raised">`
  on the landing. `competences.tsx` and `programme-accordion.tsx` do **not**
  use the `Card` component at all — they hand-roll `shadow-[var(--shadow-2)]`
  / `hover:shadow-[var(--shadow-3)]` classes on a raw `<li>` and on
  `AccordionItem`'s `className` respectively. `faq.tsx`'s `AccordionItem`
  carries no override at all — it falls back to `accordion.tsx`'s base
  `"border-border overflow-hidden rounded-lg border"` (flat, no shadow). All
  three need direct Tailwind class edits, not a `variant` prop swap.
- `hero.tsx:165` (console frame) and `cta-final.tsx:36` (final card) already
  consume `--shadow-4` directly via raw `shadow-[var(--shadow-4)]` classes,
  **not** through the Card component. This is the existing, correct D-23 pair
  — do not touch it. After this run, `raised` will have zero call sites on
  the landing (Run 2/3 territory), so `.next/server/app/index.html` will show
  `--shadow-3` on 0 elements and `--shadow-4` on exactly 2 (hero, CTA final) —
  within the "at most two" ceiling.
- `variant="raised"` also appears on 4 non-landing routes (`programme.tsx`,
  `contact.tsx`, `a-propos.tsx`, `formation.tsx`) and in `empty-state.tsx`.
  These are out of scope — the brief's ceiling check targets
  `.next/server/app/index.html` only (the homepage), and the brief's scope is
  explicitly "Lot 2 landing". Do not touch these 5 files.
- `src/lib/utils.ts`'s `cn()` runs `twMerge(clsx(...))`, so conflicting
  Tailwind classes inside `cardVariants` strings (e.g. `rounded-xl` from the
  base overridden by a variant's `rounded-[20px]`) already resolve correctly
  today (`raised` already overrides `rounded-xl` → `rounded-[22px]`) — the new
  `tint` variant can override radius the same way.
- 02-CONTEXT.md: D-19 is lines 108-110, D-23 is lines 118-120, the numbered
  acceptance-criteria list starts at line 270 (item 1 = "AC-1" is lines
  272-274).
- 02-UI-SPEC.md: composition item 1 (D-19's prose twin) is lines 100-102,
  composition item 5 (D-23's prose twin) is lines 109-111, acceptance-criteria
  item 1 (AC-1's prose twin) is section 7, lines 165-167.
</discovery_notes>

<known_conflict>
The brief's card.tsx spec for niveau 2 (`default`) says hover changes
`box-shadow` from `var(--contact)` to `var(--shadow-1)` (both + `--inset-hi`).
The brief's D-19 rewrite text says niveau 1/2 hover "never triggers a new box
shadow" (`il ne déclenche jamais d'ombre portée nouvelle`). These two
instructions contradict each other for niveau 2. This plan follows the
concrete component spec (niveau 2 hover does transition `--contact` →
`--shadow-1`) since it is the more mechanically precise instruction, and
writes D-19's doc text to accurately describe that behavior instead of
copying the contradictory sentence verbatim. Flagged for the CTO to confirm.
</known_conflict>

<interfaces>
From src/components/ui/card.tsx (current, before this plan):
```
const cardVariants = cva(
  "flex flex-col gap-4 rounded-xl border py-4 transition-[transform,box-shadow,border-color,background-color] duration-[var(--duration-base)] ease-[var(--ease-brand)] outline-none ..." +
    " data-[interactive=true]:hover:shadow-[var(--shadow-2)] data-[interactive=true]:active:translate-y-px",
  {
    variants: {
      variant: {
        default: "border-border bg-background shadow-[var(--shadow-1)]",
        raised: "border-transparent bg-background shadow-[var(--shadow-3)] rounded-[22px] hover:shadow-[var(--shadow-4)] hover:-translate-y-[7px]",
        outline: "border-border bg-background shadow-none",
        success: "border-transparent bg-success-muted shadow-[var(--shadow-1)]",
        muted: "border-transparent bg-muted shadow-none",
      },
      size: { default: "px-4", sm: "gap-2 px-3 py-3" },
    },
    defaultVariants: { variant: "default", size: "default" },
  }
)
```
The base transition list already includes `background-color` and
`border-color` — new variants need no transition-property change, only
duration/ease already point at `--ease-brand` (D-17's only curve).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SURFACES tokens + three-tier Card variants</name>
  <files>src/app/globals.css, src/components/ui/card.tsx</files>
  <action>
In globals.css, add to the existing `:root` block (do not create a second
`:root`, do not touch `@theme inline`'s existing `--shadow-1..4`): `--hairline:
rgba(10,37,64,.07)`, `--hairline-2: rgba(99,91,255,.30)`, `--tint: #F7F8FC`,
`--tint-violet: #F4F3FF`, `--contact: 0 1px 2px rgba(10,37,64,.05)`,
`--inset-hi: inset 0 1px 0 rgba(255,255,255,.9)`. Group them under one
comment banner, e.g. `/* SURFACES (Run 1, 2026-08-31): the tri-level card
system consumed by card.tsx's tint/default/raised variants. */`.

In card.tsx, replace the `variant` map's `default` entry and add a new `tint`
entry; leave `outline`, `success`, `muted` untouched:
- `tint` (niveau 1): `border-[var(--hairline)] bg-[var(--tint)] shadow-none
  rounded-[20px] hover:bg-white hover:border-[var(--hairline-2)]
  hover:-translate-y-[2px]`. Zero box-shadow at rest and on hover.
- `default` (niveau 2, replaces current): `border-[var(--hairline)]
  bg-background shadow-[var(--contact),var(--inset-hi)]
  hover:-translate-y-[3px] hover:border-[var(--hairline-2)]
  hover:shadow-[var(--shadow-1),var(--inset-hi)]`.
- `raised` (niveau 3, keep tier and call sites untouched — only the token
  list changes): pair both box-shadow values with `--inset-hi`, i.e.
  `shadow-[var(--shadow-3),var(--inset-hi)] ...
  hover:shadow-[var(--shadow-4),var(--inset-hi)]`, keep
  `rounded-[22px]`/`hover:-translate-y-[7px]`/`border-transparent`.

Update the `why (D-19)` comment above `raised` (lines 14-18) — it currently
claims every floating card lifts unconditionally; rewrite it to say the
unconditional lift is now scoped to the `raised` tier only, per the rewritten
D-19 (Task 3). Do not touch `outline`/`success`/`muted` or the
`data-[interactive=true]:*` hover rules in the base string — those are a
separate, unrelated interactive-card affordance.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>globals.css has the 6 new tokens in :root with no duplicate --shadow-1..4; card.tsx exports tint/default/raised with the exact values above; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Retrograde the six landing card/surface consumers</name>
  <files>src/components/sections/pour-qui.tsx, src/components/sections/confiance.tsx, src/components/sections/format-modalites.tsx, src/components/sections/competences.tsx, src/components/sections/programme-accordion.tsx, src/components/sections/faq.tsx</files>
  <action>
Card-variant swaps (niveau assignment per the CTO brief):
- `pour-qui.tsx` line 55 and `confiance.tsx` line 59: change `<Card
  variant="raised" ...>` to `<Card variant="default" ...>` (niveau 2 — tuiles
  Pour qui, tuiles Confiance). Leave every other prop/class on the `<Card>`
  and its children untouched.
- `format-modalites.tsx` line 64: change `<Card variant="raised" ...>` to
  `<Card variant="tint" ...>` (niveau 1 — rangs du parcours Format).

Raw-class rewrites (no Card component involved — edit the inline Tailwind
classes directly to niveau 1: tinted background, hairline border, 20px
radius, zero shadow at rest, hover flips to white background + hairline-2
border + a small translateY, transition restricted to background-color/
border-color/transform on `--ease-brand`; never add a new box-shadow on
hover, per D-19):
- `competences.tsx`: on the `Reveal` `li` (currently `bg-white p-[1.15rem]
  px-[1.35rem] shadow-[var(--shadow-2)] transition-[transform,box-shadow]
  duration-[420ms] ease-[var(--ease-brand)] hover:-translate-y-[5px]
  hover:shadow-[var(--shadow-3)]`), replace with `bg-[var(--tint)]
  border border-[var(--hairline)] rounded-[20px] shadow-none
  transition-[transform,background-color,border-color]
  duration-[var(--duration-base)] ease-[var(--ease-brand)] hover:bg-white
  hover:border-[var(--hairline-2)] hover:-translate-y-[2px]`. Drop
  `shadow-[var(--shadow-2)]` and `hover:shadow-[var(--shadow-3)]` entirely —
  no box-shadow anywhere on this element.
- `programme-accordion.tsx`: on `AccordionItem`'s className (currently
  `rounded-[18px] border-none bg-white shadow-[var(--shadow-2)]
  transition-shadow duration-[420ms] ease-[var(--ease-brand)]
  data-[panel-open]:shadow-[var(--shadow-3)]`), replace with `rounded-[20px]
  border border-[var(--hairline)] bg-[var(--tint)] shadow-none
  transition-[background-color,border-color]
  duration-[var(--duration-base)] ease-[var(--ease-brand)] hover:bg-white
  hover:border-[var(--hairline-2)] data-[panel-open]:bg-white
  data-[panel-open]:border-[var(--hairline-2)]`. No translateY on this one —
  it is a full-width accordion row, not a floating tile; the open/hover state
  reads through background+border only. Leave `AccordionTrigger`'s own
  classes (line 70) untouched.
  - `faq.tsx`: each `AccordionItem` currently has no className override
  (inherits `accordion.tsx`'s base `border-border ... rounded-lg border`).
  Add `className="rounded-[20px] border border-[var(--hairline)]
  bg-[var(--tint)] transition-[background-color,border-color]
  duration-[var(--duration-base)] ease-[var(--ease-brand)] hover:bg-white
  hover:border-[var(--hairline-2)] data-[panel-open]:bg-white
  data-[panel-open]:border-[var(--hairline-2)]"` to the `<AccordionItem>`
  element. Do not edit `accordion.tsx` itself — `programme-accordion.tsx`
  already overrides its own `AccordionItem` fully, so no shared-base change
  is needed or safe (it would affect other accordion consumers outside this
  run's scope).

None of these six edits introduce a raw hex value or a visible hard-coded
string — all colors come from the new `var(--...)` tokens, and no JSX text
content changes.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>pour-qui.tsx/confiance.tsx use variant="default"; format-modalites.tsx uses variant="tint"; competences.tsx/programme-accordion.tsx/faq.tsx read var(--tint)/var(--hairline)/var(--hairline-2) with zero box-shadow classes; lint and typecheck are green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite AC-1 and D-19 in phase docs, then full verification</name>
  <files>.planning/phases/02-site-public/02-CONTEXT.md, .planning/phases/02-site-public/02-UI-SPEC.md</files>
  <action>
In 02-CONTEXT.md: replace the D-19 bullet (lines 108-110) with text
describing the tri-level contract: the lift-on-hover is now restricted to
niveau 3 (`--shadow-3` rest / `--shadow-4` + `translateY(-7px)` hover);
niveau 1 (`--tint` + `--hairline`, zero box-shadow) and niveau 2 (white +
`--hairline` + `--contact`/`--inset-hi`) change background tint, border
color (`--hairline` → `--hairline-2`) and a 2-3px translateY on hover; niveau
2 additionally transitions its box-shadow from `--contact` to `--shadow-1`
(both paired with `--inset-hi`) — niveau 1 never gains a box-shadow at any
state. Replace acceptance-criteria item 1 (lines 272-274) with: the surface
map is verifiable section by section — niveau 1 on compétences, the
programme and FAQ accordions, and the Format parcours rows; niveau 2 on the
Pour-qui tiles, the Confiance tiles, and the parcours' side frame; niveau 3
on exactly two rendered elements page-wide (hero console, CTA final card) —
the criterion is the ceiling on niveau 3 (max 2), not a floor on total card
count. Leave D-23 (lines 118-120) and acceptance-criteria item 2 (the
`--shadow-4` consumption criterion) exactly as written, but append one
inline sentence noting Run 3 changes the CTA-final call site's shape (a
card-assembly element replacing the current violet block) without changing
the criterion itself.

Mirror the same three edits in 02-UI-SPEC.md: composition item 1 (lines
100-102, D-19's prose twin), acceptance-criteria item 1 in section 7 (lines
165-167, AC-1's prose twin), and the same one-sentence Run-3 note appended
near composition item 5 (lines 109-111, D-23's prose twin) — do not alter
that item's own wording.

Read the exact current text of every bullet/line listed above before
editing (already captured verbatim in this plan's discovery_notes and
known_conflict — do not re-derive, do not hallucinate different existing
text).
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build</automated>
  </verify>
  <done>
D-19 and AC-1 (and their 02-UI-SPEC.md prose twins) describe the tri-level
surface contract; D-23/AC-2 and their twins are untouched except for the
one-sentence Run-3 note; `npm run build` exits 0 with all 14 routes
prerendered static; grepping `.next/server/app/index.html` for `--shadow-3`
and `--shadow-4` (combined) shows the substrings on at most two distinct
rendered elements, matching the hero console frame and the CTA final card.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This run is a styling/token change on an already-static, server-rendered landing page. No new input, no new endpoint, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-quick-260831-gb0-01 | Tampering | None | accept | Pure CSS/JSX class edits and doc prose; no dependency install, no new runtime input path. |
</threat_model>

<verification>
1. `npm run lint` — 0 errors.
2. `npm run typecheck` — 0 errors.
3. `npm run build` — 0 errors, 14 routes prerendered static (matches Lot 1's known-good route count from 02-CONTEXT.md's verified facts).
4. Inspect `.next/server/app/index.html` (prerendered HTML, not source) — count rendered elements carrying `--shadow-3` or `--shadow-4`; must be at most 2 combined (hero console, CTA final).
5. Grep the six edited section files plus card.tsx for a raw `#`-hex color introduced by this run — none should appear (pre-existing hex in format-modalites.tsx's `isFutur` badge is untouched, not a regression).
6. Confirm `cubic-bezier` still resolves to exactly one distinct value site-wide (D-17) — no `ease-in`/`ease-out`/`ease-in-out`/`linear` was introduced by any of this run's edits.
</verification>

<success_criteria>
- globals.css carries the 6 new SURFACES tokens, no duplicated shadow tokens.
- card.tsx exposes tint (niveau 1, zero shadow), default (niveau 2, contact shadow), raised (niveau 3, shadow-3/4 + inset-hi) — raised's call sites unchanged (hero/CTA final still use raw classes, not the Card component).
- pour-qui.tsx, confiance.tsx → variant="default"; format-modalites.tsx → variant="tint"; competences.tsx, programme-accordion.tsx, faq.tsx → direct niveau-1 Tailwind classes reading the new tokens.
- AC-1 and D-19 rewritten in both 02-CONTEXT.md and 02-UI-SPEC.md; D-23/AC-2 untouched apart from the one-sentence Run-3 note.
- `npm run build` green, 14 static routes, --shadow-3/--shadow-4 combined on ≤2 rendered elements in index.html.
- Nothing pushed; stays on `gsd/phase-02-site-public`.
</success_criteria>

<output>
Create `.planning/quick/260831-gb0-run-1-echelle-de-surface-lot2-landing-re/SUMMARY.md` when done.
</output>
