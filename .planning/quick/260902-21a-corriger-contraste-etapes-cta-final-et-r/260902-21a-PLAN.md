---
phase: quick
plan: 260902-21a
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/cta-final.tsx
  - .planning/phases/02-site-public/02-11-PLAN.md
  - .planning/phases/02-site-public/02-CONTEXT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The three 'Ce qui se passe ensuite' step rows use bg-black/10, not bg-white/8"
    - "02-11-PLAN.md no longer requires a --shadow-4 call site in cta-final.tsx anywhere in its action, acceptance_criteria or verification sections"
    - "D-69 is recorded in 02-CONTEXT.md with the measured contrast values from this task"
  artifacts:
    - path: "src/components/sections/cta-final.tsx"
      provides: "bg-black/10 step-row surface"
    - path: ".planning/phases/02-site-public/02-11-PLAN.md"
      provides: "two-call-site --shadow-4 requirement (hero.tsx, card.tsx), D-68-consistent"
    - path: ".planning/phases/02-site-public/02-CONTEXT.md"
      provides: "D-69 decision record"
  key_links:
    - from: ".planning/phases/02-site-public/02-11-PLAN.md"
      to: "src/components/sections/cta-final.tsx"
      via: "grep requirement no longer names cta-final.tsx for --shadow-4"
      pattern: "cta-final"
---

<objective>
Fix a measured contrast failure on the CTA-final step rows, and correct three stale
`--shadow-4` two-surface assertions left behind in the unexecuted 02-11-PLAN.md after
D-68 dropped cta-final.tsx's raised surface. Record the darkening-veil pattern as D-69.

Purpose: 02-11 has not run yet — if these three lines are not corrected, the executor
will re-add a --shadow-4 call site to cta-final.tsx to satisfy a stale grep, undoing D-68.
The contrast fix and the doc fix are unrelated in cause but share the same file
(cta-final.tsx) and the same decision trail (D-68), so they travel together as one task.
Output: cta-final.tsx step rows readable at AA, 02-11-PLAN.md internally consistent with
D-68, D-69 recorded in 02-CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/sections/cta-final.tsx
@.planning/phases/02-site-public/02-11-PLAN.md
@.planning/phases/02-site-public/02-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
cta-final.tsx today has TWO distinct `bg-white/8` occurrences — do not conflate them:
- Line ~85: the secondary "Voir le programme" outline button (`border-white/40 bg-white/8 text-white backdrop-blur-[8px] hover:bg-white/16 ...`). NOT in scope. Leave untouched.
- Line ~100: the step-row wrapper inside `etapes.items.map(...)` (`flex items-start gap-[0.85rem] rounded-[15px] border border-white/15 bg-white/8 px-[1rem] py-[0.9rem]`). This is the ONLY class to change, to `bg-black/10`.

02-11-PLAN.md line 17 (must_haves.truths) was already corrected in a prior run to name
only the hero assembly frame as the single --shadow-4 call site — do not touch that line.
Three other places in the same file still assert the old two-surface (hero + cta-final)
reality and must be corrected to match:
- ~lines 80-86, inside Task 1's `<action>` block, the "--shadow-4 consumed (AC-2)" paragraph
- ~line 126, Task 1's `<acceptance_criteria>` bullet on the same grep
- ~line 248, the phase-level `<verification>` bullet
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix step-row contrast in cta-final.tsx</name>
  <files>src/components/sections/cta-final.tsx</files>
  <action>
    Change the step-row wrapper's `bg-white/8` to `bg-black/10` (the row at ~line 100,
    inside `etapes.items.map(...)`). Do not touch the secondary-button `bg-white/8` at
    ~line 85 — different element, not part of this fix. Keep `border border-white/15`,
    the `rounded-[15px]` radius, `text-white/82` on the description, `text-white` on the
    title, and the badge `bg-white text-[var(--deep)]` unchanged. Do not add a shadow, and
    do not touch the panel gradient or any other part of the section. This is a contrast
    fix only, pre-measured by the CTO from DOM sampling (bg-white/8 measured 4.59-4.79 on
    the title and 3.64-3.77 on the description, both below the 4.5 AA threshold;
    bg-black/10 measures 6.25 / 4.77, both passing) — do not re-derive or second-guess the
    value.
  </action>
  <verify>
    <automated>grep -n "bg-black/10\|bg-white/8" src/components/sections/cta-final.tsx</automated>
  </verify>
  <done>
    The step-row line reads `bg-black/10` (not `bg-white/8`); the secondary-button line
    still reads `bg-white/8` unchanged. Do not claim contrast is "verified" in the
    SUMMARY — no browser tooling is available to this executor; report only that the
    class was changed as specified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct stale --shadow-4 two-surface assertions in 02-11-PLAN.md; record D-69</name>
  <files>.planning/phases/02-site-public/02-11-PLAN.md, .planning/phases/02-site-public/02-CONTEXT.md</files>
  <action>
    In 02-11-PLAN.md, make three corrections (cite D-68 inline at each site) so the file
    is internally consistent with the already-fixed line 17 truth (one persistent
    --shadow-4 surface: the hero assembly frame). Do not rewrite any other criterion in
    this file.

    (a) In Task 1's `<action>` block, the "--shadow-4 consumed (AC-2)" paragraph (~lines
    80-86): change "must return at least three lines" to "at least two lines"; drop
    `src/components/sections/cta-final.tsx` (the final CTA) from the required list,
    keeping only `src/components/sections/hero.tsx` (rename its descriptor from "the
    console frame" to "the assembly frame" — the hero no longer carries a console) and
    `src/components/ui/card.tsx` (the raised hover). Update "Verify each of the first two
    matches sits on a line containing className" to instead name only the hero.tsx match,
    since there is now one required match ahead of card.tsx, not two.

    (b) In Task 1's `<acceptance_criteria>` (~line 126): change "lists hero.tsx and
    cta-final.tsx, each on a className line" to list only hero.tsx, on a className line.

    (c) In the phase-level `<verification>` section (~line 248): change "--shadow-4
    consumed at the hero frame and the final CTA call sites" to consumed at the single
    hero frame call site only.

    In 02-CONTEXT.md, append a new decision after D-68 (same list, same bullet format:
    `- **D-69** (2026-09-02, CTO): ...`), inside `<decisions>`, before `### Claude's
    Discretion`. Content: translucent surfaces laid over the CTA-final closing-section
    gradient must darken, never lighten, because the gradient sits at its lightest end
    there — a white veil (bg-white/8) drops white ink below AA (title 4.59-4.79,
    description text-white/82 3.64-3.77, both under the 4.5 threshold), while the same
    veil in black clears it (bg-black/10: title 6.25, description 4.77; bg-black/15:
    6.79/5.14; bg-black/20: 7.39/5.54 — bg-black/10 was chosen as the minimal passing
    value). Note this is the fourth occurrence of ink-on-tinted-surface contrast failure
    in this project (after D-50, D-53, D-58), and record the fix: the step rows in
    cta-final.tsx move to bg-black/10.
  </action>
  <verify>
    <automated>grep -n "cta-final" .planning/phases/02-site-public/02-11-PLAN.md; grep -c "D-69" .planning/phases/02-site-public/02-CONTEXT.md</automated>
  </verify>
  <done>
    `grep -rn "cta-final" .planning/phases/02-site-public/02-11-PLAN.md` returns no line
    that still requires a --shadow-4 match in cta-final.tsx (any remaining cta-final.tsx
    mentions, if left by other unrelated criteria, do not assert a --shadow-4 call site
    there). D-69 is present in 02-CONTEXT.md, dated 2026-09-02, attributed to CTO, citing
    the measured values above.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — a CSS class change on static marketing copy and corrections to planning
documents. No new input surface, no data flow change, no dependency added.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-21a-01 | Tampering | 02-11-PLAN.md acceptance criteria | accept | Editing an unexecuted plan file on the working branch, reviewed by this same task's verify step; no execution-time risk |
</threat_model>

<verification>
1. `grep -n "bg-black/10\|bg-white/8" src/components/sections/cta-final.tsx` shows the
   step row on `bg-black/10` and the secondary button still on `bg-white/8`.
2. `grep -rn "cta-final" .planning/phases/02-site-public/02-11-PLAN.md` no longer requires
   a --shadow-4 call site in cta-final.tsx.
3. `grep -c "D-69" .planning/phases/02-site-public/02-CONTEXT.md` >= 1.
4. `npx tsc --noEmit`, `npm run lint`, `npm run build` all exit 0.
</verification>

<success_criteria>
- Step-row contrast class changed exactly as specified; secondary button untouched.
- 02-11-PLAN.md's three --shadow-4 assertions match the D-68 reality (hero.tsx + card.tsx
  only) and each corrected line cites D-68.
- D-69 recorded in 02-CONTEXT.md with the measured values.
- lint/typecheck/build all exit 0.
</success_criteria>

<output>
Create `.planning/quick/260902-21a-corriger-contraste-etapes-cta-final-et-r/260902-21a-SUMMARY.md` when done
</output>
