---
plan: 260902-1o8
status: complete
---

# Summary — CTA final: retirer la console fantôme, poser "Ce qui se passe ensuite"

Implemented per CTO brief `2026-09-02-brief-section-07-cta-final-console-fantome.md`, decisions D-66/D-67/D-68.

## Changes
- `src/locales/fr/common.json`: `hero.console` deleted entirely (url, liveLabel, liveAnimator, slotsLabel, slots, videoPlaceholder). `hero.chips` kept — still consumed by `hero.tsx:151`.
- `src/locales/fr/landing.json`: `ctaFinal.etapes` added (titre + 3 items), verbatim from brief. `content_section` row for `cta-final` unchanged; no seed run.
- `src/locales/fr/_mocks.public.json`: `landing.json#ctaFinal.etapes` registered under CADR-01, alongside the existing `ctaFinal.titre`/`ctaFinal.supportLine` entries.
- `src/components/sections/cta-final.tsx`: right column rewritten from the hero-console fossil to "Ce qui se passe ensuite" — an `<h3>` plus three accessible rows (numbered white pastille, `var(--deep)` digit, `bg-white/8 border-white/15`, no shadow, no `aria-hidden`). Dropped dead imports (`Play`, `formatHours`, `getModules`), the `common.hero.chips.join(" · ")` duplication line, the `getModules` call, and rewrote the JSDoc to cite D-66/D-67/D-68.
- `.planning/phases/02-site-public/02-CONTEXT.md`: recorded D-66, D-67, D-68; corrected AC-2's parenthetical enumeration and the `--shadow-4` sentence — niveau-3 ceiling now reads as one surface (hero) instead of two.
- `.planning/phases/02-site-public/02-11-PLAN.md`: corrected the `must_haves.truths` bullet asserting `--shadow-4` at two rendered call sites down to one (hero), citing D-68. Per brief scope, only that bullet was touched — the file's `<action>`/`<acceptance_criteria>` text (lines ~80-86, ~126) still describes the old two/three-call-site grep expectation (hero.tsx + cta-final.tsx [+ card.tsx]) and was left as-is because the brief named only the `must_haves.truths` line. **Flag for the CTO**: that stale text will still mislead whoever executes 02-11 unless corrected separately.

## Verification — measured values, not "done"

- `npx tsc --noEmit`: exit 0, no output.
- `npm run lint`: exit 0.
- `npm run build`: exit 0, all 16 routes compiled/prerendered successfully.
- `npm run content:check`: exit 1 (expected — registry still blocking real content). `ctaFinal.titre`, `ctaFinal.supportLine`, and the new `ctaFinal.etapes` all listed as blocked under CADR-01 (`CADR-01: 11` total keys, up from `CADR-01: 10` before this run).
- `grep -rn "hero.console\|Places restantes\|formation-sap-ariba.fr\|Emplacement vidéo\|videoPlaceholder\|slotsLabel" src/`: zero matches. (Two unrelated hits for the literal string `formation-sap-ariba.fr` remain in `src/lib/email/resend.ts` and `src/locales/fr/contact.json` — the contact-email domain, not the console's fabricated URL — left untouched, out of scope.)
- `grep -c "dashed" src/components/sections/cta-final.tsx`: 0.
- `grep -c '"use client"' src/components/sections/cta-final.tsx`: 0.
- `aria-hidden` in `cta-final.tsx`: 2 occurrences, both non-text (the decorative grid `<span>` and the `Calendar` icon in the primary button) — none on the new steps column.
- No new raw hex added: the only `#`-literal in the file (`#000` inside a `mask-image` gradient) predates this run, on the decorative grid span.
- No `min-[` arbitrary breakpoint, no second `cubic-bezier` introduced.
- `--shadow-4` in compiled CSS: confirmed present, `.next/static/chunks/2fk1wmuk2a4ti.css` (built this run, timestamp 2026-09-02 01:18), 16 occurrences including the token declaration and its consuming `shadow-[var(--shadow-4)]` utility class — this is the hero's only remaining call site (`cta-final.tsx` no longer references `--shadow-4`).

## Not verified — explicitly deferred to the CTO

No browser tool available in this run (allowlist: `Skill Task Bash Read Write Edit Glob Grep WebSearch WebFetch mcp__context7__*`). The following require `next dev` restarted fresh and DOM inspection, per the brief's own split:
- `[class*="dashed"]` document-wide count (expected 0, was 1).
- "Places restantes" innerText count document-wide (expected 0, was 3).
- Section 07 height (`[data-slot="section"]` index 5) at 1536px — expected ~750-800px, was 891px — and at 375px, a new measurement.
- Contrast ratios: pastille digit (`var(--deep)` on white) and step description (`text-white/82` on the panel gradient).
- Keyboard tab order (two buttons only, no stop on the new steps column) and that `/reservation` + `/programme` respond 200.
- "8 participants" appears exactly once page-wide (section 06 only).

## Other common.json keys asserting public facts (reported, not touched, per brief instruction)

Beyond `hero.chips` (already flagged in the brief as carrying "Expert certifié"):
- `footer.baseline`: "Une formation SAP Ariba en direct, animée par un expert certifié — pas une vidéo de plus à regarder." — same "expert certifié" claim, footer-wide.
- `assemblage.badge` / `assemblage.pills`: "Parcours SAP Ariba", "Appel découverte", "Session live", "Support PDF" — structural labels rather than factual claims, likely not registry candidates.
- `photoSlot.description`: "Emplacement réel, dimensionné 16/9 et prêt pour next/image..." — describes an implementation detail, not a public-facing claim.

None of these were modified or added to `_mocks.public.json`; the CTO decides whether any need registering.
